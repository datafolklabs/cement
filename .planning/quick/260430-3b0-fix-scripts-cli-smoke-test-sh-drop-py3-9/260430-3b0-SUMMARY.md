---
phase: 260430-3b0-fix-scripts-cli-smoke-test-sh-drop-py3-9
plan: 01
type: quick
subsystem: ci
tags:
  - ci
  - python-matrix
  - pep-517
  - phase-1-gap-closure
requires: []
provides:
  - cli-smoke-test driver aligned with phase-1 python matrix (3.10–3.14)
  - PEP 517 install path for generated cement projects
affects:
  - .github/workflows/build_and_test.yml (cli-smoke-test job, transitively)
tech-stack:
  added: []
  patterns:
    - "PEP 517 build-backend fallback for legacy setup.py via `pip install .`"
key-files:
  created: []
  modified:
    - scripts/cli-smoke-test.sh
decisions:
  - "Drop `pip install -r requirements.txt` from generated-project install: `pip install .` covers the project + declared deps, and the smoke test only needs `myapp --version` import-path validation (cement already installed at script line 22)."
  - "Keep pypy3.10 out of the script's default matrix: docker scheme uses `python:$pyver` (CPython images); pypy stays a CI-only matrix entry."
metrics:
  duration_min: 1
  duration_sec: 41
  tasks_completed: 1
  files_modified: 1
  commits: 1
  completed: "2026-04-30"
---

# Quick 260430-3b0: Fix `scripts/cli-smoke-test.sh` — drop py3.9 + PEP 517

CI smoke-test driver realigned with the phase-1 python matrix and modern PEP 517 install path; closes the only phase-1 PYVER surface that escaped the original audit.

## Changes

**File:** `scripts/cli-smoke-test.sh`

### Diff 1 — Line 5 (default Python matrix)

Before:
```bash
[ -z "$PYTHON_VERSIONS" ] && PYTHON_VERSIONS="3.9 3.10 3.11 3.12 3.13 3.14"
```

After:
```bash
[ -z "$PYTHON_VERSIONS" ] && PYTHON_VERSIONS="3.10 3.11 3.12 3.13 3.14"
```

### Diff 2 — Lines 52–54 (generated-project install)

Before:
```bash
docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; pip install -r requirements.txt"
docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; pip install setuptools"
docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; python setup.py install"
```

After:
```bash
docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; pip install ."
```

## Commit

| Hash       | Subject                                                                 |
| ---------- | ----------------------------------------------------------------------- |
| `020ec7b7` | `fix(ci): align cli-smoke-test.sh with py3.10+ matrix and PEP 517 install` |

Conventional commit, subject 72 chars (≤78), body wrapped at ≤67 chars.

## Verification

All seven plan-defined checks pass:

| # | Check                                                                                | Result    |
| - | ------------------------------------------------------------------------------------ | --------- |
| 1 | `bash -n scripts/cli-smoke-test.sh` exit 0                                           | PASS      |
| 2 | `grep -n '3\.9' scripts/cli-smoke-test.sh` empty                                     | PASS      |
| 3 | `grep -n 'setup\.py install' scripts/cli-smoke-test.sh` empty                        | PASS      |
| 4 | `grep -n 'pip install setuptools' scripts/cli-smoke-test.sh` empty                   | PASS      |
| 5 | `grep -n 'pip install \.' scripts/cli-smoke-test.sh` returns line 52                 | PASS      |
| 6 | `PYTHON_VERSIONS` line 5: guard preserved + new matrix `"3.10 3.11 3.12 3.13 3.14"`  | PASS      |
| 7 | `CEMENT_VERSION` line 4: guard + value unchanged                                     | PASS      |

`git status` shows only `scripts/cli-smoke-test.sh` as modified+committed; diff is exactly two hunks (line-5 swap, lines-52–54 collapse). No collateral changes.

The full end-to-end docker-driven matrix run is the GitHub Actions cli-smoke-test job — final pass/fail moves to CI on next push of the `modernization-phase-1` branch.

## Deviations from Plan

None — plan executed exactly as written.

## Phase-1 Retrospective Note

`scripts/cli-smoke-test.sh` was the one PYVER-01/02 surface not caught by `01-VERIFICATION.md` (which scored 11/11 on its grep audit). The verification surface checklist for the next milestone should expand to include `scripts/**` shell drivers — they tend to encode python-version assumptions outside the pyproject.toml / GitHub Actions / Dockerfile triad. Recording this as a phase-1 retrospective input rather than a process change in this quick task.

## Requirements Closed

- PYVER-01 — Python 3.9 dropped from default matrix in cli-smoke-test driver.
- PYVER-02 — Generated-project install path no longer relies on deprecated `setup.py install` / `distutils`; PEP 517 path works on Python 3.12+.

## Self-Check: PASSED

- `scripts/cli-smoke-test.sh` modified file: FOUND (verified via `git status` and `git diff`)
- Commit `020ec7b7`: FOUND (verified via `git log -1 --oneline`)
- SUMMARY.md will be at `.planning/quick/260430-3b0-fix-scripts-cli-smoke-test-sh-drop-py3-9/260430-3b0-SUMMARY.md` (this file)
