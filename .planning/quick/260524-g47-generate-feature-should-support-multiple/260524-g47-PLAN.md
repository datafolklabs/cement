---
quick_id: 260524-g47
type: execute
wave: 1
depends_on: []
autonomous: true
description: |
  Generate feature should support multiple valid inputs to enable prompt
  (GH Issue #779) — extend cement/ext/ext_generate.py with `prompt_mode:
  select` schema, silent variables (`prompt: false`), demo coverage, and
  CHANGELOG entry.
issue: 779
related_pr: 768
files_modified:
  - cement/ext/ext_generate.py
  - tests/ext/test_ext_generate.py
  - tests/data/templates/generate/test16/.generate.yml
  - tests/data/templates/generate/test16/take-me
  - tests/data/templates/generate/test16/branch-1-only
  - tests/data/templates/generate/test16/branch-2-only
  - tests/data/templates/generate/test16/branch-N-file
  - tests/data/templates/generate/test17/.generate.yml
  - tests/data/templates/generate/test17/take-me
  - tests/data/templates/generate/test18/.generate.yml
  - tests/data/templates/generate/test18/take-me
  - tests/data/templates/generate/test19/.generate.yml
  - tests/data/templates/generate/test19/take-me
  - tests/data/templates/generate/test19/file-rendered-with-silent-var
  - tests/data/templates/generate/test20/.generate.yml
  - tests/data/templates/generate/test20/take-me
  - tests/data/templates/generate/test21/.generate.yml
  - tests/data/templates/generate/test21/take-me
  - tests/data/templates/generate/test22/.generate.yml
  - tests/data/templates/generate/test22/take-me-silent
  - tests/data/templates/generate/test23/.generate.yml
  - tests/data/templates/generate/test23/take-me
  - tests/data/templates/generate/test24/.generate.yml
  - tests/data/templates/generate/test24/take-me
  - tests/data/templates/generate/test25/.generate.yml
  - tests/data/templates/generate/test25/take-me
  - demo/generate-features/README.md
  - demo/generate-features/templates/generate/webapp/.generate.yml
  - demo/generate-features/templates/generate/webapp/requirements.txt
  - demo/generate-features/templates/generate/webapp/wsgi.py
  - CHANGELOG.md

requirements: [issue-779]

notes:
  adjudication_pitfall_a: |
    IN-SCOPE. Generalize `block.get('variables', [])` →
    `block.get('variables') or []` for BOTH the legacy boolean merge path
    (current ext_generate.py:100-102) AND the new select-mode merge.
    Rationale: extends commit 6895aa52's defensive idiom (already applied
    to lines 125-127 at top-level YAML keys) to the per-block keys for
    consistency. Zero behavior change against the current in-tree fixture
    matrix (none of test1..test15 ship `enabled: { variables: null }`).
    Cost: 3 lines. Task 2 acceptance requires both paths to use the
    `or []` form; task 1 fixtures (test23/test24) exercise the
    null-coalesce explicitly on both paths.
  adjudication_dockerignore_gap: |
    OUT-OF-SCOPE. Pre-existing demo inconsistency (README lists
    `.dockerignore` as generated; demo template directory does not ship
    one). Not caused by GH #779. Recorded as a future quick-task seed but
    NOT addressed here — fixing pre-existing demo inconsistencies
    inflates quick-task scope.
  adjudication_a5_pragma_placement: |
    LOCKED. The unmatched-input `AssertionError` raise (Decision B in
    CONTEXT.md) MUST NOT carry `# pragma: nocover`. It is exercised by
    test_generate_features_select_unmatched_input via
    `patch.object(shell.Prompt, 'prompt', return_value=...)` — same idiom
    as existing test_no_default at tests/ext/test_ext_generate.py:89-97.
    Structurally: the `assert val in valid_values` line MUST be placed
    OUTSIDE the `# pragma: nocover` block surrounding the new
    `FeaturePrompt` class + `p.prompt()` call so coverage tracks it. Per
    RESEARCH.md A5 mitigation, the executor must NOT reflexively pragma
    this line after seeing the surrounding `# pragma: nocover`
    precedent at ext_generate.py:162/171/197.

must_haves:
  truths:
    - "A feature with `prompt_mode: select` dispatches into one of N `selected` branches keyed by the user's prompt input (or by feature-level `default` under `--defaults`)"
    - "A feature WITHOUT `prompt_mode` keeps byte-identical behavior with current HEAD (regression: every existing test1..test15 fixture and the demo `webapp` template walk through the new code without change)"
    - "A variable with `prompt: false` is silent — the variable's value is set to `default` without rendering a prompt; `case` + `validate` still apply to `default`"
    - "Schema-collision (`prompt_mode: select` AND `enabled`/`disabled`) raises `ValueError` at config-validation time with a message naming both offending keys"
    - "Invalid `prompt_mode` (anything other than `select` or absent) raises `ValueError` at config-validation time"
    - "Empty/missing `selected:` under `prompt_mode: select` raises `ValueError`"
    - "A `selected` branch missing `value:` raises `ValueError`"
    - "Feature-level `default` not in `selected` values raises `ValueError` at config-validation time"
    - "Runtime input that passes `validate` regex but does not match any `selected` value raises `AssertionError` (no pragma — it is testable via mocked Prompt and MUST be covered)"
    - "100% line coverage is held — all 13 new branches enumerated in RESEARCH.md §4 are exercised by a test OR carry an explicit `# pragma: nocover  # <locked-vocabulary>` per Phase 03 Plan 07 categories"
    - "Demo `webapp/.generate.yml` exercises the new `prompt_mode: select` schema via a new `web_framework` feature (N/Flask/FastAPI) without altering the existing `docker` + `docker_compose` features"
    - "CHANGELOG.md has one new line under `## 3.0.15 - DEVELOPMENT` → `Features:` bucket, `[ext.generate]` area tag, ≤78 chars, linking GH Issue #779"
  artifacts:
    - path: "cement/ext/ext_generate.py"
      provides: "Three edit zones from RESEARCH.md §1 grafted in: validation pass, _resolve closure, merge step; plus variable-loop short-circuit on `prompt: false`"
      contains: "prompt_mode == 'select'"
    - path: "tests/ext/test_ext_generate.py"
      provides: "10 new test functions covering: select happy path (`--defaults`), validate regex pass-through, case normalization, silent variable, schema collision, invalid prompt_mode, empty selected, missing branch value, default-not-in-values, unmatched input runtime AssertionError, top-level silent variable, requires-cascade-with-select, null-safe `or []` on legacy boolean merge path, null-safe `or []` on select-mode merge path"
      contains: "def test_generate_features_select_"
    - path: "tests/data/templates/generate/test16/.generate.yml"
      provides: "Select-mode happy-path fixture (3 branches: N/1/2)"
      contains: "prompt_mode: select"
    - path: "tests/data/templates/generate/test19/.generate.yml"
      provides: "Silent variable fixture (`prompt: false` under selected, `case: upper` on feature prompt)"
      contains: "prompt: false"
    - path: "tests/data/templates/generate/test22/.generate.yml"
      provides: "Top-level silent variable fixture (`prompt: false` outside selected)"
      contains: "prompt: false"
    - path: "tests/data/templates/generate/test23/.generate.yml"
      provides: "Null-safe regression fixture for LEGACY boolean merge (`enabled: { variables: null }`) — exercises Pitfall A generalization"
      contains: "variables: null"
    - path: "tests/data/templates/generate/test24/.generate.yml"
      provides: "Null-safe regression fixture for SELECT mode merge (`selected: [{value: X, variables: null}]`) — exercises Pitfall A in select path"
      contains: "variables: null"
    - path: "tests/data/templates/generate/test25/.generate.yml"
      provides: "Requires-cascade-with-select fixture: select-mode feature has `requires:` a disabled boolean feature → no branch dispatched"
      contains: "requires:"
    - path: "demo/generate-features/templates/generate/webapp/.generate.yml"
      provides: "Demo extended with new `web_framework` feature using prompt_mode: select (ADDITIVE — existing `docker` and `docker_compose` features byte-identical)"
      contains: "web_framework"
    - path: "demo/generate-features/templates/generate/webapp/requirements.txt"
      provides: "Jinja2-templated file rendered by select branches 1/2, ignored by branch N"
      contains: "{{ framework_name }}"
    - path: "demo/generate-features/templates/generate/webapp/wsgi.py"
      provides: "Jinja2-templated file rendered by select branches 1/2, ignored by branch N"
      contains: "{{ framework_name }}"
    - path: "demo/generate-features/README.md"
      provides: "New 'Multi-Valued Feature (prompt_mode: select)' section + extended 'What to Expect' with one new branch-picked tree"
      contains: "prompt_mode: select"
    - path: "CHANGELOG.md"
      provides: "One Features-bucket line under 3.0.15 — DEVELOPMENT"
      contains: "[ext.generate]"
  key_links:
    - from: "cement/ext/ext_generate.py:_process_features validation pass"
      to: "cement/ext/ext_generate.py:_process_features `_resolve` closure"
      via: "feature_states widened from dict[str, bool] to dict[str, bool | str]; only `prompt_mode == 'select'` populates the str variant"
      pattern: "feature_states\\[name\\]"
    - from: "cement/ext/ext_generate.py:_process_features `_resolve` closure"
      to: "cement/ext/ext_generate.py:_process_features merge step"
      via: "isinstance(state, bool) branch (legacy) vs str branch (select), with select branch walking `selected` for the matching value"
      pattern: "isinstance\\(state, bool\\)"
    - from: "cement/ext/ext_generate.py:_generate variable loop (lines 147-186 today)"
      to: "Silent variable short-circuit"
      via: "`if var['prompt'] is False:` short-circuit, with `case`/`validate` still applied via shared post-resolve block (lines 173-184 byte-identical)"
      pattern: "var\\['prompt'\\] is False"
    - from: "tests/ext/test_ext_generate.py:test_generate_features_select_unmatched_input"
      to: "AssertionError raise (no pragma)"
      via: "patch.object(shell.Prompt, 'prompt', return_value=...) returns a value passing the validate regex but absent from `selected` values"
      pattern: "patch.object\\(shell.Prompt"
---

<objective>
Extend `cement/ext/ext_generate.py` (PR #768 schema) with a new
multi-valued feature mode (`prompt_mode: select`) plus silent
variables (`prompt: false`), closing GH Issue #779. Zero public-API
breakage on the 3.0.x track: every existing in-tree `.generate.yml`
(demo `webapp` + `tests/data/templates/generate/test1..test15`)
walks through the new code byte-identically. 100% line coverage
held. Demo and CHANGELOG updated.

Purpose: GH Issue #779 — current feature prompt is boolean-only
(`Enable Feature: name [Y/n]`); template authors who need a
three-way (or N-way) toggle have no clean path. This adds an
explicit `prompt_mode: select` opt-in that dispatches to one of N
named branches keyed by the user's input (or by feature-level
`default` under `--defaults`).

Output: One implementation + tests + fixtures atomic commit, one
demo+README commit, one CHANGELOG commit. PR #780 (target) closes
#779.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/derks/Development/DFL/cement/.planning/quick/260524-g47-generate-feature-should-support-multiple/260524-g47-CONTEXT.md
@/Users/derks/Development/DFL/cement/.planning/quick/260524-g47-generate-feature-should-support-multiple/260524-g47-RESEARCH.md
@/Users/derks/Development/DFL/cement/.planning/PROJECT.md
@/Users/derks/Development/DFL/cement/CLAUDE.md
@/Users/derks/Development/DFL/cement/cement/ext/ext_generate.py
@/Users/derks/Development/DFL/cement/tests/ext/test_ext_generate.py
@/Users/derks/Development/DFL/cement/demo/generate-features/README.md
@/Users/derks/Development/DFL/cement/demo/generate-features/templates/generate/webapp/.generate.yml
@/Users/derks/Development/DFL/cement/tests/data/templates/generate/test6/.generate.yml
@/Users/derks/Development/DFL/cement/CHANGELOG.md

<interfaces>
<!-- Key signatures + line ranges the executor needs. Extracted from HEAD. -->
<!-- Executor should target these EXACT line ranges from RESEARCH.md §1. -->

From cement/ext/ext_generate.py (current HEAD):

- Lines 29-48: `_process_features(self, features, vars, exclude_list, ignore_list, data) -> None`
  - Lines 35-42: Validation loop 1 (feature_by_name build, raises ValueError on missing `name`)
  - Lines 43-48: Validation loop 2 (raises ValueError on unknown `requires`)
  - **Edit Zone A grafts AFTER line 48** — adds validation loop 3 for select-mode schema
- Lines 56: `feature_states: dict[str, bool] = {}`
  - **Edit Zone B widens type:** `feature_states: dict[str, bool | str] = {}`
- Lines 58-84: `_resolve(name: str) -> bool` closure
  - **Edit Zone B grafts on `feature.get('prompt_mode') == 'select'`** — closure return type
    widens to `bool | str`; legacy boolean branch at lines 69-83 stays BYTE-IDENTICAL
- Lines 89-102: Merge step (block_key dispatch on enabled/disabled)
  - **Edit Zone C grafts isinstance(state, bool) branch** — legacy bool path unchanged
    in behavior; new str path walks `selected` for matching `value`
  - **Pitfall A: lines 100-102 generalize `block.get('variables', [])` →
    `block.get('variables') or []` (and same for `exclude`, `ignore`)** —
    applied to BOTH legacy and select-mode merges
- Lines 150-152: `for key in ['name', 'prompt']: assert var[key] is not None`
  - **Edit Zone D leaves byte-identical** — `is not None` already accepts `False`
- AFTER line 152 (before line 154): **Edit Zone D grafts** `if var['prompt'] is False:`
  short-circuit setting `val = var['default']` (with `assert var['default'] is not None`)

From tests/ext/test_ext_generate.py (test idioms to mirror):

- Line 10: `def exists_join(*paths): return os.path.exists(os.path.join(*paths))`
- Lines 19-25: `GenerateApp(TestApp)` test app harness
- Lines 89-97: `test_no_default` — `patch.object(shell.Prompt, 'prompt', return_value='Bogus')`
  idiom (REUSE for unmatched-input AssertionError test)
- Lines 226-231: `test_generate_features_missing_name` — `raises(ValueError, match='...')`
  idiom (REUSE for all schema-validation tests)
- Lines 80-86: `test_invalid_variable_value` — `raises(AssertionError, match=...)` via
  `--defaults` (REUSE for variable-level assertions in silent-var branch)
</interfaces>

<adjudications>
**Pitfall A (RESEARCH.md §6, Assumption A3) — null-safety scope.** IN-SCOPE.
Generalize `block.get('variables', [])` → `block.get('variables') or []`
for BOTH legacy boolean merge (current lines 100-102) AND new select-mode
merge. Extends commit 6895aa52's defensive idiom (already in lines 125-127
at top-level YAML keys) to per-block keys. Zero behavior change against
the in-tree fixture matrix. Cost: 3 lines. Task 1 ships test23
(`enabled: { variables: null }`) + test24 (`selected: [{value: X,
variables: null}]`) as regression fixtures exercising both paths.

**Pitfall F (RESEARCH.md §6, Assumption A4) — demo `.dockerignore` gap.**
OUT-OF-SCOPE. Pre-existing demo README lists `.dockerignore` as generated
but the template directory ships only Dockerfile/app.py/docker-compose.yml/
README.md. Not caused by GH #779. Captured as future quick-task seed
(record in commit body if helpful; do NOT add the file in this task).

**Assumption A5 (RESEARCH.md §3c) — unmatched-input AssertionError
pragma placement.** LOCKED. The `assert val in valid_values` line in
the new select-mode `_resolve` closure MUST be placed OUTSIDE the
`# pragma: nocover` block that wraps the new `FeaturePrompt` class +
`p.prompt()` call. Coverage MUST track this assertion. It is exercised
by `test_generate_features_select_unmatched_input` via
`patch.object(shell.Prompt, 'prompt', return_value='X')` where X passes
the `validate` regex but is not in `selected` values. Executor MUST NOT
reflexively add `# pragma: nocover` to this line.
</adjudications>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement prompt_mode: select + silent variables + tests + fixtures (atomic)</name>
  <files>
    cement/ext/ext_generate.py,
    tests/ext/test_ext_generate.py,
    tests/data/templates/generate/test16/.generate.yml,
    tests/data/templates/generate/test16/take-me,
    tests/data/templates/generate/test16/branch-1-only,
    tests/data/templates/generate/test16/branch-2-only,
    tests/data/templates/generate/test16/branch-N-file,
    tests/data/templates/generate/test17/.generate.yml,
    tests/data/templates/generate/test17/take-me,
    tests/data/templates/generate/test18/.generate.yml,
    tests/data/templates/generate/test18/take-me,
    tests/data/templates/generate/test19/.generate.yml,
    tests/data/templates/generate/test19/take-me,
    tests/data/templates/generate/test19/file-rendered-with-silent-var,
    tests/data/templates/generate/test20/.generate.yml,
    tests/data/templates/generate/test20/take-me,
    tests/data/templates/generate/test21/.generate.yml,
    tests/data/templates/generate/test21/take-me,
    tests/data/templates/generate/test22/.generate.yml,
    tests/data/templates/generate/test22/take-me-silent,
    tests/data/templates/generate/test23/.generate.yml,
    tests/data/templates/generate/test23/take-me,
    tests/data/templates/generate/test24/.generate.yml,
    tests/data/templates/generate/test24/take-me,
    tests/data/templates/generate/test25/.generate.yml,
    tests/data/templates/generate/test25/take-me
  </files>

  <behavior>
    Implementation + tests + fixtures land as ONE atomic commit because (a)
    100% coverage gate fails if tests land before implementation, (b)
    implementation without tests has no acceptance signal under the
    coverage gate. The fixture content files are needed for the tests to
    have file-presence/absence assertions to verify against.

    Test names + invocations (these are the EXACT 13 new tests; verifier
    will grep-count `def test_generate_features_select_` + 1 top-level
    silent test + 2 null-coalesce tests + 1 requires-cascade-with-select
    test in test_ext_generate.py and assert >= 13 new tests):

    1. test_generate_features_select_defaults (test16): `--defaults` picks
       branch "2", asserts `branch-2-only` present + `branch-1-only` and
       `branch-N-file` absent (covers RESEARCH.md §4 rows 1, 7, 8, 9 —
       the populated-`variables`-list path).
    2. test_generate_features_select_collision (test17): `prompt_mode:
       select` AND `enabled:` block coexist → `raises(ValueError,
       match="'enabled'/'disabled' blocks are not allowed")` (row 3).
    3. test_generate_features_select_invalid_prompt_mode: fixture with
       `prompt_mode: bogus` → `raises(ValueError, match="invalid
       prompt_mode")` (row 2). Inline-fixture or new dir
       (use test18 alt or new test number — `test_invalid_prompt_mode`
       MAY share test17's dir by overriding via a new `.generate.yml`).
       SIMPLEST: introduce as inline test using `prompt_mode: bogus` in
       a dedicated fixture if the test count budget allows; otherwise
       fold into test17 with a parametric variant. Recommended: a new
       fixture at test18 reserved name above is FREE for this case OR
       fold into test17 as a second `.generate.yml` variant.
       FINAL CHOICE: Reuse the test18 slot reserved in `files_modified`
       above for `test_generate_features_select_invalid_prompt_mode`
       (the unmatched-input test does not need its own fixture — it
       runs against test16 with a mocked Prompt return value).
    4. test_generate_features_select_unmatched_input: REUSES test16
       fixture; runs WITHOUT `--defaults`; mocks `shell.Prompt.prompt`
       to return "3" (passes test16's validate regex `^[N123]$` per
       fixture definition — see fixture spec below — but is absent
       from `selected: [{value: "N"}, {value: "1"}, {value: "2"}]`) →
       `raises(AssertionError, match="must be one of")` (row 13).
       ⚠ The `assert val in valid_values` line MUST be coverable
       (i.e. NOT inside the pragma block) per adjudication A5.
    5. test_generate_features_select_silent_variable (test19): select-mode
       feature with `prompt: false` on a variable under one selected
       branch + `case: upper` on the feature-level prompt + `--defaults`;
       asserts the rendered file contains the silent variable's default
       value verbatim (row 10).
    6. test_generate_features_select_default_not_in_values (test20):
       fixture where feature `default: "X"` but `selected` only contains
       `value: "Y"` → `raises(ValueError, match="is not in selected
       values")` (row 6).
    7. test_generate_features_select_missing_value (test21): fixture with
       a `selected:` branch missing `value:` → `raises(ValueError,
       match="missing required key: value")` (row 5).
    8. test_generate_features_select_empty_selected: fixture with
       `prompt_mode: select` and `selected: []` → `raises(ValueError,
       match="no 'selected' branches")` (row 4). REUSE one of the
       reserved fixture slots above (test21's dir can hold a second
       `.generate.yml` variant, OR add inline — RECOMMEND: add a new
       per-test fixture under the reserved test21 number if needed; the
       simplest path is to use test21's fixture for the missing-value
       case and add `tests/data/templates/generate/test21b/` for the
       empty-list case. PIVOT: just add it as a separate fixture under
       a slot reserved in `files_modified`. The fixture count `test16..
       test25` reserves slots; pick whichever number is free).
    9. test_generate_features_select_no_default_branch: silent-var
       branch's `assert var['default'] is not None` → fixture with
       `prompt: false` and no `default:` → `raises(AssertionError,
       match="prompt: false but no default")` (row 11). Use a new
       fixture (any free slot in the reserved range).
    10. test_generate_features_top_level_silent_variable (test22):
        top-level `variables: [{name: x, prompt: false, default: "v"}]`
        (NOT under a feature) → asserts the silent default lands in
        the rendered file (row 12, CONTEXT.md "Silent vars usable
        outside `selected`").
    11. test_generate_features_legacy_null_variables (test23):
        regression — `enabled: { variables: null }` on a boolean
        feature with `default: true` and `--defaults`; the new `or []`
        on the legacy merge path must coalesce the None safely (covers
        Pitfall A on the legacy path).
    12. test_generate_features_select_null_variables (test24):
        regression — `selected: [{value: "1", variables: null}]` with
        feature `default: "1"` and `--defaults`; the new `or []` on
        the select merge path must coalesce the None safely (covers
        Pitfall A on the select path, RESEARCH.md §4 row 9 explicit).
    13. test_generate_features_select_requires_cascade (test25):
        select-mode feature has `requires: [boolean_feature]` where
        the boolean feature is `default: false`; the cascade must
        return `state is False` for the select feature and the merge
        step must fall into the `isinstance(state, bool) and state is
        False` branch (legacy disabled path, which the validation step
        guarantees is empty `{}`); asserts that NO `selected` branch's
        files appear in the output (Pitfall B verification).

    NOTE on test count vs files_modified slots: `files_modified` reserves
    10 fixture directories (test16..test25). Some tests (the inline-fixture
    cases — empty-selected, no-default-on-silent-var, invalid-prompt_mode,
    unmatched-input) reuse or share directories. The executor may
    consolidate by adding `*.generate.yml`-only fixtures (no content files
    needed when the test only asserts a ValueError raise before any copy
    occurs). The verifier checks: every test listed above exists in
    test_ext_generate.py AND `pytest --cov` passes at 100%.

    Behavior contracts (every branch enumerated in RESEARCH.md §4):

    - Branch 2 (mode != 'select' raise): test_invalid_prompt_mode
    - Branch 3 (collision raise): test_select_collision (test17)
    - Branch 4 (empty selected raise): test_select_empty_selected
    - Branch 5 (missing value raise): test_select_missing_value (test21)
    - Branch 6 (default not in values raise): test_select_default_not_in_values (test20)
    - Branch 7 (mode == 'select' + --defaults assignment): test_select_defaults (test16)
    - Branch 8 (select-mode merge walks selected): test_select_defaults (test16) — implicit via file presence
    - Branch 9 (variables `or []` null-coalesce on select path): test_select_null_variables (test24)
    - Branch 10 (variable-loop short-circuit on prompt is False): test_select_silent_variable (test19) + test_top_level_silent_variable (test22)
    - Branch 11 (silent-var assert var['default'] is not None): test_select_no_default_branch
    - Branch 12 (top-level silent var): test_top_level_silent_variable (test22)
    - Branch 13 (unmatched-input assert val in valid_values): test_select_unmatched_input (no pragma)

    Pragma-able branch (the new interactive FeaturePrompt class body +
    `p.prompt()` call, lines analogous to current 73-83): `# pragma:
    nocover  # defensive: unreachable` per Phase 03 Plan 07 locked
    vocabulary. The case-normalization, validate-regex, and
    value-in-`selected` assertion lines MUST be OUTSIDE this pragma
    block (Decision B + Adjudication A5).

    Fixture content for test16 (the keystone happy-path fixture):

      tests/data/templates/generate/test16/.generate.yml:
        ---
        variables:
            -   name: app_name
                prompt: App Name
                default: myapp
        features:
            -   name: feature_choice
                prompt_mode: select
                prompt: "Pick [(N)one/1=v1/2=v2]"
                validate: "^[N12]$"
                case: upper
                default: "2"
                selected:
                    -   value: "N"
                        ignore:
                            - '.*branch-1-only.*'
                            - '.*branch-2-only.*'
                    -   value: "1"
                        variables:
                            -   name: silent_var
                                prompt: false
                                default: "v1"
                        ignore:
                            - '.*branch-2-only.*'
                            - '.*branch-N-file.*'
                    -   value: "2"
                        variables:
                            -   name: silent_var
                                prompt: false
                                default: "v2"
                        ignore:
                            - '.*branch-1-only.*'
                            - '.*branch-N-file.*'

      tests/data/templates/generate/test16/take-me — single-line
      Jinja2 file rendering `{{ app_name }}`.
      tests/data/templates/generate/test16/branch-1-only — empty
      file (marker for branch "1" assertions).
      tests/data/templates/generate/test16/branch-2-only — empty
      file (marker for branch "2" assertions).
      tests/data/templates/generate/test16/branch-N-file — empty
      file (marker for branch "N" assertions).

    NOTE: test_select_unmatched_input reuses test16's validate regex
    `^[N12]$` — the mocked Prompt MUST return a value that passes
    this regex but is not in `["N", "1", "2"]`. Per the regex, the
    only such values are NONE — the regex by design only accepts
    chars that ARE in `selected`. ADJUSTMENT: widen test16's validate
    to `^[N123]$` so the unmatched-input test can pass `"3"` (regex
    OK, dispatch fail). OR (cleaner) — give the unmatched-input test
    its own fixture with a deliberately-loose regex `^[A-Z0-9]+$` and
    selected `["N", "1", "2"]`. RECOMMENDED: separate fixture. Use a
    reserved slot under the `files_modified` range.
  </behavior>

  <action>
**Edit Zone A — fast-fail validation pass** (per RESEARCH.md §1, in
`cement/ext/ext_generate.py` `_process_features`, AFTER current line 48):

Append a third validation loop that enforces (per CONTEXT.md
Decisions A/D and RESEARCH.md §1 zone A):

1. Skip features where `feature.get('prompt_mode') is None` (legacy
   boolean — no further checks; covers RESEARCH.md §4 row 1, exercised
   by existing test6..15 invocations).
2. Raise `ValueError(f"Feature '{name}' has invalid prompt_mode '{mode}'
   (must be 'select' or omitted)")` if mode is not None and not
   `'select'` (row 2, covered by test_invalid_prompt_mode fixture).
3. Raise `ValueError(f"Feature '{name}' uses prompt_mode: select;
   'enabled'/'disabled' blocks are not allowed in this mode")` if
   `'enabled' in feature or 'disabled' in feature` (row 3, covered by
   test17).
4. Raise `ValueError(f"Feature '{name}' uses prompt_mode: select but
   has no 'selected' branches")` if `selected := feature.get('selected')
   or []` is empty (row 4, covered by empty-selected fixture).
5. For each branch: raise `ValueError(f"Feature '{name}' has a 'selected'
   branch missing required key: value")` if `branch.get('value') is
   None` (row 5, covered by test21).
6. Build `values: list[str] = [str(branch['value']) for branch in
   selected]` — explicit `str()` cast per Pitfall C (YAML may decode
   unquoted `1` as `int`).
7. If `feature.get('default') is not None` and `str(feature['default'])
   not in values`: raise `ValueError(f"Feature '{name}' default
   '{feature['default']}' is not in selected values {values}")` (row 6,
   covered by test20).

**Edit Zone B — `_resolve` closure** (REPLACING current lines 69-83):

Widen `feature_states` type annotation at current line 56 from
`dict[str, bool]` to `dict[str, bool | str]`. Closure return type widens
to `bool | str` correspondingly.

Branch on `feature.get('prompt_mode') == 'select'`:

- Select path:
  - If `self.app.pargs.defaults`: `feature_states[name] = str(default)`
    (row 7, covered by test16 with `--defaults`).
  - Else (interactive): construct a new `FeaturePrompt` subclass of
    `shell.Prompt` (mirroring lines 76-83 today), call `p.prompt()`,
    then apply (per Pitfall D, in this exact order):
    1. `case` normalization: `if feature.get('case') in ['lower',
       'upper', 'title']: val = getattr(val, feature['case'])()`
    2. `validate` regex: `if feature.get('validate') is not None:
       assert re.match(feature['validate'], val), f"Invalid Response
       (must match: '{feature['validate']}')"`
  - Then unconditionally (OUTSIDE any pragma block, per Adjudication A5):
    `valid_values = [str(b['value']) for b in feature.get('selected')
    or []]` then `assert val in valid_values, f"Invalid Response (must
    be one of: {valid_values})"` (row 13, covered by
    test_select_unmatched_input).
  - `feature_states[name] = val`.
  - **Pragma policy on the interactive select branch:** the
    `FeaturePrompt` class body + `p = FeaturePrompt(auto=False)` + `val
    = p.prompt() or str(default)` lines carry `# pragma: nocover  #
    defensive: unreachable` mirroring current lines 73-83. The
    case/validate/in-`selected` assertion lines MUST be OUTSIDE this
    pragma block so coverage tracks them through the mocked-Prompt
    path of test_select_unmatched_input.
- Legacy boolean path (the `else` branch): KEEP byte-identical to
  current lines 69-83. The `bool(feature.get('default', False))`
  coercion at line 69 stays INSIDE the legacy branch only (per Pitfall E).

**Edit Zone C — merge step** (REPLACING current lines 89-102):

Split the merge on `isinstance(feature_states[name], bool)`:

- bool branch (legacy): `block_key = 'enabled' if state else
  'disabled'; block = feature.get(block_key) or {}` — byte-identical to
  current lines 97-98.
- str branch (select): walk `feature.get('selected') or []` for the
  branch where `str(branch['value']) == state` and set `block =
  branch`. The validation pass (Edit Zone A row 5/6) guarantees a
  match.

Then unify the three list-extensions (Pitfall A — apply to BOTH paths):

```python
vars.extend(block.get('variables') or [])
exclude_list.extend(block.get('exclude') or [])
ignore_list.extend(block.get('ignore') or [])
```

This generalizes commit 6895aa52 to the per-block keys.

Also: `data['features'][name] = state` keeps the existing exposure
of feature state to downstream Jinja templates (bool OR str — both
fine to render).

**Edit Zone D — variable loop** (current lines 147-186):

Two surgical edits:

1. AT current line 150: leave the `for key in ['name', 'prompt']`
   assertion BYTE-IDENTICAL — `assert var[key] is not None` already
   accepts `False`. No change needed.
2. AFTER the assertion block (between current lines 152 and 154),
   short-circuit on `prompt is False`:

   ```python
   if var['prompt'] is False:
       assert var['default'] is not None, \
           f"Variable '{var['name']}' has prompt: false but no default"
       val = var['default']
   else:
       # existing lines 154-171 unchanged, indented under else:
       val: Any = None
       if var['default'] is not None and self.app.pargs.defaults:
           val = var['default']
       elif var['default'] is not None:
           default_text = f" [{var['default']}]"
       else:
           default_text = ''   # pragma: nocover  # defensive: unreachable
       if val is None:
           class MyPrompt(shell.Prompt):
               class Meta:
                   text = f"{var['prompt']}{default_text}:"
                   default = var.get('default', None)
           p = MyPrompt()
           val = p.prompt()    # pragma: nocover  # defensive: unreachable
   ```

3. KEEP current lines 173-184 (case + validate post-resolve) BYTE-
   IDENTICAL after this re-indentation — they apply to `val` regardless
   of which branch produced it. This delivers CONTEXT.md's "Validate /
   case interaction on silent vars: reusing it, not branching from it"
   guarantee for free.

**Tests + fixtures** — see `<behavior>` block above for the full list
of 13 tests + their fixture layouts. Add tests to
`tests/ext/test_ext_generate.py` at the end of the file (after current
line 367), in the same idiomatic style as `test_generate_features_*`.
Use the `exists_join` helper (line 10) and the `GenerateApp` harness
(lines 19-25). For ValueError tests use `raises(ValueError,
match='...')` (line 230 pattern). For the mocked-prompt unmatched-input
test use `patch.object(shell.Prompt, 'prompt', return_value=...)`
(line 90 pattern).

Reserve fixture slots `tests/data/templates/generate/test16..test25/`
(10 slots). The fixture files for ValueError-raising tests need only
contain a `.generate.yml` (the error fires before any copy happens) +
a `take-me` file (so jinja2 has at least one content file to attempt
on the successful-validation path). Happy-path fixtures need the full
content file set per the behavior block.

**Commit message** (Conventional Commits, ≤78 char subject, ≤78 char
body wrap):

```
feat(ext.generate): add prompt_mode: select for multi-valued features

Extend the feature schema introduced in PR #768 with an explicit
`prompt_mode: select` opt-in that dispatches to one of N `selected:`
branches keyed by the user's prompt input (or by the feature-level
`default` under `--defaults`). Adds a complementary `prompt: false`
sentinel on variable definitions for silent (no-prompt) variables —
usable inside `selected` branches or at the top level.

Schema collisions (`prompt_mode: select` alongside `enabled:`/
`disabled:` blocks), invalid prompt_mode values, missing `value:` on
a `selected:` branch, empty `selected:` lists, and a feature `default`
not in `selected` values all raise `ValueError` at config-validation
time. A runtime input that passes the `validate` regex but is absent
from `selected` values raises `AssertionError`.

Generalize commit 6895aa52's `or []` null-safety idiom from the
top-level YAML keys to the per-feature merge step (legacy boolean
AND new select-mode), closing a latent fragility against
`variables: null` / `exclude: null` / `ignore: null` inside a feature
block.

Zero behavior change for templates not using `prompt_mode:` —
existing `tests/data/templates/generate/test1..test15` and the demo
`webapp` template walk the new code byte-identically. 100% line
coverage held; 13 new branches enumerated in the plan are exercised
by a test or carry an explicit `# pragma: nocover` with a Phase 03
Plan 07 locked-vocabulary category.

Refs: #779
```
  </action>

  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run pytest --cov=cement.ext.ext_generate --cov-fail-under=100 tests/ext/test_ext_generate.py -x</automated>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run pytest --cov=cement tests/ -x</automated>
    <automated>cd /Users/derks/Development/DFL/cement && make comply</automated>
    <automated>cd /Users/derks/Development/DFL/cement && make audit-public-api</automated>
  </verify>

  <done>
    - cement/ext/ext_generate.py contains the four edit zones (A, B, C,
      D) per RESEARCH.md §1, with line numbers shifted as the grafts
      land.
    - `feature_states` is annotated `dict[str, bool | str]` at line 56
      (or wherever it lands post-graft).
    - Legacy boolean path in `_resolve` closure (current lines 69-83) is
      BYTE-IDENTICAL — verified by `git diff` showing zero
      changes inside that branch's body.
    - Pitfall A generalization applied to BOTH merge paths (`block.get(
      'variables') or []` and same for exclude, ignore).
    - The `assert val in valid_values` line in the new select-mode
      _resolve closure carries NO `# pragma: nocover` annotation, and is
      exercised by test_generate_features_select_unmatched_input
      (verified by `grep -B2 -A1 "must be one of" cement/ext/ext_generate.py`
      showing no pragma in the surrounding lines, and `grep
      "select_unmatched_input" tests/ext/test_ext_generate.py` returning
      a hit).
    - 13 new tests pass; coverage at 100% on cement.ext.ext_generate
      AND on the cement package overall.
    - `make comply` (ruff + mypy) exits 0.
    - `make audit-public-api` exits 0 (no public-API surface change —
      the schema is internal to `_process_features` + the variable
      loop; no new exports).
    - All existing tests in test_ext_generate.py still pass (regression
      check — the demo webapp template and test6..test15 walk
      byte-identically).
    - One atomic commit: subject `feat(ext.generate): add prompt_mode:
      select for multi-valued features` (≤78 chars, verified by
      `git log -1 --format='%s' | wc -c`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend demo with web_framework select feature + README walkthrough</name>
  <files>
    demo/generate-features/README.md,
    demo/generate-features/templates/generate/webapp/.generate.yml,
    demo/generate-features/templates/generate/webapp/requirements.txt,
    demo/generate-features/templates/generate/webapp/wsgi.py
  </files>

  <action>
**Extend the demo additively** — the existing `docker` + `docker_compose`
features in `demo/generate-features/templates/generate/webapp/.generate.yml`
MUST stay byte-for-byte (verified by `git diff` showing only ADDITIONS
in that file). The existing demo content files
(`Dockerfile`, `app.py`, `docker-compose.yml`, `README.md`) stay
byte-identical.

**Add a third feature** to `.generate.yml` per RESEARCH.md §5:

```yaml
-   name: web_framework
    prompt_mode: select
    prompt: "Web Framework [(N)one/1=Flask/2=FastAPI]"
    validate: "^[N12]$"
    case: upper
    default: "N"
    selected:
        -   value: "N"
            ignore:
                - '.*requirements\.txt.*'
                - '.*wsgi\.py.*'
        -   value: "1"
            variables:
                -   name: framework_name
                    prompt: false
                    default: "flask"
                -   name: framework_version
                    prompt: false
                    default: "3.0"
            ignore:
                - '.*fastapi.*'
        -   value: "2"
            variables:
                -   name: framework_name
                    prompt: false
                    default: "fastapi"
                -   name: framework_version
                    prompt: false
                    default: "0.115"
            ignore:
                - '.*flask.*'
```

**Add two new template files** (Jinja2-templated):

`demo/generate-features/templates/generate/webapp/requirements.txt`:
```
{{ framework_name }}=={{ framework_version }}
```

`demo/generate-features/templates/generate/webapp/wsgi.py`:
```
# {{ framework_name }} wsgi entrypoint for {{ project_name }}
```

**Extend `demo/generate-features/README.md`**:

1. Update the "Template" section bullet list (currently lines 7-15) to
   add a third bullet for `web_framework`:
   ```
   - **Feature: `web_framework`** (default: `N` — none) — multi-valued
     select-mode feature; `N` (none) ignores `requirements.txt` and
     `wsgi.py`; `1` selects Flask (silent vars `framework_name=flask`,
     `framework_version=3.0`); `2` selects FastAPI (silent vars
     `framework_name=fastapi`, `framework_version=0.115`).
   ```

2. Insert a NEW section between "Configuration" (around line 32) and
   "What to Expect" (around line 67), titled **"Multi-Valued Feature
   (prompt_mode: select)"**, that walks through:
   - The new schema keys: `prompt_mode`, `validate`, `case`, `default`,
     `selected`.
   - How `case: upper` normalizes user input ("n", "N", "fLask" → "N",
     "N", "FLASK") BEFORE the `validate` regex runs and BEFORE the
     `selected` lookup. Cite the same case-then-validate-then-lookup
     ordering used for variables.
   - The `prompt: false` sentinel and what "silent" means — the
     variable's value is set to `default` without rendering a prompt.
     The variable's `case` and `validate` still apply if declared.
   - One concrete invocation:
     ```bash
     pdm run python myapp.py generate webapp /tmp/myproject --defaults
     # → web_framework = "N" (the feature-level `default`)
     # → requirements.txt and wsgi.py both ignored (not rendered)
     ```

3. Extend the "What to Expect" section with one new example tree
   showing `web_framework=1` (Flask) selected interactively — add it
   after the existing three trees:
   ```text
   **With docker + docker_compose enabled, web_framework=1 (Flask)**:

   /tmp/myproject/
   ├── .dockerignore
   ├── Dockerfile
   ├── README.md
   ├── app.py
   ├── docker-compose.yml
   ├── requirements.txt        # → flask==3.0
   └── wsgi.py                 # → flask wsgi entrypoint for myproject
   ```

   And one for `web_framework=N` (the default-defaults case):
   ```text
   **With all defaults** (docker on, docker_compose on, web_framework=N):

   /tmp/myproject/
   ├── .dockerignore
   ├── Dockerfile
   ├── README.md
   ├── app.py
   └── docker-compose.yml
   ```

   Note: the existing "With defaults" tree at lines 69-78 currently
   shows only the docker outputs. Replace its narrative caption to
   reflect that `web_framework=N` is now the default of the new feature.

**Sanity-check the demo runs end-to-end** (no automated test, but the
executor must verify locally):

```bash
cd demo/generate-features
pdm run python myapp.py generate webapp /tmp/g47-demo-test --defaults --force
# Verify: requirements.txt and wsgi.py are NOT present (default branch N
# ignores them).
ls /tmp/g47-demo-test
```

**Commit message** (Conventional Commits, ≤78 char subject):

```
docs(demo/generate-features): demo prompt_mode: select via web_framework

Extend the generate-features demo with a third feature using the new
`prompt_mode: select` schema landed in this branch — `web_framework`
with three branches (N=none, 1=Flask, 2=FastAPI). The N branch ignores
two new template files (requirements.txt, wsgi.py); the 1 and 2
branches each declare two silent variables (`prompt: false`) for
framework name and version, demonstrating the no-prompt sentinel.

The existing `docker` and `docker_compose` features stay byte-for-byte;
the demo content files (Dockerfile, app.py, docker-compose.yml,
README.md) are unchanged. Two new Jinja2 template files added
(requirements.txt, wsgi.py).

README extended with a new "Multi-Valued Feature (prompt_mode:
select)" section walking through the new schema keys, the case-then-
validate-then-lookup ordering, and the `prompt: false` sentinel.
"What to Expect" extended with two new example trees.

Refs: #779
```
  </action>

  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run python -c "import yaml; d=yaml.full_load(open('demo/generate-features/templates/generate/webapp/.generate.yml')); fs=[f['name'] for f in d['features']]; assert fs==['docker','docker_compose','web_framework'], fs; wf=[f for f in d['features'] if f['name']=='web_framework'][0]; assert wf['prompt_mode']=='select'; assert wf['default']=='N'; assert [b['value'] for b in wf['selected']]==['N','1','2']"</automated>
    <automated>test -f /Users/derks/Development/DFL/cement/demo/generate-features/templates/generate/webapp/requirements.txt && test -f /Users/derks/Development/DFL/cement/demo/generate-features/templates/generate/webapp/wsgi.py && grep -F "{{ framework_name }}" /Users/derks/Development/DFL/cement/demo/generate-features/templates/generate/webapp/requirements.txt && grep -F "{{ framework_name }}" /Users/derks/Development/DFL/cement/demo/generate-features/templates/generate/webapp/wsgi.py</automated>
    <automated>grep -q "prompt_mode: select" /Users/derks/Development/DFL/cement/demo/generate-features/README.md && grep -q "Multi-Valued Feature" /Users/derks/Development/DFL/cement/demo/generate-features/README.md && grep -q "prompt: false" /Users/derks/Development/DFL/cement/demo/generate-features/README.md</automated>
    <automated>cd /Users/derks/Development/DFL/cement/demo/generate-features && rm -rf /tmp/g47-demo-test && pdm run python myapp.py generate webapp /tmp/g47-demo-test --defaults && test ! -f /tmp/g47-demo-test/requirements.txt && test ! -f /tmp/g47-demo-test/wsgi.py && test -f /tmp/g47-demo-test/Dockerfile && rm -rf /tmp/g47-demo-test</automated>
    <automated>cd /Users/derks/Development/DFL/cement && git diff demo/generate-features/templates/generate/webapp/.generate.yml | grep -E "^-" | grep -v "^--- " | wc -l | grep -q "^0$"</automated>
  </verify>

  <done>
    - `web_framework` feature added to webapp `.generate.yml`; existing
      `docker` + `docker_compose` features byte-identical (verified by
      `git diff`: no `-` lines in the source-controlled block for those
      features).
    - Two new template files (`requirements.txt`, `wsgi.py`) created
      with Jinja2 placeholders for `framework_name`, `framework_version`,
      `project_name`.
    - README updated with: third bullet in Template section, new
      "Multi-Valued Feature (prompt_mode: select)" section between
      Configuration and What to Expect, and one new example tree under
      What to Expect for `web_framework=1` (Flask) + updated caption
      for the default tree noting `web_framework=N`.
    - End-to-end demo invocation succeeds with `--defaults` and
      produces the expected tree (no `requirements.txt`, no `wsgi.py`).
    - One atomic commit: subject `docs(demo/generate-features): demo
      prompt_mode: select via web_framework`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CHANGELOG entry under 3.0.15 Features bucket</name>
  <files>CHANGELOG.md</files>

  <action>
Open `/Users/derks/Development/DFL/cement/CHANGELOG.md` and locate the
`Features:` bucket of the active `## 3.0.15 - DEVELOPMENT (will be
released as stable/3.0.16)` section. Per the current file structure,
the `Features:` line is at line 157 and the existing bucket has two
entries (template_dirs config override at lines 159-165, ext.generate
optional features at lines 166-171).

**Append one new entry** at the end of the `Features:` bucket (before
the blank line that precedes `Refactoring:`), following the existing
multi-line indented style:

```
- `[ext.generate]` Add multi-valued feature support: `prompt_mode:
  select` dispatches a feature prompt to one of N `selected:` branches
  keyed by the user's input (or by the feature-level `default` under
  `--defaults`). Companion `prompt: false` sentinel on a variable
  definition declares silent (no-prompt) variables, usable inside
  `selected:` branches or at the top level. Existing boolean features
  are unchanged.
  - [Issue #779](https://github.com/datafolklabs/cement/issues/779)
```

The first line of the entry MUST fit within 78 characters total (i.e.
`- ` + content ≤78). Verify with: the head line is
`- \`[ext.generate]\` Add multi-valued feature support: \`prompt_mode:`
which is exactly 65 chars. Continuation lines wrap at 78 per the
existing entries in this section.

Do NOT touch any other section, bucket, or entry.

**Commit message** (Conventional Commits, ≤78 char subject):

```
docs(changelog): record prompt_mode: select feature for ext.generate

Append a Features-bucket line under 3.0.15 - DEVELOPMENT capturing the
new `prompt_mode: select` schema + `prompt: false` silent-variable
sentinel for `cement/ext/ext_generate.py`. Links GH Issue #779.

Refs: #779
```
  </action>

  <verify>
    <automated>grep -A1 "Features:" /Users/derks/Development/DFL/cement/CHANGELOG.md | head -20 && grep -c "prompt_mode: select" /Users/derks/Development/DFL/cement/CHANGELOG.md | grep -q "^[1-9]"</automated>
    <automated>awk '/^## 3.0.15/,/^## 3.0.14/' /Users/derks/Development/DFL/cement/CHANGELOG.md | grep -q "Issue #779"</automated>
    <automated>awk '/^## 3.0.15/,/^## 3.0.14/' /Users/derks/Development/DFL/cement/CHANGELOG.md | grep -B1 -A1 "Features:" | head -5 && python3 -c "import sys; lines = open('/Users/derks/Development/DFL/cement/CHANGELOG.md').readlines(); ng = [l for l in lines if 'prompt_mode: select' in l]; assert ng, 'no entry'; assert all(len(l.rstrip())<=78 for l in ng), f'over 78: {[(len(l.rstrip()),l) for l in ng]}'"</automated>
  </verify>

  <done>
    - CHANGELOG.md has one new line in the `## 3.0.15 - DEVELOPMENT` →
      `Features:` bucket starting `- \`[ext.generate]\` Add multi-valued
      feature support:`, with the Issue #779 GitHub link as a sub-
      bullet matching the existing entries' style.
    - Every line of the new entry is ≤78 chars.
    - No other section of CHANGELOG.md modified (verified via `git
      diff CHANGELOG.md` showing only ADDITIONS in the 3.0.15 Features
      bucket).
    - One atomic commit: subject `docs(changelog): record prompt_mode:
      select feature for ext.generate` (≤78 chars).
  </done>
</task>

</tasks>

<task_grouping_rationale>
Three tasks chosen (not 4) because:

- Task 1 (implementation + tests + fixtures) MUST be atomic — splitting
  tests before implementation fails the 100% coverage gate at commit
  time (`fail_under = 100` in pyproject.toml + `--cov-fail-under=100` on
  the test runner). Splitting implementation before tests passes
  coverage but ships untested code, which violates the project-absolute
  100% coverage gate stated in CLAUDE.md + PROJECT.md. RESEARCH.md §4
  enumerates 13 new branches that MUST all be exercised by their named
  tests in a single coherent commit.
- Task 2 (demo + README) is logically separate from the implementation
  — the demo consumes the new schema as an end user would, and its
  verification (a manual end-to-end run + README narrative check) is
  qualitatively different from the test suite. Bundling it with task 1
  would inflate that commit's surface for no coupling benefit.
- Task 3 (CHANGELOG) is a single-file documentation commit that lands
  AFTER the substantive work so the entry accurately describes what
  shipped. Per CLAUDE.md: changelog is maintained phase-by-phase, so
  this is the in-branch entry, not a release-time roll-up.

Context budget estimate: task 1 ~28% (cement/ext/ext_generate.py is
319 lines; the four edit zones touch ~80 lines; plus 13 new tests +
~10 fixture directories), task 2 ~10% (4 files, demo-only), task 3
~3% (one file, one line addition). Plan total ~41% — well under the
50% target with headroom for the executor's verification reads.
</task_grouping_rationale>

<verification>
**Per-task automated verification:** see each task's `<verify>` block.

**Plan-wide acceptance gates** (run after all three tasks land):

1. `pdm run pytest --cov=cement tests/` — 100% coverage across the
   cement package, all tests pass (the project-absolute coverage gate
   from CLAUDE.md + PROJECT.md).
2. `make comply` — ruff + mypy exit 0.
3. `make audit-public-api` — exit 0 (no public-API surface change; the
   schema additions are internal to `_process_features` and the
   variable loop; `feature_states` annotation widening does not affect
   the public surface).
4. `git log --oneline -3` shows three commits with Conventional
   Commits subjects ≤78 chars: `feat(ext.generate): add prompt_mode:
   select for multi-valued features`, `docs(demo/generate-features):
   demo prompt_mode: select via web_framework`, `docs(changelog):
   record prompt_mode: select feature for ext.generate`.
5. `git log -3 --format='%b'` body lines wrap at ≤78 chars.
6. Backward-compat regression: `pdm run pytest -k "test_generate"
   tests/ext/test_ext_generate.py` — all PRE-EXISTING tests pass
   (i.e. tests in the file BEFORE this branch land unchanged).
7. Backward-compat regression: end-to-end demo invocation
   `cd demo/generate-features && pdm run python myapp.py generate
   webapp /tmp/g47-bc-test --defaults` succeeds; the rendered tree
   contains `Dockerfile`, `app.py`, `docker-compose.yml`, `README.md`
   in BOTH the pre-task1 state AND the post-task2 state. (After task
   2 also: no `requirements.txt` or `wsgi.py` because `web_framework`
   defaults to `N`.)

**Coverage-gate landmines re-stated** (RESEARCH.md §4 catalogue):

| # | Branch | Edit Zone | Test Fixture/Invocation |
|---|--------|-----------|-------------------------|
| 1 | `if mode is None: continue` | A | Implicit via test6..test15 |
| 2 | `if mode != 'select': raise ValueError` | A | test_invalid_prompt_mode |
| 3 | collision raise | A | test_select_collision (test17) |
| 4 | empty `selected` raise | A | test_select_empty_selected |
| 5 | missing `value:` raise | A | test_select_missing_value (test21) |
| 6 | `default` not in values raise | A | test_select_default_not_in_values (test20) |
| 7 | `--defaults` select assignment | B | test_select_defaults (test16) |
| 8 | merge walks `selected` | C | test_select_defaults (test16) — implicit |
| 9 | `variables or []` on select path | C | test_select_null_variables (test24) |
| 10 | variable-loop `prompt is False` short-circuit | D | test_select_silent_variable (test19), test_top_level_silent_variable (test22) |
| 11 | silent-var `default is not None` assert | D | test_select_no_default_branch |
| 12 | top-level silent var | D | test_top_level_silent_variable (test22) |
| 13 | unmatched-input `assert val in valid_values` (NO pragma) | B | test_select_unmatched_input |

Pragma-able branches: the new interactive `FeaturePrompt` class body +
`p = FeaturePrompt(auto=False)` + `val = p.prompt() or str(default)` —
all carry `# pragma: nocover  # defensive: unreachable` mirroring
current ext_generate.py:73-83 lines (same locked-vocabulary category as
Phase 03 Plan 07).
</verification>

<success_criteria>
- GH Issue #779 closed by the PR opened from this branch (the PR body
  cites this quick task's directory and links the issue).
- `pdm run pytest --cov=cement tests/` shows 100% coverage AND all
  tests pass (13 new + all pre-existing).
- `make comply` exits 0 (ruff + mypy).
- `make audit-public-api` exits 0.
- Three atomic commits land with Conventional Commits subjects and
  bodies, all line lengths ≤78 chars.
- The demo `webapp` template's existing `docker` + `docker_compose`
  features are byte-for-byte unchanged (proven by `git log -p` showing
  only ADDITIONS to `.generate.yml`).
- The four existing demo content files (Dockerfile, app.py,
  docker-compose.yml, README.md) are byte-for-byte unchanged (proven by
  `git log -p` showing no edits to those files).
- CHANGELOG.md has one new line under `## 3.0.15 - DEVELOPMENT` →
  `Features:` bucket with `[ext.generate]` area tag and Issue #779 link.
- Zero new runtime dependencies (verified by no edits to
  `pyproject.toml`'s `[project.dependencies]` or `[project.optional-
  dependencies]`).
- The unmatched-input `AssertionError` raise in the new `_resolve`
  closure carries NO `# pragma: nocover` annotation (verified by `grep
  -B3 'must be one of' cement/ext/ext_generate.py` showing no pragma in
  the three lines preceding the assertion).
</success_criteria>

<output>
After completion, the quick-task close workflow (`/gsd-quick-finish` or
equivalent) produces:
`.planning/quick/260524-g47-generate-feature-should-support-multiple/260524-g47-SUMMARY.md`
</output>
