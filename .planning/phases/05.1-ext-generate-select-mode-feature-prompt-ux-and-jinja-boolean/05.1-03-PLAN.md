---
phase: 05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean
plan: 03
type: execute
wave: 3
depends_on: ["05.1-02"]
files_modified:
  - cement/ext/ext_generate.py
  - tests/ext/test_ext_generate.py
  - tests/data/templates/generate/test8/.generate.yml
  - tests/data/templates/generate/test9/.generate.yml
  - tests/data/templates/generate/test10/.generate.yml
  - tests/data/templates/generate/test11/.generate.yml
  - tests/data/templates/generate/test12/.generate.yml
  - tests/data/templates/generate/test14/.generate.yml
  - tests/data/templates/generate/test15/.generate.yml
  - tests/data/templates/generate/test17/.generate.yml
  - tests/data/templates/generate/test18/.generate.yml
  - tests/data/templates/generate/test23/.generate.yml
  - tests/data/templates/generate/test24/.generate.yml
  - tests/data/templates/generate/test25/.generate.yml
autonomous: true
requirements: [GEN-03, GEN-05, GEN-06]
must_haves:
  truths:
    - "extend.when matches the resolved value as a single value, an in-list membership, or (string type only) a re.match regex; rules compose (all matching rules fire and accumulate)"
    - "extend.variables are nested and prompted depth-first in place, only when their parent rule fires"
    - "Top-level requires: gates a variable using the same match vocabulary as extend.when, AND-ed across keys, resolved order-independently via lazy recursion"
    - "A requires-gated-out variable is set to its default at data[name] (never KeyErrors in the template) and its extend rules do not fire"
  artifacts:
    - path: "cement/ext/ext_generate.py"
      provides: "extend.when value/list/regex matching, depth-first nested extend.variables, top-level requires: with full match vocabulary"
      contains: "requires"
    - path: "tests/data/templates/generate/test14/.generate.yml"
      provides: "Migrated transitive requires: chain fixture"
      contains: "requires"
  key_links:
    - from: "cement/ext/ext_generate.py"
      to: "resolve_and_emit (recursion)"
      via: "depth-first nested extend.variables when a rule fires"
      pattern: "extend"
    - from: "cement/ext/ext_generate.py"
      to: "re.match"
      via: "extend.when string-type regex matching"
      pattern: "re\\.match"
---

<objective>
Complete the conditional-effects and dependency surface of the unified resolver:
full `extend.when` matching (single value, in-list membership, and string-type
`re.match` regex — composing across multiple rules), depth-first nested
`extend.variables` (prompted in place only when the parent rule fires, D-08), and
top-level `requires:` (the referential dependency form sharing `extend.when`'s
match vocabulary, AND-ed across keys, resolved order-independently via the lazy
recursion from plan 01). Resolve Open Questions Q1 (gated-out var → its default)
and Q2 (boolean `when` compares by Python equality). Migrate the remaining
requires/nesting/null-block fixtures (test8-12, 14, 15, 23, 24, 25) and
remove/repurpose the obsolete schema-validation fixtures (test17, test18).

Purpose: closes GEN-05 (conditional effects + dependencies inject
variables/exclude/ignore correctly) and finishes GEN-03 (declaration-order with
nested depth-first prompting). This is the last engine plan; plans 04-05 are
migration + docs only.
Output: complete resolver in ext_generate.py, migrated requires/nesting fixtures,
removed obsolete validation tests, new extend.when list/regex tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-CONTEXT.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-RESEARCH.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-PATTERNS.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate requires/nesting/null fixtures + remove obsolete schema tests (Wave 0 scaffold)</name>
  <files>tests/ext/test_ext_generate.py, tests/data/templates/generate/test8/.generate.yml, tests/data/templates/generate/test9/.generate.yml, tests/data/templates/generate/test10/.generate.yml, tests/data/templates/generate/test11/.generate.yml, tests/data/templates/generate/test12/.generate.yml, tests/data/templates/generate/test14/.generate.yml, tests/data/templates/generate/test15/.generate.yml, tests/data/templates/generate/test17/.generate.yml, tests/data/templates/generate/test18/.generate.yml, tests/data/templates/generate/test23/.generate.yml, tests/data/templates/generate/test24/.generate.yml, tests/data/templates/generate/test25/.generate.yml</files>
  <read_first>
    - tests/ext/test_ext_generate.py (current tests: _features_missing_name (test8), requires_satisfied (test9), requires_unknown (test10), requires_not_satisfied (test11), null_block (test12), transitive_requires (test14), requires_out_of_order (test15), select_collision (test17), invalid_prompt_mode (test18), legacy_null_variables (test23), select_null_variables (test24), select_requires_cascade (test25))
    - tests/data/templates/generate/test8..test15, test17, test18, test23, test24, test25 (the .generate.yml fixtures — current features:/requires:/enabled:/disabled: shapes)
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"Migration Surface" (the per-fixture disposition table — note test17/test18 must be REMOVED/REPLACED, not renamed) and §"Open Questions" Q1/Q2/Q3
    - .planning/phases/05.1-.../05.1-CONTEXT.md D-07..D-11 (extend.when forms; requires vocab: [name]→truthy, {name: value}→equality, {name: [v1,v2]}→in-list; nesting ≡ requires: {parent: true})
    - .planning/phases/05.1-.../05.1-PATTERNS.md §"ValueError-asserting test pattern" and §"No Analog Found" (compose extend.when from equality + in-list + re.match; generalize the requires truthy short-circuit)
  </read_first>
  <action>
    Migrate fixtures to top-level `requires:` + `extend:` + nesting:
    test8 (feature missing name → adapt to "variable missing name" — `name` still
    required, message updated); test9 (`requires: [feature1]` both true →
    top-level `requires:`); test10 (requires nonexistent → ValueError, message
    "requires unknown variable"); test11 (requires gate fails → dependent gated
    out); test12 (`enabled:` null block → `extend:` with null block, `... or []`
    coalesce); test14 (3-level transitive `requires:` chain); test15 (requires
    out-of-declaration-order → order-independent); test23/test24 (null blocks
    inside extend → coalesce, locks the `or []` idiom); test25 (choice with a
    `requires:` bool prereq that is false → the branch ignore does NOT fire).

    REMOVE or REPURPOSE the obsolete schema tests: test17 (select +
    enabled/disabled collision → that error case no longer exists) — remove the
    fixture + test, OR repurpose to a new validation case. test18
    (`prompt_mode: bogus` → ValueError) → REPLACE with a `type: bogus` whitelist
    test (`raises(ValueError, match="...invalid.*type.*bogus...")`) reusing the
    fixture dir. Do NOT mechanically rename — these assert on schema that ceases
    to exist.

    Add NEW fixtures + tests for `extend.when` forms not yet covered: a list
    `when: [a, b]` membership match, and a STRING-type `when: <regex>` `re.match`
    case (D-07). Add a test asserting Q1 (a `requires`-gated-out variable renders
    as its `default` in the template, never KeyErrors) and Q2 (a `type: boolean`
    carrying var with `extend: [{when: true, ...}]` fires by Python equality when
    the value is True). All run via `--defaults` or patched prompt. RED until Task 2.
  </action>
  <behavior>
    - test9/test14/test15: requires chains resolve order-independently; dependents render only when all prereqs satisfied.
    - test11/test25: a failed requires gate omits the dependent's effects; the gated-out var renders as its default (Q1).
    - test12/test23/test24: null extend blocks coalesce to empty lists (no crash).
    - extend.when list match: value in [a,b] fires the rule. extend.when string regex: re.match(pattern, value) fires the rule.
    - test10: requires unknown variable → ValueError. test18 repurposed: type: bogus → ValueError. test8: variable missing name → ValueError.
  </behavior>
  <verify>
    <automated>pdm run pytest tests/ext/test_ext_generate.py -x  # whole file: after plans 01/02 the rest is GREEN, so the only RED is the new plan 03 tests/migrations</automated>
  </verify>
  <done>test8-15/23/24/25 migrated to top-level `requires:`/`extend:`/nesting; test17 removed or repurposed; test18 replaced with a `type:` whitelist ValueError test; new extend.when list/regex + Q1 + Q2 fixtures and tests exist; the whole-file run fails ONLY on the new plan 03 tests/migrations (no `-k` filter, so newly-named tests cannot be silently skipped).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize extend.when matching, depth-first nesting, and top-level requires:</name>
  <files>cement/ext/ext_generate.py, tests/ext/test_ext_generate.py</files>
  <read_first>
    - cement/ext/ext_generate.py (post-plan-02 state: the unified resolver with string/boolean/choice paths, the bool `==` + choice/string scalar str-coerce extend.when handling from plans 01/02, the memoize-on-data short-circuit, the lazy recursion structure)
    - .planning/phases/05.1-.../05.1-PATTERNS.md §"Unified resolver" Analog A (lazy recursive memoized _resolve — the requires DAG to generalize) and §"No Analog Found" (compose match helper: equality str-coerce L219, in-list, re.match L322; booleans by Python ==)
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"extend.when matching semantics" (scalar equality str-coerce, list membership, regex re.match string-only) and §"Recommended control-flow" (resolve_and_emit: requires-first then prompt then fire-rules depth-first) and §"Open Questions" Q1/Q2
    - .planning/phases/05.1-.../05.1-CONTEXT.md D-07..D-11
    - tests/data/templates/generate fixtures migrated in Task 1 (the oracle)
  </read_first>
  <action>
    Generalize `_when_matches(value, when, var_type)` to compose all three forms
    (D-07): scalar → equality (Python `==` for booleans per Q2; `str()`-coerce
    both sides for choice/string per the L219 discipline — this scalar form
    already landed in plan 02 for choice/string; preserve it); list → membership
    (`value in when`, str-coerce members for choice/string, `==` for bool); regex →
    `re.match(when, value)` ONLY for `var_type == 'string'` (mirror validate: L322).
    Use this helper for BOTH `extend.when` rule firing and `requires:` gating.

    Generalize the resolver's rule application (extending plans 01/02's scalar-only
    handling with list + regex): for each var, after computing its value, iterate
    `var['extend'] or []` in order — every rule whose `when` matches fires and
    ACCUMULATES its `exclude`/`ignore` (via `... or []`) AND recurses depth-first
    into its `variables` via `resolve_and_emit(nested)` (D-08 — nested vars prompt
    in place, in declaration order, only when the parent rule fired). Multiple
    matching rules compose (all fire, accumulate).

    Generalize `requires:` (D-09/D-10/D-11): before prompting a var, resolve each
    `requires` entry on demand (memoized via `data` membership — the lazy recursion
    from Analog A, now keyed on `data` instead of a separate feature_states dict),
    so a required var declared LATER still resolves first. Support the three
    vocab forms AND-ed: `requires: [name]` → require `name` truthy;
    `requires: {name: value}` → `_when_matches` equality; `requires: {name: [v]}`
    → `_when_matches` in-list. If any gate fails, the var is GATED OUT: set it to
    its `default` at `data[name]` (Q1 — never KeyError in templates; bool default
    → its bool, choice/string default → that value) and do NOT fire its extend
    rules. `requires` against an unknown variable → `raise ValueError` ("requires
    unknown variable", adapting L46-48). Keep the nesting ≡ requires equivalence
    (D-10): a nested extend.variables child only resolving when the parent rule
    fired is structurally the same gate.

    Preserve all released behavior (test1-5/22/27/29) and all plan 01/02 behavior.
    Interactive `.prompt()` reads stay `# pragma: nocover  # defensive: unreachable`;
    matching/requires logic is covered by Task 1 `--defaults`/patched-prompt tests.
  </action>
  <behavior>
    - extend.when scalar/list/regex matching fires the right rules; multiple matching rules compose and accumulate variables/exclude/ignore.
    - nested extend.variables prompt depth-first in place only when the parent rule fires (declaration order preserved).
    - requires: [name] / {name: value} / {name: [v1,v2]} gate the dependent, AND-ed; resolved order-independently.
    - a gated-out var renders as its default at data[name]; its extend rules do not fire.
    - requires unknown variable → ValueError; type: bogus → ValueError; variable missing name → ValueError.
  </behavior>
  <verify>
    <automated>pdm run pytest tests/ext/test_ext_generate.py -x</automated>
  </verify>
  <done>extend.when value/list/regex matching composes correctly; nested extend.variables recurse depth-first only when their rule fires; top-level requires: gates with the full vocab AND-ed, order-independent; gated-out vars render as default (Q1); booleans match by == (Q2); test1-5/22/27/29 still unchanged; `make comply` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author `.generate.yml` (`extend.when` regex, `requires:`) → matcher | author-controlled local template; not untrusted network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-07 | Tampering | `requires:` referencing an unknown variable | mitigate | `raise ValueError` ("requires unknown variable") fail-fast before any prompt; ASVS V5 |
| T-05.1-08 | DoS | author-supplied `extend.when` regex (ReDoS) | accept | author-controlled local template, not untrusted input; same `re.match` posture as the unchanged `validate:` (RESEARCH §Security Domain) — no mitigation required |
| T-05.1-09 | Information Disclosure | gated-out variable in template context | mitigate | Q1: gated-out var set to its `default` (never absent) so templates render deterministically; no leakage of unrelated values |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | no package installs (RESEARCH §Package Legitimacy Audit: not applicable) |

No HIGH threats. ASVS V5 only; ReDoS accepted (author-controlled, matches unchanged validate: posture).
</threat_model>

<verification>
- `pdm run pytest tests/ext/test_ext_generate.py -x` exits 0
- `make test-core` exits 0 (100% coverage)
- `make comply` exits 0 (ruff + mypy)
- `grep -n "prompt_mode\|'enabled'\|'disabled'\|features:" tests/data/templates/generate/test*/.generate.yml` returns no remaining feature-schema usage
- Commits authored per CLAUDE.md commit conventions (Conventional Commits, ≤78-char subject/body)
</verification>

<success_criteria>
- extend.when value/list/regex + composing + nesting inject variables/exclude/ignore (GEN-05)
- top-level requires: with full match vocab, order-independent (GEN-05)
- depth-first nested prompting in declaration order (GEN-03)
- obsolete prompt_mode/enabled/disabled validation tests removed/repurposed; 100% cov + ruff + mypy green (GEN-06)
</success_criteria>

<output>
Create `.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-03-SUMMARY.md` when done
</output>
</output>
