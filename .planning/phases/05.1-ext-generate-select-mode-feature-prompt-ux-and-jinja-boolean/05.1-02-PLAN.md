---
phase: 05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean
plan: 02
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - cement/ext/ext_generate.py
  - tests/ext/test_ext_generate.py
  - tests/data/templates/generate/test16/.generate.yml
  - tests/data/templates/generate/test19/.generate.yml
  - tests/data/templates/generate/test20/.generate.yml
  - tests/data/templates/generate/test21/.generate.yml
  - tests/data/templates/generate/test26/.generate.yml
  - tests/data/templates/generate/test28/.generate.yml
  - tests/data/templates/generate/test30/.generate.yml
autonomous: true
requirements: [GEN-02, GEN-04, GEN-06]
must_haves:
  truths:
    - "type: choice resolves to the chosen str at top-level data[name] via a numbered shell.Prompt picker"
    - "type: boolean object-form prompt {text, accept, reject} lets the author own the prompt text and maps accept/reject token lists to bool"
    - "An object-form bool answer matching neither accept nor reject asserts and aborts like a failed validate:"
    - "An accept:/reject: member that loaded as a Python bool (YAML 1.1 coercion) raises ValueError telling the author to quote it"
    - "extend.when SCALAR form fires by str()-coerced equality for choice/string values so the migrated choice fixtures (test16/test19) pass at this commit"
    - "A type: choice variable that declares case: or validate: raises ValueError (D-17 string-only semantics)"
  artifacts:
    - path: "cement/ext/ext_generate.py"
      provides: "type: choice numbered picker + polymorphic boolean object-form prompt + accept/reject mapping + YAML bool-coercion load guard + extend.when scalar str-coerce equality"
      contains: "accept"
    - path: "tests/data/templates/generate/test16/.generate.yml"
      provides: "Migrated select fixture on type: choice + options + extend:"
      contains: "type: choice"
  key_links:
    - from: "cement/ext/ext_generate.py"
      to: "shell.Prompt options/numbered"
      via: "choice numbered picker"
      pattern: "numbered"
    - from: "cement/ext/ext_generate.py"
      to: "assert"
      via: "accept/reject junk-input abort (mirrors validate:)"
      pattern: "assert"
---

<objective>
Add the remaining two `type:` resolutions to the unified resolver from plan 01:
`type: choice` (numbered `shell.Prompt` picker, emits the chosen `str` at
`data[name]`) and the `type: boolean` polymorphic OBJECT-form `prompt:`
(`{text, accept, reject}` where the author owns the full text and `accept:`/
`reject:` case-insensitive token lists map input → bool, junk input asserts +
aborts). Add the YAML 1.1 boolean-coercion load guard for `accept:`/`reject:`
members. Extend the `extend.when` rule-firing handler to support SCALAR
str()-coerced equality for choice/string values (so the migrated choice fixtures
fire their per-option effects at this commit). Migrate the select-mode fixtures
(test16/19/20/21/26/28/30) to the new `type: choice` + `options:` + `extend:`
schema.

Purpose: completes #782 pts 2-3 (custom per-feature prompt text, vars-style
input) via the object-form override, and finishes the typed-variable surface
(bool string-form + bool object-form + choice). The accept/reject mapping is the
bug-prone logic — it gets real patched-prompt coverage, not a pragma.
Output: choice + object-form bool resolution in ext_generate.py, the YAML guard,
the extend.when scalar-equality extension, migrated select fixtures, new
accept/reject + junk-abort tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-CONTEXT.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-RESEARCH.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-PATTERNS.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate select fixtures + write choice/accept/reject/junk-abort tests (Wave 0 scaffold)</name>
  <files>tests/ext/test_ext_generate.py, tests/data/templates/generate/test16/.generate.yml, tests/data/templates/generate/test19/.generate.yml, tests/data/templates/generate/test20/.generate.yml, tests/data/templates/generate/test21/.generate.yml, tests/data/templates/generate/test26/.generate.yml, tests/data/templates/generate/test28/.generate.yml, tests/data/templates/generate/test30/.generate.yml</files>
  <read_first>
    - tests/ext/test_ext_generate.py (current select-mode tests: test_..._select_defaults, _select_silent_variable, _select_default_not_in_values, _select_missing_value, _select_empty_options, _select_missing_default, _select_duplicate_labels; the select_collision/invalid_prompt_mode ValueError pattern L388-405; test_no_default L89-97 patch-prompt analog)
    - tests/data/templates/generate/test16/.generate.yml (current prompt_mode: select, 3 options, ignore per branch)
    - tests/data/templates/generate/test19/.generate.yml (select branch with silent variable)
    - tests/data/templates/generate/test20/.generate.yml (default not in options → ValueError)
    - tests/data/templates/generate/test21/.generate.yml (options branch missing value → ValueError)
    - tests/data/templates/generate/test26/.generate.yml (options: [] → ValueError)
    - tests/data/templates/generate/test28/.generate.yml (select no default → ValueError)
    - tests/data/templates/generate/test30/.generate.yml (duplicate option labels → ValueError)
    - .planning/phases/05.1-.../05.1-PATTERNS.md §"Choice numbered picker" (SelectPrompt L165-176 re-keyed) and §"Fixture migration template" (select→choice mapping) and §"Accept/reject junk-input abort"
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"Pitfall 1" (YAML 1.1 accept/reject bool coercion — quote bool-like tokens; recommend a ValueError load guard) and §"shell.Prompt Capability Map"
    - .planning/phases/05.1-.../05.1-CONTEXT.md D-17 (case:/validate: string-only — a choice declaring either is a schema misconfig)
  </read_first>
  <action>
    Migrate the select fixtures to `type: choice`: `prompt_mode: select` →
    `type: choice`; the inline `options: [{value, prompt, ignore, variables}]`
    effects move OUT of `options:` into an `extend:` list keyed by `when: <value>`
    carrying the `ignore`/`variables`; `options:` becomes either a scalar list
    (`[none, flask, fastapi]`) OR per-option `{value, prompt}` objects when a
    custom picker label is wanted (D-16). Keep `default:` and the same rendered-
    output assertions. test19's silent branch variable stays `prompt: false`.
    For the ValueError fixtures: test20 (default not in options), test21 (option
    missing value — adapt to the D-16 options shape), test26 (empty options),
    test28 (missing default), test30 (duplicate labels) — keep them as fail-fast
    `ValueError` cases under the new `type: choice` schema (Q3 decision: choice
    keeps empty-options / default-not-in-options / duplicate-label validation).

    Rewrite the corresponding test functions to assert the migrated behavior with
    `--defaults` (rendered output) and the ValueError cases with
    `raises(ValueError, match=...)`. test16/test19 with `--defaults` MUST assert
    that the default option's `extend.when: <default value>` rule FIRES — i.e. its
    per-branch `ignore`/`variables` take effect (this is what the plan 02 Task 2
    scalar-equality extension makes pass). Add NEW fixtures + patched-prompt tests
    for: (a) `type: boolean` OBJECT-form prompt
    `{text, accept: [y, "yes", yay], reject: [n, "no", nay]}` — patch
    `shell.Prompt.prompt` to return a member of accept → data[name] True,
    a member of reject → False, empty → default; (b) junk input (matching neither)
    → `raises(AssertionError, match="Invalid Response.*")` (D-14, mirrors
    test_invalid_variable_value); (c) a fixture whose `accept:` list contains a
    BARE bool-like token (`accept: [y, yes]` unquoted) → `raises(ValueError, ...)`
    from the load guard; (d) a D-17 schema-misconfig fixture: a `type: choice`
    variable that ALSO declares `case:` (or `validate:`) →
    `raises(ValueError, match=...)` (string-only semantics — the choice side of
    D-17; the boolean side is covered in plan 01). Quote bool-like tokens
    (`"yes"`, `"no"`) in all VALID accept/reject fixtures. These tests FAIL until
    Task 2 — RED state.
  </action>
  <behavior>
    - test16 with --defaults: type:choice default option resolves; its extend.when:<default> ignore patterns apply (scalar str-coerced equality match).
    - test19 with --defaults: type:choice default branch resolves; its extend.when:<default> silent variable + ignore patterns apply.
    - choice picker (patched prompt returns the numbered label) → data[name] is the chosen option str.
    - object-form bool: accept member → True; reject member → False; empty → default; junk → AssertionError "Invalid Response.*".
    - unquoted bool-like accept/reject member → ValueError at load.
    - test20/26/28/30 → ValueError on choice misconfig.
    - D-17: a type:choice var declaring case: or validate: → ValueError (schema misconfig, fail-fast).
  </behavior>
  <verify>
    <automated>pdm run pytest tests/ext/test_ext_generate.py -k "choice or select or accept or reject or bool_prompt or invalid or string_only or case_on_choice" -x  # RED before Task 2</automated>
  </verify>
  <done>test16/19/20/21/26/28/30 migrated to `type: choice`/`options:`/`extend:`; test16/test19 assert their default-branch extend.when scalar rule fires under --defaults; new object-form accept/reject + junk-abort + YAML-guard + D-17 choice case:/validate: fixtures and tests exist; all bool-like accept/reject tokens are quoted; tests fail only because the engine resolution is not yet added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add choice numbered picker + boolean object-form prompt + accept/reject mapping + YAML load guard + extend.when scalar equality</name>
  <files>cement/ext/ext_generate.py, tests/ext/test_ext_generate.py</files>
  <read_first>
    - cement/ext/ext_generate.py (post-plan-01 state: the unified resolver, the type: boolean string-form prompt, the var_defaults additive extension, the D-17 case/validate-on-typed ValueError gate landed in plan 01, the YAML load via yaml.full_load at the old L245, AND plan 01's minimal bool-only `extend.when` `==` rule-firing handler)
    - cement/utils/shell.py:386-541 (options + numbered=True numbered picker; case_insensitive; the built-in retry/whitelist on options membership at L500-534)
    - .planning/phases/05.1-.../05.1-PATTERNS.md §"Choice numbered picker" (SelectPrompt construction, str()-coercion of value+state, values[labels.index(...)] mapping) and §"Accept/reject junk-input abort" (assert model, NOT re-prompt, NOT silent-False) and §"Config-validation fail-fast"
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"Key finding — re-prompt vs assert-abort" (bool parse is external assert-abort; choice uses options retry) and §"Pitfall 1" and §"extend.when matching semantics" (L219 str-coerce discipline for choice/string scalar equality)
    - .planning/phases/05.1-.../05.1-CONTEXT.md D-17 (case:/validate: string-only — choice side)
    - tests/data/templates/generate fixtures migrated in Task 1 (the oracle)
  </read_first>
  <action>
    Extend the unified resolver with three paths plus the scalar-equality `when`
    extension:

    (1) `type: choice`: validate fail-fast with `raise ValueError` (NOT assert) —
    empty `options:` → ValueError; `default` not in option values → ValueError;
    duplicate effective picker labels → ValueError; an option object missing
    `value` → ValueError (preserve the test20/21/26/28/30 spirit, Q3). Accept
    D-16 `options:` as either a scalar list or per-option `{value, prompt}` objects
    — coerce both `value` and the resolved state with `str()` (YAML may decode
    `value: 1` as int; reuse the L142-143/L219 discipline). For `--defaults` emit
    `str(default)`; for the interactive path reuse the SelectPrompt numbered-picker
    construction (text + options=labels + numbered=True + case_insensitive +
    default_label), mark the raw `.prompt()` read `# pragma: nocover  # defensive: unreachable`,
    and map the chosen label → value via `values[labels.index(chosen_label)]`
    (cover the mapping via a patched-prompt test where reachable). Emit the chosen
    `str` at `data[name]` (top level). The D-17 case:/validate:-on-choice ValueError
    gate is already wired in plan 01 (step 5 covers boolean AND choice); confirm it
    fires for `type: choice` and is covered by the Task 1 choice D-17 test (add the
    branch here if plan 01 only validated the boolean type).

    (2) `type: boolean` OBJECT-form `prompt: {text, accept, reject}`: build the
    Prompt with `Meta.text = var['prompt']['text']` and append NOTHING (author owns
    the text — D-12 object form; contrast the string-form which appends
    `[(Y)es/(N)o] [default]:`). Read `.prompt()`, lowercase the input, membership-
    test against the lowercased `accept`/`reject` token lists: in accept → True,
    in reject → False, empty → default, matching NEITHER → `assert <matched>,
    "Invalid Response (must be one of accept/reject)"` (D-14 — same assert+abort UX
    as validate: at L321-323; NO re-prompt, NO silent coercion to False). For
    `--defaults` emit `bool(var['default'])`.

    (3) YAML load guard (Pitfall 1): immediately after parsing a `type: boolean`
    var with an object-form `prompt:`, check each `accept:`/`reject:` member — if
    any member loaded as a Python `bool` (i.e. `isinstance(member, bool)`), `raise
    ValueError` instructing the author to quote the bool-like token (e.g.
    `accept: [y, "yes"]`). This converts the silent YAML 1.1 coercion landmine into
    a fast explicit failure. Validate at config-parse time so it survives `python -O`.

    (4) Extend the `extend.when` rule-firing handler to cover SCALAR equality for
    choice/string values — NOT just the bool-only `==` path plan 01 landed. The
    migrated choice fixtures (test16, test19) carry `extend: [{when: <scalar value>,
    ignore: [...], variables: [...]}]` where `when` is the chosen option string
    (e.g. `when: flask`). Update the rule-firing logic so that, for the SCALAR form
    of `when`, a choice/string value matches by `str()`-coerced equality
    (`str(when) == str(value)`, per RESEARCH L219 discipline and Q2: booleans
    compare by Python `==`, but choice/string values compare str-coerced — so
    `when: 1` matches a `"1"` option value and `when: flask` matches `"flask"`).
    Keep plan 01's boolean `==` match intact (a boolean-typed var's `when`
    short-circuits to `==`, NOT str-coerce, so `when: true` still matches `True`).
    Name the helper concretely (e.g. a `_when_matches`-style branch or an inline
    scalar-coerce check) and dispatch on `var['type']`: bool → `==`, choice/string
    scalar → `str(when) == str(value)`. This task ONLY adds the SCALAR-equality
    form for choice/string so the migrated choice fixtures (test16, test19) fire
    their per-option `ignore`/`variables` at THIS commit. The LIST-form
    (`when: [a, b]` membership) and the STRING-type REGEX-form (`when: <regex>` via
    `re.match`) `when` matching legitimately remain deferred to plan 03 — do NOT
    add them here; the test16/test19 fixtures use only scalar `when`.

    Keep the accept/reject MAPPING and the choice label→value MAPPING reachable
    under the Task 1 patched-prompt tests (real coverage); only the raw interactive
    `.prompt()` reads carry `# pragma: nocover  # defensive: unreachable`. `# noqa: E501`
    may sit AFTER a pragma label if the combined trailing comment exceeds 100 chars.
  </action>
  <behavior>
    - choice with --defaults emits str(default) at data[name]; bad choice config (empty options / default-not-in-options / dup labels / option missing value) → ValueError.
    - bool object-form: accept token → True, reject token → False, empty → default, junk → AssertionError "Invalid Response.*".
    - unquoted bool-like accept/reject member → ValueError at config load.
    - choice picker maps the chosen numbered label to its option value str.
    - extend.when SCALAR form fires by str()-coerced equality for choice/string (when: flask matches a "flask" choice value); booleans still match by Python ==. test16/test19's per-option extend.when ignore/variables fire at this commit.
    - D-17: a type:choice var declaring case: or validate: → ValueError (config-time, fail-fast).
  </behavior>
  <verify>
    <automated>pdm run pytest tests/ext/test_ext_generate.py -x</automated>
  </verify>
  <done>type: choice emits the chosen str at data[name] with fail-fast ValueError validation; the D-17 case:/validate:-on-choice ValueError fires and is covered; the `extend.when` SCALAR form fires by str()-coerced equality for choice/string (test16 + test19 migrated choice fixtures PASS at this commit — their per-option ignore/variables apply); boolean object-form maps accept/reject→bool and asserts on junk; the YAML bool-coercion guard raises ValueError; all migrated select fixtures + new tests pass; the full `pdm run pytest tests/ext/test_ext_generate.py -x` (under --defaults) is GREEN so the 100%-coverage-at-every-commit invariant holds; `make comply` clean. (List-form and string-type regex `when` matching remain deferred to plan 03.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author `.generate.yml` (`options:`, `accept:`/`reject:`) → parser | author-controlled local template; YAML 1.1 bool coercion is the parse hazard |
| interactive user → shell.Prompt | local tty input mapped to choice value / bool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04 | Tampering | `accept:`/`reject:` YAML 1.1 bool coercion | mitigate | load guard `raise ValueError` if a member `isinstance(_, bool)` — surfaces the silent-coercion landmine as a fast explicit failure; ASVS V5 |
| T-05.1-05 | Tampering | `type: choice` `options:`/`default:` misconfig | mitigate | fail-fast `raise ValueError` (empty options, default-not-in-options, dup labels, option missing value); survives `python -O` |
| T-05.1-06 | Tampering | object-form bool junk input | mitigate | `assert` + abort ("Invalid Response") — no silent coercion to False (D-14); ASVS V5 runtime-response validation |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | no package installs (RESEARCH §Package Legitimacy Audit: not applicable) |

No HIGH threats. ASVS V5 (Input Validation) only — handled by ValueError (schema) + assert (runtime response).
</threat_model>

<verification>
- `pdm run pytest tests/ext/test_ext_generate.py -x` exits 0
- `make test-core` exits 0 (100% coverage)
- `make comply` exits 0 (ruff + mypy)
- `grep -rn "accept:\s*\[.*\b\(yes\|no\|on\|off\|true\|false\)\b" tests/data/templates/generate/` returns no UNQUOTED bool-like accept/reject tokens
- Commits authored per CLAUDE.md commit conventions (Conventional Commits, ≤78-char subject/body; `make commit`)
</verification>

<success_criteria>
- type: choice emits chosen str at top-level data[name] (GEN-02)
- polymorphic boolean prompt object-form {text, accept, reject} works; junk → abort (GEN-04)
- YAML bool-coercion guard raises ValueError (GEN-04 / GEN-06 robustness)
- extend.when scalar str-coerce equality fires for choice/string; test16/test19 GREEN at this commit (GEN-04 / GEN-06)
- type:choice declaring case:/validate: → ValueError (D-17)
- select fixtures migrated; 100% coverage + ruff + mypy green (GEN-06)
</success_criteria>

<output>
Create `.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-02-SUMMARY.md` when done
</output>
</output>
