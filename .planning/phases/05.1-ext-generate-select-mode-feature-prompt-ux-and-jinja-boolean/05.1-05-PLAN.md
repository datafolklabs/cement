---
phase: 05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean
plan: 05
type: execute
wave: 5
depends_on: ["05.1-04"]
files_modified:
  - cement/ext/ext_generate.py
  - tests/ext/test_ext_generate.py
  - CHANGELOG.md
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06]
must_haves:
  truths:
    - "make test passes at 100% coverage with ruff and mypy clean across the full refactored ext.generate surface"
    - "Every remaining # pragma: nocover in ext_generate.py carries a locked-vocabulary category label (defensive: unreachable / untestable: dynamic import)"
    - "The accept/reject mapping, y/n→bool mapping, and choice label→value mapping are covered by real patched-prompt tests (not pragma'd)"
    - "CHANGELOG.md documents the unified type:/extend:/requires: schema and the #782 fixes under the active DEVELOPMENT section"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Phase 05.1 user-facing changelog entries (#782 fixes)"
      contains: "#782"
    - path: "tests/ext/test_ext_generate.py"
      provides: "Full migrated + new test suite at 100% coverage of ext_generate.py"
      contains: "patch.object"
  key_links:
    - from: "CHANGELOG.md"
      to: "ext.generate unified schema"
      via: "[ext.generate] / [cli] bucketed entries"
      pattern: "ext.generate"
---

<objective>
Close the phase: prove the 100% coverage / ruff / mypy gate holds across the full
refactored `ext.generate` surface, audit every surviving `# pragma: nocover` for
a locked-vocabulary category label, ensure the bug-prone mapping logic
(y/n→bool, accept/reject→bool, choice label→value) is under REAL patched-prompt
coverage rather than pragma, and write the user-facing CHANGELOG entries for the
unified schema and the #782 fixes.

Purpose: GEN-06's gate-green requirement and the CLAUDE.md phase-by-phase
changelog discipline. This is the acceptance plan — it does not add engine
behavior; it hardens coverage quality and records the change for the 3.0.16 cut.
Output: a coverage-audited ext_generate.py, any final mapping-coverage tests,
CHANGELOG.md entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-CONTEXT.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-RESEARCH.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-VALIDATION.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Coverage audit — pragma vocabulary + real coverage for mapping logic</name>
  <files>cement/ext/ext_generate.py, tests/ext/test_ext_generate.py</files>
  <read_first>
    - cement/ext/ext_generate.py (final post-plan-04 state — every # pragma: nocover line and every interactive .prompt() branch)
    - tests/ext/test_ext_generate.py (the full migrated suite — which mapping branches are pragma'd vs patched-prompt covered)
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"100% Coverage Strategy" (pragma the input read, patch-test the mapping) and §"Pitfall 4" and §"Project Constraints" (locked pragma vocabulary: defensive: unreachable / untestable: dynamic import — do NOT invent new labels; # noqa: E501 may sit after a pragma label)
    - .planning/STATE.md Phase 03 Plan 07 notes (the D-15 locked-vocabulary pragma audit precedent)
    - .planning/phases/05.1-.../05.1-VALIDATION.md (sampling rate, manual-only verifications)
  </read_first>
  <action>
    Run `make test` and confirm 100% coverage on ext_generate.py. For every line
    that drops below 100%, decide: is it a raw interactive `shell.Prompt().prompt()`
    READ (pragma `# pragma: nocover  # defensive: unreachable` — cannot run under
    captured stdin) OR a value-MAPPING branch (y/n→bool, accept/reject→bool, choice
    label→value, gated-out default) that CAN be driven via
    `patch.object(shell.Prompt, 'prompt', return_value=...)`? Per RESEARCH, mapping
    branches must get REAL patched-prompt tests, not pragmas — add any missing ones
    so the bug-prone logic is genuinely covered. Audit every surviving
    `# pragma: nocover` to ensure it carries one of the locked-vocabulary labels
    (`defensive: unreachable` for interactive Prompt branches, `untestable: dynamic
    import` for import failures); do NOT invent new labels. `# noqa: E501` may sit
    AFTER a pragma label if the combined trailing comment exceeds 100 chars
    (established at the old ext_generate.py:162). Verify the inverse grep (a
    `# pragma: nocover` without a category label) returns empty.
  </action>
  <verify>
    <automated>make test && grep -n "pragma: no\?cover" cement/ext/ext_generate.py | grep -v "defensive: unreachable\|untestable: dynamic import\|TYPE_CHECKING import" | grep -vc "^$" | grep -q "^0$"</automated>
  </verify>
  <done>`make test` passes at 100% coverage; every # pragma: nocover in ext_generate.py carries a locked-vocabulary label (inverse grep empty); accept/reject, y/n→bool, choice label→value, and gated-out-default mappings are covered by real patched-prompt tests; ruff + mypy clean.</done>
</task>

<task type="auto">
  <name>Task 2: Write CHANGELOG.md entries for the unified schema + #782 fixes</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the active `## 3.0.15 - DEVELOPMENT (will be released as stable/3.0.16)` section and its Bugs/Features/Refactoring buckets; the existing [area]-prefixed one-line entry format)
    - ./CLAUDE.md §"Changelog Maintenance" (append to active DEVELOPMENT section; bucket by Conventional Commit type; one line prefixed with [area]; filter planning-artifact commits; wrap at 78 chars)
    - .planning/phases/05.1-.../05.1-CONTEXT.md (the #782 four points and the D-01..D-19 design) and the GEN-01..GEN-06 requirement descriptions in .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Append entries to the active `## 3.0.15 - DEVELOPMENT` section of CHANGELOG.md,
    bucketed per CLAUDE.md (one line each, `[ext.generate]` prefix, wrapped at 78):
    - Features: `ext.generate` template variables now support `type: string |
      boolean | choice`; former `features:` are expressed under `variables:` via
      `type:` + `extend:` + `requires:`, prompted in declaration order, exposed at
      the top level of the template context.
    - Features: `type: boolean` variables render a default `[(Y)es/(N)o] [default]:`
      prompt; `prompt:` is polymorphic (string label / `false` silent / object
      `{text, accept, reject}`).
    - Bugs: `{% if feature_x %}` now works for a boolean variable — values are
      exposed at the top level of the template context, not under
      `data['features']` (resolves #782 pt 4).
    - Bugs: former features prompt in declaration order alongside variables, not in
      a pre-pass (resolves #782 pt 1); custom per-feature prompt text + vars-style
      input via the polymorphic `prompt:` (resolves #782 pts 2-3).
    Note the `features:`/`prompt_mode`/`enabled:`/`disabled:` schema was unreleased
    (first shipping in 3.0.16) — present this as the shipped design, not a
    breaking change. Do NOT add changelog entries for the planning-artifact commits.
  </action>
  <verify>
    <automated>grep -q "#782" CHANGELOG.md && grep -q "ext.generate" CHANGELOG.md && awk 'length > 78 {print FILENAME":"NR": "length" chars"; bad=1} END{exit bad}' CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md active DEVELOPMENT section has bucketed `[ext.generate]` Features + Bugs entries covering the unified type:/extend:/requires: schema and all four #782 points; no line exceeds 78 chars; no planning-artifact entries added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Acceptance + docs plan; no new code paths or external input boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-12 | Tampering | covered-but-functionally-dead code masked by 100% gate | accept | Phase 03 D-21 risk acknowledged precedent; 100% coverage gate is the strongest available signal at zero dev-dep cost; vulture deliberately not added |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | no package installs (RESEARCH §Package Legitimacy Audit: not applicable) |

No HIGH threats. Acceptance plan — ASVS V5 controls already landed in plans 01-03; this plan verifies the gate and records the change.
</threat_model>

<verification>
- `make test` exits 0 at 100% coverage (full suite)
- `make comply` exits 0 (ruff + mypy)
- inverse pragma grep on ext_generate.py returns empty (every pragma labeled)
- CHANGELOG.md updated, all lines ≤ 78 chars
</verification>

<success_criteria>
- 100% coverage + ruff + mypy green across the full refactored surface (GEN-06)
- every surviving pragma carries a locked-vocabulary label; mapping logic really covered (GEN-04 robustness)
- CHANGELOG documents the unified schema + all four #782 fixes (GEN-01..GEN-06)
</success_criteria>

<output>
Create `.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-05-SUMMARY.md` when done
</output>
