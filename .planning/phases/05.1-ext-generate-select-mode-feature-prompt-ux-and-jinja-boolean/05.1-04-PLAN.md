---
phase: 05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean
plan: 04
type: execute
wave: 4
depends_on: ["05.1-03"]
files_modified:
  - demo/generate-features/templates/generate/webapp/.generate.yml
  - demo/generate-features/README.md
  - demo/generate-features/templates/generate/webapp/Dockerfile
  - demo/generate-features/templates/generate/webapp/docker-compose.yml
  - demo/generate-features/templates/generate/webapp/app.py
  - demo/generate-features/templates/generate/webapp/wsgi.py
  - demo/generate-features/templates/generate/webapp/requirements.txt
autonomous: true
requirements: [GEN-02, GEN-05, GEN-06]
must_haves:
  truths:
    - "The demo/generate-features/ webapp .generate.yml uses the new type:/extend:/requires: schema (no features:/prompt_mode/enabled:/disabled:)"
    - "The demo webapp template conditionals use top-level bool/choice exposure ({% if docker %}, {% if web_framework == 'flask' %}) and render correctly"
    - "The demo README describes the new type:/extend: vocabulary and the {{ bool }} → True/False interpolation gotcha"
    - "All 5 cli generate templates remain byte-identical (they never used features:) and still generate"
  artifacts:
    - path: "demo/generate-features/templates/generate/webapp/.generate.yml"
      provides: "Migrated demo generate config on the unified schema"
      contains: "type: choice"
    - path: "demo/generate-features/README.md"
      provides: "Updated demo docs describing type:/extend:/requires:"
      contains: "type"
  key_links:
    - from: "demo/generate-features/templates/generate/webapp/.generate.yml"
      to: "demo webapp template files"
      via: "top-level bool/choice variables consumed by {% if %} conditionals"
      pattern: "type:"
---

<objective>
Migrate the in-repo `demo/generate-features/` template to the new unified schema
and verify the 5 cli generate templates are unaffected. The demo's webapp
`.generate.yml` is the richest migration exemplar: `docker`/`docker_compose`
(boolean, the latter `requires: [docker]`) and `web_framework` (select → choice
with per-branch ignore + silent `framework_version`). Rewrite it to
`type: boolean`/`type: choice` + `extend:` + top-level `requires:`, update the
demo template files' conditionals to consume top-level bool/choice exposure, and
rewrite the demo README to the new `type:`/`extend:` vocabulary (including the
`{{ bool }}` → `True`/`False` interpolation gotcha).

Purpose: D-19 in-repo migration scope; demonstrates the #782 fix end-to-end
(`{% if docker %}` now works because `docker` is a top-level bool). The cli
templates use NO features (verified in RESEARCH §Migration Surface) — this plan
proves they still generate, it does not invent feature usage in them.
Output: migrated demo `.generate.yml` + template files + README; a verification
that all 5 cli templates still generate unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-CONTEXT.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-RESEARCH.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-PATTERNS.md
@.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the demo webapp .generate.yml + template conditionals + README</name>
  <files>demo/generate-features/templates/generate/webapp/.generate.yml, demo/generate-features/README.md, demo/generate-features/templates/generate/webapp/Dockerfile, demo/generate-features/templates/generate/webapp/docker-compose.yml, demo/generate-features/templates/generate/webapp/app.py, demo/generate-features/templates/generate/webapp/wsgi.py, demo/generate-features/templates/generate/webapp/requirements.txt</files>
  <read_first>
    - demo/generate-features/templates/generate/webapp/.generate.yml (current: features: docker [boolean], docker_compose [boolean, requires: [docker]], web_framework [select, 3 options none/flask/fastapi with per-branch ignore + silent framework_version])
    - demo/generate-features/README.md (describes prompt_mode: boolean and prompt_mode: select — to rewrite)
    - demo/generate-features/templates/generate/webapp/Dockerfile, docker-compose.yml, app.py, wsgi.py, requirements.txt (the rendered template files — check their {% if %}/{{ }} conditionals against top-level bool/choice exposure)
    - demo/generate-features/myapp.py (the demo app entry point — how the template is invoked)
    - .planning/phases/05.1-.../05.1-PATTERNS.md §"Fixture migration template" (select→choice + bool→extend mapping; the demo web_framework/docker are named the richest exemplars) and §"YAML 1.1 landmine"
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"Migration Surface — Demo" (exact rewrite instructions: docker/docker_compose → type: boolean; web_framework → type: choice + extend: keyed by value) and §"Template-context exposure" ({{ bool }} → True/False gotcha)
  </read_first>
  <action>
    Rewrite `demo/generate-features/templates/generate/webapp/.generate.yml` under
    `variables:` (no `features:` key): `project_name` stays as-is (released string
    var); `docker` → `type: boolean` `default: true` with
    `extend: [{when: true, variables: [{name: python_version, prompt: "Python Version (for Docker)", default: "3.13"}]}, {when: false, ignore: ['.*Dockerfile.*', '.*\.dockerignore.*']}]`;
    `docker_compose` → `type: boolean` `default: true` `requires: [docker]` with
    `extend: [{when: false, ignore: ['.*docker-compose.*']}]`; `web_framework` →
    `type: choice` `default: "none"` `options:` (per-option `{value, prompt}` per
    D-16 to keep the descriptive picker labels) + `extend:` keyed by
    `when: <value>` carrying the per-branch `ignore` patterns and the silent
    `framework_version` (`prompt: false`) variable for flask/fastapi. Quote any
    bool-like YAML tokens.

    Inspect the webapp template files (Dockerfile, docker-compose.yml, app.py,
    wsgi.py, requirements.txt) and update their Jinja conditionals to consume the
    top-level bool/choice exposure: `{% if docker %}`, `{% if docker_compose %}`,
    `{% if web_framework == "flask" %}` / `== "fastapi"` etc. — these now work
    because the values land at `data[name]` (the #782 fix). Where a template
    previously read `{% if features.docker %}` (the old namespace), switch to
    `{% if docker %}`.

    Rewrite `demo/generate-features/README.md`: replace all `prompt_mode: boolean`
    / `prompt_mode: select` / `enabled:`/`disabled:` vocabulary with the new
    `type:`/`extend:`/`requires:` schema; document that `{% if x %}` works for a
    top-level bool, and call out the `{{ x }}` → `True`/`False` (capitalized
    Python repr) interpolation gotcha for bool text interpolation.
  </action>
  <verify>
    <automated>cd /src/cement/demo/generate-features && rm -rf /tmp/gen-demo-out && pdm run python myapp.py generate webapp /tmp/gen-demo-out --defaults && test -f /tmp/gen-demo-out/app.py</automated>
  </verify>
  <done>The demo `.generate.yml` has no `features:`/`prompt_mode`/`enabled:`/`disabled:`; `myapp.py generate webapp <dir> --defaults` generates the webapp with `docker`/`docker_compose`/`web_framework` resolved; template `{% if docker %}`/`{% if web_framework == ... %}` conditionals render against top-level exposure; README describes the new schema + the {{ bool }} gotcha.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 5 cli generate templates generate byte-identically (no feature usage)</name>
  <files>cement/cli/templates/generate/project/.generate.yml, cement/cli/templates/generate/plugin/.generate.yml, cement/cli/templates/generate/extension/.generate.yml, cement/cli/templates/generate/script/.generate.yml, cement/cli/templates/generate/todo-tutorial/.generate.yml</files>
  <read_first>
    - cement/cli/templates/generate/project/.generate.yml
    - cement/cli/templates/generate/plugin/.generate.yml
    - cement/cli/templates/generate/extension/.generate.yml
    - cement/cli/templates/generate/script/.generate.yml
    - cement/cli/templates/generate/todo-tutorial/.generate.yml
    - .planning/phases/05.1-.../05.1-RESEARCH.md §"Migration Surface — CLI templates" (all 5 use ONLY variables:/exclude:/ignore: — NONE use features:; D-19's "rewrite the 5 cli templates" reduces to "verify unchanged" — do NOT invent feature usage)
    - tests/cli/test_main.py (the cli generate smoke test, if any — how cli generate is exercised)
  </read_first>
  <action>
    Confirm via grep that none of the 5 cli `.generate.yml` files contain
    `features:`/`prompt_mode`/`enabled:`/`disabled:` — they are already on the
    released `variables:` schema and MUST remain byte-identical (D-06/GEN-06). Do
    NOT edit them. Run each cli generate template with `--defaults` against a temp
    dir to prove the refactored engine still generates them unchanged (the released
    string-variable identity path from plan 01). If `scripts/cli-smoke-test.sh` or
    a cli test already exercises `generate project`, run it; otherwise invoke
    `pdm run cement generate <template> <tmpdir> --defaults` for each of the 5.
    Record the verification (not a rewrite) in the SUMMARY.
  </action>
  <verify>
    <automated>grep -rL "features:\|prompt_mode" cement/cli/templates/generate/*/.generate.yml | wc -l | grep -q 5 && rm -rf /tmp/gen-cli-out && pdm run cement generate project /tmp/gen-cli-out --defaults && test -f /tmp/gen-cli-out/pyproject.toml</automated>
  </verify>
  <done>All 5 cli `.generate.yml` files confirmed free of `features:`/`prompt_mode` (byte-identical); `cement generate project <dir> --defaults` generates successfully against the refactored engine; verification recorded in SUMMARY (no cli template files modified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo/cli `.generate.yml` → ext.generate parser | author-controlled in-repo template files (codegen inputs, not untrusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-10 | Tampering | demo `.generate.yml` migrated schema | mitigate | engine ValueError fail-fast (from plans 01-03) validates the migrated demo config; `--defaults` generate run proves correctness; ASVS V5 |
| T-05.1-11 | Information Disclosure | demo-generated output | accept | local codegen demo only; no secrets; output to temp dir |
| T-05.1-SC | Tampering | npm/pip/cargo installs | n/a | no package installs (RESEARCH §Package Legitimacy Audit: not applicable) |

No HIGH threats. Migration-only plan; ASVS V5 controls inherited from the engine plans.
</threat_model>

<verification>
- demo: `myapp.py generate webapp <dir> --defaults` generates the webapp tree
- cli: all 5 templates generate with `--defaults`; grep confirms zero feature usage
- `make test` exits 0 (full suite still green after demo migration)
- `make comply` exits 0
</verification>

<success_criteria>
- demo/generate-features migrated to type:/extend:/requires:; {% if docker %} works (GEN-02/GEN-05)
- demo README rewritten to the new vocabulary + {{ bool }} gotcha (GEN-06)
- 5 cli templates verified unchanged + still generate (GEN-06)
- 100% coverage + ruff + mypy green
</success_criteria>

<output>
Create `.planning/phases/05.1-ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean/05.1-04-SUMMARY.md` when done
</output>
