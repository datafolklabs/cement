---
phase: 05.1
slug: ext-generate-select-mode-feature-prompt-ux-and-jinja-boolean
status: planned
nyquist_compliant: true
wave_0_complete: true  # scaffolded as Task 1 (RED) of plans 01/02/03
created: 2026-05-29
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 05.1-RESEARCH.md §Validation Architecture (all claims verified).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-cov (100% coverage gate via `fail_under = 100`) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`, `[tool.coverage]`) |
| **Quick run command** | `pdm run pytest tests/ext/test_ext_generate.py -x` |
| **Full suite command** | `make test` (full suite + coverage + PEP8) |
| **Estimated runtime** | ~quick: a few seconds; full `make test`: ~minutes |

---

## Sampling Rate

- **After every task commit:** Run `pdm run pytest tests/ext/test_ext_generate.py -x`
- **After every plan wave:** Run `make test-core` then `make comply`
- **Before `/gsd-verify-work`:** `make test` green (100% coverage + ruff + mypy)
- **Max feedback latency:** quick run < ~15s

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|--------|----------|-----------|-------------------|-------------|--------|
| GEN-01 | `features:` removed; `ValueError` if `prompt_mode`/`enabled`/`disabled` present | unit | `pdm run pytest tests/ext/test_ext_generate.py -k "type or invalid" -x` | ❌ W0 (rewrite test17/18) | ⬜ pending |
| GEN-02 | `type` produces bool/str at `data[name]`; `{% if x %}` works | integration | `pdm run pytest tests/ext/test_ext_generate.py -k "boolean or choice or jinja" -x` | ❌ W0 (new fixtures) | ⬜ pending |
| GEN-03 | declaration-order prompting (features no longer pre-pass) | integration | adapt test15 ordering fixture | ⚠️ adapt | ⬜ pending |
| GEN-04 | bool `[(Y)es/(N)o]` prompt; polymorphic `prompt:`; object junk→assert-abort | unit (patch `shell.Prompt.prompt`) | `pdm run pytest tests/ext/test_ext_generate.py -k "accept or reject or bool_prompt" -x` | ❌ W0 | ⬜ pending |
| GEN-05 | `extend.when` value/list/regex + nesting + top-level `requires:` inject `variables`/`exclude`/`ignore` | integration | adapt test6/7/9/11/14/15/16/19/25 | ⚠️ adapt | ⬜ pending |
| GEN-06 | plain variable byte-identical; cli/demo/fixtures migrated; 100% cov | full suite | `make test` | ✅ test1–5/22/27/29 are the oracle | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Rewrite `tests/ext/test_ext_generate.py` feature tests (test6–test30 cases) for the new schema
- [ ] Migrate ~25 `tests/data/templates/generate/testNN/.generate.yml` fixtures to `type:`/`extend:`/`requires:`
- [ ] New fixtures: `type: boolean` string-prompt parse; polymorphic `prompt:` object (accept/reject); D-14 junk-input abort; `extend.when` regex+list matching
- [ ] New unit tests patching `shell.Prompt.prompt` to cover y/n→bool and accept/reject mapping (the coverable, bug-prone logic) — pattern from `test_no_default`
- [ ] Remove obsolete `prompt_mode`/`enabled`/`disabled` validation tests (test17, test18) or repurpose to a `type:` whitelist check

*Framework already present — no install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live interactive prompt rendering (`[(Y)es/(N)o] [Y]:` text, numbered choice picker) | GEN-04 | Real-tty Prompt I/O cannot run under captured stdin (existing code pragmas these branches) | Run `cd demo/generate-features && python myapp.py generate webapp /tmp/out` (no `--defaults`) and eyeball the prompts and ordering |

*All value-mapping/validation logic (the bug-prone part) is automated via patched `prompt()`; only raw tty rendering is manual.*

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (fixture migration + new mapping tests)
- [ ] No watch-mode flags
- [ ] Feedback latency < ~15s (quick run)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
