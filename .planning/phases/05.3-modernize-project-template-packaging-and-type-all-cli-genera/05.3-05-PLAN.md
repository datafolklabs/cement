---
phase: 05.3
plan: 05
type: execute
wave: 2
depends_on: ["05.3-01", "05.3-03"]
files_modified:
  - "scripts/cli-smoke-test.sh"
autonomous: true
requirements: ["D-07"]
must_haves:
  truths:
    - "The CI cli-smoke-test, after generating a project, runs the generated project's OWN make comply + make test inside each Docker Python version and fails the build (set -e) if either is non-green — the regression guard for D-04/D-05 (D-07)"
    - "The cli-smoke-test also generates todo-tutorial and runs pip install . in the generated dir, proving the D-02/D-03 pdm-backend build-isolation fix — but does NOT run todo's make test (its tutorial stub test deliberately raises Exception)"
    - "A regression (an untyped def, a %-format, a broken generated pyproject) surfaces as a red CI smoke-test, not a silent template drift"
  artifacts:
    - "scripts/cli-smoke-test.sh (project make comply + make test gate; todo generate + build/install)"
  key_links:
    - "The generated project Makefile targets call `pdm run ...` — the smoke-test container must have pdm and a pdm-managed env: pip install pdm, then `pdm install` in the generated project (installs the PEP 735 dev group: ruff/mypy/pytest) BEFORE make comply/make test"
    - "set -e (scripts/cli-smoke-test.sh:2) already fails the build on any non-zero exit — no explicit error handling needed"
    - "todo gate is build/install ONLY (pip install .); NOT make test — todo-tutorial/tests/test_main.py::test_todo does raise Exception as a tutorial stub"
    - "Cost: pdm install + comply + test inside each of 5 Docker versions is noticeably slower; acceptable. May be scoped via PYTHON_VERSIONS if runtime becomes a concern"
---

<objective>
Harden the CI `cli-smoke-test` (scripts/cli-smoke-test.sh, Python 3.10–3.14 Docker matrix) into
the regression guard for this phase's "green out of the box" bar. After generating a `project`,
run the generated project's OWN `make comply` (ruff + mypy) + `make test` inside each container and
let `set -e` fail the build if either is not green (D-04/D-05/D-07). Also generate `todo-tutorial`
and `pip install .` it to prove the D-02/D-03 pdm-backend build-isolation fix — build/install only,
NOT `make test` (todo's tutorial stub test deliberately raises).

Depends on Plan 01 (generated project must be green before the gate enforces it) and Plan 03 (todo
must be pdm-backend-installable). This is the phase's canonical validation surface.

Purpose: D-07 — enforce D-04/D-05 in CI as a regression guard; prove D-02/D-03 via todo install.
Output: An extended cli-smoke-test that fails the build on any non-green generated project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-CONTEXT.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-RESEARCH.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-PATTERNS.md

# The harness to extend
@scripts/cli-smoke-test.sh
</context>

## Artifacts this phase produces (Plan 05)

- `scripts/cli-smoke-test.sh` — after the existing project `pip install .` (L52): install pdm +
  `pdm install` + `make comply` + `make test` on the generated project; and a new
  todo-tutorial section: `cement generate todo-tutorial` + `pip install .` (build/install only).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add generated-project make comply + make test gate to cli-smoke-test (D-04/D-05/D-07)</name>
  <files>scripts/cli-smoke-test.sh</files>
  <read_first>
    - scripts/cli-smoke-test.sh (set -e L2; project generation block L49-56: generate to $tmp/myapp, `pip install .` L52, `myapp --version` grep L53-56; container name cement-cli-smoke-test)
  </read_first>
  <action>
    Immediately after the project `myapp --version` verification block (ends L56), add a
    "generated project green out of the box" section that runs the generated project's own gate
    targets inside the same container. Because the generated `Makefile` targets call `pdm run ...`,
    first make pdm available and install the project's PEP 735 dev group, then run the targets:
    `docker exec cement-cli-smoke-test /bin/bash -c "pip install pdm"`;
    `docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; pdm install"`;
    `docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; make comply"`;
    `docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/myapp ; make test"`.
    `set -e` (already at L2) makes any non-zero exit fail the whole smoke-test — no extra error
    handling needed. Do not remove the existing `pip install .` / `myapp --version` checks. Keep the
    script's `docker exec ... /bin/bash -c "..."` idiom and the `$tmp`/`$pyver` variables intact.
    (Cost note for the SUMMARY: this adds a `pdm install` + comply + test per Python version; it is
    acceptable but slower, and can be scoped via `PYTHON_VERSIONS` if needed.)
  </action>
  <verify>
    <automated>bash -n scripts/cli-smoke-test.sh && grep -q 'pip install pdm' scripts/cli-smoke-test.sh && grep -q 'cd $tmp/myapp ; pdm install' scripts/cli-smoke-test.sh && grep -q 'cd $tmp/myapp ; make comply' scripts/cli-smoke-test.sh && grep -q 'cd $tmp/myapp ; make test' scripts/cli-smoke-test.sh && echo SMOKE_PROJECT_GATE_OK</automated>
  </verify>
  <done>
    cli-smoke-test installs pdm, runs `pdm install` + `make comply` + `make test` on the generated
    project inside each container; the script passes `bash -n` syntax check.
  </done>
  <acceptance_criteria>
    scripts/cli-smoke-test.sh contains the pdm install + generated-project `make comply` + `make test`
    steps after the project generation block; a full/single-version smoke-test run fails the build if
    the generated project is not green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add todo-tutorial generate + build/install to cli-smoke-test (D-07, proves D-02/D-03)</name>
  <files>scripts/cli-smoke-test.sh</files>
  <read_first>
    - scripts/cli-smoke-test.sh (plugin block ends ~L77; `### finish` block ~L79-83; `cement generate -h` already greps for `todo-tutorial` at L40 but todo is never generated/built today)
  </read_first>
  <action>
    Before the `### finish` section (~L79), add a new "generate + build/install todo-tutorial"
    section that proves the D-02/D-03 packaging fix under PEP 517 build isolation:
    `docker exec cement-cli-smoke-test /bin/bash -c "cement generate todo-tutorial -D $tmp/todo"`;
    `docker exec cement-cli-smoke-test /bin/bash -c "cd $tmp/todo ; pip install ."`.
    `pip install .` builds the todo wheel via pdm-backend (regex-reads `__version__`, no build-time
    import) and installs its deps — a green exit proves the migration. Do NOT run `make test` or
    `make comply` on todo: `todo-tutorial/tests/test_main.py::test_todo` deliberately
    `raise Exception` as a tutorial stub, so a test gate would always fail. `set -e` fails the build
    if the todo build/install is non-zero.
  </action>
  <verify>
    <automated>bash -n scripts/cli-smoke-test.sh && grep -q 'cement generate todo-tutorial -D $tmp/todo' scripts/cli-smoke-test.sh && grep -q 'cd $tmp/todo ; pip install .' scripts/cli-smoke-test.sh && ! grep -q 'cd $tmp/todo ; make test' scripts/cli-smoke-test.sh && echo SMOKE_TODO_GATE_OK</automated>
  </verify>
  <done>
    cli-smoke-test generates todo-tutorial and runs pip install . (build/install only, no make test);
    the script passes bash -n.
  </done>
  <acceptance_criteria>
    scripts/cli-smoke-test.sh generates `todo-tutorial` and `pip install .`s it (proving D-02/D-03),
    with no todo `make test` step; a smoke-test run fails the build if todo does not build/install.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → generated-project install/build inside Docker matrix | The smoke-test builds and installs generated projects in ephemeral containers with network access to PyPI |

The smoke-test is a CI-only verification harness; it introduces no runtime service. Its integrity
value is catching template regressions before release.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.3-07 | Tampering | template drift shipping non-green generated output | medium | mitigate | This plan IS the mitigation — `make comply` + `make test` (project) and `pip install .` (todo) under `set -e` fail the build on any regression |
| T-05.3-SC | Tampering | pdm + generated-project deps pulled from PyPI in-container | low | accept | Established packages (pdm, cement, tinydb, ruff, mypy); no packages installed INTO cement; ephemeral containers; RESEARCH Package Legitimacy Audit clean |
</threat_model>

<verification>
- `bash -n scripts/cli-smoke-test.sh` passes; the new steps are present (greps above).
- Phase gate (canonical): `make cli-smoke-test` green on the matrix, or a single-version run
  `PYTHON_VERSIONS=3.12 make cli-smoke-test` green locally where Docker is available — the generated
  project's `make comply` + `make test` pass and todo `pip install .` succeeds.
- cement's own suite unaffected (`tests/cli/test_main.py::test_generate` still passes).
</verification>

<success_criteria>
- The cli-smoke-test enforces generated-project `make comply` + `make test` and fails on non-green (D-07).
- The cli-smoke-test builds/installs todo-tutorial, proving the D-02/D-03 fix.
- No todo `make test` gate (tutorial stub raises by design).
</success_criteria>

<output>
Create `.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-05-SUMMARY.md` when done
</output>
