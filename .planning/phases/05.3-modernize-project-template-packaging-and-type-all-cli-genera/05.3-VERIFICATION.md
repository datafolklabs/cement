---
phase: 05.3-modernize-project-template-packaging-and-type-all-cli-genera
verified: 2026-07-11T23:55:00Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/12
  gaps_closed:

    - "todo-tutorial's shipped `make comply` (ruff check .) is green out of the box"
    - "Generated project's canonical test template validates real rendered output (membership assertions, not `.find()`)"
    - "CHANGELOG.md is updated phase-by-phase per CLAUDE.md's Changelog Maintenance policy"
  gaps_remaining: []
  regressions: []
human_verification:

  - test: "Run the full CI cli-smoke-test matrix: `make cli-smoke-test` (or `PYTHON_VERSIONS=3.12 make cli-smoke-test`) with Docker + network access to PyPI."
    expected: "All 5 python versions complete without error: project pip install + `myapp --version` grep, `pdm install` + `make comply` + `make test` on the generated project, script/extension/plugin generation grep checks, and todo-tutorial `pip install .` all succeed under `set -e`."
    why_human: "Requires Docker containers pulling python:3.10-3.14 images and live PyPI network access; not runnable in this sandboxed verification environment. Independent local reproduction of the equivalent commands (ruff/mypy/pytest on generated project and todo-tutorial; `pip install --dry-run .` on both) all passed in this re-verification pass, and the previously-uncaught todo ruff-comply gap is now closed AND independently guarded by a new in-suite regression test (`test_generate_todo_ruff_clean`), so this is a high-confidence pass pending the actual CI run."
---

# Phase 05.3: Modernize project template packaging and type all CLI-generated templates Verification Report

**Phase Goal:** Modernize the packaging/setup of all 5 `cement generate` templates and make every generated file fully typed and idiomatically clean. Extends Phase 01.1 (which modernized only the `project` build) to `script`, `extension`, `plugin`, and `todo-tutorial`; migrates `todo-tutorial` off setup.py to pdm-backend; ships ruff/mypy/pytest config so a freshly generated project is `make comply` + `make test` green out of the box, enforced via the CI cli-smoke-test. Resolves #735.
**Verified:** 2026-07-11
**Status:** human_needed
**Re-verification:** Yes — after gap closure (Plan 05.3-06)

## Goal Achievement

This is a re-verification after gap-closure Plan 05.3-06. All three previously-failed
truths were independently re-checked against the actual codebase — by generating fresh
`todo-tutorial` and `project` output in this sandbox and running the pinned tools
(`ruff 0.15.12`, `mypy`, `pytest`) against the RENDERED output, not by trusting
05.3-06-SUMMARY.md's claims. A live regression-catch proof was also run for the new guard
test (temporarily reintroducing an unsorted import and confirming the test fails, then
restoring the file cleanly).

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: All 5 templates (`project`, `script`, `extension`, `plugin`, `todo-tutorial`) are in scope and modified | ✓ VERIFIED | Regression-checked: generated all 5; extension has `TYPE_CHECKING`/`load(app: "App") -> None`; plugin keeps `self._parser`; script keeps `VERSION` tuple |
| 2 | D-04: A freshly generated `project` passes `ruff check .` + `mypy` + `pytest` with zero manual fixups | ✓ VERIFIED | Re-generated `project --defaults`; `ruff check .` → "All checks passed!"; `mypy` → "Success: no issues found in 12 source files"; `pytest tests/` → "3 passed" |
| 3 | D-02/D-03: `todo-tutorial` builds/installs under PEP 517 default build isolation | ✓ VERIFIED (carried, unaffected by Plan 06) | Not touched by gap-closure; regression-checked via generation, unchanged |
| 4 | D-02: Legacy setup.py-era files deleted from todo-tutorial; version.py is Shape B | ✓ VERIFIED (carried) | Unaffected by Plan 06; regression-checked |
| 5 | D-05: script/extension/plugin controller code typed; extension TYPE_CHECKING idiom; plugin `self._parser` | ✓ VERIFIED (carried) | Confirmed unchanged in fresh generation |
| 6 | D-08: script VERSION tuple retained; VERSION_BANNER f-string | ✓ VERIFIED (carried) | Confirmed unchanged |
| 7 | D-05: `todo/main.py` TodoError NameError fixed | ✓ VERIFIED (carried) | `except TodoError as e:` still present |
| 8 | D-05: All generated `todo/` package typed and mypy-clean | ✓ VERIFIED | Re-ran mypy on freshly generated todo-tutorial → "Success: no issues found in 11 source files" |
| 9 | D-07: cli-smoke-test structurally gates generated project's `make comply`+`make test`, builds/installs todo-tutorial | ✓ VERIFIED (structural, carried) | `bash -n scripts/cli-smoke-test.sh` passes; content unchanged by Plan 06; full Docker-matrix run remains a human item (see below) |
| 10 (was gap 1) | todo-tutorial's shipped `make comply` (ruff) gate is green out of the box | ✓ VERIFIED | Freshly generated todo-tutorial: `ruff check .` → "All checks passed!" (exit 0). `[tool.ruff]` now carries `extend-exclude = ["tests"]` mirroring the mypy Option-B scope. `tests/conftest.py` confirmed unmodified (`git diff --quiet` on that path is clean). New regression guard `test_generate_todo_ruff_clean` in `tests/cli/test_main.py` passes, and was proven to actually catch a regression: reintroducing an unsorted import into the template made the test FAIL with the real ruff output in the assertion message; file was then restored and confirmed clean via `git diff --quiet` |
| 11 (was gap 2) | Canonical project test template validates real rendered output, not a false-green assertion | ✓ VERIFIED | `cement/cli/templates/generate/project/tests/test_{{ label }}.py` now uses `assert 'Foo => bar' in output` / `assert 'Foo => not-bar' in output`; `grep -c 'output.find('` on the file returns 0; generated a fresh project and ran `test_command1` — 3 passed |
| 12 (was gap 3) | CHANGELOG.md updated phase-by-phase per CLAUDE.md policy | ✓ VERIFIED | `git diff main...HEAD -- CHANGELOG.md` is non-empty; active `## 3.0.15 - DEVELOPMENT` section (confirmed at file head) carries new `[cli]` Bugs/Refactoring entries and `[dev]` Misc entries covering the todo-tutorial migration, gate config, TodoError fix, assertion fix, smoke-test gate, and the new ruff guard — all referencing #735 |

**Score:** 12/12 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cement/cli/templates/generate/todo-tutorial/todo/main.py` | isort-clean imports | ✓ VERIFIED | Rendered output confirmed clean via `ruff check .`; groups: stdlib / third-party / local, blank-line separated |
| `cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py` | isort-clean imports | ✓ VERIFIED | Read directly: third-party group then blank line then local `from ..core.version import get_version` |
| `cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py` | isort-clean imports | ✓ VERIFIED | Read directly: stdlib `from time import strftime` then blank line then third-party `from cement import Controller, ex` |
| `cement/cli/templates/generate/todo-tutorial/pyproject.toml` | `[tool.ruff]` tests-excluded scope | ✓ VERIFIED | `extend-exclude = ["tests"]` present under `[tool.ruff]`, with explanatory comment mirroring the mypy scope comment |
| `tests/cli/test_main.py` | generated-todo ruff-clean regression guard | ✓ VERIFIED + WIRED | `test_generate_todo_ruff_clean(tmp)` generates todo-tutorial in-process then runs `subprocess.run(['ruff','check','.'], cwd=tmp.dir)`; passes on green, and independently proven to fail on a reintroduced regression |
| `cement/cli/templates/generate/project/tests/test_{{ label }}.py` | membership assertions | ✓ VERIFIED | Both `output.find(...)` replaced with `'...' in output`; zero occurrences of `output.find(` remain |
| `CHANGELOG.md` | `[cli]`/`[dev]` entries under active 3.0.15 - DEVELOPMENT | ✓ VERIFIED | Diff confirmed non-empty, entries present in Bugs/Refactoring/Misc buckets, correctly attributed to #735, active section (not a stale one) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| Generated `todo-tutorial/pyproject.toml` `[tool.ruff]` (`extend-exclude=["tests"]`) | Makefile `comply-ruff` (`ruff check .`) | full-directory ruff run resolves scoped config | ✓ WIRED | Freshly generated + run: exit 0, "All checks passed!" — previously ✗ NOT WIRED, now closed |
| `tests/cli/test_main.py::test_generate_todo_ruff_clean` | generated todo-tutorial rendered output | in-process generate + `subprocess.run(['ruff','check','.'])` | ✓ WIRED | Passes on current template state; regression-proven to fail when template regresses (live test performed during this verification) |
| `project/tests/test_{{ label }}.py::test_command1` | `app.last_rendered` output | membership assertion | ✓ WIRED | Ran against real generated+rendered output; 3 passed |
| `scripts/cli-smoke-test.sh` generated-project block | `pdm install` -> `make comply` -> `make test` | `docker exec` sequence, `set -e` | ✓ WIRED (structural, carried) | Unchanged by Plan 06; full execution remains a human/CI item |
| `scripts/cli-smoke-test.sh` todo block | `cement generate todo-tutorial` -> `pip install .` | `docker exec` sequence | ✓ WIRED (structural, carried) | Unchanged; deliberately build/install-only per D-07 — the new `test_generate_todo_ruff_clean` guard is what now covers the ruff-comply gap CI doesn't reach |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Generated todo-tutorial ruff green | `cement generate todo-tutorial --defaults` then `ruff check .` (pinned 0.15.12) | "All checks passed!", exit 0 | ✓ PASS |
| Generated todo-tutorial mypy green | same generation, `mypy` (Option-B scope) | "Success: no issues found in 11 source files" | ✓ PASS |
| Generated project ruff/mypy/pytest green | `cement generate project --defaults` then all three | ruff: all passed; mypy: 12 files clean; pytest: 3 passed, 70% cov | ✓ PASS |
| New guard test passes | `pdm run pytest tests/cli/test_main.py -k generate_todo_ruff_clean -v` | 1 passed | ✓ PASS |
| New guard test actually catches regressions | Reintroduced an unsorted import (`from cement import App2` misplaced) into `todo/main.py` template, re-ran the guard test | Test FAILED with real ruff I001/F401 output in assertion message; file restored via backup, `git diff --quiet` confirms clean | ✓ PASS |
| No `output.find(` remains in project test template | `grep -c "output.find(" ".../test_{{ label }}.py"` | `0` | ✓ PASS |
| CHANGELOG.md diff present | `git diff main...HEAD -- CHANGELOG.md` | non-empty, `[cli]`/`[dev]` entries under active section | ✓ PASS |
| cement's own full suite unaffected | `pdm run pytest --cov=cement tests/` | 386 passed, 100.00% coverage | ✓ PASS |
| cement's own comply gate unaffected | `make comply` | ruff: all checks passed; mypy: 51 source files clean | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes exist for this phase; N/A.

### Requirements Coverage

Phase-local, no REQ-IDs assigned. Decision coverage (D-01..D-08 from 05.3-CONTEXT.md), re-verified:

| Decision | Description | Status | Evidence |
|----------|-------------|--------|----------|
| D-01 | All 5 templates in scope | ✓ SATISFIED | Confirmed unchanged/regression-checked |
| D-02 | todo-tutorial pdm-backend migration + legacy file deletion | ✓ SATISFIED | Confirmed unchanged (not in Plan 06 scope) |
| D-03 | Close PEP 517 build-isolation bug | ✓ SATISFIED | Confirmed unchanged |
| D-04 | project "green out of the box" | ✓ SATISFIED | Confirmed exit 0 on all three tools; the underlying canonical test now also asserts meaningfully (gap 2 closed) |
| D-05 | All generated Python fully typed | ✓ SATISFIED | Unchanged from prior pass; `plugin/{{ label }}/__init__.py`'s untyped `add_template_dir(app)` remains an informational note (out of declared file scope, not a stated must-have) |
| D-06 | Idiom modernization | ✓ SATISFIED | Confirmed unchanged |
| D-07 | CI cli-smoke-test enforces D-04/D-05 as regression guard | ✓ SATISFIED (now closed) | CI's structural gate on `project` unchanged; the todo ruff-comply hole D-07 left open (CI's todo block is deliberately build/install-only) is now filled by the new in-suite `test_generate_todo_ruff_clean` guard, which runs on every `make test` |
| D-08 | script VERSION tuple discretion | ✓ SATISFIED | Confirmed unchanged |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `cement/cli/templates/generate/todo-tutorial/tests/conftest.py` | 29 | Pre-existing `cleanup` (should be `t.cleanup`, F821) | ℹ️ Info (carried, unchanged) | Pre-existing, predates this phase, deliberately excluded from the ruff gate via `extend-exclude = ["tests"]` (LOCKED decision) rather than fixed — this is the intended, documented resolution of what was gap 1b, not a new anti-pattern |
| `cement/cli/templates/generate/script/{{ label }}.py`, `todo-tutorial/todo/main.py` | — | `close_on_exit = True` (invalid `App.Meta` attribute; should be `exit_on_close`) | ⚠️ Warning (carried, unchanged) | Pre-existing, predates this phase's scope (confirmed via `git log`), out of D-01..D-08 scope, unaffected by Plan 06 |
| `cement/cli/templates/generate/plugin/{{ label }}/__init__.py` | 5 | `def add_template_dir(app):` untyped | ℹ️ Info (carried, unchanged) | Outside Plan 02's declared file scope; unaffected by Plan 06 |

No new anti-patterns introduced by Plan 06. No debt markers (`TBD`/`FIXME`/`XXX`) found in any of the 7 files Plan 06 modified — the `TODO` string matches found are all part of the tutorial's "TODO Application" domain name, not debt markers.

## Deferred Items

None. No later milestone phase addresses any remaining item — there are no remaining gaps.

## Human Verification Required

1. **Test:** Run `make cli-smoke-test` (full 5-version Docker matrix) or `PYTHON_VERSIONS=3.12 make cli-smoke-test`.
   **Expected:** All steps green, including the generated project's `pdm install`+`make comply`+`make test` gate and the todo-tutorial `pip install .`.
   **Why human:** Requires Docker + live PyPI network access not available in this verification sandbox. This is a carried-over item from the initial verification pass — it was never about the 3 closed gaps (CI's todo block is deliberately build/install-only per D-07, so it would not have caught the ruff-comply gap anyway; that hole is now independently covered by the new `test_generate_todo_ruff_clean` regression guard, which WAS executed and proven in this pass). All locally-reproducible equivalents of the CI matrix steps (ruff/mypy/pytest on generated project and todo-tutorial; `pip install --dry-run .` on both) passed in this re-verification.

## Gaps Summary

None remaining. All three gaps from the initial verification pass are confirmed closed against
the actual codebase (not merely claimed in 05.3-06-SUMMARY.md):

1. **todo-tutorial `make comply` (ruff) gate** — now genuinely green out of the box on a fresh
   generation, scoped correctly to exclude the pre-existing out-of-scope `tests/` bugs, and backed
   by a new regression-guard test in cement's own suite that was independently proven (in this
   verification session) to catch a reintroduced import-order regression.

2. **Canonical project test template false-green assertion** — now uses genuine membership
   assertions; independently re-generated and re-ran the test to confirm it exercises real
   rendered output.

3. **CHANGELOG.md** — now carries `[cli]`/`[dev]` entries under the active `3.0.15 - DEVELOPMENT`
   section covering the full phase, per CLAUDE.md's phase-by-phase policy.

The only remaining item is the pre-existing (never a "gap") full CI Docker-matrix run, which
cannot execute in this sandboxed environment and was flagged the same way in the initial
verification pass. This routes the overall status to `human_needed` rather than `passed`, per the
verifier's decision tree — not because any must-have failed.

---

_Verified: 2026-07-11_
_Verifier: Claude (gsd-verifier)_
