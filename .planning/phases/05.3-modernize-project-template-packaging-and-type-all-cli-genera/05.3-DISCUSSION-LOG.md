# Phase 05.3: Modernize project template packaging and type all CLI-generated templates - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-11
**Phase:** 05.3-modernize-project-template-packaging-and-type-all-cli-genera
**Areas discussed:** Template scope, todo-tutorial packaging, Generated-code quality bar, Idiom depth, CI enforcement, Tutorial docs

---

## Template scope

| Option | Description | Selected |
|--------|-------------|----------|
| project | Main `generate project` scaffold | ✓ |
| script | Single-file `generate script` | ✓ |
| extension / plugin | `generate extension` + `generate plugin` | ✓ |
| todo-tutorial | Tutorial scaffold, setup.py-era, GitBook-coupled | ✓ |

**User's choice:** All 5 templates in scope.
**Notes:** No template excluded despite the GitBook coupling of todo-tutorial.

---

## todo-tutorial packaging

| Option | Description | Selected |
|--------|-------------|----------|
| Migrate to pdm-backend | Same modern pyproject.toml shape as `project` | ✓ |
| Leave legacy, defer to docs | Keep setup.py-era to match GitBook tutorial | |
| Exclude todo-tutorial | Only touch project/script/extension/plugin | |

**User's choice:** Migrate to pdm-backend.
**Notes:** Also closes the latent PEP 517 build-isolation bug (setup.py imports
`todo.core.version` at build time) — same class of bug Phase 01.1 fixed for `project`.

---

## Generated-code quality bar

| Option | Description | Selected |
|--------|-------------|----------|
| Full: typed + strict comply + shipped config | Annotated code + shipped ruff/mypy/pytest config; `make comply`+`make test` green out of the box | ✓ |
| Typed only, no shipped tool config | Annotate + idiom cleanup, no ruff/mypy config in generated project | |
| Types + ruff, skip mypy config | Annotate + ship ruff only | |

**User's choice:** Full — typed + strict comply + shipped config.

---

## Idiom depth

| Option | Description | Selected |
|--------|-------------|----------|
| Modernize idioms | f-strings over %-formatting, tidy imports, modern stdlib | ✓ |
| Types + minimal touch | Add annotations only, leave existing style | |

**User's choice:** Modernize idioms.
**Notes:** Generated code is a teaching surface — clean/modern idioms matter.

---

## CI enforcement (follow-up to quality bar)

| Option | Description | Selected |
|--------|-------------|----------|
| Extend cli-smoke-test to comply+test | Run generated project's own `make comply`+`make test` in CI matrix | ✓ |
| Smoke: generate + install only | Keep current CI, one-time manual verification | |
| Add a dedicated test | pytest test in cement's suite asserting ruff/mypy clean | |

**User's choice:** Extend cli-smoke-test to run comply+test across Py 3.10–3.14.

---

## Tutorial docs (follow-up to todo-tutorial migration)

| Option | Description | Selected |
|--------|-------------|----------|
| Capture follow-up todo | Migrate template now; file pending todo to update GitBook | ✓ |
| Update tutorial docs in-phase | Attempt GitBook update within this phase | |
| Add migration note to template | Ship note in README/CHANGELOG + follow-up todo | |

**User's choice:** Capture follow-up todo (created:
`.planning/todos/pending/2026-07-11-update-gitbook-todo-tutorial-for-pdm-backend.md`).
**Notes:** Ship a brief in-template packaging note as well (folded into CONTEXT specifics).

---

## Claude's Discretion

- Plan/wave breakdown, per-template ordering, and how the shipped
  ruff/mypy/pytest config is templated (subject to the "green out of the box" test).
- Whether `script`'s `VERSION` tuple convention is unified with `project`'s
  `__version__` file-source convention or kept as-is.

## Deferred Ideas

- Update the external GitBook `todo-tutorial` walkthrough for pdm-backend packaging —
  captured as a pending todo (blocked on the Phase 05.3 migration landing).
