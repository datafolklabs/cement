---
phase: 05.3
plan: 06
type: execute
wave: 1
depends_on: ["05.3-01", "05.3-03", "05.3-04", "05.3-05"]
files_modified:
  - "cement/cli/templates/generate/todo-tutorial/todo/main.py"
  - "cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py"
  - "cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py"
  - "cement/cli/templates/generate/todo-tutorial/pyproject.toml"
  - "tests/cli/test_main.py"
  - "cement/cli/templates/generate/project/tests/test_{{ label }}.py"
  - "CHANGELOG.md"
autonomous: true
gap_closure: true
requirements: ["D-01", "D-04", "D-05", "D-06", "D-07"]
must_haves:
  truths:
    - "A freshly generated todo-tutorial (`cement generate todo-tutorial <dest> --defaults`) passes its shipped `make comply` ruff gate (`ruff check .`) with exit 0 out of the box — the three Plan-04-modified files (todo/main.py, todo/controllers/base.py, todo/controllers/items.py) are isort-clean (no I001) (gap 1a, D-04/D-06)"
    - "todo-tutorial's `[tool.ruff]` is scoped to exclude the out-of-scope tests/ dir (mirroring the mypy `files=['todo/']` Option-B scope), so the pre-existing tests/conftest.py bugs (F821 cleanup->t.cleanup, I001, UP004) do not block comply-ruff (gap 1b, LOCKED user decision)"
    - "todo-tutorial/tests/conftest.py is NOT modified by this plan (its pre-existing bugs predate and are out of this phase's scope; they are excluded from the gate, not fixed)"
    - "cement's own test suite has a guard that generates todo-tutorial and runs ruff against the RENDERED output, failing on any future import-order regression — because CI cli-smoke-test's todo block is deliberately build/install-only per D-07 and never runs make comply on todo (gap 1c)"
    - "The canonical project test template (project/tests/test_{{ label }}.py) asserts membership (`'Foo => bar' in output` / `'Foo => not-bar' in output`) not `output.find(...)`, so it genuinely fails when the template renders wrong or empty output (gap 2)"
    - "CHANGELOG.md's active `## 3.0.15 - DEVELOPMENT` section carries [cli]-bucketed entries covering the 05.3 packaging/typing changes and these gap fixes (gap 3, per CLAUDE.md phase-by-phase policy)"
  artifacts:
    - "cement/cli/templates/generate/todo-tutorial/todo/main.py (isort-clean imports)"
    - "cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py (isort-clean imports)"
    - "cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py (isort-clean imports)"
    - "cement/cli/templates/generate/todo-tutorial/pyproject.toml ([tool.ruff] tests-excluded scope)"
    - "tests/cli/test_main.py (generated-todo ruff-clean regression guard test)"
    - "cement/cli/templates/generate/project/tests/test_{{ label }}.py (membership assertions)"
    - "CHANGELOG.md ([cli] entries under 3.0.15 - DEVELOPMENT)"
  key_links:
    - "Generated todo Makefile `comply-ruff` runs `ruff check .` -> resolves the generated pyproject.toml [tool.ruff] tests-excluded scope -> lints only the todo/ package -> exit 0 after the import-sort fix"
    - "The new tests/cli/test_main.py guard generates todo-tutorial to a tmp dir and runs ruff against the RENDERED output (not the template source) -> catches a future template import-order regression on every `make test`, not only in the slow Docker matrix"
    - "Root cause of gap 1: Plan 03/04 self-checks ran only mypy, never ruff, against generated todo — the guard closes that hole by re-running ruff on generated output"
---

<objective>
Close the three blocker gaps from 05.3-VERIFICATION.md with the smallest correct diffs. The
architecture, packaging modernization (D-01/D-02/D-03/D-08), and typing work (D-05/D-06) are all
verified working end-to-end — do NOT replan or touch them. Scope is exactly:

- Gap 1 (blocker): todo-tutorial `make comply` (ruff) is broken out of the box — I001 unsorted
  imports in the three Plan-04-modified template files, plus the gate lints the out-of-scope
  tests/ dir. Fix the imports (1a), scope [tool.ruff] to exclude tests/ (1b, LOCKED decision),
  and add a regression guard (1c) since CI's todo block is build/install-only (D-07).
- Gap 2 (blocker): the canonical project test template uses `assert output.find(...)`, a
  false-green idiom (str.find() returns -1, which is truthy). Switch to `in output` membership.
- Gap 3 (blocker): CHANGELOG.md was never updated this phase — add [cli]-bucketed entries under
  the active DEVELOPMENT section per CLAUDE.md's phase-by-phase policy.

CRITICAL: these are Jinja2 TEMPLATE sources under cement/cli/templates/generate/. Fixes are made
to the templates and verified by GENERATING a fresh project/todo-tutorial and running the real
tools (ruff, pytest) against the RENDERED output — never by running tools on the template source.

Purpose: make the flagship "green out of the box" bar actually hold for todo-tutorial and the
canonical project test, and satisfy the changelog policy. Output: a clean re-verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-CONTEXT.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-VERIFICATION.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-PATTERNS.md

# The plan that shipped the todo ruff gate + smoke-test structure
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-05-PLAN.md
</context>

## Artifacts this plan produces (Plan 06)

- New test function in `tests/cli/test_main.py` (e.g. `test_generate_todo_ruff_clean`) — generates
  todo-tutorial to a tmp dir and asserts `ruff check .` on the rendered output exits 0.
- New `[tool.ruff]` scope key in `cement/cli/templates/generate/todo-tutorial/pyproject.toml`
  (`extend-exclude = ["tests"]`) with an explanatory comment mirroring the existing mypy scope
  comment.
- Corrected (isort-grouped) import blocks in the three todo template source files.
- New `[cli]`/`[dev]`-bucketed CHANGELOG.md entries under `## 3.0.15 - DEVELOPMENT`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix todo-tutorial ruff comply — isort imports (gap 1a) + scope [tool.ruff] to exclude tests/ (gap 1b)</name>
  <files>cement/cli/templates/generate/todo-tutorial/todo/main.py, cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py, cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py, cement/cli/templates/generate/todo-tutorial/pyproject.toml</files>
  <read_first>
    - cement/cli/templates/generate/todo-tutorial/todo/main.py (import block lines 2-9: `import os`, tinydb, cement, relative imports all un-grouped)
    - cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py (import block lines 2-4: cement + relative, no blank-line group separation)
    - cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py (import block lines 2-3: stdlib `time` + third-party cement, no blank line between groups)
    - cement/cli/templates/generate/todo-tutorial/pyproject.toml (existing [tool.ruff] lines 46-49; [tool.ruff.lint] lines 51-60; [tool.mypy] Option-B `files = ["todo/"]` + its scope comment lines 62-74 — the pattern to mirror)
    - cement/cli/templates/generate/todo-tutorial/Makefile (comply-ruff target runs `pdm run ruff check .` — the gate command to satisfy)
    - cement/cli/templates/generate/todo-tutorial/tests/conftest.py (DO NOT MODIFY — its pre-existing F821 cleanup->t.cleanup, I001, UP004 bugs are out of scope and are excluded from the gate, not fixed)
  </read_first>
  <action>
    Part A (gap 1a) — fix the I001 unsorted-imports in the three files by applying isort grouping
    (stdlib, then third-party, then local-relative, each group separated by a single blank line and
    alphabetically sorted within group). The reliable method: run `ruff check --fix .` (or
    `ruff check --fix --select I .`) on a freshly GENERATED todo-tutorial and port the corrected
    import blocks verbatim back into these three TEMPLATE files (there are no Jinja2 substitutions
    inside these import blocks, so the rendered fix is byte-identical to the template edit). Target
    ordering to expect:
    In todo/main.py: `import os`; blank line; third-party group `from cement import App, TestApp, init_defaults`, `from cement.core.exc import CaughtSignal`, `from cement.utils import fs`, `from tinydb import TinyDB`; blank line; local group `from .controllers.base import Base`, `from .controllers.items import Items`, `from .core.exc import TodoError`.
    In todo/controllers/base.py: third-party group `from cement import Controller, ex`, `from cement.utils.version import get_version_banner`; blank line; local `from ..core.version import get_version`.
    In todo/controllers/items.py: stdlib `from time import strftime`; blank line; third-party `from cement import Controller, ex`.
    Do NOT change any non-import code, keep all runtime behavior identical, and do not touch the
    close_on_exit / other pre-existing template quirks (out of scope).
    Part B (gap 1b, LOCKED user decision) — scope the todo-tutorial `[tool.ruff]` to the todo/
    package only, mirroring the mypy Option-B `files = ["todo/"]` scope. Add
    `extend-exclude = ["tests"]` to the `[tool.ruff]` table (keep the existing target-version /
    line-length / indent-width keys) with a short comment mirroring the mypy scope comment already
    in this file — rationale: the tutorial tests intentionally raise Exception and use a hand-rolled
    conftest with pre-existing lint bugs, so the ruff gate does not run over todo tests. Use
    `extend-exclude` (adds to ruff's default excludes) not `exclude`. DO NOT modify tests/conftest.py.
    Validation is by GENERATING a fresh todo-tutorial and running the pinned ruff (0.15.x, matching
    the template's `ruff~=0.15.12` dev pin) against the rendered output — not against the template.
  </action>
  <verify>
    <automated>DEST=$(mktemp -d)/todo && pdm run cement generate todo-tutorial "$DEST" --defaults && ( cd "$DEST" && pdm run ruff check . ) && git -C "$(git rev-parse --show-toplevel)" diff --quiet -- cement/cli/templates/generate/todo-tutorial/tests/conftest.py && echo TODO_RUFF_GREEN_CONFTEST_UNTOUCHED</automated>
  </verify>
  <done>
    A freshly generated todo-tutorial passes `ruff check .` with exit 0; the three fixed template
    files are isort-clean (no I001); todo-tutorial/pyproject.toml `[tool.ruff]` carries
    `extend-exclude = ["tests"]`; tests/conftest.py is unmodified in git.
  </done>
  <acceptance_criteria>
    - Behavior: `cement generate todo-tutorial <dest> --defaults` then `ruff check .` (pinned ruff
      0.15.x) inside `<dest>` exits 0 (was 3+ I001 errors before).
    - Source: cement/cli/templates/generate/todo-tutorial/pyproject.toml `[tool.ruff]` table contains
      `extend-exclude = ["tests"]`; `grep -c 'extend-exclude' <file>` >= 1.
    - Source: `git diff` shows NO change to
      cement/cli/templates/generate/todo-tutorial/tests/conftest.py.
    - Source: the three todo import blocks have blank-line-separated stdlib/third-party/local groups.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add generated-todo ruff-clean regression guard to cement's own suite (gap 1c)</name>
  <files>tests/cli/test_main.py</files>
  <read_first>
    - tests/cli/test_main.py (existing `test_generate(tmp)` at lines 22-28 — the in-process pattern to mirror: `App(argv=['generate', 'project', tmp.dir, '--defaults'])` then `app.run()` + filesystem assert; module imports `os`, `CementTestApp as App`)
    - cement/cli/templates/generate/todo-tutorial/pyproject.toml (the `[tool.ruff]` config the guard must exercise — ruff resolves it from the generated dir's pyproject when run with cwd set there)
    - .planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-05-PLAN.md (why CI's todo block is build/install-only per D-07 — this guard fills that gap)
  </read_first>
  <action>
    Add a new test (e.g. `test_generate_todo_ruff_clean(tmp)`) to tests/cli/test_main.py that
    generates a todo-tutorial and re-runs ruff against the RENDERED output — the check Plan 03/04
    self-checks never performed (root cause of gap 1). Steps in the test: generate in-process using
    the existing pattern with `argv = ['generate', 'todo-tutorial', tmp.dir, '--defaults']` inside
    `with App(argv=argv) as app: app.run()`; then invoke ruff via `subprocess.run(['ruff', 'check',
    '.'], cwd=tmp.dir, capture_output=True, text=True)` and assert `returncode == 0` (include
    `result.stdout` / `result.stderr` in the assertion message so a regression is legible). ruff is
    already part of cement's dev toolchain (`make comply-ruff` invokes it), so it is on PATH in the
    test/CI env — invoke it the same way. Running `ruff check .` from the generated dir resolves the
    generated pyproject's `[tool.ruff]` tests-excluded scope (so it exercises the exact shipped
    gate) and does NOT run the tutorial-stub test (compatible with D-07's build/install-only todo
    rationale). Add the `subprocess` import at the top of the module if not already present. Keep the
    test hermetic (uses the `tmp` fixture's dir); do not leave artifacts. Preserve 100% coverage —
    every line in the new test executes on a green run.
  </action>
  <verify>
    <automated>pdm run pytest tests/cli/test_main.py -k "generate_todo_ruff_clean" -v</automated>
  </verify>
  <done>
    tests/cli/test_main.py has a guard test that generates todo-tutorial and asserts
    `ruff check .` on the rendered output exits 0; the test passes and would fail if a future
    template edit reintroduced an I001 (or any selected ruff violation) in the todo/ package.
  </done>
  <acceptance_criteria>
    - Test-command: `pdm run pytest tests/cli/test_main.py -k generate_todo_ruff_clean` exits 0.
    - Behavior: temporarily reintroducing an unsorted import into a todo template makes this test
      FAIL with a non-zero ruff returncode in the assertion message (regression-catching proven).
    - Source: the test generates via `['generate', 'todo-tutorial', tmp.dir, '--defaults']` and runs
      ruff against `tmp.dir` (rendered output), not against the template source tree.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix flagship project test false-green assertions (gap 2) + add CHANGELOG entries (gap 3)</name>
  <files>cement/cli/templates/generate/project/tests/test_{{ label }}.py, CHANGELOG.md</files>
  <read_first>
    - cement/cli/templates/generate/project/tests/test_{{ label }}.py (lines 29 and 40: `assert output.find('Foo => bar')` and `assert output.find('Foo => not-bar')` — str.find() returns -1 when absent, and `assert -1` is truthy, so these never fail)
    - CHANGELOG.md (active section header `## 3.0.15 - DEVELOPMENT` at line 3; existing `[cli]` PEP 517 Bug entry lines 16-18; existing `[cli]` project pdm-backend Refactoring entries lines 265-276 — the exact format/wording to mirror; buckets: Bugs / Features / Refactoring / Misc / Deprecations)
    - CLAUDE.md (Changelog Maintenance policy: one line per entry, `[area]` prefix, bucket by conventional-commit type, filter planning-artifact commits, max 78 chars/line)
  </read_first>
  <action>
    Part A (gap 2) — in cement/cli/templates/generate/project/tests/test_{{ label }}.py, change the
    two false-green assertions to membership: `assert 'Foo => bar' in output` (line 29) and
    `assert 'Foo => not-bar' in output` (line 40). Change nothing else in the file; keep the `{{
    label }}` / `{{ class_name }}` Jinja substitution points intact. This is the canonical test new
    users copy; it must genuinely fail when the template renders wrong/empty output.
    Part B (gap 3) — add [cli]-bucketed entries under the ACTIVE `## 3.0.15 - DEVELOPMENT` section
    (append into its existing Bugs / Features / Refactoring / Misc buckets, matching the existing
    entry format exactly; wrap prose at 78 chars). Cover the 05.3 phase changes AND these gap fixes,
    bucketed per CLAUDE.md:
    - Bugs: `[cli]` generated `cement generate todo-tutorial` now builds under pip's default PEP 517
      isolation (mirror the existing project entry at L16-18) — the legacy setup.py self-imported the
      package at build time; `[cli]` fix `todo/main.py` TodoError handler NameError
      (`except TodoError as e:`); `[cli]` fix false-green `output.find(...)` assertion in the
      generated project test template (now membership `in output`); `[cli]` generated todo-tutorial
      imports now isort-clean so its shipped `make comply` is green out of the box.
    - Refactoring: `[cli]` migrate todo-tutorial packaging from setup.py/setup.cfg/requirements*.txt/
      MANIFEST.in to pdm-backend with PEP 621 metadata + PEP 735 dev group (mirror the project entry
      at L265-269); `[cli]` ship `[tool.ruff]`/`[tool.mypy]`/`[tool.pytest]` gate config in the
      generated project and todo-tutorial pyproject.toml so `make comply` + `make test` are green out
      of the box; `[cli]` type-annotate all generated templates (project/script/extension/plugin/
      todo) and modernize idioms to f-strings (D-05/D-06).
    - Misc: `[dev]` extend the CI `cli-smoke-test` to gate the generated project's own
      `make comply` + `make test` and to build/install todo-tutorial (D-07); `[dev]` add a
      generated-todo ruff-clean regression guard to cement's test suite.
    Reference GitHub issue #735 where the existing entries do (the phase driver). Do NOT invent a new
    version section; use the active DEVELOPMENT one.
  </action>
  <verify>
    <automated>grep -q "'Foo => bar' in output" "cement/cli/templates/generate/project/tests/test_{{ label }}.py" && grep -q "'Foo => not-bar' in output" "cement/cli/templates/generate/project/tests/test_{{ label }}.py" && ! grep -q "output.find(" "cement/cli/templates/generate/project/tests/test_{{ label }}.py" && awk '/^## 3.0.15 - DEVELOPMENT/{f=1} /^## 3.0.14/{f=0} f && /todo-tutorial/{c++} END{exit !(c>0)}' CHANGELOG.md && echo GAP2_GAP3_OK</automated>
  </verify>
  <done>
    Both project-test assertions use `in output` and no `output.find(` remains in the template;
    CHANGELOG.md's active 3.0.15 - DEVELOPMENT section has new [cli]/[dev] entries covering the
    todo-tutorial migration, gate config, typing, TodoError fix, the assertion fix, the smoke-test
    gate, and the ruff guard.
  </done>
  <acceptance_criteria>
    - Behavior: a freshly generated project's test_<label>.py, if the command1 template is broken to
      render empty output, now FAILS (was false-green) — verified by generating and running pytest,
      or by source inspection that both asserts use `in output`.
    - Source: `grep -c 'output.find(' cement/cli/templates/generate/project/tests/test_{{ label }}.py`
      == 0; both `'Foo => bar' in output` and `'Foo => not-bar' in output` present.
    - Source: `git diff main...HEAD -- CHANGELOG.md` is non-empty; the active `## 3.0.15 -
      DEVELOPMENT` section contains at least one `[cli]` todo-tutorial entry.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cement CLI → generated-project consumer | Generated template output is copied and run by downstream users; shipping non-green output is a template-integrity regression, not a runtime-service risk |

These are template-text, gate-config, test, and changelog edits. No runtime service or network
surface is introduced. No packages are installed INTO cement (the regression guard uses the ruff
dev dependency already pinned in cement's toolchain).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.3-06 | Tampering | template drift re-shipping non-green generated todo/project output | medium | mitigate | Task 2's ruff-on-rendered-output guard (runs every `make test`) + the corrected import blocks + tests-excluded ruff scope close the exact hole gap 1 exposed |
| T-05.3-SC | Tampering | no new package installs; guard reuses cement's pinned ruff dev dep | low | accept | No npm/pip/cargo install added; ruff already vetted in RESEARCH Package Legitimacy Audit and pinned `ruff~=0.15.12` |
</threat_model>

<verification>
- Gap 1: `cement generate todo-tutorial <dest> --defaults` then `ruff check .` in `<dest>` exits 0;
  todo-tutorial/tests/conftest.py unmodified; `pdm run pytest tests/cli/test_main.py -k
  generate_todo_ruff_clean` passes.
- Gap 2: no `output.find(` in the project test template; both assertions use `in output`.
- Gap 3: `git diff main...HEAD -- CHANGELOG.md` non-empty; [cli] entries present under the active
  DEVELOPMENT section.
- Regression safety: cement's own suite stays green and 100% covered
  (`pdm run pytest --cov=cement tests/`), ruff+mypy clean (`make comply`).
- Phase gate (human/CI): `PYTHON_VERSIONS=3.12 make cli-smoke-test` (or full matrix) green where
  Docker + PyPI are available.
</verification>

<success_criteria>
- Freshly generated todo-tutorial is `make comply` (ruff) green out of the box; the three fixed
  files are isort-clean and the gate excludes the out-of-scope tests/ dir (gap 1 closed).
- A regression guard in cement's suite re-runs ruff on generated todo output and fails on future
  import-order drift (gap 1c closed) — the CI todo block stays build/install-only per D-07.
- The canonical project test template asserts membership, not `.find()` (gap 2 closed).
- CHANGELOG.md's active DEVELOPMENT section documents the 05.3 changes and these fixes (gap 3
  closed).
- cement's own tests, ruff, and mypy remain green.
</success_criteria>

<output>
Create `.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-06-SUMMARY.md` when done
</output>
