---
status: complete
phase: 05.3-modernize-project-template-packaging-and-type-all-cli-genera
source: [05.3-VERIFICATION.md]
started: 2026-07-11T23:55:00Z
updated: 2026-07-12T01:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Run the full CI cli-smoke-test matrix (`make cli-smoke-test` or `PYTHON_VERSIONS=3.12 make cli-smoke-test`)
expected: All 5 python versions complete without error: project pip install + `myapp --version` grep, `pdm install` + `make comply` + `make test` on the generated project, script/extension/plugin generation grep checks, and todo-tutorial `pip install .` all succeed under `set -e`.
result: pass
reported: |
  Two sequential harness failures found and fixed, then confirmed green:
  (1) [FileNotFoundError] '/tmp/tmp.XXXX\\n/myapp/.venv/bin/python' — embedded CR
      in the temp-dir path.
  (2) [ResolutionError] Unable to find a resolution for cement — `pdm install` in
      the generated project's fresh isolated venv resolved the dev pin
      cement==3.0.15 from PyPI, where 3.0.15 is unpublished.
  User re-ran the full matrix: 3.10 -> 3.14 all green.
severity: blocker
fix_commits: [a75f35a6, eefda582]
note: |
  Both fixed inline in scripts/cli-smoke-test.sh (harness-only; no template change):
  (1) a75f35a6 — dropped `docker exec -t` on the `mktemp -d` capture so $tmp has no
      trailing CR.
  (2) eefda582 — injected a find_links [[tool.pdm.source]] pointing at the mounted
      /src/dist so the unpublished dev cement pin resolves for `pdm install`.
  Confirmed green across all 5 Python versions (3.10-3.14) on user re-run.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "The shipped CI cli-smoke-test (scripts/cli-smoke-test.sh) runs green across the Python matrix, exercising generated project/script/extension/plugin/todo-tutorial (D-07)"
  status: failed
  reason: "User reported: `make cli-smoke-test` fails on 3.10 with `[FileNotFoundError] ... '/tmp/tmp.BwHP3HYou1\\n/myapp/.venv/bin/python'`. Root cause: line 23 captures `mktemp -d` output via `docker exec -t` command substitution. The `-t` flag allocates a pseudo-TTY, so Docker emits CRLF; command substitution strips the trailing `\\n` but keeps the `\\r`, making `$tmp` = `/tmp/tmp.XXXX\\r`. Every derived path (`$tmp/myapp`, `$tmp/todo`, etc.) then carries an embedded carriage return, corrupting the venv/interpreter path. Affects all 5 versions; 3.10 fails first because it is first in the loop. The script has never passed since this line shipped in Plan 05."
  severity: blocker
  test: 1
  artifacts:
    - path: "scripts/cli-smoke-test.sh"
      issue: "Line 23: `tmp=$(docker exec -t cement-cli-smoke-test /bin/bash -c \"mktemp -d\")` — remove `-t` so the captured path has no trailing CR. Lines 53/77/84/91 also use `-t` but only pipe `$res` to grep (CR harmless there), so only line 23 needs changing."
  missing:
    - "A regression guard: even after removing `-t`, cement's own `make test` never runs the Docker smoke script, so a future re-introduction of `-t` on a path-capturing exec would go uncaught until CI. Consider a lightweight lint/assertion, or accept CI-only coverage per D-07 scope."
