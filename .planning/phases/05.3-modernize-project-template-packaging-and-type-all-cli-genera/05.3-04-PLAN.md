---
phase: 05.3
plan: 04
type: execute
wave: 2
depends_on: ["05.3-03"]
files_modified:
  - "cement/cli/templates/generate/todo-tutorial/todo/main.py"
  - "cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py"
  - "cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py"
autonomous: true
requirements: ["D-05", "D-06"]
must_haves:
  truths:
    - "Generated todo/main.py no longer raises NameError in its TodoError branch — except TodoError as e: binds e before print(f'TodoError > {e.args[0]}') (fixes the F821/NameError latent bug)"
    - "All generated todo/ package Python is fully type-annotated: extend_tinydb(app: App) -> None, main() -> None, and every controller action (_default, command1, list, create, update, complete, delete) is -> None, clean under todo's shipped mypy config (files=['todo/'], tinydb override) (D-05)"
    - "todo controllers/items.py and base.py keep their existing f-strings and behavior; only annotations are added (D-06 already satisfied — no %-format in todo source)"
  artifacts:
    - "cement/cli/templates/generate/todo-tutorial/todo/main.py (NameError fix + typed hook/main)"
    - "cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py (typed actions)"
    - "cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py (typed @ex actions)"
  key_links:
    - "todo/main.py already imports `from cement import App` at top (L4) — annotate extend_tinydb(app: App) directly; no TYPE_CHECKING block needed (unlike the extension template)"
    - "todo mypy scope is Option B (files=['todo/'], tests excluded) per Plan 03 — do NOT touch todo tests/ or conftest.py; their intentionally-incomplete tutorial stubs (test_todo raises Exception) are out of the typing scope"
    - "items.py `id = int(...)` and the `list` method shadow builtins but the shipped ruff config omits the A family, so no rename is needed"
---

<objective>
Fully type the generated `todo-tutorial` package code and fix its one latent runtime bug: the
`except TodoError:` branch in `todo/main.py` references `e.args[0]` but never binds `e` (a
`NameError` at runtime / ruff `F821`). Add return annotations to the `extend_tinydb` post_setup
hook, `main`, and every controller action across `base.py` and `items.py`, matching the strict
mypy config `todo-tutorial` ships (Plan 03, Option B — package-scoped). The todo source is already
f-string clean, so D-06 needs no `%`-format work here.

Depends on Plan 03 (which creates todo's pyproject/mypy gate config and version.py). Touches only
`todo/` package files — no overlap with Plan 03's file set.

Purpose: D-05 (all generated todo Python typed) + the D-03/RESEARCH-flagged NameError fix.
Output: A typed, bug-free `todo-tutorial` package that is clean under its shipped mypy config.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-CONTEXT.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-RESEARCH.md
@.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 04)

- `todo/main.py` — `except TodoError:` → `except TodoError as e:` (NameError fix);
  `def extend_tinydb(app: App) -> None:`; `def main() -> None:`.
- `todo/controllers/base.py` — `_default`/`command1` typed `-> None`.
- `todo/controllers/items.py` — `list`/`create`/`update`/`complete`/`delete` typed `-> None`.

No new symbols; f-strings and behavior unchanged.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix TodoError NameError + type todo/main.py hook and main (D-05)</name>
  <files>cement/cli/templates/generate/todo-tutorial/todo/main.py</files>
  <read_first>
    - cement/cli/templates/generate/todo-tutorial/todo/main.py (imports `from cement import App, TestApp, init_defaults` L4; def extend_tinydb(app): L19 registered as post_setup hook L74; BUG `except TodoError:` L98 with body referencing e.args[0] L99; def main(): L85)
  </read_first>
  <action>
    Fix the latent bug at the `except TodoError:` branch: change it to `except TodoError as e:` so the
    following `print(f'TodoError > {e.args[0]}')` resolves (currently a `NameError`/ruff `F821`).
    Annotate the post_setup hook `def extend_tinydb(app: App) -> None:` — `App` is already imported at
    the top of the file (`from cement import App, ...`), so annotate directly; do NOT add a
    TYPE_CHECKING block. Annotate `def main() -> None:`. Leave everything else unchanged: CONFIG,
    the TinyDB `app.extend('db', ...)` body, the `os.path`/`fs.abspath` usage, the Todo/TodoTest
    classes, and the already-f-string exception prints in the other branches.
  </action>
  <verify>
    <automated>D=$(mktemp -d); pdm run cement generate todo-tutorial -D "$D" >/dev/null && grep -q 'except TodoError as e:' "$D/todo/main.py" && ! grep -qE 'except TodoError:$' "$D/todo/main.py" && grep -q 'def extend_tinydb(app: App) -> None:' "$D/todo/main.py" && grep -q 'def main() -> None:' "$D/todo/main.py" && python -c "import ast,sys; ast.parse(open('$D/todo/main.py').read())" && echo TODO_MAIN_OK</automated>
  </verify>
  <done>
    Generated todo/main.py binds `e` in the TodoError branch, types extend_tinydb(app: App) -> None
    and main() -> None, and parses cleanly.
  </done>
  <acceptance_criteria>
    A generated `todo/main.py` has `except TodoError as e:`, a typed `extend_tinydb(app: App) -> None`
    hook and `main() -> None`; `mypy` over `todo/` reports no errors and no `F821` remains.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Type todo controllers base.py + items.py actions (D-05)</name>
  <files>cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py, cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py</files>
  <read_first>
    - cement/cli/templates/generate/todo-tutorial/todo/controllers/base.py (already f-string banner L6-9; def _default(self): L31; def command1(self): L49)
    - cement/cli/templates/generate/todo-tutorial/todo/controllers/items.py (@ex actions: list L13, create L26, update L51, complete L72, delete L102; strftime format codes at L28/54/74 are NOT %-format — leave)
  </read_first>
  <action>
    In `base.py`: annotate `def _default(self) -> None:` and `def command1(self) -> None:`; keep the
    f-string `VERSION_BANNER`, `self.app.args.print_help()`, and `self.app.render(...)` unchanged.
    In `items.py`: annotate every `@ex` action `-> None`: `def list(self) -> None:`,
    `def create(self) -> None:`, `def update(self) -> None:`, `def complete(self) -> None:`,
    `def delete(self) -> None:`. Do NOT rename `id = int(...)` or the `list` method (they shadow
    builtins but the shipped ruff config omits the A family — no violation). Do NOT touch the
    `strftime("%Y-%m-%d %H:%M:%S")` calls — those are strftime directives, not `%`-string formatting.
    Behavior and all existing f-strings unchanged.
  </action>
  <verify>
    <automated>D=$(mktemp -d); pdm run cement generate todo-tutorial -D "$D" >/dev/null && grep -q 'def _default(self) -> None:' "$D/todo/controllers/base.py" && grep -q 'def command1(self) -> None:' "$D/todo/controllers/base.py" && for m in list create update complete delete; do grep -q "def $m(self) -> None:" "$D/todo/controllers/items.py" || { echo "MISSING $m"; exit 1; }; done && grep -q 'strftime("%Y-%m-%d' "$D/todo/controllers/items.py" && echo TODO_CONTROLLERS_OK</automated>
  </verify>
  <done>
    All generated todo controller actions (base + items) are typed -> None; strftime and f-strings
    are untouched.
  </done>
  <acceptance_criteria>
    A generated `todo/controllers/base.py` and `items.py` have every action typed `-> None`;
    `mypy` over `todo/` reports no errors; `id`/`list` builtin-shadowing is not flagged (A family
    omitted from the shipped ruff config).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cement dev → generated todo application code | The generated todo app is a user's runnable code; a shipped NameError is a correctness defect propagated to every generated tutorial |

No runtime service or untrusted input is introduced. The one integrity concern is the shipped
`NameError`, fixed here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.3-06 | Tampering | todo/main.py TodoError branch (NameError) | low | mitigate | Bind `e` via `except TodoError as e:`; typed + AST-parse verified; covered transitively by mypy over `todo/` |
</threat_model>

<verification>
- Generate todo-tutorial; confirm the greps above and that `todo/main.py` parses via `ast.parse`.
- Local type check (optional): in the generated dir, `pdm run mypy` reports no errors over `todo/`.
- cement's own suite unaffected (todo source is template-excluded from cement lint/type).
</verification>

<success_criteria>
- Generated todo package is fully typed and clean under its shipped mypy config (D-05).
- The TodoError-branch NameError is fixed; no F821 remains.
- todo tests/conftest deliberately untouched (Option B scope).
</success_criteria>

<output>
Create `.planning/phases/05.3-modernize-project-template-packaging-and-type-all-cli-genera/05.3-04-SUMMARY.md` when done
</output>
