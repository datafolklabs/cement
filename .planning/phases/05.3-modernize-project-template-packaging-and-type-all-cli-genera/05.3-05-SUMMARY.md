---
phase: 05.3-modernize-project-template-packaging-and-type-all-cli-genera
plan: 05
subsystem: testing
tags: [cli-smoke-test, ci, docker, pdm, pdm-backend, pep517, regression-guard, project-template, todo-tutorial]

# Dependency graph
requires:
  - phase: 05.3-01
    provides: green-out-of-the-box project template (ruff + mypy + pytest exit 0 after --defaults)
  - phase: 05.3-03
    provides: todo-tutorial migrated to pdm-backend (PEP 517 build-isolation-safe, source="file" version)
provides:
  - "cli-smoke-test enforces the generated project's OWN make comply + make test inside each Python 3.10-3.14 container (D-04/D-05/D-07)"
  - "cli-smoke-test builds/installs todo-tutorial via pip install . under PEP 517 isolation (proves D-02/D-03)"
  - "template drift shipping non-green generated output now surfaces as a red CI smoke-test, not silent drift"
affects: [ci-smoke-test, release-3.0.16, phase-06-release-cut]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Generated-project gate runs inside the smoke-test container: pip install pdm -> pdm install (PEP 735 dev group) -> make comply -> make test, under set -e"
    - "todo gate is build/install ONLY (pip install .) — no make test, because todo-tutorial/tests/test_main.py::test_todo raises Exception as a tutorial stub"

key-files:
  created: []
  modified:
    - "scripts/cli-smoke-test.sh"

key-decisions:
  - "No explicit error handling added — set -e (scripts/cli-smoke-test.sh:2) already fails the build on any non-zero docker exec, so the new comply/test/install steps are self-gating"
  - "pdm made available in-container (pip install pdm) + pdm install BEFORE make comply/make test, because the generated Makefile targets call `pdm run ...` and need the PEP 735 dev group (ruff/mypy/pytest)"
  - "todo gets pip install . (build/install) but NOT make test — its stub test deliberately raises; a test gate would always fail the build"

patterns-established:
  - "The cli-smoke-test is the phase's canonical validation surface: it exercises the generated artifact's own quality gates in the same matrix container that installs it"

requirements-completed: ["D-07"]

coverage:
  - id: D1
    description: "cli-smoke-test runs the generated project's own make comply + make test inside each container and fails the build (set -e) on any non-green output (D-04/D-05/D-07 regression guard)"
    requirement: "D-07"
    verification:
      - kind: automated
        ref: "bash -n scripts/cli-smoke-test.sh && grep -F 'cd $tmp/myapp ; pdm install' / 'make comply' / 'make test' / 'pip install pdm' (SMOKE_PROJECT_GATE_OK)"
        status: pass
    human_judgment: true
    rationale: "The full behavioral gate (make cli-smoke-test on the Docker matrix, or PYTHON_VERSIONS=3.12 single-version) requires Docker + network to PyPI and is not runnable in this execution environment. Structural verification (syntax + step presence) passes; a human/CI must run the actual matrix to confirm the generated project is green end-to-end."
  - id: D2
    description: "cli-smoke-test generates todo-tutorial and runs pip install . (build/install only, no make test), proving the D-02/D-03 pdm-backend build-isolation fix"
    requirement: "D-07"
    verification:
      - kind: automated
        ref: "bash -n scripts/cli-smoke-test.sh && grep -F 'cement generate todo-tutorial -D $tmp/todo' / 'cd $tmp/todo ; pip install .' && ! grep 'cd $tmp/todo ; make test' (SMOKE_TODO_GATE_OK)"
        status: pass
    human_judgment: true
    rationale: "Actual PEP 517 build/install of the generated todo wheel requires Docker + PyPI; only structural presence is verifiable here. CI matrix run confirms the wheel builds via pdm-backend without a build-time cement import."

# Metrics
duration: 2min
completed: 2026-07-11
status: complete
---

# Phase 05.3 Plan 05: CI Smoke-Test Regression Guard Summary

**cli-smoke-test now runs the generated project's own `make comply` + `make test` inside each Python 3.10-3.14 container and builds/installs todo-tutorial via `pip install .`, turning template drift into a red CI build (D-07, proving D-02/D-03/D-04/D-05).**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-07-11T22:58:11Z
- **Completed:** 2026-07-11T22:59:42Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Added a "generated project: green out of the box" section to the smoke-test that installs pdm, runs `pdm install`, then runs the generated project's own `make comply` (ruff + mypy) and `make test` inside each matrix container — `set -e` fails the whole build on any non-green output (D-04/D-05/D-07).
- Added a "generate + build/install todo-tutorial" section that runs `cement generate todo-tutorial` then `pip install .`, proving the D-02/D-03 pdm-backend build-isolation migration under PEP 517.
- Kept the todo gate build/install-only (no `make test`, no `make comply`) because the tutorial's `test_todo` deliberately raises — a test gate there would always fail.
- Preserved all existing checks (project `pip install .` / `myapp --version`, script/extension/plugin generation) and the `$tmp`/`$pyver` variable + `docker exec ... /bin/bash -c "..."` idioms intact.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add generated-project make comply + make test gate** - `22a65d9f` (test)
2. **Task 2: Add todo-tutorial generate + build/install** - `0ee8b982` (test)

## Files Created/Modified
- `scripts/cli-smoke-test.sh` - Added the generated-project quality gate (pdm install + make comply + make test) after project generation, and a todo-tutorial generate + `pip install .` build-isolation proof before the `### finish` block.

## Decisions Made
- Relied on the existing `set -e` (L2) rather than adding explicit error handling — every new `docker exec` step is self-gating.
- Installed pdm and ran `pdm install` in-container before the gate targets, because the generated Makefile drives `pdm run ...` and needs the PEP 735 dev group.
- Build/install-only for todo (no `make test`), matching the plan's key_links intent around the intentionally-raising stub test.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- The plan's `<verify>` blocks use `grep -q 'cd $tmp/myapp ; ...'`; on this platform grep treats the mid-pattern `$` as a regex anchor, so the un-escaped BRE never matches even though the literal text is present. Confirmed the intended semantics with fixed-string grep (`grep -qF`), which yields `SMOKE_PROJECT_GATE_OK` and `SMOKE_TODO_GATE_OK`. The file content matches the plan byte-for-byte (verified via `cat -A`); this is a quoting quirk in the verify command, not an implementation gap.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The regression guard is wired but its full behavioral confirmation is a CI/Docker action: run `make cli-smoke-test` (full matrix) or `PYTHON_VERSIONS=3.12 make cli-smoke-test` locally where Docker + PyPI are available. Both new gates should be green given Plans 01 and 03.
- Cost: the generated-project `pdm install` + comply + test now runs per Python version, making the smoke-test noticeably slower; scope via `PYTHON_VERSIONS` if runtime becomes a concern.
- cement's own suite is unaffected (shell-script-only change; no framework code touched). `tests/cli/test_main.py::test_generate` is untouched.

## Self-Check: PASSED

- FOUND: scripts/cli-smoke-test.sh
- FOUND: 05.3-05-SUMMARY.md
- FOUND commit: 22a65d9f (Task 1)
- FOUND commit: 0ee8b982 (Task 2)

---
*Phase: 05.3-modernize-project-template-packaging-and-type-all-cli-genera*
*Completed: 2026-07-11*
