---
phase: 05.3
slug: modernize-project-template-packaging-and-type-all-cli-genera
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-11
---

# Phase 05.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (cement's own suite) + generated-project pytest via CI smoke-test |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `pdm run pytest tests/ext/test_ext_generate.py` |
| **Full suite command** | `make test` |
| **Estimated runtime** | ~60 seconds (unit) / smoke-test runs in CI Docker matrix |

---

## Sampling Rate

- **After every task commit:** Run the quick command (generate-related unit tests + `make comply` on the modified template render path)
- **After every plan wave:** Run `make test`
- **Before `/gsd-verify-work`:** Full suite green + `scripts/cli-smoke-test.sh` green locally where feasible
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

> Executable per-task `<automated>` verify commands live in the 5 PLAN.md files
> (what `/gsd-execute-phase` consumes); this table records the plan-level validation
> surface. The canonical acceptance signal for this phase is the CI `cli-smoke-test`
> "green out of the box" gate (D-04/D-05/D-07): a freshly generated `project` must
> pass its own `make comply` + `make test`.

| Plan | Wave | Decisions | Validation Surface | Automated Command | Status |
|------|------|-----------|--------------------|-------------------|--------|
| 05.3-01 | 1 | D-04, D-05, D-06 | generated `project` gate config green | generated `make comply` + `make test` (via smoke-test) | ⬜ pending |
| 05.3-02 | 1 | D-05, D-06, D-08 | script/extension/plugin typed & mypy-clean | `pdm run mypy` on rendered templates | ⬜ pending |
| 05.3-03 | 1 | D-01, D-02, D-03 | todo pdm-backend build (PEP 517 isolation) | `pip install .` on generated todo | ⬜ pending |
| 05.3-04 | 2 | D-05 | todo source typed; `TodoError` NameError fixed | `pdm run mypy` / import check | ⬜ pending |
| 05.3-05 | 2 | D-07 | CI smoke-test gates on generated green | `scripts/cli-smoke-test.sh` | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Generated-project gate config (`[tool.ruff]`/`[tool.mypy]`/`[tool.pytest]` in `project/pyproject.toml`) — the "green out of the box" contract under test
- [ ] `scripts/cli-smoke-test.sh` extension running the generated project's `make comply` + `make test`

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GitBook `todo-tutorial` narrative resync | (deferred) | External repo, out of phase scope | Tracked as a pending todo; not gated here |

*If none: "All phase behaviors have automated verification."*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (confirmed by plan-checker across all 5 PLAN.md files)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (gate config + smoke-test extension)
- [x] No watch-mode flags
- [x] Feedback latency < 60s (unit) / CI smoke-test for the green-out-of-the-box gate
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-11
