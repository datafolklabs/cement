---
phase: 05-deprecations-docs-security-stubs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cement/core/deprecations.py
  - cement/ext/ext_logging.py
  - cement/ext/ext_smtp.py
  - CHANGELOG.md
autonomous: true
requirements: [DEPREC-01, DEPREC-03, DOCS-04]
requirements_addressed: [DEPREC-01, DEPREC-03, DOCS-04]

must_haves:
  truths:
    - "Every entry in cement/core/deprecations.py:DEPRECATIONS names a concrete removal version (no vague 'future versions of Cement' wording remains)"
    - "set_level() and fatal() docstrings in cement/ext/ext_logging.py name v3.2.0 as the removal target"
    - "send() docstring in cement/ext/ext_smtp.py contains a Sphinx '.. deprecated:: 3.0.16' admonition pointing to v3.2.0"
    - "All 4 existing deprecation tests still pass (DEPREC-03 already-satisfied; no regression)"
    - "make test exits 0 at 100% coverage on every commit"
    - "make comply exits 0 (ruff + mypy clean) on every commit"
  artifacts:
    - path: "cement/core/deprecations.py"
      provides: "Tightened DEPRECATIONS dict — 3.0.10-1 and 3.0.16-1 pinned to v3.2.0"
      contains: "Cement v3.2.0"
    - path: "cement/ext/ext_logging.py"
      provides: "set_level + fatal docstrings name Cement v3.2.0"
      contains: "Cement v3.2.0"
    - path: "cement/ext/ext_smtp.py"
      provides: "send() docstring with .. deprecated:: 3.0.16 admonition"
      contains: ".. deprecated:: 3.0.16"
    - path: "CHANGELOG.md"
      provides: "3 new Refactoring entries appended to active 3.0.15 - DEVELOPMENT section"
      contains: "[core.deprecations] Pin 3.0.10-1 and 3.0.16-1 removal version to v3.2.0"
  key_links:
    - from: "cement/core/deprecations.py:15"
      to: "https://docs.builtoncement.com/release-information/deprecations#<id>"
      via: "deprecate() helper preserves the dotted GitBook anchor format"
      pattern: "release-information/deprecations#"
    - from: "tests/core/test_deprecations.py + tests/ext/test_ext_smtp.py:631"
      to: "DEPRECATIONS dict (4 IDs)"
      via: "by-ID assertion (NOT message-text assertion, so message-text edits are safe)"
      pattern: "deprecate\\(.3\\.0\\."
---

<objective>
Tighten the registry message text in `cement/core/deprecations.py` (D-02 /
D-03) so every entry names a concrete removal version, and sweep the
adjacent deprecation docstrings in `cement/ext/ext_logging.py` (set_level
+ fatal) and `cement/ext/ext_smtp.py` (send) per CONTEXT.md D-04.

Purpose: Resolve DEPREC-01 acceptance criterion #5 (every DEPRECATIONS
entry names a removal version) and refresh the load-bearing user-visible
deprecation docstrings to match. This is the source-code half of the
phase. DEPREC-03 is already-satisfied — existing tests assert by ID,
not by message text, so no test work is required (verified in
05-RESEARCH.md A1, A6 + Phase 4 in-place-ratification precedent).

Output: 3 atomic source-code commits + 3 CHANGELOG entries appended to
the active `## 3.0.15 - DEVELOPMENT (will be released as stable/3.0.16)`
section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md
@.planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md
@.planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md
@CLAUDE.md
@CHANGELOG.md
@cement/core/deprecations.py
@cement/ext/ext_logging.py
@cement/ext/ext_smtp.py

<interfaces>
<!-- Locked file:line surfaces this plan touches. Executor uses these directly. -->

cement/core/deprecations.py (17 lines total):
  DEPRECATIONS = {
      '3.0.8-1': "...will be removed in Cement v3.2.0",                 # already correct
      '3.0.8-2': "...will be changed or removed in Cement v3.2.0",      # already correct
      '3.0.10-1': "...removed in future versions of Cement.",           # FIX → "Cement v3.2.0."
      '3.0.16-1': "...senderrs dict in a future version of Cement",     # FIX → "Cement v3.2.0"
  }
  def deprecate(deprecation_id: str) -> None: ...                       # unchanged
  # GitBook URL format: f"...#{deprecation_id}" → dotted anchor (e.g., #3.0.10-1)

cement/ext/ext_logging.py:
  - set_level docstring: lines 146-147 ("future versions of Cement" → "Cement v3.2.0")
  - fatal() docstring:    lines 368-369 (same change pattern)
  - Both also: "us" → "use" typo + single-backtick → double-backtick RST (zero-risk inline cleanup)

cement/ext/ext_smtp.py:
  - send() docstring Returns block: lines 140-143
  - Insert .. deprecated:: 3.0.16 admonition between Returns and Example:
  - Existing inline comment + deprecate() call at lines 189-190 stays unchanged

NOT in scope:
  - cement/core/foundation.py:608 framework_logging docstring (already correct per D-04)
  - cement/utils/shell.py exec_cmd() docstring (parallel surface; not in live warning list)
  - tests/core/test_deprecations.py + tests/ext/test_ext_smtp.py (DEPREC-03 satisfied)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pin removal-version language in DEPRECATIONS dict (commit 1 of D-16)</name>
  <files>cement/core/deprecations.py, CHANGELOG.md</files>
  <read_first>
    - cement/core/deprecations.py (whole file, 17 lines)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-02, D-03
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 1 (verbatim replacement strings)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "cement/core/deprecations.py" section (acceptance grep)
    - CHANGELOG.md lines 1-160 (active section header at line 3 + Refactoring bucket header at line 142 + sample entries below)
  </read_first>
  <action>
Edit `cement/core/deprecations.py` to pin `3.0.10-1` and `3.0.16-1`
removal versions to v3.2.0 per D-02 / D-03. Two-line value swap;
`# noqa: E501` lines stay (strings remain >100 chars after swap).

EXACT BEFORE (lines 7-8):

```python
    '3.0.10-1': "The FATAL logging facility is deprecated in favor of CRITICAL, and will be removed in future versions of Cement.",  # noqa: E501
    '3.0.16-1': "SMTPMailHandler.send() returning bool is deprecated. It will return the smtplib senderrs dict in a future version of Cement",  # noqa: E501
```

EXACT AFTER (lines 7-8):

```python
    '3.0.10-1': "The FATAL logging facility is deprecated in favor of CRITICAL, and will be removed in Cement v3.2.0.",  # noqa: E501
    '3.0.16-1': "SMTPMailHandler.send() returning bool is deprecated. It will return the smtplib senderrs dict in Cement v3.2.0",  # noqa: E501
```

Notes:
- `3.0.10-1`: trailing period preserved (original had one).
- `3.0.16-1`: no trailing period (original didn't have one).
- `3.0.8-1` and `3.0.8-2` lines 5-6 stay unchanged — already pinned.

Then append the CHANGELOG entry for this commit. Open `CHANGELOG.md`,
locate the active `Refactoring:` bucket inside the `## 3.0.15 -
DEVELOPMENT (will be released as stable/3.0.16)` section, and append
this exact line at the END of that bucket (NOT a new section, NOT a
new bucket — append after the last existing `[core....]` /
`[ext....]` entry):

```
- `[core.deprecations]` Pin 3.0.10-1 and 3.0.16-1 removal version to v3.2.0
```

Run gates BEFORE committing:

```bash
make comply && make test && make audit-public-api
```

All three MUST exit 0. Then commit. Use `make commit` (cz) OR a hand-
formed `git commit` with the body wrapped at 78 chars. Conventional
prefix `refactor(core.deprecations):`. Subject under 78 chars. Suggested
commit message (per RESEARCH.md Pattern 1):

```
refactor(core.deprecations): tighten removal-version language

Pin 3.0.10-1 (FATAL logging) and 3.0.16-1 (SMTPMailHandler.send bool
return) to v3.2.0 in DEPRECATIONS dict. Aligns with the existing
3.0.8-1 / 3.0.8-2 policy. Removal target is the next breakage-allowed
minor; downstream apps that have ignored the warning since 3.0.10
have had ample time.

DEPREC-01 acceptance criterion #5: every entry in
cement/core/deprecations.py:DEPRECATIONS now names a removal version.
DEPREC-03 already-satisfied — existing tests assert by ID, not by
message text.
```
  </action>
  <verify>
    <automated>grep -E '"3\.[0-9]+\.[0-9]+-[0-9]+":' cement/core/deprecations.py | wc -l | grep -qx 4 &amp;&amp; grep -cE 'Cement v3\.2\.0' cement/core/deprecations.py | grep -qx 4 &amp;&amp; ! grep -E 'future version[s]? of Cement' cement/core/deprecations.py &amp;&amp; pdm run pytest tests/core/test_deprecations.py -x &amp;&amp; pdm run pytest tests/ext/test_ext_smtp.py::TestExtSMTP -x &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; grep -F '[core.deprecations]' CHANGELOG.md | grep -F 'Pin 3.0.10-1 and 3.0.16-1 removal version to v3.2.0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '"3\.[0-9]+\.[0-9]+-[0-9]+":' cement/core/deprecations.py | wc -l` returns exactly `4`
    - `grep -cE 'Cement v3\.2\.0' cement/core/deprecations.py` returns exactly `4`
    - `grep -E 'future version[s]? of Cement' cement/core/deprecations.py` returns 0 hits (the vague phrasing is gone)
    - `pdm run pytest tests/core/test_deprecations.py -x` exits 0 (DEPREC-03 regression hold)
    - `make test` exits 0 with 100% coverage (Phase 2 D-10/D-11 gate)
    - `make comply` exits 0 (ruff + mypy clean)
    - `make audit-public-api` exits 0 (no public-API drift; D-18 #3)
    - `CHANGELOG.md` contains the literal line `- \`[core.deprecations]\` Pin 3.0.10-1 and 3.0.16-1 removal version to v3.2.0` inside the `## 3.0.15 - DEVELOPMENT` section's `Refactoring:` bucket
    - Commit subject is `refactor(core.deprecations): tighten removal-version language` (≤78 chars; verify with `git log -1 --pretty=%s | awk '{ if (length($0) > 78) exit 1 }'`)
    - The commit touches exactly 2 files: `cement/core/deprecations.py` and `CHANGELOG.md` (`git show --stat HEAD | grep -E '\.py|\.md'` shows only those two)
  </acceptance_criteria>
  <done>
    DEPRECATIONS dict has 4 entries; all 4 name v3.2.0; tests pass; comply
    clean; audit-public-api clean; CHANGELOG entry appended; commit
    landed under D-16 step 1 with a Conventional Commits subject ≤78
    chars and a 78-char-wrapped body.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten ext_logging.py set_level + fatal() docstrings (commit 2 of D-16)</name>
  <files>cement/ext/ext_logging.py, CHANGELOG.md</files>
  <read_first>
    - cement/ext/ext_logging.py lines 140-160 (set_level method docstring)
    - cement/ext/ext_logging.py lines 360-385 (fatal() method docstring)
    - cement/core/foundation.py:608 (framework_logging docstring — confirms target shape; do NOT edit)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-04
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Examples 2 + 3 (exact replacement text)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "cement/ext/ext_logging.py:140-151" and ":364-383" sections
  </read_first>
  <action>
Edit `cement/ext/ext_logging.py` set_level docstring (lines 146-147)
and fatal() docstring (lines 368-369). Three identical token-level
changes per docstring (D-04 + zero-risk RST cleanup):

**set_level docstring (lines 146-147) — EXACT BEFORE:**

```python
        As of Cement 3.0.10, the FATAL facility is deprecated and will be
        removed in future versions of Cement. Please us `CRITICAL` instead.
```

**EXACT AFTER:**

```python
        As of Cement 3.0.10, the FATAL facility is deprecated and will be
        removed in Cement v3.2.0. Please use ``CRITICAL`` instead.
```

**fatal() docstring (lines 368-369) — EXACT BEFORE:**

```python
        As of Cement 3.0.10, this method is deprecated and will be removed in
        future versions of Cement. Please us `critical()` instead.
```

**EXACT AFTER:**

```python
        As of Cement 3.0.10, this method is deprecated and will be removed in
        Cement v3.2.0. Please use ``critical()`` instead.
```

Three changes per docstring (per RESEARCH.md Examples 2-3):
  1. `"future versions of Cement"` → `"Cement v3.2.0"` (D-04 load-bearing)
  2. `"us"` → `"use"` (typo carry-over fix)
  3. Single-backtick `` `CRITICAL` ``/`` `critical()` `` → double-backtick
     `` ``CRITICAL`` ``/`` ``critical()`` `` (RST inline-literal — single
     backticks render as emphasis, not literal, in autodoc)

Both docstrings ship in ONE commit (same file, same change pattern, single
`[ext.logging]` CHANGELOG entry).

Append CHANGELOG entry to `Refactoring:` bucket in active section:

```
- `[ext.logging]` Tighten FATAL deprecation removal version in docstrings
```

Run gates BEFORE committing:

```bash
make comply && make test && make audit-public-api
```

Commit subject: `refactor(ext.logging): tighten FATAL deprecation removal
version in docstrings` (note 78-char limit — this is exactly 73 chars).

Suggested body:

```
Pin set_level() and fatal() docstrings to v3.2.0 (matches the
3.0.10-1 DEPRECATIONS entry tightened in the prior commit). Also
fix the "us"/"use" typo and switch single-backtick CRITICAL /
critical() to RST inline-literal double-backtick form so autodoc
renders them as code, not emphasis.

DEPREC-01 acceptance carried at the docstring layer for the
load-bearing user-visible surface.
```
  </action>
  <verify>
    <automated>grep -F 'removed in Cement v3.2.0' cement/ext/ext_logging.py | wc -l | grep -qE '^[[:space:]]*2$' &amp;&amp; ! grep -E 'future versions of Cement|Please us \`' cement/ext/ext_logging.py &amp;&amp; grep -F '``CRITICAL``' cement/ext/ext_logging.py &amp;&amp; grep -F '``critical()``' cement/ext/ext_logging.py &amp;&amp; pdm run pytest tests/ext/test_ext_logging.py -x &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; grep -F '[ext.logging]' CHANGELOG.md | grep -F 'Tighten FATAL deprecation removal version in docstrings'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'removed in Cement v3.2.0' cement/ext/ext_logging.py | wc -l` returns ≥2 (one per docstring)
    - `grep -E 'future versions of Cement' cement/ext/ext_logging.py` returns 0 hits
    - `grep -E 'Please us \`' cement/ext/ext_logging.py` returns 0 hits (typo fixed)
    - `grep -F '``CRITICAL``' cement/ext/ext_logging.py` matches at least once
    - `grep -F '``critical()``' cement/ext/ext_logging.py` matches at least once
    - `pdm run pytest tests/ext/test_ext_logging.py -x` exits 0 (no test regression)
    - `make test` exits 0 at 100% coverage; `make comply` exits 0; `make audit-public-api` exits 0
    - `CHANGELOG.md` contains the literal line `- \`[ext.logging]\` Tighten FATAL deprecation removal version in docstrings` inside the `Refactoring:` bucket
    - Commit touches exactly 2 files: `cement/ext/ext_logging.py` and `CHANGELOG.md`
    - Commit subject ≤78 chars
  </acceptance_criteria>
  <done>
    set_level and fatal() docstrings name v3.2.0; typo fixed; RST
    double-backticks applied; tests pass; gates clean; CHANGELOG appended;
    commit landed under D-16 step 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add .. deprecated:: 3.0.16 admonition to ext_smtp.py send() docstring (commit 3 of D-16)</name>
  <files>cement/ext/ext_smtp.py, CHANGELOG.md</files>
  <read_first>
    - cement/ext/ext_smtp.py lines 110-200 (covers send() method, Returns block, Example block, runtime callsite at 189-191)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-04
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 4 (exact admonition text + indentation rules)
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 5 (this edit MUST land before Plan 04 Makefile -W flip)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "cement/ext/ext_smtp.py:116-158" section (4-space directive body indent)
  </read_first>
  <action>
Edit the `send()` docstring in `cement/ext/ext_smtp.py` to insert a
Sphinx `.. deprecated:: 3.0.16` admonition between the existing
`Returns:` block and the `Example:` block. Two changes (per RESEARCH.md
Example 4):

1. Fix `bool:``True``` → `bool: ``True``` (insert one space — RST
   formatting bug; zero-risk inline cleanup)
2. Insert blank line + `.. deprecated::` directive + 3-line indented
   body + blank line, immediately before `Example:`.

**EXACT BEFORE (lines 140-143 area):**

```python
        Returns:
            bool:``True`` if message is sent successfully, ``False`` otherwise

        Example:
```

**EXACT AFTER:**

```python
        Returns:
            bool: ``True`` if message is sent successfully, ``False`` otherwise

        .. deprecated:: 3.0.16
            The ``bool`` return is deprecated and will change to the
            ``smtplib`` senderrs ``dict`` in Cement v3.2.0. See
            ``DEPRECATIONS['3.0.16-1']``.

        Example:
```

Critical indentation rule (matches PATTERNS.md):
- `.. deprecated:: 3.0.16` sits at 8-space indent (same column as
  `Returns:` / `Example:` section labels).
- Body lines (`The bool return ...`) sit at 12-space indent (4 spaces
  past the directive — canonical RST `.. directive::` body indentation).

Do NOT touch the runtime callsite at lines 189-191 (the existing
`# Deprecation: bool return will change to senderrs dict` comment +
`deprecate('3.0.16-1')` call — that is the runtime anchor and stays).

Append CHANGELOG entry to `Refactoring:` bucket:

```
- `[ext.smtp]` Document send() bool-return removal in v3.2.0
```

Run gates BEFORE committing:

```bash
make comply && make test && make audit-public-api
```

NOTE: Do NOT run `make docs` yet — Plan 04 is responsible for flipping
the `-W` flag. Plan 02 will fix the existing 4 sphinx warnings; this
admonition must NOT introduce a NEW warning. After Plan 02 lands,
running `pdm run sphinx-build ./docs/source ./docs/build` should still
produce exactly the original 4 warnings (this admonition is well-formed
RST and renders as a "Deprecated since version 3.0.16" admonition in
autodoc HTML — no new warning).

Commit subject: `refactor(ext.smtp): document send() bool-return removal
in v3.2.0` (≤78 chars).

Suggested body:

```
Insert a Sphinx .. deprecated:: 3.0.16 admonition into the send()
docstring between Returns and Example. Pairs with the runtime
deprecate('3.0.16-1') call at line 189-190 (unchanged) so the
deprecation is visible at both runtime AND in autodoc HTML.

Also fix a zero-space-after-colon RST formatting bug
(bool:``True`` → bool: ``True``) in the same Returns block.

DEPREC-01 acceptance carried at the docstring layer for the
SMTPMailHandler.send return-type deprecation.
```
  </action>
  <verify>
    <automated>grep -F '.. deprecated:: 3.0.16' cement/ext/ext_smtp.py &amp;&amp; grep -F 'senderrs ``dict`` in Cement v3.2.0' cement/ext/ext_smtp.py &amp;&amp; grep -F "DEPRECATIONS['3.0.16-1']" cement/ext/ext_smtp.py &amp;&amp; grep -F 'bool: ``True``' cement/ext/ext_smtp.py &amp;&amp; ! grep -F 'bool:``True``' cement/ext/ext_smtp.py &amp;&amp; grep -F "deprecate('3.0.16-1')" cement/ext/ext_smtp.py &amp;&amp; pdm run pytest tests/ext/test_ext_smtp.py -x &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; grep -F '[ext.smtp]' CHANGELOG.md | grep -F 'Document send() bool-return removal in v3.2.0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '.. deprecated:: 3.0.16' cement/ext/ext_smtp.py` matches exactly once
    - `grep -F 'senderrs ``dict`` in Cement v3.2.0' cement/ext/ext_smtp.py` matches at least once
    - `grep -F "DEPRECATIONS['3.0.16-1']" cement/ext/ext_smtp.py` matches at least once (the back-reference to the registry)
    - `grep -F 'bool: ``True``' cement/ext/ext_smtp.py` matches (space-after-colon fix)
    - `grep -F 'bool:``True``' cement/ext/ext_smtp.py` returns 0 hits (the buggy form is gone)
    - `grep -F "deprecate('3.0.16-1')" cement/ext/ext_smtp.py` still matches (runtime callsite preserved)
    - `pdm run pytest tests/ext/test_ext_smtp.py -x` exits 0 (DEPREC-03 regression hold for `3.0.16-1`)
    - `make test` exits 0 at 100% coverage; `make comply` exits 0; `make audit-public-api` exits 0
    - Plan 02 prerequisite untouched: `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic warning` returns the SAME number as the pre-task baseline (no new warning introduced — value depends on whether Plan 02 has landed; before Plan 02 it should still be 4)
    - CHANGELOG contains literal `- \`[ext.smtp]\` Document send() bool-return removal in v3.2.0` in the `Refactoring:` bucket
    - Commit subject ≤78 chars; touches exactly 2 files (`cement/ext/ext_smtp.py` + `CHANGELOG.md`)
  </acceptance_criteria>
  <done>
    send() docstring has a well-formed `.. deprecated:: 3.0.16` admonition
    pointing to v3.2.0 and the registry; runtime callsite intact; tests
    pass; gates clean; CHANGELOG appended; commit landed under D-16 step 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → autodoc HTML | Docstring text in `cement/ext/ext_logging.py` and `cement/ext/ext_smtp.py` is rendered into Sphinx-generated HTML. Phase 5 only edits prose; no user-input pipeline involved. |
| Source code → runtime DeprecationWarning emission | `DEPRECATIONS` dict values are interpolated into `warnings.warn(...)` strings at runtime via `cement/core/deprecations.py:15`. Phase 5 only edits the message-text values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-noop-01 | I (Information disclosure) | `cement/core/deprecations.py` DEPRECATIONS dict message-text edit | accept | No new attack surface — module-scope literal dict; no user-input pipeline; no escape-route (warnings.warn handles encoding). Doc-only edit. |
| T-05-noop-02 | T (Tampering) | `cement/ext/ext_smtp.py` send() docstring `.. deprecated::` admonition insertion | accept | RST directive is hand-authored, well-formed; no user-controlled string interpolation. Indentation verified per PATTERNS.md (8-space directive, 12-space body). |
| T-05-03 | I (Information disclosure / wrong-info) | `cement/core/interface.py:102` (NOT this plan — recorded for traceability) | mitigate | This plan does NOT touch interface.py. Cross-reference: Plan 02 owns the string-quote fix; D-18 #3 acceptance gate is `make audit-public-api` exit 0 (verified by Plan 02). |

**ASVS L1 applicability:** V7 (Errors & Logging) partial — DeprecationWarning emission is via `warnings.warn()` (stdlib), already wired in `cement/core/deprecations.py:16`. Phase 5 only edits the message strings, not the emission mechanism. No new code paths. Block on: high (none of the threats are high; all accept/mitigate).
</threat_model>

<verification>
After all 3 commits:

```bash
# DEPREC-01 acceptance — every entry names a removal version
grep -E '"3\.[0-9]+\.[0-9]+-[0-9]+":' cement/core/deprecations.py | wc -l   # → 4
grep -cE 'Cement v3\.2\.0' cement/core/deprecations.py                     # → 4
! grep -E 'future version[s]? of Cement' cement/core/deprecations.py       # 0 hits

# DEPREC-03 regression hold — existing tests still pass
pdm run pytest tests/core/test_deprecations.py tests/ext/test_ext_smtp.py -x

# DOCS-04 (adjacent docstring sweep) — both ext_logging docstrings name v3.2.0
grep -F 'removed in Cement v3.2.0' cement/ext/ext_logging.py | wc -l       # → 2

# DOCS-04 (smtp send() admonition)
grep -F '.. deprecated:: 3.0.16' cement/ext/ext_smtp.py                    # → 1 hit

# Cumulative gates
make test && make comply && make audit-public-api

# CHANGELOG — 3 new entries appended
git diff <wave-0-base>..HEAD -- CHANGELOG.md | grep -E '^\+- \`\[' | wc -l  # → 3
```
</verification>

<success_criteria>
- [ ] Commits 1, 2, 3 of the D-16 sequence landed atomically (one Conventional Commits subject ≤78 chars per commit; bodies wrapped at 78 chars)
- [ ] Each commit touches exactly 2 files (the source file + `CHANGELOG.md`)
- [ ] `cement/core/deprecations.py` DEPRECATIONS dict: 4 entries, all naming `Cement v3.2.0`
- [ ] `cement/ext/ext_logging.py`: set_level + fatal() docstrings name `Cement v3.2.0`; double-backtick RST literals applied; `us` → `use` typo fixed
- [ ] `cement/ext/ext_smtp.py`: send() docstring contains `.. deprecated:: 3.0.16` admonition; runtime callsite at line 189-190 unchanged
- [ ] `tests/core/test_deprecations.py` and `tests/ext/test_ext_smtp.py` pass without modification (DEPREC-03 already-satisfied)
- [ ] `make test` exits 0 at 100% coverage on every commit
- [ ] `make comply` exits 0 on every commit
- [ ] `make audit-public-api` exits 0 on every commit (no public-API drift)
- [ ] `CHANGELOG.md` `## 3.0.15 - DEVELOPMENT` section's `Refactoring:` bucket has 3 new `[core.deprecations]`, `[ext.logging]`, `[ext.smtp]` entries
- [ ] No new entries in any other bucket; no entries appended outside the active section
</success_criteria>

<output>
After completion, create
`.planning/phases/05-deprecations-docs-security-stubs/05-01-SUMMARY.md`
recording: 3 commit SHAs, the exact CHANGELOG diff, the DEPREC-01 grep
output (`4`), the `make test` final coverage line, and any deviations
from the BEFORE/AFTER text in this plan (expected: none).
</output>
