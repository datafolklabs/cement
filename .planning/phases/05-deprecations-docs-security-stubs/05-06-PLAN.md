---
phase: 05-deprecations-docs-security-stubs
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - .planning/codebase/CONVENTIONS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOCS-04, SEC-01, SEC-02, SEC-03]
requirements_addressed: [DOCS-04, SEC-01, SEC-02, SEC-03]

must_haves:
  truths:
    - ".planning/codebase/CONVENTIONS.md ruff target-version example reflects py310 (matching the live pyproject.toml:88 since Phase 1 PYVER-01)"
    - ".planning/REQUIREMENTS.md SECv2-01..03 entries are expanded to multi-line phase-shaped scope notes per D-14"
    - "Each expanded SECv2 entry contains all 6 locked sub-bullets: Tool, Make target, CI placement, Config, New dev-dep, Exit behavior (+ optional Anchor)"
    - "SECv2-04 entry stays as the original one-liner (D-13 — distinct concern; out of SEC-01..03 scope)"
    - "Both commits are .planning/ planning-artifact edits — NO CHANGELOG entries (filtered per CLAUDE.md Changelog Maintenance / RESEARCH.md Pitfall 7)"
    - "make test, make comply, make docs (with -W), make audit-public-api all exit 0"
  artifacts:
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Ruff target-version example reflects py310"
      contains: 'target-version = "py310"'
    - path: ".planning/REQUIREMENTS.md"
      provides: "SECv2-01..03 expanded with phase-shaped scope notes"
      contains: "**Tool:** `pdm run pip-audit`"
  key_links:
    - from: ".planning/REQUIREMENTS.md SECv2-01..03"
      to: ".planning/phases/02-dependencies-ci-pipeline/02-CONTEXT.md D-03 + 02-PIP-AUDIT.md"
      via: "Anchor sub-bullet"
      pattern: "02-PIP-AUDIT\\.md"
    - from: ".planning/codebase/CONVENTIONS.md line 35"
      to: "pyproject.toml:88 target-version = py310"
      via: "example sync (planning intel mirrors live config)"
      pattern: 'target-version = "py310"'
---

<objective>
Close two planning-intel deferreds that surfaced from Phase 3:

1. **CONVENTIONS.md PEP 604/585 closeout (D-12 / D-16 step 13):** Sync the
   single residual stale surface — the ruff `target-version = "py39"`
   example at `.planning/codebase/CONVENTIONS.md:35` — to `py310`,
   matching the live `pyproject.toml:88` (updated in Phase 1 PYVER-01).
   Per RESEARCH.md Pitfall 6 + Assumption A6, the body of the Type
   annotations section is ALREADY in PEP 604/585 form (Phase 3 Plan 03
   UP cascade landed it). This is a 1-line edit, NOT a wholesale rewrite.

2. **SECv2-01..03 phase-shaped scope notes expansion (D-13 / D-14 / D-15
   / D-16 step 14):** Expand the existing one-liner SECv2-01 (pip-audit),
   SECv2-02 (bandit), SECv2-03 (SAST) entries in `.planning/REQUIREMENTS.md`
   to multi-line stubs with the locked 6 sub-bullets (Tool, Make target,
   CI placement, Config, New dev-dep, Exit behavior + optional Anchor).
   SECv2-04 (`SECURITY.md`) stays as a one-liner — distinct concern, out
   of `SEC-01..03` scope.

Purpose: Close DOCS-04 (planning-intel side), SEC-01, SEC-02, SEC-03.
Both commits are `.planning/` planning-artifact edits and get NO
CHANGELOG entries (CLAUDE.md filter / RESEARCH.md Pitfall 7 row 13/14).

Output: 2 atomic commits (D-16 steps 13, 14). NO new CHANGELOG entries
(planning-artifact filter).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md
@.planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md
@.planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-05-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/02-dependencies-ci-pipeline/02-CONTEXT.md
@.planning/phases/02-dependencies-ci-pipeline/02-PIP-AUDIT.md
@CLAUDE.md
@CHANGELOG.md
@pyproject.toml

<interfaces>
<!-- CONVENTIONS.md target surface (Pitfall 6: 1-line edit, NOT wholesale). -->

.planning/codebase/CONVENTIONS.md:35 (the ONLY stale surface):
  target-version = "py39"

Live source-of-truth (pyproject.toml:88 — already py310 since Phase 1):
  target-version = "py310"

Pre-task verification grep (RESEARCH.md Pitfall 6):
  grep -nE 'Dict\[|List\[|Optional\[|Union\[|target-version' \
       .planning/codebase/CONVENTIONS.md
Expected: ONLY line 35 hits the `target-version` substring; no Dict/
List/Optional/Union body-text hits. If body-text hits surface, the
doc was reverted — escalate to wholesale refresh.

<!-- REQUIREMENTS.md SECv2 surface (current one-liner shape). -->

.planning/REQUIREMENTS.md:89-94:
  ### Security Audit Tooling Implementation

  - **SECv2-01**: `pip-audit` integrated into CI on every PR
  - **SECv2-02**: `bandit` integrated into CI on every PR with project-tuned ruleset
  - **SECv2-03**: SAST tool (CodeQL or Semgrep) selected and integrated into CI
  - **SECv2-04**: Documented security disclosure process (`SECURITY.md`)

D-14 locked 6 sub-bullets per stub:
  - Tool (e.g., `pdm run pip-audit`)
  - Make target (independent, NOT chained into make test/comply)
  - CI placement (new .github/workflows/security.yml or equivalent)
  - Config-file shape
  - New dev-dep (PyPI package name)
  - Exit behavior (advisory on first run; flip to blocking later)
  Optional 7th: Anchor (Phase 02 D-03 / 02-PIP-AUDIT.md precedent)

CLAUDE.md Changelog Maintenance filter rule (Pitfall 7):
  Both commits in this plan touch ONLY .planning/ files →
  PLANNING-ARTIFACT → NO CHANGELOG ENTRY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sync CONVENTIONS.md ruff target-version example to py310 (commit 13 of D-16)</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md lines 22-100 (covers the Type annotations section at 22-26 + the Ruff Configuration example at 37-45 with the line-35 target + the sister Type Annotations section at 83-100)
    - pyproject.toml lines 80-100 (live ruff config; confirms `target-version = "py310"` at line 88)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-12
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 6 (CRITICAL — 1-line edit, NOT wholesale rewrite)
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Assumptions Log` A6
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md ".planning/codebase/CONVENTIONS.md" section
  </read_first>
  <action>
**Verify execution order:** the `<automated>` block below is run AFTER `git commit`. The trailing `! git show HEAD --stat | grep -F 'CHANGELOG.md'` assertion confirms HEAD has no CHANGELOG entry (planning-artifact commit per CLAUDE.md filter).

**Pre-task verification (Pitfall 6):**

    grep -nE 'Dict\[|List\[|Optional\[|Union\[|target-version' \
         .planning/codebase/CONVENTIONS.md

Expected: ONLY line 35 (`target-version = "py39"`) hits the
`target-version` substring; no body-text Dict/List/Optional/Union
hits. If body-text hits surface, the doc has been reverted —
ESCALATE to user (wholesale refresh required, NOT a 1-line edit).

If pre-task grep is as expected, proceed with the 1-line edit.

EXACT BEFORE (`.planning/codebase/CONVENTIONS.md:35`):

    target-version = "py39"

EXACT AFTER:

    target-version = "py310"

That is the entire diff. Do NOT touch the body of the Type
annotations section (already in PEP 604/585 form per RESEARCH.md A6).

**NO CHANGELOG ENTRY** — this is a `.planning/` planning-artifact edit
filtered per CLAUDE.md "Changelog Maintenance" rule. RESEARCH.md
Pitfall 7 row 13 is explicit: "DO NOT add — `.planning/` is planning
intel, not user-visible".

Run gates BEFORE committing:

    make comply && make test && make audit-public-api && make docs

(Plan 04's `-W` gate is now active; `make docs` is mandatory.)

Commit subject: `docs(codebase): refresh CONVENTIONS to PEP 604/585
syntax` (54 chars).

Suggested body:

    The body of CONVENTIONS.md "Type annotations" section was already
    in PEP 604/585 form — Phase 3 Plan 03's UP-family cascade landed
    those changes. The single residual stale surface was the ruff
    `target-version = "py39"` example at line 35.

    Sync to `py310` to match the live pyproject.toml:88 (updated in
    Phase 1 PYVER-01). 1-line edit; the rest of the doc is already
    correct.

    Closes Phase 3 D-22 step 5 / D-19 deferral.

    Planning-artifact commit per CLAUDE.md "Changelog Maintenance"
    filter (no user-visible surface) — no CHANGELOG entry.
  </action>
  <verify>
    <automated>grep -F 'target-version = "py310"' .planning/codebase/CONVENTIONS.md &amp;&amp; ! grep -F 'target-version = "py39"' .planning/codebase/CONVENTIONS.md &amp;&amp; ! grep -nE 'Dict\[|List\[|Optional\[|Union\[' .planning/codebase/CONVENTIONS.md &amp;&amp; make comply &amp;&amp; make test &amp;&amp; make audit-public-api &amp;&amp; make docs &amp;&amp; ! git show HEAD --stat | grep -F 'CHANGELOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'target-version = "py310"' .planning/codebase/CONVENTIONS.md` matches at least once
    - `grep -F 'target-version = "py39"' .planning/codebase/CONVENTIONS.md` returns 0 hits
    - `grep -nE 'Dict\[|List\[|Optional\[|Union\[' .planning/codebase/CONVENTIONS.md` returns 0 hits (body-text Pitfall 6 guard — no regressions to old-form annotations)
    - The diff shows exactly 1 line changed (`git show --stat HEAD` reports `1 insertion(+), 1 deletion(-)` on `.planning/codebase/CONVENTIONS.md`)
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0; `make docs` exits 0 (Plan 04's `-W` gate)
    - **NO CHANGELOG ENTRY (after commit):** `git show HEAD --stat | grep -F 'CHANGELOG.md'` runs AFTER `git commit` and returns 0 hits (the commit MUST NOT touch CHANGELOG.md per CLAUDE.md filter)
    - Commit subject ≤78 chars
    - Commit touches exactly 1 file: `.planning/codebase/CONVENTIONS.md`
  </acceptance_criteria>
  <done>
    `.planning/codebase/CONVENTIONS.md:35` reads `target-version = "py310"`;
    body of Type annotations section unchanged (already in modern form);
    NO CHANGELOG entry (planning-artifact filter); gates clean; commit
    landed under D-16 step 13.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand SECv2-01..03 with phase-shaped scope notes (commit 14 of D-16)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 70-100 (covers the v1 SEC-01..03 one-liners at 73-75 + the v2 SECv2-01..04 block at 89-94 to be expanded + surrounding ### Release / ### Performance Pass blocks for nearby format precedent)
    - .planning/phases/02-dependencies-ci-pipeline/02-PIP-AUDIT.md (skim — confirms it's the right "capture artifact" pattern that the SECv2-01 stub references)
    - .planning/phases/02-dependencies-ci-pipeline/02-CONTEXT.md `<decisions>` D-03 (the one-shot pip-audit precedent that the Anchor sub-bullet points to)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-13, D-14, D-15
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 10 (full replacement)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md ".planning/REQUIREMENTS.md (SECv2-01..03 expansion)" section
  </read_first>
  <action>
**Verify execution order:** the `<automated>` block below is run AFTER `git commit`. The trailing `! git show HEAD --stat | grep -F 'CHANGELOG.md'` assertion confirms HEAD has no CHANGELOG entry (planning-artifact commit per CLAUDE.md filter).

Expand the SECv2-01, SECv2-02, SECv2-03 one-liner entries in
`.planning/REQUIREMENTS.md` to multi-line phase-shaped scope notes per
D-14. SECv2-04 stays UNCHANGED (D-13 — distinct concern, out of
`SEC-01..03` scope).

EXACT BEFORE (`.planning/REQUIREMENTS.md:89-94`):

    ### Security Audit Tooling Implementation

    - **SECv2-01**: `pip-audit` integrated into CI on every PR
    - **SECv2-02**: `bandit` integrated into CI on every PR with project-tuned ruleset
    - **SECv2-03**: SAST tool (CodeQL or Semgrep) selected and integrated into CI
    - **SECv2-04**: Documented security disclosure process (`SECURITY.md`)

EXACT AFTER (per RESEARCH.md Example 10 + PATTERNS.md ".planning/
REQUIREMENTS.md (SECv2-01..03 expansion)" — preserve indentation and
exact sub-bullet labels):

    ### Security Audit Tooling Implementation

    - **SECv2-01**: `pip-audit` integrated into CI on every PR
        - **Tool:** `pdm run pip-audit` (read deps from `pdm.lock`)
        - **Make target:** `make audit-deps` — independent target, NOT
          chained into `make test` or `make comply` (matches Phase 03
          D-03 `audit-public-api` discipline)
        - **CI placement:** new dedicated workflow file
          `.github/workflows/security.yml`, triggers on `pull_request`
          + weekly `cron`. NOT serialized into `build_and_test.yml`
          (mirrors Phase 02 D-16 fail-fast vs feedback-time tradeoff)
        - **Config:** none initially; `pip-audit --skip` for accepted
          CVEs documented inline if any surface
        - **New dev-dep:** `pip-audit` (PyPI; latest stable)
        - **Exit behavior:** advisory on first run (Phase 02 D-03
          one-shot precedent — capture accepted CVEs in an in-repo
          artifact mirroring `02-PIP-AUDIT.md`); flip to blocking once
          baseline is clean
        - **Anchor:** `.planning/phases/02-dependencies-ci-pipeline/02-CONTEXT.md`
          D-03 + `02-PIP-AUDIT.md` capture artifact

    - **SECv2-02**: `bandit` integrated into CI on every PR with project-tuned ruleset
        - **Tool:** `pdm run bandit -r cement/`
        - **Make target:** `make audit-bandit` — independent target
        - **CI placement:** same `.github/workflows/security.yml` lane
          as SECv2-01 (one workflow, three jobs)
        - **Config:** `.bandit` allowlist file at repo root —
          project-tuned to skip false positives on framework-intentional
          patterns (e.g., `subprocess` call sites in `cement/utils/shell.py`
          that are deliberate per the public API contract)
        - **New dev-dep:** `bandit` (PyPI; latest stable)
        - **Exit behavior:** advisory on first run; flip to blocking
          once `.bandit` allowlist is curated
        - **Anchor:** `02-CONTEXT.md` D-03 (same one-shot precedent)

    - **SECv2-03**: SAST tool (CodeQL or Semgrep) selected and integrated into CI
        - **Tool:** TBD per evaluation; candidates are CodeQL (GitHub-
          native, free for public repos, deeper Python rules) and
          Semgrep (rule customization, more permissive licensing)
        - **Make target:** `make audit-sast` (only if CLI-runnable;
          CodeQL is GitHub-Actions-only)
        - **CI placement:** dedicated job in `security.yml`; weekly
          cron preferred over per-PR (run-time cost)
        - **Config:** `.semgrep.yml` (Semgrep) OR
          `.github/codeql/codeql-config.yml` (CodeQL) — rule selection
          pinned to Python OWASP top-N + cement-specific patterns
        - **New dev-dep:** `semgrep` if Semgrep selected; none if
          CodeQL (GitHub-hosted)
        - **Exit behavior:** advisory on first run; per-finding triage
          before any blocking flip
        - **Anchor:** `02-CONTEXT.md` D-03

    - **SECv2-04**: Documented security disclosure process (`SECURITY.md`)

Critical preservation rules:
- 4-space indent for sub-bullets (markdown list nesting).
- Sub-bullet labels are bold (`**Tool:**`, `**Make target:**`, etc.) —
  match the exact bold formatting per Example 10.
- SECv2-04 line stays unchanged — distinct concern (disclosure
  process, not tooling). D-13 explicit.
- The leading `### Security Audit Tooling Implementation` H3 header
  stays unchanged.

**NO CHANGELOG ENTRY** — `.planning/REQUIREMENTS.md` edit, filtered
per CLAUDE.md "Changelog Maintenance" / RESEARCH.md Pitfall 7 row 14
("DO NOT add — `.planning/REQUIREMENTS.md` is planning intel"). The
`docs(05): ...` subject prefix is itself the GSD planning-artifact
convention.

Run gates BEFORE committing:

    make comply && make test && make audit-public-api && make docs

Commit subject: `docs(05): expand SECv2-01..03 with phase-shaped scope
notes` (60 chars).

Suggested body:

    Expand SECv2-01 (pip-audit), SECv2-02 (bandit), SECv2-03 (SAST)
    with phase-shaped scope notes per D-14: Tool, Make target, CI
    placement, Config-file shape, New dev-dep, Exit behavior, Anchor.

    Closes SEC-01, SEC-02, SEC-03 — the next security-tooling
    milestone has a phase-shaped starting point and does not need to
    re-discover.

    SECv2-04 (SECURITY.md / disclosure process) stays as a one-liner
    per D-13 — distinct concern, separate future-milestone work.

    Planning-artifact commit per CLAUDE.md "Changelog Maintenance"
    filter (no user-visible surface) — no CHANGELOG entry.
  </action>
  <verify>
    <automated>awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*Tool:\*\*' | grep -qE '^[3-9]$' &amp;&amp; awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*Make target:\*\*' | grep -qE '^[3-9]$' &amp;&amp; awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*CI placement:\*\*' | grep -qE '^[3-9]$' &amp;&amp; awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*Config:\*\*' | grep -qE '^[3-9]$' &amp;&amp; awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*New dev-dep:\*\*' | grep -qE '^[3-9]$' &amp;&amp; awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*Exit behavior:\*\*' | grep -qE '^[3-9]$' &amp;&amp; grep -F '`SECURITY.md`' .planning/REQUIREMENTS.md &amp;&amp; make comply &amp;&amp; make test &amp;&amp; make audit-public-api &amp;&amp; make docs &amp;&amp; ! git show HEAD --stat | grep -F 'CHANGELOG.md'</automated>
  </verify>
  <acceptance_criteria>
    - **6-properties × 3-stubs grep gate (D-14 acceptance):** Each of these counts is exactly `3` (one per SECv2-01, SECv2-02, SECv2-03):
      - `awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' .planning/REQUIREMENTS.md | grep -cE '\*\*Tool:\*\*'` → `3`
      - same with `\*\*Make target:\*\*` → `3`
      - same with `\*\*CI placement:\*\*` → `3`
      - same with `\*\*Config:\*\*` → `3`
      - same with `\*\*New dev-dep:\*\*` → `3`
      - same with `\*\*Exit behavior:\*\*` → `3`
    - SECv2-04 stays as one-liner: `grep -E '^- \*\*SECv2-04\*\*: Documented security disclosure process' .planning/REQUIREMENTS.md` matches; the line below `SECv2-04` MUST NOT have any 4-space-indented sub-bullets (`grep -A 1 '^- \*\*SECv2-04\*\*' .planning/REQUIREMENTS.md | grep -E '^    -'` returns 0 hits)
    - Anchor sub-bullet on SECv2-01 references `02-PIP-AUDIT.md`: `grep -F '02-PIP-AUDIT.md' .planning/REQUIREMENTS.md` matches at least once
    - Live `pdm run pip-audit` command name appears: `grep -F 'pdm run pip-audit' .planning/REQUIREMENTS.md` matches
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0; `make docs` exits 0
    - **NO CHANGELOG ENTRY (after commit):** `git show HEAD --stat | grep -F 'CHANGELOG.md'` runs AFTER `git commit` and returns 0 hits (the commit MUST NOT touch CHANGELOG.md per CLAUDE.md filter)
    - Commit subject ≤78 chars (`docs(05): expand SECv2-01..03 with phase-shaped scope notes`)
    - Commit touches exactly 1 file: `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>
    SECv2-01, SECv2-02, SECv2-03 expanded with all 6 locked sub-bullets;
    SECv2-04 unchanged; gates clean; NO CHANGELOG entry (planning-artifact
    filter); commit landed under D-16 step 14. SEC-01, SEC-02, SEC-03
    requirements satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning-intel docs → future-milestone discuss-phase | Both `.planning/codebase/CONVENTIONS.md` and `.planning/REQUIREMENTS.md` are read by future GSD discuss/plan/research phases. Stale or under-specified content multiplies re-discovery cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-noop-13 | I (Information disclosure / wrong-info) | `.planning/codebase/CONVENTIONS.md:35` ruff `target-version = "py39"` example | mitigate | 1-line sync to `py310` matches live `pyproject.toml:88` (updated in Phase 1 PYVER-01). Pitfall 6 guard: pre-task grep confirms only line 35 is stale; if body-text Dict/List/Optional/Union hits surface, escalate. |
| T-05-noop-14 | I (Information disclosure) | `.planning/REQUIREMENTS.md` SECv2-01..03 one-liners insufficient for a future security-tooling milestone | mitigate | Phase-shaped scope notes per D-14 (6 locked sub-bullets per stub) — sufficient for the future discuss-phase to refine without re-discovery. SECv2-04 unchanged (D-13 — distinct concern). |
| T-05-03 | I (Information disclosure / wrong-info) | (Cross-reference; not this plan's surface) | mitigate | Plan 02 owned the `cement/core/interface.py:102` string-quote and the `make audit-public-api` gate. By this plan, the gate has held green across all preceding plans. |

**ASVS L1 applicability:** V14 (Configuration) partial — both edits are
planning-intel hardening (no runtime impact). Block on: high (T-05-noop-13
guard via Pitfall 6 pre-task grep; T-05-noop-14 acceptance via 6-property
× 3-stub grep gate).
</threat_model>

<verification>
After commits 13 and 14:

    # CONVENTIONS.md sync
    grep -F 'target-version = "py310"' .planning/codebase/CONVENTIONS.md
    ! grep -F 'target-version = "py39"' .planning/codebase/CONVENTIONS.md

    # SECv2-01..03 expansion (6 properties × 3 stubs = 18 total)
    awk '/^- \*\*SECv2-01\*\*/,/^- \*\*SECv2-04\*\*/' \
        .planning/REQUIREMENTS.md | \
      grep -cE '\*\*Tool:\*\*|\*\*Make target:\*\*|\*\*CI placement:\*\*|\*\*Config:\*\*|\*\*New dev-dep:\*\*|\*\*Exit behavior:\*\*'
    # → ≥18

    # SECv2-04 still one-liner
    grep -A 1 '^- \*\*SECv2-04\*\*' .planning/REQUIREMENTS.md | grep -E '^    -' | wc -l   # → 0

    # Cumulative gates
    make test && make comply && make audit-public-api && make docs

    # CHANGELOG — ZERO new entries from this plan (planning-artifact filter)
    git diff <plan-05-summary-head>..HEAD -- CHANGELOG.md | grep -E '^\+- \`\[' | wc -l   # → 0
</verification>

<success_criteria>
- [ ] `.planning/codebase/CONVENTIONS.md:35` reads `target-version = "py310"`; body of Type annotations section unchanged (Pitfall 6 hold-the-line)
- [ ] `.planning/REQUIREMENTS.md` SECv2-01..03 each have all 6 locked sub-bullets (Tool, Make target, CI placement, Config, New dev-dep, Exit behavior); Anchor sub-bullet present on at least SECv2-01
- [ ] SECv2-04 unchanged (still a one-liner — D-13)
- [ ] Both commits use Conventional Commits subjects ≤78 chars
- [ ] **NO CHANGELOG entries** for either commit (planning-artifact filter per CLAUDE.md / RESEARCH.md Pitfall 7 rows 13, 14)
- [ ] `make test` exits 0 at 100% coverage; `make comply` exits 0; `make audit-public-api` exits 0; `make docs` (with Plan 04's `-W` flag) exits 0
- [ ] Commit 13 touches exactly 1 file (`.planning/codebase/CONVENTIONS.md`)
- [ ] Commit 14 touches exactly 1 file (`.planning/REQUIREMENTS.md`)
</success_criteria>

<output>
After completion, create
`.planning/phases/05-deprecations-docs-security-stubs/05-06-SUMMARY.md`
recording: 2 commit SHAs, the CONVENTIONS.md 1-line diff, the
6-property × 3-stub grep evidence (18 hits) for the SECv2 expansion,
explicit confirmation that NEITHER commit touched CHANGELOG.md
(planning-artifact filter), and the final cumulative phase gate
status (`make test`, `make comply`, `make docs`, `make audit-public-api`
all exit 0). This is the final plan in Phase 5; the summary should
also enumerate which D-18 acceptance criteria (1-12) are satisfied
across all 6 plans.
</output>
