---
phase: 05-deprecations-docs-security-stubs
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - DEPRECATIONS.md
  - CHANGELOG.md
autonomous: true
requirements: [DEPREC-02]
requirements_addressed: [DEPREC-02]

must_haves:
  truths:
    - "DEPRECATIONS.md exists at repo root (sibling of README.md, CHANGELOG.md, CONTRIBUTORS.md)"
    - "DEPRECATIONS.md contains exactly 4 H2 sections — one per registry ID (3.0.8-1, 3.0.8-2, 3.0.10-1, 3.0.16-1)"
    - "Each H2 section carries the locked 5-element layout: Surface, Since, Removal, migration prose with code example, GitBook back-link"
    - "GitBook back-links use the dotted-anchor format (`#3.0.8-1`, NOT `#3-0-8-1`) matching cement/core/deprecations.py:15 deprecate() runtime URL convention"
    - "DEPREC-03 still passes (no test work; existing tests unaffected by new repo-root markdown)"
  artifacts:
    - path: "DEPRECATIONS.md"
      provides: "Top-level repo-root markdown mirroring GitBook deprecation narrative"
      contains: "# Cement Deprecations"
      min_lines: 60
    - path: "CHANGELOG.md"
      provides: "1 new Misc entry"
      contains: "[docs] Add top-level DEPRECATIONS.md mirroring GitBook narrative"
  key_links:
    - from: "DEPRECATIONS.md (each H2 block)"
      to: "https://docs.builtoncement.com/release-information/deprecations#<id>"
      via: "GitBook back-link with dotted anchor format"
      pattern: "release-information/deprecations#3\\.0\\."
    - from: "DEPRECATIONS.md"
      to: "cement/core/deprecations.py DEPRECATIONS dict (4 IDs)"
      via: "ID set must match (4 IDs in dict ↔ 4 H2 sections in markdown)"
      pattern: "^## 3\\.0\\."
---

<objective>
Add a new top-level `DEPRECATIONS.md` (repo root, sibling of `README.md`,
`CHANGELOG.md`, `CONTRIBUTORS.md`) containing one H2 section per
registered deprecation ID with the locked 5-element block format
(Surface / Since / Removal / migration prose / GitBook back-link).

Purpose: Close DEPREC-02 acceptance — the in-repo touchpoint for the
deprecation narrative. CONTEXT.md D-05/D-06 reinterprets the literal
REQUIREMENTS.md DEPREC-02 wording ("a dedicated docs/source/deprecations.rst
page") because Sphinx is **strictly API reference** per
docs/source/index.rst note. Free-form developer narrative — including
deprecation details — lives on the GitBook site at builtoncement.com;
this repo-root markdown mirrors that narrative for in-repo
discoverability.

Output: 1 new file (`DEPRECATIONS.md`) + 1 CHANGELOG entry appended to
the active `Misc:` bucket. D-16 step 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md
@.planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md
@.planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-01-SUMMARY.md
@.planning/phases/05-deprecations-docs-security-stubs/05-02-SUMMARY.md
@CLAUDE.md
@CHANGELOG.md
@CONTRIBUTORS.md
@cement/core/deprecations.py

<interfaces>
<!-- Sibling shape (no frontmatter, H1 + H2): CHANGELOG.md, CONTRIBUTORS.md. -->
<!-- Per-deprecation block structure has no in-repo precedent — D-07 establishes it. -->

DEPRECATIONS dict source-of-truth (cement/core/deprecations.py:4-9):
  '3.0.8-1': Environment variable CEMENT_FRAMEWORK_LOGGING (→ CEMENT_LOG)
  '3.0.8-2': App.Meta.framework_logging
  '3.0.10-1': FATAL logging facility (→ CRITICAL)
  '3.0.16-1': SMTPMailHandler.send() bool return type

GitBook anchor format (verified per RESEARCH.md Pitfall 8):
  https://docs.builtoncement.com/release-information/deprecations#<id>
  where <id> is dotted (e.g., #3.0.8-1, NOT #3-0-8-1)
  This matches cement/core/deprecations.py:15 deprecate() runtime URL.

Locked block format per H2 (D-07):
  ## <id>
  **Surface:** <symbol/file/concept>
  **Since:** Cement <since-version>
  **Removal:** Cement v<removal-version>
  <migration prose with optional code example>
  [GitBook reference](https://docs.builtoncement.com/release-information/deprecations#<id>)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DEPRECATIONS.md with 4 H2 blocks (commit 4 of D-16)</name>
  <files>DEPRECATIONS.md, CHANGELOG.md</files>
  <read_first>
    - cement/core/deprecations.py (whole file — confirms the 4 ID set)
    - CHANGELOG.md lines 1-5 (sibling no-frontmatter shape: H1 + H2)
    - CONTRIBUTORS.md lines 1-10 (sibling shape — confirms repo-root markdown convention)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-05, D-06, D-07
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 8 (full template)
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 8 (anchor format MUST be dotted)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "DEPRECATIONS.md (NEW, repo root)" section
  </read_first>
  <action>
Create `DEPRECATIONS.md` at the repo root with the EXACT content below
(per RESEARCH.md Example 8 + PATTERNS.md). Anchor format is **dotted**
(`#3.0.8-1`, NOT `#3-0-8-1`) per Pitfall 8. ID order matches
since-version ascending so new IDs append at the bottom under their
since-version section without re-grouping.

EXACT CONTENT (write verbatim):

```markdown
# Cement Deprecations

This document mirrors the canonical GitBook narrative at
[docs.builtoncement.com/release-information/deprecations](https://docs.builtoncement.com/release-information/deprecations)
for in-repo discoverability. Each deprecation has an H2 section with
its registry ID. New IDs append at the bottom under their
since-version section.

For runtime behavior, the `cement.core.deprecations.deprecate()`
helper emits a `DeprecationWarning` whose message includes a
back-link to the corresponding GitBook anchor.

## 3.0.8-1

**Surface:** Environment variable `CEMENT_FRAMEWORK_LOGGING`
**Since:** Cement 3.0.8
**Removal:** Cement v3.2.0

Use `CEMENT_LOG` instead. Single-pass migration:

```bash
# Before
export CEMENT_FRAMEWORK_LOGGING=true

# After
export CEMENT_LOG=true
```

[GitBook reference](https://docs.builtoncement.com/release-information/deprecations#3.0.8-1)

## 3.0.8-2

**Surface:** `App.Meta.framework_logging`
**Since:** Cement 3.0.8
**Removal:** Cement v3.2.0

Will be changed or removed. Migrate to the `CEMENT_LOG` environment
variable surface (see `3.0.8-1`).

[GitBook reference](https://docs.builtoncement.com/release-information/deprecations#3.0.8-2)

## 3.0.10-1

**Surface:** `cement.ext.ext_logging.LoggingLogHandler.fatal()` /
`set_level('FATAL')`
**Since:** Cement 3.0.10
**Removal:** Cement v3.2.0

Use `critical()` / `set_level('CRITICAL')` instead.

```python
# Before
app.log.fatal('something broke')
app.log.set_level('FATAL')

# After
app.log.critical('something broke')
app.log.set_level('CRITICAL')
```

[GitBook reference](https://docs.builtoncement.com/release-information/deprecations#3.0.10-1)

## 3.0.16-1

**Surface:** `cement.ext.ext_smtp.SMTPMailHandler.send()` return type
**Since:** Cement 3.0.16
**Removal:** Cement v3.2.0

The `bool` return value is deprecated; in v3.2.0 the method will
return the `smtplib` senderrs `dict`.

```python
# Before (3.0.x)
ok = app.mail.send('hello')
if not ok:
    handle_error()

# After (3.2.0+)
errs = app.mail.send('hello')
if errs:
    handle_error_per_recipient(errs)
```

[GitBook reference](https://docs.builtoncement.com/release-information/deprecations#3.0.16-1)
```

Use the Write tool to create the file. NO frontmatter (matches the
sibling `CHANGELOG.md` / `CONTRIBUTORS.md` no-frontmatter convention).
4 H2 sections, ordered by since-version ascending.

Append CHANGELOG entry to `Misc:` bucket of active 3.0.15 - DEVELOPMENT
section:

```
- `[docs]` Add top-level DEPRECATIONS.md mirroring GitBook narrative
```

(RESEARCH.md Pitfall 7 table allows `Features:` for new files but leans
`Misc:` for the modest scope. Use `Misc:` to match the table.)

Run gates BEFORE committing:

```bash
make comply && make test && make audit-public-api
```

Commit subject: `docs: add top-level DEPRECATIONS.md` (38 chars).

Suggested body:

```
Mirror the canonical GitBook deprecation narrative at
docs.builtoncement.com/release-information/deprecations into a
top-level repo-root markdown file (sibling of README.md,
CHANGELOG.md, CONTRIBUTORS.md). Anchors use the dotted-version
format (#3.0.8-1, NOT #3-0-8-1) per the runtime URL emitted by
cement.core.deprecations.deprecate().

Per CONTEXT.md D-05/D-06: Sphinx is strictly API reference per
docs/source/index.rst — free-form developer narrative lives on
GitBook. The literal REQUIREMENTS.md DEPREC-02 wording ("a dedicated
docs/source/deprecations.rst page") was authored without this
docs-split context and is reinterpreted, not followed verbatim.

DEPREC-02 acceptance: in-repo touchpoint with one H2 section per
registry ID; per-block format = surface, since, removal, migration
prose with code example, GitBook back-link.
```
  </action>
  <verify>
    <automated>test -f DEPRECATIONS.md &amp;&amp; grep -cE '^## 3\.0\.' DEPRECATIONS.md | grep -qx 4 &amp;&amp; grep -cE '^# Cement Deprecations' DEPRECATIONS.md | grep -qx 1 &amp;&amp; grep -cE '\*\*Surface:\*\*' DEPRECATIONS.md | grep -qx 4 &amp;&amp; grep -cE '\*\*Since:\*\*' DEPRECATIONS.md | grep -qx 4 &amp;&amp; grep -cE '\*\*Removal:\*\*' DEPRECATIONS.md | grep -qx 4 &amp;&amp; grep -cE 'docs\.builtoncement\.com/release-information/deprecations#3\.0\.' DEPRECATIONS.md | grep -qE '^[[:space:]]*[4-9]$|^1[0-9]+$' &amp;&amp; ! grep -E 'release-information/deprecations#3-0-' DEPRECATIONS.md &amp;&amp; make comply &amp;&amp; make test &amp;&amp; make audit-public-api &amp;&amp; grep -F '[docs] Add top-level DEPRECATIONS.md mirroring GitBook narrative' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f DEPRECATIONS.md` returns true
    - `wc -l DEPRECATIONS.md` returns ≥60 lines (covers H1 + intro + 4 H2 blocks with code examples)
    - `grep -cE '^# Cement Deprecations' DEPRECATIONS.md` returns exactly `1`
    - `grep -cE '^## 3\.0\.' DEPRECATIONS.md` returns exactly `4` (one H2 per registry ID)
    - `grep -cE '^## 3\.0\.8-1' DEPRECATIONS.md` returns `1`
    - `grep -cE '^## 3\.0\.8-2' DEPRECATIONS.md` returns `1`
    - `grep -cE '^## 3\.0\.10-1' DEPRECATIONS.md` returns `1`
    - `grep -cE '^## 3\.0\.16-1' DEPRECATIONS.md` returns `1`
    - `grep -cE '\*\*Surface:\*\*' DEPRECATIONS.md` returns exactly `4`
    - `grep -cE '\*\*Since:\*\*' DEPRECATIONS.md` returns exactly `4`
    - `grep -cE '\*\*Removal:\*\*' DEPRECATIONS.md` returns exactly `4`
    - `grep -cE 'docs\.builtoncement\.com/release-information/deprecations#3\.0\.[0-9]+-[0-9]+' DEPRECATIONS.md` returns ≥4 (one back-link per ID, possibly +1 for the H1 intro link)
    - `grep -E 'release-information/deprecations#3-0-' DEPRECATIONS.md` returns 0 hits (Pitfall 8 — anchor format MUST be dotted, NOT GitHub-slugified)
    - `grep -F 'Cement v3.2.0' DEPRECATIONS.md | wc -l` returns ≥4 (one Removal: line per ID)
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0
    - CHANGELOG `Misc:` bucket contains literal `- \`[docs]\` Add top-level DEPRECATIONS.md mirroring GitBook narrative`
    - Commit subject ≤78 chars; touches exactly 2 files (`DEPRECATIONS.md` + `CHANGELOG.md`)
  </acceptance_criteria>
  <done>
    `DEPRECATIONS.md` exists at repo root with 4 H2 blocks matching the
    locked D-07 format; GitBook anchors use dotted format; ID set matches
    `cement/core/deprecations.py` DEPRECATIONS dict (4 IDs); gates clean;
    CHANGELOG appended; commit landed under D-16 step 4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| In-repo markdown → GitBook external site | `DEPRECATIONS.md` back-links go to `https://docs.builtoncement.com/release-information/deprecations#<id>`. The GitBook content is hand-authored off-band by the user (Phase 5 does NOT modify GitBook). |
| Repo-root markdown → downstream developer | A downstream developer reading the source tree finds `DEPRECATIONS.md` as a sibling of `README.md`. The content is hand-authored, not auto-generated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I (Information disclosure / wrong-info delivery) | `DEPRECATIONS.md` GitBook back-links | mitigate | Anchor format hard-coded to `https://docs.builtoncement.com/release-information/deprecations#<id>` (dotted ID) verified against `cement/core/deprecations.py:15` runtime URL convention. Acceptance criterion enforces anchor format via grep. RESEARCH.md A2 confirms GitBook accepts dotted-version anchors. |
| T-05-noop-07 | T (Tampering) | Markdown injection in `DEPRECATIONS.md` (e.g., user-controlled deprecation IDs in a future workflow) | accept | `DEPRECATIONS.md` is hand-authored repo-root markdown; no user-input pipeline. Future automated generation would need to escape `<`, `>`, `_` in IDs — but IDs are constrained to `\d+\.\d+\.\d+-\d+` per the existing convention. Out of scope this phase. |

**ASVS L1 applicability:** V5 (Input Validation) does not apply —
`DEPRECATIONS.md` is read-only markdown; the `DEPRECATIONS` dict is
module-scope literal. Block on: high (T-05-01 anchor-format mismatch
would break round-trip navigation; mitigated by acceptance grep).
</threat_model>

<verification>
After commit 4:

```bash
# DEPREC-02 acceptance — in-repo touchpoint exists
test -f DEPRECATIONS.md
grep -cE '^## 3\.0\.' DEPRECATIONS.md                                              # → 4

# Per-block layout (5-element)
grep -cE '\*\*Surface:\*\*' DEPRECATIONS.md                                        # → 4
grep -cE '\*\*Since:\*\*' DEPRECATIONS.md                                          # → 4
grep -cE '\*\*Removal:\*\*' DEPRECATIONS.md                                        # → 4

# Pitfall 8 — anchor format dotted, not GitHub-slugified
grep -cE 'docs\.builtoncement\.com/release-information/deprecations#3\.0\.' DEPRECATIONS.md   # ≥ 4
! grep -E 'release-information/deprecations#3-0-' DEPRECATIONS.md                   # 0 hits

# ID set parity with the runtime registry
diff \
  <(grep -oE "'3\\.0\\.[0-9]+-[0-9]+'" cement/core/deprecations.py | sort -u) \
  <(grep -oE '^## 3\\.0\\.[0-9]+-[0-9]+' DEPRECATIONS.md | sed 's/^## //; s/$/&/' | awk '{print "'\''" $0 "'\''" }' | sort -u)
# ↑ both lists must be identical; if not, an ID was missed

# Cumulative gates
make test && make comply && make audit-public-api

# CHANGELOG — 1 new Misc entry
git diff <plan-02-summary-head>..HEAD -- CHANGELOG.md | grep -E '^\+- \`\[' | wc -l   # → 1
```
</verification>

<success_criteria>
- [ ] `DEPRECATIONS.md` created at repo root (sibling of `README.md`, `CHANGELOG.md`, `CONTRIBUTORS.md`)
- [ ] File contains exactly 4 H2 sections (one per registry ID); ID set matches `cement/core/deprecations.py` DEPRECATIONS dict
- [ ] Each H2 block contains all 5 locked elements: Surface, Since, Removal, migration prose with code example, GitBook back-link
- [ ] All GitBook back-links use dotted anchor format (`#3.0.8-1`, NOT `#3-0-8-1`)
- [ ] `make test` exits 0 at 100% coverage; `make comply` exits 0; `make audit-public-api` exits 0
- [ ] CHANGELOG `Misc:` bucket has 1 new `[docs]` entry
- [ ] Commit uses Conventional Commits subject ≤78 chars; touches exactly 2 files
</success_criteria>

<output>
After completion, create
`.planning/phases/05-deprecations-docs-security-stubs/05-03-SUMMARY.md`
recording: 1 commit SHA, the file size and line count of `DEPRECATIONS.md`,
the dotted-anchor grep evidence, and confirmation that the ID set
matches the runtime DEPRECATIONS dict.
</output>
