---
phase: 05-deprecations-docs-security-stubs
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - README.md
  - .github/CONTRIBUTING.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-02, DOCS-04]
requirements_addressed: [DOCS-02, DOCS-04]

must_haves:
  truths:
    - "README.md contains zero references to travis-ci.org or app.travis-ci.com (Travis was retired in Phase 1; CI moved to GitHub Actions)"
    - ".github/CONTRIBUTING.md references 'Conventional Commits' and `make commit` (Cement's actual current discipline)"
    - "Single 'A single commit per issue' line in CONTRIBUTING.md is removed (it conflicted with the project's atomic-per-concern Conventional Commits discipline)"
    - "DOCS-04 fix-on-find sweep: `grep -rn -i 'travis|python 3.9' cement/ --include='*.py'` returns 0 docstring hits (RESEARCH.md A5 pre-verified — should already be 0)"
    - "make test, make comply, make docs (with -W from Plan 04), make audit-public-api all exit 0"
  artifacts:
    - path: "README.md"
      provides: "Travis-free README; existing badges/links preserved (only the 2 Travis surfaces removed)"
      contains: "GitHub Actions"
    - path: ".github/CONTRIBUTING.md"
      provides: "Conventional Commits + atomic-per-concern guidance; back-link to CLAUDE.md Commit Conventions"
      contains: "Conventional Commits"
    - path: "CHANGELOG.md"
      provides: "2 new Misc entries (1 [docs] for README, 1 [docs] for CONTRIBUTING)"
      contains: "[docs] Drop Travis CI link/badge (CI moved to GitHub Actions)"
  key_links:
    - from: ".github/CONTRIBUTING.md Commit Conventions section"
      to: "CLAUDE.md Commit Conventions"
      via: "back-relative link `../CLAUDE.md`"
      pattern: "\\.\\./CLAUDE\\.md"
    - from: ".github/CONTRIBUTING.md"
      to: "https://www.conventionalcommits.org/"
      via: "reference-link block at file bottom"
      pattern: "conventionalcommits\\.org"
---

<objective>
Drop the two Travis CI references from `README.md` (the badge at line 5
and the More Information list-item link at line 60) and replace the
"single commit per issue" guidance in `.github/CONTRIBUTING.md` with
Cement's actual current discipline (atomic-per-concern Conventional
Commits + 78-char wrap + `make commit`). Also run the DOCS-04 fix-on-find
docstring sweep (RESEARCH.md A5 pre-verified to return 0 hits — this is
a hold-the-line check, not a body-of-work step).

Purpose: Close DOCS-02 (README + CONTRIBUTING walkthroughs accurate
against the 3.0.16 release) and the bulk of DOCS-04 (fix-on-find sweep;
the load-bearing docstring tightening already landed in Plan 01).

Output: 2 atomic commits (D-16 steps 10, 11) + 2 CHANGELOG entries to
the active Misc bucket. Plus an inert "verify zero hits" check for
DOCS-04 fix-on-find — produces zero new commits if RESEARCH.md A5's
pre-verification holds (which it should as of 2026-05-07).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md
@.planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md
@.planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-04-SUMMARY.md
@CLAUDE.md
@CHANGELOG.md
@README.md
@.github/CONTRIBUTING.md

<interfaces>
<!-- Travis surfaces in README (verified live; 2 hits). -->

README.md:5  (badge):
  [![Continuous Integration Status](https://app.travis-ci.com/datafolklabs/cement.svg?branch=master)](https://app.travis-ci.com/github/datafolklabs/cement/)

README.md:60 (list-item link in More Information section):
  - [Travis CI](https://travis-ci.org/datafolklabs/cement/)

D-10 strict scope:
  - No walkthrough verification (cli-smoke-test CI job already covers).
  - No Cement 4 forward-signaling (PROJECT.md "Out of Scope" already
    says Cement 4 is future work).
  - Two atomic commits, one per file.

CONTRIBUTING.md replacement section: full text per RESEARCH.md
Example 9, with reference-link [Conventional Commits] target appended
at the bottom of the existing reference-link block.

DOCS-04 fix-on-find grep:
  grep -rn -i 'travis\|python 3\.9\|setup\.py\b' cement/ --include='*.py'
  Pre-verified 2026-05-07: 0 hits.
  RESEARCH.md A5: should remain 0; if non-zero at executor time,
  surface each hit as a docstring-edit follow-up (D-16 step 12).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop Travis CI link and badge from README.md (commit 10 of D-16)</name>
  <files>README.md, CHANGELOG.md</files>
  <read_first>
    - README.md lines 1-65 (covers the badge block at lines 1-10, the More Information list at lines 55-65, and confirms no other Travis references exist)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-10 (targeted edits only; no walkthrough verification; no Cement 4 forward-signaling)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "README.md (drop Travis CI surfaces)" section
  </read_first>
  <action>
Delete the two Travis surfaces in `README.md`:

**Surface 1 — line 5 (the badge):**

EXACT BEFORE (line 5, full content):

    [![Continuous Integration Status](https://app.travis-ci.com/datafolklabs/cement.svg?branch=master)](https://app.travis-ci.com/github/datafolklabs/cement/)

EXACT AFTER: line removed entirely. The surrounding lines (3, 4, 6)
are preserved as-is. The badge block shrinks from N badges to N-1.

**Surface 2 — line 60 (the list-item link):**

EXACT BEFORE (line 60):

    - [Travis CI](https://travis-ci.org/datafolklabs/cement/)

EXACT AFTER: line removed entirely. The surrounding More Information
list items (lines 57, 58, 59, 61) are preserved.

No other edits to README.md per D-10 strict scope: no walkthrough
verification, no Cement 4 forward-signaling.

Append CHANGELOG entry to the Misc bucket of active 3.0.15 -
DEVELOPMENT section:

    - `[docs]` Drop Travis CI link/badge (CI moved to GitHub Actions)

Run gates BEFORE committing:

    make comply && make test && make audit-public-api

Commit subject: `docs(readme): drop Travis CI link (moved to GitHub
Actions)` (60 chars).

Suggested body:

    Travis CI was retired in Phase 1 (.travis.yml deleted, CI moved
    to GitHub Actions). The README still carried two stale references:
    the build-status badge at line 5 (app.travis-ci.com) and a More
    Information list-item link at line 60 (travis-ci.org).

    Both deleted. The remaining badges and link list are unchanged.

    DOCS-02 acceptance: README is accurate against the 3.0.16 release.
  </action>
  <verify>
    <automated>! grep -E 'travis-ci' README.md &amp;&amp; ! grep -E 'app\.travis-ci\.com' README.md &amp;&amp; make comply &amp;&amp; make test &amp;&amp; make audit-public-api &amp;&amp; grep -F '[docs] Drop Travis CI link/badge (CI moved to GitHub Actions)' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'travis-ci' README.md` returns 0 hits
    - `grep -E 'app\.travis-ci\.com' README.md` returns 0 hits (covers the `app.travis-ci.com` variant in the badge URL)
    - `grep -E 'travis-ci\.org' README.md` returns 0 hits (covers the legacy `travis-ci.org` variant in the list-item link)
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0
    - `make docs` exits 0 (Plan 04's `-W` gate stays clean — README.md is not in `docs/source/` so this is just a smoke test)
    - CHANGELOG Misc bucket contains literal `- \`[docs]\` Drop Travis CI link/badge (CI moved to GitHub Actions)`
    - Commit subject ≤78 chars; touches exactly 2 files (README.md + CHANGELOG.md)
    - The `## More Information` section in README.md still has all non-Travis links intact (verify with `grep -E '## More Information' README.md` matches AND the surrounding list-item count is 4 instead of the original 5)
  </acceptance_criteria>
  <done>
    README.md no longer contains any Travis surfaces; surrounding badges
    and list items preserved; gates clean; CHANGELOG appended; commit
    landed under D-16 step 10.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align CONTRIBUTING.md with Conventional Commits (commit 11 of D-16)</name>
  <files>.github/CONTRIBUTING.md, CHANGELOG.md</files>
  <read_first>
    - .github/CONTRIBUTING.md (whole file, ~86 lines; especially lines 33-80 with the existing Guidelines for Code Contributions section)
    - CLAUDE.md "Commit Conventions" section (the canonical commit-shape doc the replacement section anchors on)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-10
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 9 (full replacement section text)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md ".github/CONTRIBUTING.md (Conventional Commits alignment)" section
  </read_first>
  <action>
Replace the Guidelines for Code Contributions section (lines 33-80
in `.github/CONTRIBUTING.md`) with the text below. Critically: the
"A single commit per issue" line MUST be removed — it conflicts with
the project's atomic-per-concern Conventional Commits discipline that
Phase 1/2/3 established.

EXACT REPLACEMENT for the Guidelines for Code Contributions section
(per RESEARCH.md Example 9 — preserves the existing reference-link
block at the bottom of the file):

    ## Guidelines for Code Contributions

    All contributors should attempt to abide by the following:

    - Contributors fork the project on GitHub onto their own account
    - All changes should be committed and pushed to their repository
    - All pull requests are from a topic branch, not an existing Cement
      branch
    - Contributors make every effort to comply with [PEP8]
    - Before starting on a new feature or bug fix, do the following:
        - `git pull --rebase` to get latest changes from upstream
        - Checkout a new branch. For example:
            - `git checkout -b feature/slug-name`
            - `git checkout -b bug/github-issue-number`
    - Code must include the following:
        - All tests pass successfully (`make test`)
        - Coverage reports 100% code coverage (`make test`)
        - Compliance passes (`make comply` — runs `ruff` and `mypy`)
        - New features are documented in the appropriate API docstring
        - User-visible changes are recorded in `CHANGELOG.md` under the
          active development section, in one of the standard buckets
          (`Bugs`, `Features`, `Refactoring`, `Misc`, `Deprecations`)
    - All contributions should be associated with at least one issue in
      GitHub. If the issue does not exist, create one (per the
      guidelines above).
    - Contributors should add their full name or handle to the
      `CONTRIBUTORS` file.

    ### Commit Conventions

    Cement follows [Conventional Commits] for all commit messages.
    Commits are atomic per concern — one logical change per commit, not
    "a single commit per issue" (which often lumps unrelated edits).

    - **Subject line:** `<type>(<area>): <imperative summary>`
      (max 78 chars)
    - **Type:** `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `ci`
    - **Body:** wrap at 78 chars; explain the *why*, not just the *what*
    - **Authoring:** Run `make commit` to use the project's
      `commitizen` (`cz`) interactive prompt. It enforces the format and
      the wrap.

    See [`CLAUDE.md`](../CLAUDE.md) §"Commit Conventions" for the
    canonical commit-shape doc.

Critical preservation rules:
- The reference-link block at the bottom of the file (containing
  `[Open Source Initiative]`, `[issue tracker]`, `[PEP8]`,
  `[Commit Guidelines]`) MUST be preserved.
- ADD `[Conventional Commits]: https://www.conventionalcommits.org/`
  as a sibling reference-link target appended to the existing block.
- Sections BEFORE line 33 (the Welcome / Issue Reporting sections)
  stay unchanged.

Do NOT remove the existing `[Commit Guidelines]` reference link if it
exists — old references to it elsewhere in the doc may still resolve
through it.

Append CHANGELOG entry to Misc bucket:

    - `[docs]` Align CONTRIBUTING with Conventional Commits + atomic-per-concern

(78-char wrap if needed; the line above is 79 chars — wrap manually:

    - `[docs]` Align CONTRIBUTING with Conventional Commits +
      atomic-per-concern

)

Run gates BEFORE committing:

    make comply && make test && make audit-public-api

Commit subject: `docs(contributing): align with Conventional Commits +
atomic-per-concern` — this is 75 chars (≤78 OK).

Suggested body:

    Replace "A single commit per issue" with the project's actual
    current discipline: atomic per concern + Conventional Commits +
    78-char wrap + `make commit` (cz). Add a Commit Conventions
    subsection with subject/body/type/authoring rules and a back-link
    to CLAUDE.md as the canonical commit-shape doc.

    The "A single commit per issue" guidance was the inverse of how
    Phases 1, 2, and 3 actually shipped (84 atomic commits in Phase 3
    alone) and would have given external contributors the wrong
    forward-looking signal.

    Existing reference-link block (PEP8, issue tracker, etc.) is
    preserved; [Conventional Commits] target appended to the block.

    DOCS-02 acceptance: CONTRIBUTING aligns with the 3.0.16 release
    discipline.
  </action>
  <verify>
    <automated>grep -cE 'Conventional Commits' .github/CONTRIBUTING.md | grep -qE '^[2-9]$|^[1-9][0-9]+$' &amp;&amp; grep -c 'make commit' .github/CONTRIBUTING.md | grep -qE '^[1-9][0-9]*$' &amp;&amp; ! grep -F 'A single commit per issue' .github/CONTRIBUTING.md &amp;&amp; grep -F '../CLAUDE.md' .github/CONTRIBUTING.md &amp;&amp; grep -F 'conventionalcommits.org' .github/CONTRIBUTING.md &amp;&amp; make comply &amp;&amp; make test &amp;&amp; make audit-public-api &amp;&amp; grep -E '\[docs\] Align CONTRIBUTING with Conventional Commits' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'Conventional Commits' .github/CONTRIBUTING.md` returns ≥2 (the prose reference + the reference-link target)
    - `grep -c 'make commit' .github/CONTRIBUTING.md` returns ≥1 (the Authoring bullet)
    - `grep -F 'A single commit per issue' .github/CONTRIBUTING.md` returns 0 hits (the conflicting line is removed)
    - `grep -F '../CLAUDE.md' .github/CONTRIBUTING.md` matches at least once (back-link to CLAUDE.md)
    - `grep -F 'conventionalcommits.org' .github/CONTRIBUTING.md` matches at least once (the reference-link target appended to the bottom block)
    - `grep -F '[PEP8]' .github/CONTRIBUTING.md` still matches (existing reference link preserved)
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0
    - CHANGELOG Misc bucket contains the literal `[docs] Align CONTRIBUTING with Conventional Commits + atomic-per-concern` (allowing for 78-char wrap)
    - Commit subject ≤78 chars
    - Commit touches exactly 2 files: `.github/CONTRIBUTING.md` + `CHANGELOG.md`
  </acceptance_criteria>
  <done>
    `.github/CONTRIBUTING.md` reflects atomic-per-concern Conventional
    Commits discipline; `make commit` named; CLAUDE.md back-linked;
    reference-link block preserved with [Conventional Commits] appended;
    "A single commit per issue" line removed; gates clean; CHANGELOG
    appended; commit landed under D-16 step 11.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DOCS-04 fix-on-find docstring sweep verification (commit 12 of D-16 — expected 0 commits)</name>
  <files>(none expected — verification step; if hits surface, executor authors per-file fix commits)</files>
  <read_first>
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-11
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 6 + Assumption A5 (pre-verified 0 hits as of 2026-05-07)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md (no per-file analog — this is a hold-the-line check)
  </read_first>
  <action>
Run the DOCS-04 fix-on-find grep against `cement/`:

    grep -rn -i 'travis\|python 3\.9\|setup\.py\b' cement/ --include='*.py'

Per RESEARCH.md A5, this returns 0 hits as of 2026-05-07. Re-run at
executor time to confirm hold-the-line.

**If 0 hits (expected):** No commits land for D-16 step 12. Record the
grep output (empty) in 05-05-SUMMARY.md as evidence. CHANGELOG: NO
ENTRY (no source change to record).

**If non-zero hits (regression case — RESEARCH.md A5 stale or new
commit introduced a stale reference between research and execution):**
For each hit, edit the docstring to remove the stale reference. One
commit per file affected:

    docs(<area>): drop stale <thing> reference

CHANGELOG entry per fix in Misc bucket:

    - `[<area>]` Drop stale <thing> reference in <file>

Run gates between each commit:

    make comply && make test && make audit-public-api && make docs

Note: `make docs` is mandatory after each docstring edit because
Plan 04's `-W` gate is now active — any unintended new sphinx warning
breaks the build.

Scope guard (per RESEARCH.md Open Question 3): the grep is locked to
`cement/`. Do NOT extend to `tests/`, `scripts/`, or other non-shipping
trees. If the user wants broader scope, surface as a follow-up.

Acceptance is the same regardless of hit count: the grep returns 0
hits when the task completes.
  </action>
  <verify>
    <automated>grep -rn -i 'travis\|python 3\.9\|setup\.py' cement/ --include='*.py' | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn -i 'travis\|python 3\.9\|setup\.py' cement/ --include='*.py' | wc -l` returns exactly `0`
    - `make comply` exits 0; `make test` exits 0 at 100% coverage; `make audit-public-api` exits 0; `make docs` exits 0
    - **If 0 hits at start (expected per RESEARCH.md A5):** no new commits, no CHANGELOG entries — task is a hold-the-line confirmation only.
    - **If hits surface:** each hit gets its own commit with subject `docs(<area>): drop stale <thing> reference` (≤78 chars) + CHANGELOG entry in Misc bucket; `make docs` exit 0 after each commit (Plan 04's `-W` gate); per-commit grep narrows toward 0.
    - 05-05-SUMMARY.md records the executor-time grep output verbatim (empty or otherwise) as DOCS-04 fix-on-find evidence.
  </acceptance_criteria>
  <done>
    `grep -rn -i 'travis\|python 3.9\|setup.py' cement/ --include='*.py'`
    returns 0 hits; gates clean; if 0 commits landed (expected) recorded
    in summary as RESEARCH.md A5 hold-the-line; if N commits landed,
    each is atomic per file with a CHANGELOG entry in Misc bucket.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public-facing GitHub render | README is the project's public face on GitHub. Stale Travis links suggest the project is unmaintained or split-CI; removing them clarifies the actual CI surface (GitHub Actions, in-repo `.github/workflows/`). |
| CONTRIBUTING.md → external contributor PRs | External contributors read this doc before opening PRs. The "A single commit per issue" guidance conflicts with the actual project discipline; replacing it preserves the spirit (one logical change per commit) with Conventional Commits vocabulary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-noop-10 | I (Information disclosure / wrong-info) | README.md Travis surfaces (lines 5, 60) | mitigate | Both surfaces deleted; acceptance grep enforces zero `travis-ci` references. |
| T-05-noop-11 | I (Information disclosure / wrong-info) | CONTRIBUTING.md "A single commit per issue" guidance | mitigate | Replaced with Conventional Commits + atomic-per-concern guidance per RESEARCH.md Example 9. The `[Commit Guidelines]` reference link kept (legacy callers preserve). |
| T-05-noop-12 | T (Tampering) | DOCS-04 fix-on-find sweep — possible new stale reference between research (2026-05-07) and execution | mitigate | Task action requires re-running the grep at executor time; if non-zero hits surface, each fix is an atomic per-file commit gated by `make docs` exit 0 (Plan 04's `-W` flag catches any new sphinx warning). |

**ASVS L1 applicability:** V5 (Input Validation) does not apply. V14
(Configuration) partial — CONTRIBUTING.md governs commit-shape
discipline; the change preserves the spirit while adopting current
vocabulary. Block on: high (T-05-noop-12 — if a fresh stale reference
slipped in between research and execution, the `-W` gate catches any
new docstring warning and blocks the build).
</threat_model>

<verification>
After commits 10 and 11 (and possibly 12+ if Task 3 finds hits):

    # DOCS-02 (a) — README is Travis-free
    ! grep -E 'travis-ci' README.md

    # DOCS-02 (b) — CONTRIBUTING references Conventional Commits + make commit
    grep -cE 'Conventional Commits' .github/CONTRIBUTING.md          # ≥ 2
    grep -c 'make commit' .github/CONTRIBUTING.md                    # ≥ 1
    ! grep -F 'A single commit per issue' .github/CONTRIBUTING.md

    # DOCS-04 fix-on-find — 0 hits in cement/
    grep -rn -i 'travis\|python 3\.9\|setup\.py' cement/ --include='*.py' | wc -l   # → 0

    # Cumulative gates
    make test && make comply && make audit-public-api && make docs

    # CHANGELOG — 2 new Misc entries (3+ if Task 3 finds hits)
    git diff <plan-04-summary-head>..HEAD -- CHANGELOG.md | grep -E '^\+- \`\[' | wc -l   # ≥ 2
</verification>

<success_criteria>
- [ ] README.md is Travis-free (`grep -E 'travis-ci' README.md` → 0)
- [ ] `.github/CONTRIBUTING.md` references Conventional Commits + `make commit` + back-link to CLAUDE.md
- [ ] CONTRIBUTING.md no longer says "A single commit per issue"
- [ ] DOCS-04 fix-on-find grep returns 0 hits in `cement/`
- [ ] `make test` exits 0 at 100% coverage; `make comply` exits 0; `make audit-public-api` exits 0; `make docs` exits 0 (with Plan 04's `-W` flag)
- [ ] CHANGELOG Misc bucket has 2 new `[docs]` entries (3+ if DOCS-04 fix-on-find finds hits — expected 2)
- [ ] All commits use Conventional Commits subjects ≤78 chars
- [ ] Each commit touches exactly 2 files (the source file + CHANGELOG.md)
</success_criteria>

<output>
After completion, create
`.planning/phases/05-deprecations-docs-security-stubs/05-05-SUMMARY.md`
recording: 2-3 commit SHAs, the Travis grep output (empty), the
CONTRIBUTING grep output (≥2 Conventional Commits hits, ≥1 make commit
hit), the DOCS-04 fix-on-find grep output verbatim (expected: empty),
and confirmation that `make docs` (with Plan 04's `-W` flag) still
exits 0.
</output>
