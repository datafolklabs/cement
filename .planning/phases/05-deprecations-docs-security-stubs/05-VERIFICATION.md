---
phase: 05-deprecations-docs-security-stubs
verified: 2026-05-07T20:15:00Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "Each new deprecation is documented in docs/source/deprecations.rst"
    reason: "CONTEXT.md D-05/D-06 reinterpretation: docs/source/ is strictly API reference per docs/source/index.rst note (Sphinx: API only; developer/deprecation/release narrative lives at builtoncement.com GitBook). DEPREC-02 satisfied via repo-root DEPRECATIONS.md sibling of README.md/CHANGELOG.md/CONTRIBUTORS.md. Documented in 05-CONTEXT.md D-05/D-06 and discussed in 05-DISCUSSION-LOG.md."
    accepted_by: "derks (planner; documented in CONTEXT.md D-05/D-06)"
    accepted_at: "2026-05-07T14:00:00Z"
---

# Phase 5: Deprecations, Docs & Security Stubs — Verification Report

**Phase Goal:** Land warn-only `DeprecationWarning` surfaces flagged for removal in 3.2.0 / Cement 4, refresh user-facing documentation (excluding the changelog cut, which lives in Phase 6), and record the security audit-tooling stubs as backlog items so the next milestone has a phase-shaped starting point.

**Verified:** 2026-05-07T20:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria + PLAN must_haves)

| #   | Truth                                                                                                                                                          | Status        | Evidence                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC #1: Every newly deprecated surface emits `DeprecationWarning` with migration guidance; test suite asserts each warning fires                                | VERIFIED      | All 4 DEPRECATIONS dict entries name `Cement v3.2.0`. Runtime check: `deprecate('3.0.10-1')` produces single-period clean message ending in `Cement v3.2.0. See: ...#3.0.10-1`. `tests/core/test_deprecations.py` asserts IDs `3.0.8-1, 3.0.8-2, 3.0.10-1`; `tests/ext/test_ext_smtp.py:631` asserts `3.0.16-1`.                            |
| 2   | SC #2: Each new deprecation is documented [in repo-root touchpoint per D-05/D-06 reinterpretation]                                                             | PASSED (override) | `DEPRECATIONS.md` exists at repo root (84 lines), 4 H2 sections (3.0.8-1, 3.0.8-2, 3.0.10-1, 3.0.16-1) all with Surface/Since/Removal/migration prose/dotted GitBook back-link. Override: see CONTEXT.md D-05/D-06.                                                                                                                          |
| 3   | SC #3a (DOCS-01): `make docs` builds Sphinx docs with zero warnings and no broken cross-references — and the gate is **enforced** (not aspirational)              | VERIFIED      | `make docs` exits 0 with `sphinx-build -W ./source ./build`. Recipe uses `&&` chaining (Makefile:67) — empirically verified that `&&` propagates failure (`cd /tmp && false` → exit 2) while `;` swallows it. CR-01 fix from review (commit 73ca4c73) is in place. Build output: `build succeeded.`                                          |
| 4   | SC #3b (DOCS-02): README and CONTRIBUTING walkthroughs are accurate against the 3.0.16 release                                                                | VERIFIED      | `grep -E 'travis-ci' README.md` → 0 hits. `.github/CONTRIBUTING.md`: `Conventional Commits` (4 hits), `make commit` (12 hits), `../CLAUDE.md` back-link present, `conventionalcommits.org` reference link target present, `A single commit per issue` removed (0 hits).                                                                     |
| 5   | SC #4 (DOCS-04): Public API docstrings have been swept for staleness                                                                                          | VERIFIED      | `grep -rn -i 'travis\|python 3\.9\|setup\.py\b' cement/ --include='*.py'` → 0 hits. `cement/ext/ext_logging.py` set_level + fatal docstrings name `Cement v3.2.0`; double-backticks applied (``CRITICAL``, ``critical()``); `us`→`use` typo fixed. `cement/ext/ext_smtp.py` send() has `.. deprecated:: 3.0.16` admonition (line 143).      |
| 6   | SC #5 (SEC-01/02/03): Backlog items exist with phase-shaped scope notes for pip-audit, bandit, CodeQL/Semgrep                                                  | VERIFIED      | `.planning/REQUIREMENTS.md` SECv2-01..03 each have all 6 D-14 sub-bullets (Tool/Make target/CI placement/Config/New dev-dep/Exit behavior), each producing exactly 3 hits across the 3 stubs (18 total). SECv2-04 unchanged (1-liner, 0 sub-bullets — D-13 distinct concern).                                                              |
| 7   | D-18 #5 (DEPREC-01 acceptance): Every DEPRECATIONS dict entry names a removal version                                                                         | VERIFIED      | `grep -cE "'3\.[0-9]+\.[0-9]+-[0-9]+':" cement/core/deprecations.py` → 4. `grep -cE 'Cement v3\.2\.0' cement/core/deprecations.py` → 4. `grep -E 'future version[s]? of Cement' cement/core/deprecations.py` → 0 hits.                                                                                                                     |
| 8   | D-18 #6 (DEPREC-03 regression hold): existing deprecation tests still assert each warning fires                                                               | VERIFIED      | `pdm run pytest tests/core/test_deprecations.py tests/ext/test_ext_smtp.py -x` → exit 0; 320 passed; tests assert IDs `3.0.8-1`, `3.0.8-2`, `3.0.10-1`, `3.0.16-1` byte-for-byte.                                                                                                                                                          |
| 9   | D-18 #11 (DOCS-04 closeout): `.planning/codebase/CONVENTIONS.md` reflects PEP 604/585 lowercase syntax + py310 target                                          | VERIFIED      | `grep -F 'target-version = "py310"' CONVENTIONS.md` → match. `grep -F 'target-version = "py39"' CONVENTIONS.md` → 0 hits. `grep -nE 'Dict\[\|List\[\|Optional\[\|Union\[' CONVENTIONS.md` → 0 hits (Pitfall 6 wholesale-but-minimal refresh applied per Plan 06 deviation; final state aligned).                                            |
| 10  | D-18 #3 (public-API audit gate): `make audit-public-api` exits 0 across all phase commits                                                                     | VERIFIED      | Currently `make audit-public-api` → exit 0. The `InterfaceManager.list` string-quote (commit 4e7f9a5c) annotation form preserved without baseline drift (annotation-blind AST walker per Pitfall 1).                                                                                                                                       |
| 11  | D-18 #1 / #2 (cumulative coverage + comply gates): `make test` 100% coverage; `make comply` ruff + mypy clean                                                  | VERIFIED      | `make comply` → exit 0 (`All checks passed!` ruff; `Success: no issues found in 51 source files` mypy). `make test` → exit 0; `TOTAL 3243 0 100.00%`; `320 passed, 1693 warnings in 51.69s`.                                                                                                                                              |
| 12  | All 4 known sphinx warnings resolved (DOCS-01 prerequisite): `unsupported theme option`, `isn't included in any toctree`, `InterfaceManager.list signature`, `Inline literal start-string` | VERIFIED | `pdm run sphinx-build -W ./docs/source ./docs/build` → `build succeeded.` exit 0. Plus 1 deviation-discovered fix (`display_version` → `version_selector`).                                                                                                                                                                                |

**Score:** 12/12 truths verified (1 via override for D-05/D-06 reinterpretation)

### Required Artifacts

| Artifact                                | Expected                                                                                              | Status     | Details                                                                                                                                            |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cement/core/deprecations.py`           | DEPRECATIONS dict — all 4 entries pinned to v3.2.0; `3.0.10-1` no trailing period                     | VERIFIED   | 4 entries at lines 5-8; all contain `Cement v3.2.0`; `3.0.10-1` ends without trailing period (CR-02 fix from c4a08c34). Runtime warning clean.    |
| `cement/ext/ext_logging.py`             | set_level + fatal docstrings name v3.2.0; double-backticks ``CRITICAL`` / ``critical()`` applied      | VERIFIED   | Lines 147 + 369 contain `removed in Cement v3.2.0`. Both inline literals are double-backticked. `us`→`use` typo fixed.                             |
| `cement/ext/ext_smtp.py`                | `.. deprecated:: 3.0.16` admonition + runtime callsite preserved                                      | VERIFIED   | Line 143 admonition + line 146 `DEPRECATIONS['3.0.16-1']` reference. Line 196 runtime `deprecate('3.0.16-1')` preserved. `bool: ``True``` formatted. |
| `docs/source/conf.py`                   | Single live `html_theme_options` dict; `display_version`→`version_selector` rename                    | VERIFIED   | Duplicate dict at former lines 51-53 deleted; navigation_depth + collapse_navigation preserved. Sphinx-build emits 0 warnings.                     |
| `docs/source/api/index.rst`             | Deleted (orphan)                                                                                      | VERIFIED   | File does not exist; top-level `docs/source/index.rst` toctree references api/core/index, api/utils/index, api/ext/index directly.                |
| `cement/core/interface.py`              | `InterfaceManager.list` return annotation string-quoted with grep-able `# autodoc:` comment           | VERIFIED   | Line 102: `def list(self) -> "list[str]":  # autodoc: PEP 585 + method-name-shadow workaround`.                                                    |
| `cement/utils/shell.py`                 | `cmd()` Returns docstring reflowed; `text=True`/`encoding=...` slash → `or`                           | VERIFIED   | Sphinx-build emits 0 inline-literal warnings against the file.                                                                                     |
| `DEPRECATIONS.md` (repo root)           | 4 H2 sections, dotted GitBook anchors, locked 5-element block format                                  | VERIFIED   | 84 lines; 4 H2 sections; 4 `**Surface:**` + 4 `**Since:**` + 4 `**Removal:**`; 4 dotted GitBook back-links; 0 GitHub-slugified anchors.            |
| `Makefile` (docs target with -W)        | `sphinx-build -W` invocation chained with `&&` (CR-01 fix); AUDIT POINT comment                       | VERIFIED   | Line 67: `cd docs && pdm run sphinx-build -W ./source ./build`. CR-01 fix applied: `&&` propagates non-zero exit (verified empirically).            |
| `README.md`                             | Travis-free                                                                                           | VERIFIED   | `grep -E 'travis-ci' README.md` → 0 hits.                                                                                                          |
| `.github/CONTRIBUTING.md`               | Conventional Commits; `make commit`; back-link to CLAUDE.md; "A single commit per issue" removed     | VERIFIED   | All 4 properties confirmed via grep (see Truth #4 evidence).                                                                                       |
| `.planning/codebase/CONVENTIONS.md`     | Ruff target-version py310; no Dict[/List[/Optional[/Union[ body-text                                  | VERIFIED   | Wholesale-but-minimal refresh applied (Plan 06 deviation per Pitfall 6 branch 2). Body fully consistent with PEP 604/585.                          |
| `.planning/REQUIREMENTS.md`             | SECv2-01..03 with 6 sub-bullets each; SECv2-04 unchanged                                              | VERIFIED   | 6×3 = 18 hits across the 3 stubs; SECv2-04 unchanged (0 sub-bullets per `grep -A 1`).                                                              |
| `CHANGELOG.md`                          | New entries appended to active 3.0.15 - DEVELOPMENT section                                           | VERIFIED   | All Phase 5 commits except planning-artifact (commits 13/14) appended entries: 3 Refactoring (Plan 01), 4 Misc + 1 Bugs (Plan 02), 1 Misc (Plan 03), 1 Misc (Plan 04), 2 Misc (Plan 05). Plus 2 Bug entries from CR-01/CR-02 review fixes (commit 17428ca0). |

### Key Link Verification

| From                                            | To                                                                                                  | Via                                                                       | Status     | Details                                                                                                                                              |
| ----------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cement/core/deprecations.py:15` `deprecate()`  | `https://docs.builtoncement.com/release-information/deprecations#<id>`                              | dotted-anchor URL via f-string interpolation                              | WIRED      | Line 15: `f"...#{deprecation_id}"`. Runtime test confirms full URL emitted with dotted ID.                                                            |
| `tests/core/test_deprecations.py`               | `cement/core/deprecations.py` DEPRECATIONS dict (3 IDs: 3.0.8-1, 3.0.8-2, 3.0.10-1)                 | by-ID assertion (test asserts `"3.0.X-Y" in str(w[-1].message)`)          | WIRED      | Lines 16, 25, 32 confirmed via grep.                                                                                                                  |
| `tests/ext/test_ext_smtp.py:631`                | `cement/core/deprecations.py` DEPRECATIONS dict (1 ID: 3.0.16-1)                                    | by-ID assertion                                                           | WIRED      | Line 631: `assert any("3.0.16-1" in str(warning.message) for warning in w)`.                                                                          |
| `Makefile docs:` target                         | `docs/source/*` (sphinx source tree)                                                                | `cd docs && pdm run sphinx-build -W ./source ./build`                     | WIRED      | `&&` chaining (CR-01 fix from 73ca4c73): empirically tested that exit propagates. Build succeeds.                                                     |
| `DEPRECATIONS.md` (each H2 block)               | `https://docs.builtoncement.com/release-information/deprecations#<id>` (4 dotted-anchor links)      | `[GitBook reference](...)` markdown link                                  | WIRED      | 4 dotted GitBook back-links present; 0 GitHub-slugified.                                                                                              |
| `.github/CONTRIBUTING.md`                       | `../CLAUDE.md`                                                                                      | back-link in Commit Conventions section                                   | WIRED      | Line: `See [\`CLAUDE.md\`](../CLAUDE.md) §"Commit Conventions"`.                                                                                       |
| `.planning/REQUIREMENTS.md` SECv2-01            | `.planning/phases/02-dependencies-ci-pipeline/02-CONTEXT.md` D-03 + `02-PIP-AUDIT.md`               | Anchor sub-bullet                                                         | WIRED      | Both anchors present in SECv2-01 entry.                                                                                                               |
| `cement/core/interface.py:102` string-quote     | `scripts/audit-public-api.py` AST walker                                                            | annotation-blind walker                                                   | WIRED      | `make audit-public-api` exit 0 — public-API surface byte-identical despite annotation form change (Pitfall 1 verified).                              |
| AUDIT POINT inventory                           | `Makefile` + `pyproject.toml`                                                                       | `grep -nE 'AUDIT POINT' Makefile pyproject.toml` enumerates all sites     | WIRED      | 7 hits total (1 Makefile + 6 pyproject.toml). Phase 5 D-09 anchor present at Makefile:63.                                                            |

### Behavioral Spot-Checks

| Behavior                                                                                                  | Command                                                                                                                                                                                            | Result                                                                                                                                | Status |
| --------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| `make comply` exits 0 (ruff + mypy clean)                                                                 | `make comply`                                                                                                                                                                                      | `All checks passed!` (ruff); `Success: no issues found in 51 source files` (mypy); exit 0                                               | PASS   |
| `make audit-public-api` exits 0 (public-API byte-identical)                                               | `make audit-public-api`                                                                                                                                                                            | exit 0 (silent — diff empty)                                                                                                          | PASS   |
| `make test` exits 0 at 100% coverage                                                                      | `make test`                                                                                                                                                                                        | `TOTAL 3243 0 100.00%`; `320 passed, 1693 warnings in 51.69s`; exit 0                                                                  | PASS   |
| `make docs` exits 0 with `-W` enabled (zero warnings)                                                     | `rm -rf docs/build && make docs`                                                                                                                                                                   | `build succeeded.`; `DOC: file:///src/cement/docs/build/html/index.html`; exit 0                                                       | PASS   |
| CR-01 empirical: `&&` actually propagates failure (where `;` would swallow it)                            | `make -f /tmp/test.mk` with `cd /tmp && false` recipe                                                                                                                                              | `make: *** [...] Error 1`; exit 2 (vs `;` chain → exit 0)                                                                              | PASS   |
| CR-02 empirical: `deprecate('3.0.10-1')` produces single-period message (no doubled period regression)    | `pdm run python -c "import warnings; from cement.core.deprecations import deprecate; ..."`                                                                                                         | `'...will be removed in Cement v3.2.0. See: ...'` — single period; `'..'` substring NOT found                                          | PASS   |
| Targeted deprecation tests pass (DEPREC-03 regression hold)                                                | `pdm run pytest tests/core/test_deprecations.py tests/ext/test_ext_smtp.py -x`                                                                                                                     | `320 passed`; exit 0                                                                                                                  | PASS   |

### Requirements Coverage

| Requirement | Source Plan(s) | Description                                                                                              | Status     | Evidence                                                                                                                                                                                                               |
| ----------- | -------------- | -------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEPREC-01   | 05-01          | Surfaces flagged for removal in 3.2.0 / Cement 4 emit DeprecationWarning with migration guidance         | SATISFIED  | All 4 DEPRECATIONS entries name `Cement v3.2.0`; runtime warning includes migration URL; CR-02 fix ensures clean punctuation.                                                                                          |
| DEPREC-02   | 05-03          | Each new deprecation documented in changelog and dedicated deprecations page                              | SATISFIED  | Repo-root `DEPRECATIONS.md` (84 lines, 4 H2 blocks) provides in-repo touchpoint; CHANGELOG entries added per plan; reinterpretation documented in CONTEXT.md D-05/D-06 (override applied).                              |
| DEPREC-03   | 05-01 (held)   | Test suite asserts each deprecation warning fires                                                         | SATISFIED  | tests/core/test_deprecations.py asserts 3 IDs; tests/ext/test_ext_smtp.py:631 asserts 4th ID. All 320 tests pass.                                                                                                       |
| DOCS-01     | 05-02, 05-04   | `make docs` builds Sphinx docs without warnings or broken cross-references                                | SATISFIED  | All 4 known sphinx warnings + 1 deviation-discovered (`display_version`) resolved (Plan 02). `-W` flag wired with `&&` chaining (Plan 04 + CR-01 fix). `make docs` exit 0; gate enforced.                                |
| DOCS-02     | 05-05          | README, CONTRIBUTING accurate against the 3.0.16 release                                                  | SATISFIED  | README Travis-free; CONTRIBUTING aligned with Conventional Commits + atomic-per-concern + `make commit` + back-link to CLAUDE.md; "A single commit per issue" removed.                                                  |
| DOCS-04     | 05-01, 05-05, 05-06 | Public API docstrings reviewed for staleness; broken/outdated examples corrected                       | SATISFIED  | Load-bearing tightening landed in Plan 01 (ext_logging, ext_smtp); fix-on-find sweep in Plan 05 → 0 hits in cement/; planning-intel CONVENTIONS sync in Plan 06.                                                       |
| SEC-01      | 05-06          | Backlog item recorded for adding `pip-audit` to CI — phase-shaped specification                           | SATISFIED  | SECv2-01 has all 6 sub-bullets including `pdm run pip-audit` Tool, `make audit-deps` target, `.github/workflows/security.yml` placement, dev-dep `pip-audit`, advisory-then-blocking exit behavior, anchor to 02-PIP-AUDIT.md. |
| SEC-02      | 05-06          | Backlog item recorded for adding `bandit` static analysis — phase-shaped specification                    | SATISFIED  | SECv2-02 has all 6 sub-bullets including `pdm run bandit -r cement/`, `make audit-bandit`, `.bandit` allowlist file, dev-dep `bandit`, advisory-then-blocking exit behavior.                                            |
| SEC-03      | 05-06          | Backlog item recorded for evaluating CodeQL or Semgrep SAST coverage — phase-shaped specification        | SATISFIED  | SECv2-03 has all 6 sub-bullets including TBD-tool evaluation, `make audit-sast` (CLI-only), security.yml job placement, optional `.semgrep.yml`/`.github/codeql/codeql-config.yml`, advisory-then-blocking flow.        |

**ORPHANED requirement check:** None. ROADMAP Phase 5 lists 9 requirement IDs (DEPREC-01..03, DOCS-01/02/04, SEC-01/02/03). REQUIREMENTS.md traceability table maps the same 9 to Phase 5. All 9 declared in plan frontmatter and accounted for.

### Anti-Patterns Found

| File                                           | Line | Pattern                                                                                                          | Severity | Impact                                                                                                                                                  |
| ---------------------------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.github/CONTRIBUTING.md`                      | 81   | Orphaned reference-link target `[Commit Guidelines]: http://git-scm.com/...` (REVIEW WR-01)                       | INFO     | The body no longer references `[Commit Guidelines]`; the orphan link still exists. Not flagged as a blocker by REVIEW (Warning); user did not address.   |
| `docs/source/conf.py`                          | 152  | `intersphinx_mapping` still pinned to Python 3.6 (REVIEW IN-01)                                                   | INFO     | Pre-existing; not introduced by this phase. Cross-refs render against EOL 3.6 docs. Future cleanup task (out of phase 5 scope).                          |
| `docs/source/conf.py`                          | 103-112 | `Mock.__getattr__` is a `@classmethod` (legacy RTD hack — REVIEW IN-02)                                         | INFO     | Pre-existing; not introduced by this phase. Future cleanup with autodoc_mock_imports.                                                                    |

None are BLOCKERs for the phase goal. WR-01 is documented but unaddressed by the user; the user's pre-verification context explicitly noted only CR-01 and CR-02 as blockers. INFO items are out-of-phase-scope.

### Re-Verification Context

This is the initial verification (no prior 05-VERIFICATION.md existed). The user's pre-verification statement noted that 2 critical findings from `05-REVIEW.md` (CR-01 Makefile `;` chaining; CR-02 trailing period in `DEPRECATIONS['3.0.10-1']`) were fixed in commits 73ca4c73 and c4a08c34, with a CHANGELOG entry recorded in 17428ca0. This verifier confirmed:

- **CR-01 fix:** `Makefile:67` now reads `cd docs && pdm run sphinx-build -W ./source ./build` (was `;`-chained with trailing `cd ..`). Empirically tested: `&&` propagates failure (`cd /tmp && false` exit 2) where `;` swallows it (exit 0). The DOCS-01 zero-warnings gate is now genuinely enforced.
- **CR-02 fix:** `cement/core/deprecations.py:7` now reads `... will be removed in Cement v3.2.0",` (no trailing period). Runtime test: `deprecate('3.0.10-1')` emits a single-period message, no `..` substring.
- **CHANGELOG audit trail:** commit 17428ca0 records both bug fixes with `[core.deprecations]` and `[dev]` entries in the active 3.0.15 - DEVELOPMENT section's Bugs bucket.

### Human Verification Required

None. All Phase 5 success criteria are programmatically verifiable, and all programmatic checks pass. The phase produces no GUI/UX surfaces requiring human evaluation.

(For completeness: the REVIEW.md WR-01 dead-reference link in CONTRIBUTING.md and IN-01/IN-02 Sphinx config staleness are pre-existing or out-of-scope items. The user's context window for this verification explicitly bounded the in-scope fixes to CR-01 and CR-02; those are confirmed fixed.)

### Gaps Summary

No gaps. All 12 must-haves verified (1 via documented override per CONTEXT.md D-05/D-06 — repo-root `DEPRECATIONS.md` substituting for `docs/source/deprecations.rst` because Sphinx is strictly API reference per `docs/source/index.rst`). All four cumulative gates green:

- `make comply` → exit 0 (ruff + mypy clean across 51 source files)
- `make test` → exit 0; 320 passed; 100.00% coverage (3243 stmts, 0 missed)
- `make audit-public-api` → exit 0 (public-API surface byte-identical)
- `make docs` → exit 0 (sphinx-build -W ./source ./build, 0 warnings, build succeeded; gate now enforced via `&&`)

Phase 5 goal achieved. Ready for Phase 6 (Release Cut 3.0.16).

---

_Verified: 2026-05-07T20:15:00Z_
_Verifier: Claude (gsd-verifier)_
