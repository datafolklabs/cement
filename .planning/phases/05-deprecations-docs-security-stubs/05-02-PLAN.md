---
phase: 05-deprecations-docs-security-stubs
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/source/conf.py
  - docs/source/api/index.rst
  - cement/core/interface.py
  - cement/utils/shell.py
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-01]
requirements_addressed: [DOCS-01]

must_haves:
  truths:
    - "All 4 known sphinx warnings are resolved (`pdm run sphinx-build ./docs/source ./docs/build` produces 0 warnings)"
    - "InterfaceManager.list return annotation is string-quoted (defers evaluation past the method-name shadow)"
    - "html_theme_options retains all existing toc-config keys (collapse_navigation, navigation_depth, etc.) — the duplicate dict is gone, NOT the active dict"
    - "make audit-public-api exits 0 — public-API surface is byte-identical (annotation form is not part of audit output per RESEARCH.md Pitfall 1)"
    - "make comply exits 0 (ruff + mypy clean) on every commit"
  artifacts:
    - path: "docs/source/conf.py"
      provides: "Single html_theme_options dict (lines 19-33); the duplicate (formerly lines 51-53) is deleted"
      contains: "navigation_depth"
    - path: "cement/core/interface.py"
      provides: "InterfaceManager.list with string-quoted return annotation + grep-able autodoc comment"
      contains: '"list[str]"'
    - path: "cement/utils/shell.py"
      provides: "cmd() docstring with parser-clean inline-literal separator (`or` instead of `/`)"
      contains: "``text=True`` or"
    - path: "CHANGELOG.md"
      provides: "4 new entries appended (3 Misc, 1 Bugs)"
      contains: "[core.interface] String-quote list[str] return annotation"
  key_links:
    - from: "docs/source/index.rst toctree"
      to: "api/core/index, api/utils/index, api/ext/index"
      via: "single top-level toctree (api/index.rst orphan deleted per Pitfall 4 option (a))"
      pattern: "api/core/index"
    - from: "cement/core/interface.py:102 string-quote"
      to: "scripts/audit-public-api.py AST walker"
      via: "annotation-blind walker (verified Pitfall 1)"
      pattern: "audit-public-api"
---

<objective>
Resolve all 4 known Sphinx warnings (D-08) so that `pdm run sphinx-build
./docs/source ./docs/build` produces zero warnings, preparing the way
for Plan 04 to flip `make docs` to use the `-W` flag (D-09 / commit 9
of D-16).

Purpose: Close DOCS-01 acceptance criterion #1 (`make docs` builds
without warnings or broken cross-references). Each fix is independent;
all four land in this plan as 4 atomic commits per Phase 3 D-22 shape.

Output: 4 atomic commits (D-16 steps 5, 6, 7, 8) + 4 CHANGELOG entries
(3 in `Misc:`, 1 in `Bugs:`) appended to the active 3.0.15 - DEVELOPMENT
section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md
@.planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md
@.planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md
@.planning/phases/05-deprecations-docs-security-stubs/05-01-SUMMARY.md
@CLAUDE.md
@CHANGELOG.md
@docs/source/conf.py
@docs/source/index.rst
@docs/source/api/index.rst
@cement/core/interface.py
@cement/utils/shell.py
@scripts/audit-public-api.py

<interfaces>
<!-- Locked surfaces and verified pre-conditions per RESEARCH.md. -->

The 4 sphinx warnings (verified verbatim against sphinx 8.1.3 +
sphinx_rtd_theme 3.1.0 in RESEARCH.md):
  1. WARNING: Failed to get a method signature for
     cement.core.interface.InterfaceManager.list:
     'function' object is not subscriptable
     → cause: PEP 585 generic on a method named `list` shadows builtin
     → fix: string-quote the return annotation (D-08 #3)
  2. cement/utils/shell.py:docstring of cement.utils.shell.cmd:19:
     WARNING: Inline literal start-string without end-string. [docutils]
     → cause: `/` between two adjacent inline literals confuses parser
     → fix: replace `/` with `or` (D-08 #4)
  3. checking consistency... .../docs/source/api/index.rst:
     WARNING: document isn't included in any toctree
     → cause: orphan duplicate file with own toctree
     → fix: delete docs/source/api/index.rst entirely (option (a) per
       Pitfall 4 — top-level docs/source/index.rst already references
       api/core/index, api/utils/index, api/ext/index directly)
  4. preparing documents... WARNING: unsupported theme option 'logo'
     given
     → cause: duplicate html_theme_options dict at conf.py:51-53
       silently overwrites the line-19 dict
     → fix: delete the entire duplicate (lines 51-53), NOT just the
       'logo' key (Pitfall 2 — naive fix leaves empty dict that still
       overwrites the active config)

Pre-verified safety nets:
  - audit-public-api.py is annotation-blind (RESEARCH.md Pitfall 1).
    The string-quote in interface.py does NOT change `make audit-public-api`
    output. Pre-flight check expected to be no-op.
  - exec_cmd() docstring at cement/utils/shell.py:86-91 has the SAME
    inline-literal pattern but is NOT in the live warning list — D-08 #4
    names only cmd(); leave exec_cmd() alone (defer to a future quick
    task per RESEARCH.md Example 5 + Pitfall 9).

Pitfall guards (encode in tasks):
  - Pitfall 2: The conf.py fix MUST delete lines 51-53 entirely; verify
    `navigation_depth: 4` is still present in the surviving dict at
    lines 19-33.
  - Pitfall 3: The shell.py fix is a single-token swap (`/` → ` or`);
    do NOT add stray backticks.
  - Pitfall 4: Default to option (a) (delete orphan); pre-flight grep
    confirms no external references.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop duplicate html_theme_options dict (commit 5 of D-16)</name>
  <files>docs/source/conf.py, CHANGELOG.md</files>
  <read_first>
    - docs/source/conf.py lines 14-55 (covers BOTH the line-19 active dict and the line-51 duplicate, so the executor sees the duplication firsthand)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-08 #1
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 2 (CRITICAL — naive fix is wrong)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "docs/source/conf.py" section
    - CHANGELOG.md lines 1-5 + Misc bucket header (line ~293)
  </read_first>
  <action>
Per Pitfall 2: delete the duplicate `html_theme_options = {...}`
assignment at lines 51-53 ENTIRELY. Do NOT remove only the `'logo'`
key — that leaves `html_theme_options = {}` which still overwrites
the active dict at lines 19-33.

EXACT BEFORE (`docs/source/conf.py:51-53`, the lines to DELETE):

```python
html_theme_options = {
    'logo': 'logo-text.png',
}
```

EXACT AFTER: those 3 lines are removed. The line-19 dict (with
`canonical_url`, `display_version`, `collapse_navigation`,
`navigation_depth: 4`, etc.) becomes the live config.

Pre-flight check (run BEFORE editing): confirm the line-19 dict still
contains the toc-config keys:

```bash
grep -nE "'navigation_depth'|'collapse_navigation'|'titles_only'" docs/source/conf.py
```

Expected: ≥3 hits all clustered together (the active dict).

Append CHANGELOG entry to `Misc:` bucket of active 3.0.15 - DEVELOPMENT
section:

```
- `[docs]` Drop unsupported 'logo' theme option from sphinx conf
```

Run gates BEFORE committing:

```bash
make comply
# Manually verify warning gone:
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "unsupported theme option"
# expected: 0
```

Commit subject: `docs(sphinx): drop unsupported 'logo' theme option from
conf.py` (62 chars).

Suggested body:

```
sphinx_rtd_theme 3.1.0 rejects 'logo' as an unsupported option.
The duplicate html_theme_options dict at conf.py:51-53 silently
overwrote the active dict at lines 19-33; deleting it (3 lines)
restores the toc-config (collapse_navigation, navigation_depth,
titles_only, etc.) AND drops the warning.

DOCS-01 progress: 1 of 4 known sphinx warnings resolved.
```
  </action>
  <verify>
    <automated>! grep -F "'logo': 'logo-text.png'" docs/source/conf.py &amp;&amp; grep -cE "'navigation_depth'" docs/source/conf.py | grep -qx 1 &amp;&amp; grep -cE "'collapse_navigation'" docs/source/conf.py | grep -qx 1 &amp;&amp; grep -cE '^html_theme_options = \{' docs/source/conf.py | grep -qx 1 &amp;&amp; pdm run sphinx-build ./docs/source ./docs/build 2>&amp;1 | grep -ic 'unsupported theme option' | grep -qx 0 &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; grep -F "[docs] Drop unsupported 'logo' theme option from sphinx conf" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "'logo': 'logo-text.png'" docs/source/conf.py` returns 0 hits
    - `grep -cE '^html_theme_options = \{' docs/source/conf.py` returns exactly `1` — count of active (non-commented) `html_theme_options = {` declarations; the `^` anchor excludes commented variants at lines 59 and 228 (the active dict at line 19 is the sole survivor; the duplicate at the former lines 51-53 is gone)
    - `grep -cE "'navigation_depth'" docs/source/conf.py` returns `1` (toc-config preserved — Pitfall 2 guard)
    - `grep -cE "'collapse_navigation'" docs/source/conf.py` returns `1`
    - `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic 'unsupported theme option'` returns `0`
    - `make comply` exits 0; `make audit-public-api` exits 0; `make test` exits 0 at 100% coverage
    - CHANGELOG contains literal `- \`[docs]\` Drop unsupported 'logo' theme option from sphinx conf`
    - Commit touches exactly 2 files: `docs/source/conf.py` + `CHANGELOG.md`
    - Commit subject ≤78 chars
  </acceptance_criteria>
  <done>
    Duplicate `html_theme_options` dict deleted; active dict at lines 19-33
    is the only definition; toc-config preserved; sphinx-build no longer
    emits the `unsupported theme option 'logo'` warning; gates clean;
    CHANGELOG appended; commit landed under D-16 step 5.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire api/index into top-level toctree (commit 6 of D-16)</name>
  <files>docs/source/api/index.rst (DELETE), CHANGELOG.md</files>
  <read_first>
    - docs/source/index.rst (14 lines — the kept top-level toctree)
    - docs/source/api/index.rst (14 lines — the orphan to delete)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-08 #2
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 4 (recommendation: option (a) delete)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "docs/source/index.rst and/or docs/source/api/index.rst" section
  </read_first>
  <action>
Per Pitfall 4 recommendation: pick option (a) — delete the orphan
`docs/source/api/index.rst` entirely. The top-level `docs/source/
index.rst` (lines 8-13) already contains a toctree referencing
`api/core/index`, `api/utils/index`, `api/ext/index` directly — no
edits to `index.rst` are needed.

Pre-flight (mandatory):

```bash
grep -rn 'api/index' .planning/ docs/ README.md
```

Expected output: only references inside `docs/source/api/index.rst`
itself (the file being deleted) and possibly within `docs/source/conf.py`
if any. If the grep finds external references (GitBook, README,
.planning/), STOP and escalate to user — switch to option (b)
(`:hidden:` toctree in `index.rst`).

If pre-flight is clean, delete `docs/source/api/index.rst`:

```bash
git rm docs/source/api/index.rst
```

Verify the toctree warning is gone:

```bash
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "isn't included in any toctree"
# expected: 0
```

Append CHANGELOG entry to `Misc:` bucket:

```
- `[docs]` Wire api/index into top-level toctree
```

Run gates:

```bash
make comply
```

Commit subject: `docs(sphinx): wire api/index into top-level toctree`
(50 chars).

Suggested body:

```
docs/source/api/index.rst was an orphan duplicate of the top-level
docs/source/index.rst toctree (both referenced api/core/index,
api/utils/index, api/ext/index). Sphinx flagged the orphan with
"document isn't included in any toctree". Deleting the orphan keeps
the top-level toctree as the single source of truth.

Pre-flight: no external references to api/index.html in .planning/,
docs/, or README.md.

DOCS-01 progress: 2 of 4 known sphinx warnings resolved.
```
  </action>
  <verify>
    <automated>! test -f docs/source/api/index.rst &amp;&amp; grep -E 'api/core/index|api/utils/index|api/ext/index' docs/source/index.rst | wc -l | grep -qx 3 &amp;&amp; pdm run sphinx-build ./docs/source ./docs/build 2>&amp;1 | grep -ic "isn't included in any toctree" | grep -qx 0 &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; grep -F '[docs] Wire api/index into top-level toctree' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/source/api/index.rst` returns false (file deleted)
    - `grep -E 'api/core/index|api/utils/index|api/ext/index' docs/source/index.rst | wc -l` returns `3` (top-level toctree preserved)
    - `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "isn't included in any toctree"` returns `0`
    - `make comply` exits 0; `make audit-public-api` exits 0; `make test` exits 0 at 100% coverage
    - CHANGELOG contains literal `- \`[docs]\` Wire api/index into top-level toctree`
    - Commit shows file deletion: `git show --stat HEAD | grep -E '^\s*docs/source/api/index\.rst'` includes the deleted file
    - Commit subject ≤78 chars
  </acceptance_criteria>
  <done>
    Orphan `api/index.rst` deleted; top-level toctree intact; sphinx-build
    no longer emits the "isn't included in any toctree" warning; gates
    clean; CHANGELOG appended; commit landed under D-16 step 6.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: String-quote InterfaceManager.list return annotation (commit 7 of D-16)</name>
  <files>cement/core/interface.py, CHANGELOG.md</files>
  <read_first>
    - cement/core/interface.py lines 90-120 (covers `get`, `list`, `define` methods so the executor confirms scope is single-line + comment)
    - scripts/audit-public-api.py (skim to confirm RESEARCH.md Pitfall 1's "annotation-blind" claim — the AST walker enumerates `Name`, `FunctionDef`, `ClassDef`, `AnnAssign.target`, `Assign.targets`, `ImportFrom`/`Import` only; never serializes annotations)
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-08 #3 + D-18 #3
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 1 (audit-public-api is annotation-blind — pre-flight is confirmation, not branch point)
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 6 (exact replacement)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "cement/core/interface.py:102" section
  </read_first>
  <action>
Per D-08 #3: string-quote the return annotation on
`InterfaceManager.list` to defer evaluation past the method-name binding,
dodging autodoc's `inspect.signature()` introspection bug. Add a
grep-able inline `# autodoc:` comment so a future cleanup doesn't
silently undo it.

Pre-flight (RESEARCH.md Pitfall 1 — confirmation, not branch point):

```bash
make audit-public-api && echo OK_BEFORE
```

Expected: exit 0. (If non-zero, escalate to user — would indicate a
pre-existing baseline drift unrelated to this task.)

EXACT BEFORE (`cement/core/interface.py:102`):

```python
    def list(self) -> list[str]:
```

EXACT AFTER:

```python
    def list(self) -> "list[str]":  # autodoc: PEP 585 + method-name-shadow workaround
```

Notes:
- The string-quote (`"list[str]"` instead of `list[str]`) defers
  annotation evaluation; autodoc's introspection no longer attempts
  to subscript the method object (`self.list` has type `function`,
  not `type` — hence the original "function object is not
  subscriptable" warning).
- Inline comment `# autodoc:` is grep-able per CONTEXT.md Discretion
  item.
- This is a LOCAL workaround for a method-name shadow, NOT a file-
  level rollback of Phase 3 D-08 (`from __future__ import annotations`
  removal stays in effect for the rest of the file).

Post-edit verification (RESEARCH.md Pitfall 1 confirmation):

```bash
make audit-public-api && echo OK_AFTER
```

Expected: exit 0, byte-identical baseline. If non-zero, this is the
escalation case — STOP and ask user whether to rebase the baseline
or use the alternative `import builtins` approach (Open Question 1
in RESEARCH.md). Pre-verified safe per Pitfall 1 — should not
trigger.

Verify warning gone:

```bash
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "InterfaceManager.list"
# expected: 0
```

Append CHANGELOG entry to `Bugs:` bucket (NOT Misc — this is a
`fix:` per Conventional Commits):

```
- `[core.interface]` String-quote list[str] return annotation for autodoc compatibility
```

(Wrap at 78 chars if needed; the line above is exactly 86 chars in
markdown — wrap onto two lines per CHANGELOG convention.)

Suggested CHANGELOG entry with proper wrap:

```
- `[core.interface]` String-quote list[str] return annotation for
  autodoc compatibility
```

Commit subject: `fix(core.interface): string-quote list[str] return
annotation for autodoc compatibility` — this is 87 chars; trim to
≤78 chars. Use:

`fix(core.interface): string-quote list[str] for autodoc` (54 chars).

Suggested body:

```
The InterfaceManager.list method-name shadows the builtin, so
autodoc's inspect.signature() introspection tries to subscript the
method object (self.list, which has type function) instead of
builtins.list, producing:

  WARNING: Failed to get a method signature for
  cement.core.interface.InterfaceManager.list:
  'function' object is not subscriptable

String-quoting the return annotation defers evaluation past the
method-name binding. The local fix preserves Phase 3 D-08
(from __future__ import annotations removal stays in effect file-wide).

Inline `# autodoc:` comment is grep-able for future cleanup. The
sibling files (hook.py, handler.py, extension.py) use a different
pattern (import builtins + builtins.list[T]) — preserved as a future
consistency-cleanup candidate per RESEARCH.md Open Question 1.

audit-public-api is annotation-blind (verified) — no baseline rebase
needed.

DOCS-01 progress: 3 of 4 known sphinx warnings resolved.
```
  </action>
  <verify>
    <automated>grep -F 'def list(self) -> "list[str]":' cement/core/interface.py &amp;&amp; grep -F '# autodoc:' cement/core/interface.py &amp;&amp; make audit-public-api &amp;&amp; pdm run sphinx-build ./docs/source ./docs/build 2>&amp;1 | grep -ic "InterfaceManager.list" | grep -qx 0 &amp;&amp; make comply &amp;&amp; pdm run pytest tests/core/test_interface.py -x &amp;&amp; grep -F '[core.interface]' CHANGELOG.md | grep -F 'String-quote list[str] return annotation'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'def list(self) -> "list[str]":' cement/core/interface.py` matches exactly once
    - `grep -F '# autodoc:' cement/core/interface.py` matches at least once (the grep-able rationale comment)
    - `make audit-public-api` exits 0 immediately BEFORE the edit (pre-flight baseline confirmation)
    - `make audit-public-api` exits 0 immediately AFTER the edit (post-edit drift gate; D-18 #3 — public-API surface unchanged)
    - `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "InterfaceManager.list"` returns `0` (no warning fires)
    - `pdm run pytest tests/core/test_interface.py -x` exits 0 (no test regression)
    - `make comply` exits 0; `make test` exits 0 at 100% coverage
    - CHANGELOG `Bugs:` bucket contains `- \`[core.interface]\`` followed by `String-quote list[str] return annotation for autodoc compatibility` (allowing for line wrap at 78 chars)
    - Commit subject ≤78 chars (exact: `fix(core.interface): string-quote list[str] for autodoc`)
    - Commit touches exactly 2 files: `cement/core/interface.py` + `CHANGELOG.md`
  </acceptance_criteria>
  <done>
    `InterfaceManager.list` return annotation is string-quoted with a
    grep-able rationale comment; sphinx warning resolved; audit-public-api
    exit 0 (no public-API drift); tests pass; CHANGELOG appended; commit
    landed under D-16 step 7.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix inline-literal RST in cmd() docstring (commit 8 of D-16)</name>
  <files>cement/utils/shell.py, CHANGELOG.md</files>
  <read_first>
    - cement/utils/shell.py lines 1-66 (covers `cmd()` AND `exec_cmd()` so the executor confirms `exec_cmd()` is intentionally left alone — D-08 #4 names only cmd())
    - .planning/phases/05-deprecations-docs-security-stubs/05-CONTEXT.md `<decisions>` D-08 #4
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Common Pitfalls` Pitfall 3 (don't add stray backticks; the fix is a token swap)
    - .planning/phases/05-deprecations-docs-security-stubs/05-RESEARCH.md `## Code Examples` Example 5 (exact replacement)
    - .planning/phases/05-deprecations-docs-security-stubs/05-PATTERNS.md "cement/utils/shell.py:35-39" section
  </read_first>
  <action>
Per D-08 #4 + Pitfall 3: replace the `/` between two adjacent inline
literals (`` ``text=True`` `` and `` ``encoding=...`` ``) with `or`.
docutils trips on the slash because it reads the closing/opening
backtick run as a continuous ambiguous literal.

EXACT BEFORE (`cement/utils/shell.py:35-39`):

```python
        tuple: When ``capture==True``, returns the ``(stdout, stderror,
            return_code)`` of the command. ``stdout`` and ``stderror`` are
            ``bytes`` (the ``Popen`` default); decode with the appropriate
            encoding if string output is needed, or pass ``text=True`` /
            ``encoding=...`` through ``**kwargs``.
```

EXACT AFTER (single-token swap on line 38: `/` → `or`):

```python
        tuple: When ``capture==True``, returns the ``(stdout, stderror,
            return_code)`` of the command. ``stdout`` and ``stderror`` are
            ``bytes`` (the ``Popen`` default); decode with the appropriate
            encoding if string output is needed, or pass ``text=True`` or
            ``encoding=...`` through ``**kwargs``.
```

Note: do NOT touch `exec_cmd()` docstring at lines 86-91 — same prose
pattern, but NOT in the live warning list. D-08 #4 names only `cmd()`.
The parallel fix is deferred per RESEARCH.md Pitfall 9 / Example 5.

Verify warning gone:

```bash
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "Inline literal start-string"
# expected: 0
```

Append CHANGELOG entry to `Misc:` bucket:

```
- `[docs]` Fix inline-literal RST in shell.cmd() docstring
```

Run gates:

```bash
make comply && make test && make audit-public-api
```

Commit subject: `docs(utils.shell): fix inline-literal RST in cmd()
docstring` (60 chars).

Suggested body:

```
docutils could not parse the slash between two adjacent inline
literals in the cmd() docstring (``text=True`` / ``encoding=...``) —
the closing/opening backtick run with a single non-word character
between confused the inline-literal state machine. Replacing the
slash with `or` gives the parser breathing room.

The same prose appears in exec_cmd() at lines 86-91 but is NOT in
the live warning list (autodoc only fired on cmd()); leaving exec_cmd
alone per D-08 #4 strict scope. A follow-up quick task may
parallel-fix it for consistency.

DOCS-01 progress: 4 of 4 known sphinx warnings resolved. After this
commit, `pdm run sphinx-build ./docs/source ./docs/build` is
warning-free and Plan 04 (D-16 step 9) can flip the `-W` flag.
```
  </action>
  <verify>
    <automated>grep -F '``text=True`` or' cement/utils/shell.py &amp;&amp; ! grep -F '``text=True`` /' cement/utils/shell.py &amp;&amp; pdm run sphinx-build ./docs/source ./docs/build 2>&amp;1 | grep -ic 'Inline literal start-string' | grep -qx 0 &amp;&amp; pdm run sphinx-build ./docs/source ./docs/build 2>&amp;1 | grep -cE '(WARNING|ERROR):' | grep -qx 0 &amp;&amp; make comply &amp;&amp; make audit-public-api &amp;&amp; pdm run pytest tests/utils/test_shell.py -x &amp;&amp; grep -F '[docs] Fix inline-literal RST in shell.cmd() docstring' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '``text=True`` or' cement/utils/shell.py` matches at least once (the fix is in place)
    - `grep -F '``text=True`` /' cement/utils/shell.py` returns 0 hits (the buggy slash form is gone)
    - `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic 'Inline literal start-string'` returns `0`
    - **PHASE-LEVEL GATE PRECONDITION:** `pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -cE '(WARNING|ERROR):'` returns `0` (ALL 4 known warnings now resolved — Plan 04 can safely flip `-W`; the `(WARNING|ERROR):` prefix avoids false-matches on incidental occurrences of the word "warning" in sphinx status output)
    - `make comply` exits 0; `make audit-public-api` exits 0; `make test` exits 0 at 100% coverage
    - `pdm run pytest tests/utils/test_shell.py -x` exits 0 (no test regression — the docstring change does not affect behavior; the existing `make test` exit-0 already catches this but a targeted run is faster)
    - CHANGELOG `Misc:` bucket contains literal `- \`[docs]\` Fix inline-literal RST in shell.cmd() docstring`
    - Commit subject ≤78 chars; touches exactly 2 files (`cement/utils/shell.py` + `CHANGELOG.md`)
    - `cement/utils/shell.py:exec_cmd` docstring at lines 86-91 is UNCHANGED (D-08 #4 strict scope; `git diff HEAD~1 -- cement/utils/shell.py | grep -c '+\|-'` shows changes only around line 38, not lines 86-91)
  </acceptance_criteria>
  <done>
    `cmd()` docstring has the slash replaced with `or`; sphinx-build emits
    zero warnings (all 4 resolved across this plan); `exec_cmd()` docstring
    untouched; gates clean; CHANGELOG appended; commit landed under D-16
    step 8. Plan 04's `-W` flip prerequisite is now met.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → autodoc HTML | Sphinx renders Python source (docstrings + signatures) into HTML. Phase 5 only edits source-code annotations and docstring prose. No user-input pipeline. |
| Public-API surface (audit-public-api) → public consumers | The audit script enumerates AST signatures byte-for-byte against `.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt`. Phase 5's interface.py:102 string-quote is an annotation form change, not a name/signature change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | I (Information disclosure / wrong-info) | `cement/core/interface.py:102` string-quote | mitigate | `make audit-public-api` exit 0 is the gate (D-18 #3). `audit-public-api.py` is annotation-blind (RESEARCH.md Pitfall 1 — verified by reading the AST walker). String-quote is purely local; no cross-file impact. |
| T-05-noop-04 | T (Tampering) | `docs/source/conf.py` html_theme_options edit | accept | sphinx config is build-time only; no runtime path. Pitfall 2 guard ensures the active dict's toc-config is not silently clobbered. |
| T-05-noop-05 | I (Information disclosure) | `docs/source/api/index.rst` deletion | accept | The deleted file was an orphan; no external links land on `api/index.html` (verified pre-flight grep). |
| T-05-noop-06 | T (Tampering) | `cement/utils/shell.py` cmd() docstring | accept | Pure docstring edit; no source-code line modification beyond the docstring (`shell=True` default behavior unchanged). T-05-02 from CONTEXT.md applies. |

**ASVS L1 applicability:** V14 (Configuration) partial — `make docs` is a
build-time gate, not runtime. The `-W` flag (Plan 04) is the
configuration-discipline anchor; Plan 02 only resolves warnings, not
gate flip. Block on: high (T-05-03 is the only "could fail" — pre-verified
safe).
</threat_model>

<verification>
After all 4 commits:

```bash
# DOCS-01 acceptance — zero sphinx warnings
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic warning   # → 0

# Specific warning regression checks (each should be 0)
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "unsupported theme option"
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "isn't included in any toctree"
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "InterfaceManager.list"
pdm run sphinx-build ./docs/source ./docs/build 2>&1 | grep -ic "Inline literal start-string"

# Pitfall 2 guard — toc-config not silently clobbered
grep -cE "'navigation_depth'" docs/source/conf.py        # → 1
grep -cE "'collapse_navigation'" docs/source/conf.py     # → 1

# Pitfall 4 — orphan deleted, top-level toctree intact
test ! -f docs/source/api/index.rst
grep -cE 'api/(core|utils|ext)/index' docs/source/index.rst   # → 3

# D-18 #3 — public-API audit gate
make audit-public-api

# Cumulative gates
make test && make comply

# CHANGELOG — 4 new entries (3 Misc, 1 Bugs)
git diff <plan-01-summary-head>..HEAD -- CHANGELOG.md | grep -E '^\+- \`\[' | wc -l   # → 4
```
</verification>

<success_criteria>
- [ ] All 4 known sphinx warnings resolved (`grep -ic warning` on `sphinx-build` output → 0)
- [ ] `docs/source/conf.py`: duplicate `html_theme_options` dict deleted; line-19 dict (with `navigation_depth: 4`, `collapse_navigation: False`, etc.) is the sole definition
- [ ] `docs/source/api/index.rst` deleted (option (a) per Pitfall 4)
- [ ] `cement/core/interface.py:102` return annotation is string-quoted with a grep-able `# autodoc:` comment
- [ ] `cement/utils/shell.py:cmd()` docstring slash separator replaced with `or`; `exec_cmd()` docstring unchanged
- [ ] `make audit-public-api` exits 0 across all 4 commits (D-18 #3)
- [ ] `make test` exits 0 at 100% coverage; `make comply` exits 0
- [ ] CHANGELOG `Misc:` bucket has 3 new entries; `Bugs:` bucket has 1 new entry
- [ ] All 4 commits use Conventional Commits subjects ≤78 chars
- [ ] Plan 04's prerequisite met: a future `make docs -W` (after Plan 04 flip) will exit 0
</success_criteria>

<output>
After completion, create
`.planning/phases/05-deprecations-docs-security-stubs/05-02-SUMMARY.md`
recording: 4 commit SHAs, the exact CHANGELOG diff (4 new entries), the
final `pdm run sphinx-build ... 2>&1 | grep -c warning` output (`0`), the
`make audit-public-api` evidence (exit code + diff empty), and any
deviations from the BEFORE/AFTER text in this plan (expected: none).
</output>
