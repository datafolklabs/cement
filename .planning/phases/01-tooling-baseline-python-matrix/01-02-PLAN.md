---
phase: 01-tooling-baseline-python-matrix
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - pyproject.toml
  - cement/
  - tests/
autonomous: true
requirements:
  - TOOL-01
  - TOOL-04
must_haves:
  truths:
    - "ruff is bumped to ~=0.15.12 in [dependency-groups] dev"
    - "ruff preview = false in pyproject.toml [tool.ruff.lint]"
    - "extend-select lists E, F, W, B, I, A, C90, N, PT, T20, YTT explicitly"
    - "ignore = [] block exists with audit-point comment (or specific entries with one-line justification each)"
    - "AUDIT POINT comment present in [tool.ruff.lint] referencing Phase 1 D-08"
    - "make comply-ruff exits 0 on cement/ and tests/ after all family fixes land"
    - "Each ruff rule family that surfaces violations has its own fix(lint): commit per D-04"
    - "make test exits 0 (coverage held; non-functional changes only)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Ruff 0.15.12 pin, preview=false, codified extend-select families, audit-point comments"
      contains: "ruff~=0.15.12"
    - path: "pyproject.toml"
      provides: "Codified ruff family list per D-06"
      contains: "extend-select"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint] extend-select"
      to: "make comply-ruff"
      via: "ruff config -> CI gate"
      pattern: "every selected family fires CI failures on new rule additions (D-08 hybrid drift detection)"
    - from: "pyproject.toml [dependency-groups] dev ruff~=0.15.12"
      to: "scheduled pdm.yml"
      via: "compatible-release pin protects pdm update from rule-set drift"
      pattern: "Phase 2 owns pdm.yml; Plan 02's pin is the structural fix"
---

<objective>
Step 2 ruff segment of Phase 1 (D-03 strict serial sequence: ruff first).
Bump ruff from `>=0.3.2` to `~=0.15.12`, flip `preview = true` -> `false`
(D-09), extend `extend-select` to the D-06 family list (E,F,W already in;
adding B,I,A,C90,N,PT,T20,YTT), add the AUDIT POINT codification comment
(D-08), then resolve the per-family fallout in separate `fix(lint):`
commits — one commit per rule family per D-04.

Per D-15: pin bump + preview flip + extend-select expansion + audit
comment ALL land in the same `chore: bump ruff` commit. Pin and bump
are coupled, never split.

Per RESEARCH.md (verified this session 2026-04-29 on ruff 0.15.12):
- 0 fallout from E/F/W (already enabled, already clean)
- B family: 29 violations (B007 unused-loop-variable x17, B904
  raise-without-from x8, plus B005/B006/B011 misc x4)
- I family: 80 violations (all I001 unsorted-imports — auto-fixable
  via `make comply-ruff-fix`)
- A family: 18 violations (A001 x14, A002 x4)
- C90 family: 12 violations (C901 complex-structure)
- N family: 10 violations (mostly N806 in tests)
- PT family: 27 violations (mostly PT013 pytest-incorrect-import)
- T20 family: 8 violations (T201 print-statements)
- YTT family: 1 violation (YTT203 at tests/ext/test_ext_argparse.py:13)

Total: ~232 violations across 8 new families.

Output: 9 atomic commits in this plan:
  1. `chore: bump ruff to 0.15` (pin + preview flip + family expansion + audit comment)
  2. `fix(lint): resolve I001 unsorted-imports (auto-fix)`
  3. `fix(lint): resolve B007/B904/B005/B006/B011 bugbear`
  4. `fix(lint): resolve A001/A002 builtin-shadowing`
  5. `fix(lint): resolve C901 complex-structure` (via ignore + audit comment)
  6. `fix(lint): resolve N806/N801/N802 naming`
  7. `fix(lint): resolve PT013/PT012 pytest-style`
  8. `fix(lint): resolve T201 print-statements`
  9. `fix(lint): resolve YTT203 sys-version-info`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md
@.planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md
@.planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md
@.planning/phases/01-tooling-baseline-python-matrix/01-VALIDATION.md
@.planning/phases/01-tooling-baseline-python-matrix/01-01-SUMMARY.md
@CLAUDE.md
@pyproject.toml

<interfaces>
<!-- Target [tool.ruff] / [tool.ruff.lint] block (Step 2 D-08/D-09 + D-06).
     This is the EXACT shape after Plan 02 Task 1. -->

[tool.ruff]
target-version = "py310"            # set in Plan 01; unchanged here
line-length = 100                   # unchanged
indent-width = 4                    # unchanged
exclude = [
    ".git",
    "cement/cli/templates",
    "cement/cli/contrib"
]
include = [
    "cement/**/*.py",
    "tests/**/*.py"
]

[tool.ruff.lint]
# AUDIT POINT (D-08): re-review on every ruff bump. New rules added by
# ruff to a selected family fire as CI failures, forcing a deliberate
# add-with-comment to `ignore` or a fix. See Phase 1 RESEARCH.md.
preview = false                     # was true (D-09)
extend-select = [
    "E",   # pycodestyle errors
    "F",   # Pyflakes
    "W",   # pycodestyle warnings
    "B",   # flake8-bugbear (NEW, D-06)
    "I",   # isort (NEW, D-06)
    "A",   # flake8-builtins (NEW, D-06)
    "C90", # mccabe complexity (NEW, D-06)
    "N",   # pep8-naming (NEW, D-06)
    "PT",  # flake8-pytest-style (NEW, D-06)
    "T20", # flake8-print (NEW, D-06)
    "YTT", # flake8-2020 (NEW, D-06)
]
ignore = [
    # AUDIT POINT (D-08): each entry MUST have a one-line justification.
    # Empty list is the green-baseline goal.
]
fixable = ["ALL"]                   # unchanged
unfixable = []                      # unchanged

# Target [dependency-groups] dev ruff line:
"ruff~=0.15.12",                    # was ruff>=0.3.2  (D-12: ~= compatible-release)

<!-- Known fallout sites (RESEARCH.md / PATTERNS.md):
     - YTT203 site: tests/ext/test_ext_argparse.py:13 — stale
       sys.version_info[0]>=3 and sys.version_info[1]>=4 check; replace
       the conditional with unconditional ARGPARSE_SUPPORTS_DEFAULTS = True
       (Python 3.4+ is universal in our py310 floor).
     - N806 in tests: per-file-ignore is the recommended escape hatch
       (RESEARCH.md Pitfall 6).
     - C901: planner authority is to absorb in ignore = ["C901"] with
       audit comment pointing to Phase 3 REFACTOR-01/02 (RESEARCH.md
       Open Question 3 explicit recommendation).
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chore — bump ruff to 0.15.12 + codify rule set + flip preview (D-15 coupling)</name>
  <files>pyproject.toml</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-06, D-07, D-08, D-09, D-12, D-15),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Hybrid Codification Pattern", § "Ruff Family Selection (D-06 Recommendation)", § "Final pyproject.toml shape"),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "[tool.ruff] (config, declarative — RULE-SET CODIFICATION)"),
    pyproject.toml,
    CLAUDE.md (Conventional Commits, 78-char wrap)
  </read_first>
  <action>
Edit `pyproject.toml`. ALL changes below MUST land in ONE commit per
D-15 (pin and bump are coupled, never split). After the edits, run
`pdm install` to refresh the local environment to ruff 0.15.12 — but do
NOT commit `pdm.lock` (Phase 2 DEPS-01 owns lockfile regeneration; per
RESEARCH.md Pitfall 4, leave lockfile to Phase 2 — verify any pdm.lock
changes do not slip into the Phase 1 PR).

EDITS:

1. `[tool.ruff.lint]` block (current lines 85-90 — read file first to get
   exact line numbers since Plan 01 may have shifted things). Replace the
   block with the target shape (see <interfaces>):
   - Add the AUDIT POINT comment ABOVE `preview = ...`.
   - Flip `preview = true` -> `preview = false` (D-09).
   - Expand `extend-select` from `["E", "F", "W"]` to the 11-entry list:
     `["E", "F", "W", "B", "I", "A", "C90", "N", "PT", "T20", "YTT"]`.
     Use multi-line format with one comment per line per the
     <interfaces> block (improves readability + future audit).
   - Replace the bare `ignore = []` with the AUDIT POINT comment block
     pattern shown in <interfaces>. Initial `ignore` list stays empty —
     C901 absorption (if needed) happens in the C90 fix-family commit
     (Task 5), not here.
   - Leave the existing commented-out `ignore = [...]` block (current
     lines 92-99 — `# ignore = [# "E402", ...]`) UNCHANGED. It's
     historical context; not in scope to clean up.
   - Leave `fixable = ["ALL"]` and `unfixable = []` unchanged.

2. `[dependency-groups] dev` block (current lines 156-167). Change ONLY
   the ruff line:
   - From: `"ruff>=0.3.2",`
   - To:   `"ruff~=0.15.12",`
   - Do NOT touch other entries in this commit. mypy stays `>=1.9.0`
     until Plan 03; pytest/pytest-cov/coverage stay on current floors
     until Plan 04. D-15 says "pin and bump COUPLED in one commit" per
     tool — so each tool's pin lands with that tool's bump commit.

VERIFICATION before commit:
  - `pdm install` — must resolve ruff 0.15.12 (a `~=0.15.12` allows
    0.15.x but excludes 0.16+).
  - `pdm run ruff --version` — confirms 0.15.x active.
  - `pdm run ruff config | grep -E "^lint\."` — confirms preview is
    false and the 11-family extend-select is active.
  - `pdm run ruff check cement/ tests/` — WILL FAIL with the new
    family fallout. Per D-04, those fails are fixed in Tasks 2-9
    (one commit per family). Do NOT commit fixes here.
  - Confirm `pdm.lock` is unchanged or has only mechanical resolution
    diffs that the planner deliberately accepts. Per RESEARCH.md
    Pitfall 4: prefer NOT to commit pdm.lock changes in Phase 1 (Phase
    2 DEPS-01 owns lockfile regen). If pdm.lock changes, `git checkout
    pdm.lock` to revert it after running `pdm install`.

COMMIT (single commit per D-15):
  Subject (≤ 78 chars): `chore: bump ruff to 0.15`
  Body (each line ≤ 78 chars):

      Bumps ruff from >=0.3.2 to ~=0.15.12 (D-12 compatible-release pin
      on a fast-evolving tool). Flips preview from true to false
      (D-09 — preview rules are the literal opposite of TOOL-04's
      "no implicit drift" goal). Codifies extend-select per D-06:
      adds B, I, A, C90, N, PT, T20, YTT to the existing E/F/W list
      (bug-and-style-only per D-07; UP/SIM/RUF/PL/S/ANN/ARG explicitly
      banned for Phase 1). Adds AUDIT POINT comment per D-08 — new
      rules in selected families fire as CI failures, forcing a
      deliberate ignore-with-justification or a fix.

      Lint fallout from this bump is resolved in subsequent
      fix(lint): commits, one per rule family per D-04.

      Refs TOOL-01, TOOL-04.
  </action>
  <verify>
    <automated>
grep -q 'ruff~=0.15.12' pyproject.toml &&
grep -q 'preview = false' pyproject.toml &&
grep -qE '"E", *"F", *"W"' pyproject.toml &&
grep -q '"B"' pyproject.toml &&
grep -q '"I"' pyproject.toml &&
grep -q '"A"' pyproject.toml &&
grep -q '"C90"' pyproject.toml &&
grep -q '"N"' pyproject.toml &&
grep -q '"PT"' pyproject.toml &&
grep -q '"T20"' pyproject.toml &&
grep -q '"YTT"' pyproject.toml &&
grep -qi 'AUDIT POINT' pyproject.toml &&
pdm run ruff --version | grep -E '^ruff 0\.15\.' &&
git log -1 --pretty=%s | grep -E '^chore: bump ruff to 0\.15$' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
ruff 0.15.x is active; pyproject.toml shows the codified
[tool.ruff.lint] block with `preview = false`, the 11-family extend-select,
and the AUDIT POINT comment; dev pin is `ruff~=0.15.12`; commit subject
matches `chore: bump ruff to 0.15`; body lines all ≤ 78 chars.
`pdm run ruff check cement/ tests/` is expected to be RED at this commit
(fallout to be fixed in Tasks 2-9). Do NOT proceed to Task 2 if any
verification step fails.
  </done>
</task>

<task type="auto">
  <name>Task 2: fix(lint) — I001 unsorted-imports (80 hits, auto-fixable)</name>
  <files>cement/, tests/</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04 one-commit-per-family, D-13 strict-minimum),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — I" entry; § "Pattern 2: Use auto-fix where safe"),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — I001 row),
    Makefile (so the executor knows make comply-ruff-fix exists)
  </read_first>
  <action>
Run `make comply-ruff-fix` (which is `pdm run ruff check --fix cement/
tests/`). All 80 I001 hits are `[*]` safe-fix per RESEARCH.md — auto-fix
is the canonical move for this family.

After auto-fix:
  - Run `pdm run ruff check cement/ tests/ --select I` — MUST report
    zero I001 violations.
  - Run `pdm run ruff check cement/ tests/` — should still show fallout
    from B/A/C90/N/PT/T20/YTT (those land in Tasks 3-9), but NOT I001.
  - Run `make test-core` — quick sanity check that import re-ordering
    didn't break anything (none expected; isort is purely declarative).
  - Inspect the diff (`git diff`) to confirm only import-section
    re-ordering changed, no other edits crept in. If anything outside
    import blocks changed, investigate before committing — auto-fix
    SHOULD only touch imports under `--select I`.

Per D-13 strict-minimum: do NOT clean up any other lint that
auto-fix-might also apply. The `--select I` filter ensures auto-fix
only touches I001. If `make comply-ruff-fix` (which is full --fix
without --select) auto-fixes other families' violations as a
side-effect, that's a problem — use the targeted form:
`pdm run ruff check --fix --select I cement/ tests/` instead. Audit
the diff to confirm.

COMMIT:
  Subject (≤ 78): `fix(lint): resolve I001 unsorted-imports (auto-fix)`
  Body (each line ≤ 78):

      Resolves all 80 I001 hits via ruff auto-fix (safe-fix [*]).
      Pure import re-ordering; no behavioral change.

      Per D-04: one commit per ruff rule family. Per D-13:
      strict-minimum-to-green — auto-fix scoped to --select I.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select I 2>&1 | tee /tmp/ruff-i.txt;
grep -qE 'All checks passed|^$' /tmp/ruff-i.txt ||
  ! grep -q 'I001' /tmp/ruff-i.txt;
make test-core &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve I001' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero I001 violations on `--select I`. `make test-core` exits 0. Commit
subject and body conform to Conventional Commits + 78-char wrap.
  </done>
</task>

<task type="auto">
  <name>Task 3: fix(lint) — B family bugbear (B007/B904/B005/B006/B011, ~29 hits)</name>
  <files>cement/, tests/</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — B" entry),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — B rows)
  </read_first>
  <action>
Resolve every B-family violation surfaced by `pdm run ruff check
cement/ tests/ --select B`. Expected fallout (verified RESEARCH.md):
  - B007 unused-loop-variable x17: replace `for x in ...:` with `for _
    in ...:` where `x` is unused inside the loop body. ruff auto-fix
    can safe-fix this — try `pdm run ruff check --fix --select B007
    cement/ tests/` first; manually verify each diff site before
    committing (auto-fix safety on B007 depends on whether the loop
    variable name is referenced elsewhere — visual review required).
  - B904 raise-without-from x8: change bare `raise FrameworkError(...)`
    inside `except Exception as e:` to `raise FrameworkError(...) from
    e` (or `from None` if intentional suppression). Mechanical, but
    NOT auto-fixable; manual edit each site.
  - B005/B006/B011 misc x4: address case-by-case. B005 strip-with-multi-
    char-string is mechanical. B006 mutable-default-arg is the canonical
    `def f(x=[]):` -> `def f(x=None): if x is None: x = []` swap. B011
    `assert False` -> `raise AssertionError("...")`.

If any B-family rule cannot be cleanly fixed under D-13 (i.e., a fix
would require modernization beyond strict-minimum), absorb it in
`[tool.ruff.lint] ignore = [...]` with a one-line justification per
the AUDIT POINT pattern — but this is escape-hatch, not default. Aim
to fix all ~29.

After fixes:
  - `pdm run ruff check cement/ tests/ --select B` -> 0 violations.
  - `make test` -> 0 (full suite, not just core, because B904 changes
    propagate exception chaining and any test that asserts on
    `__cause__` or `__context__` could break — none expected, but
    verify).

COMMIT:
  Subject (≤ 78): `fix(lint): resolve B007/B904/B005/B006/B011 bugbear`
  Body (each line ≤ 78):

      Resolves flake8-bugbear (B family) fallout from the ruff 0.15
      bump. Mechanical fixes:
        - B007: unused loop variables -> _ rename
        - B904: raise inside except -> raise ... from e
        - B005/B006/B011: misc bug-finder cases

      Per D-04 one-commit-per-family. Per D-13 strict-minimum.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select B 2>&1 | tee /tmp/ruff-b.txt;
grep -qE 'All checks passed|^$' /tmp/ruff-b.txt ||
  ! grep -E '^[^[:space:]].*B[0-9]+' /tmp/ruff-b.txt;
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve B[0-9/]+' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero B-family violations on `--select B`. `make test` exits 0. Commit
subject names the specific B-codes resolved (per CONTEXT.md D-04
example format). Body wrapped at 78 chars.
  </done>
</task>

<task type="auto">
  <name>Task 4: fix(lint) — A family flake8-builtins (A001/A002, 18 hits)</name>
  <files>cement/, tests/, pyproject.toml (only if per-file-ignore needed)</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — A" entry, including the "framework intentionally shadows builtins in handler kwargs" note),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — A row)
  </read_first>
  <action>
Triage all 18 A-family violations:
  - A001 builtin-shadowing-variable x14
  - A002 builtin-argument-shadowing x4

Two acceptable resolutions per RESEARCH.md (cement framework
intentionally shadows builtins like `type`, `format`, `id` in
handler kwargs and meta dicts — same reason attrs ignores this family):

OPTION A (preferred when callsite is internal/private):
  Rename: `type` -> `handler_type`, `format` -> `out_format`, etc.
  This MUST NOT change any public API (D-13 + 3.0.x no-breakage rule).
  If a kwarg name is part of a public Handler interface or Meta
  attribute that downstream extensions could reference, do NOT rename.

OPTION B (when renaming would break public API):
  Add a per-call `# noqa: A001` (or A002) comment with a one-line
  rationale citing the framework's intentional builtin-shadow pattern.
  Example: `def __init__(self, type: str, ...):  # noqa: A002 - public
  Handler interface; type is part of the contract`.

OPTION C (escape hatch — preferred only if broad pattern across
many files):
  Add to `[tool.ruff.lint] ignore = [...]` with a one-line justification:
    "A001",  # framework intentionally shadows builtins in handler
             # kwargs/Meta dicts (see RESEARCH.md). Justified 2026-04-29.
    "A002",  # ditto for argument-shadowing in public Handler.__init__
             # signatures. Justified 2026-04-29.
  This violates the "empty ignore = green-baseline goal" target but
  matches the "intentional pattern" framing — and is exactly the
  pattern attrs uses for the same reason.

Per D-13 strict-minimum, prefer per-call `# noqa:` (Option B) over a
broad-ignore (Option C) when the violations cluster in a small number
of files. If 14+ A001 sites are scattered across `cement/core/handler.py`,
`cement/core/meta.py`, `cement/core/foundation.py`, etc., AND most are
in public-facing kwargs, broad-ignore (Option C) is justified — but
document each rule in `ignore = []` with a one-line justification per
the AUDIT POINT comment pattern.

Inspect every A001/A002 site before deciding. If unclear whether a
kwarg is part of a public Handler interface, default to "yes, public"
and use Option B or C — D-13 forbids breaking changes.

After fixes:
  - `pdm run ruff check cement/ tests/ --select A` -> 0 violations.
  - `make test` -> 0.

COMMIT:
  Subject (≤ 78): `fix(lint): resolve A001/A002 builtin-shadowing`
  Body (≤ 78/line, document the resolution path chosen):

      Resolves flake8-builtins (A family) fallout. Cement framework
      intentionally shadows builtins (`type`, `format`, etc.) in
      public Handler kwargs and Meta dicts — D-13 strict-minimum and
      the no-breakage rule on 3.0.x mean these stay; ruff is told via
      <noqa-per-call|ignore-list> with audit comments.

      Per D-04 one-commit-per-family.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select A 2>&1 | tee /tmp/ruff-a.txt;
grep -qE 'All checks passed|^$' /tmp/ruff-a.txt ||
  ! grep -E '^[^[:space:]].*A00[12]' /tmp/ruff-a.txt;
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve A00' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero A-family violations on `--select A`. Public API surface
unchanged (verify with `git diff` — no signature changes on public
Handler/Interface classes). `make test` exits 0. If broad-ignore was
used, ignore entries have AUDIT POINT one-line justifications.
  </done>
</task>

<task type="auto">
  <name>Task 5: fix(lint) — C90 mccabe complexity (12 hits, absorb in ignore per D-13)</name>
  <files>pyproject.toml</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13 — strict-minimum forbids volunteer refactors),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — C90" entry; § "Open Questions — Question 3" — explicit recommendation to absorb in ignore),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — C901 row)
  </read_first>
  <action>
Per RESEARCH.md Open Question 3 explicit recommendation and D-13
strict-minimum: absorb the 12 C901 violations in
`[tool.ruff.lint] ignore` with an audit-point comment pointing to
Phase 3 REFACTOR-01/REFACTOR-02 for re-evaluation. Refactoring 12 hot-spot
functions in `cement/core/foundation.py`, `cement/core/handler.py`, etc.
would be a volunteer modernization beyond Phase 1's strict-minimum
boundary — explicitly disallowed.

Edit `pyproject.toml` `[tool.ruff.lint]` `ignore` block. Replace:

    ignore = [
        # AUDIT POINT (D-08): each entry MUST have a one-line
        # justification. Empty list is the green-baseline goal.
    ]

With:

    ignore = [
        # AUDIT POINT (D-08): each entry MUST have a one-line
        # justification. Empty list is the green-baseline goal.
        "C901",  # cement framework is intentionally complex
                 # (handler/interface dispatch, lifecycle hooks,
                 # extension loading). Phase 3 REFACTOR-01/02 will
                 # re-evaluate. Justified 2026-04-29 Phase 1.
    ]

Adding C90 to `extend-select` (Task 1) keeps the family ACTIVE — new
code that exceeds complexity 10 will still flag in non-C901-rule ways
(e.g., the family signal stays useful for new contributions even if
C901 itself is ignored). This is the deliberate D-08 hybrid posture.

Alternative considered and REJECTED per D-13: bumping
`[tool.ruff.lint.mccabe] max-complexity = 12` to absorb the existing
baseline. Rejected because (a) it baselines an arbitrary number, and
(b) it doesn't prevent NEW complex functions from accumulating —
ignore-with-revisit is the more honest posture per CONTEXT.md
"strict-minimum boundary."

After fix:
  - `pdm run ruff check cement/ tests/ --select C90` -> 0 violations.
  - `make test` -> 0.

COMMIT:
  Subject (≤ 78): `fix(lint): resolve C901 complex-structure`
  Body (≤ 78/line):

      Absorbs C901 (mccabe complexity) in [tool.ruff.lint] ignore
      with AUDIT POINT justification pointing to Phase 3
      REFACTOR-01/REFACTOR-02 for re-evaluation. 12 functions in
      cement/core/* exceed default threshold of 10; refactoring them
      in Phase 1 violates D-13 strict-minimum (volunteer
      modernization). Adding C90 to extend-select keeps the family
      signal active; ignoring C901 specifically defers the existing
      baseline.

      Per D-04 one-commit-per-family.
  </action>
  <verify>
    <automated>
grep -qE '"C901".*#.*Phase 3' pyproject.toml &&
pdm run ruff check cement/ tests/ --select C90 2>&1 |
  grep -qE 'All checks passed|^$' &&
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve C901' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
`pyproject.toml` `ignore = [...]` contains `"C901"` with one-line
audit justification referencing Phase 3 REFACTOR-01/02. `pdm run
ruff check --select C90` exits clean. `make test` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 6: fix(lint) — N family pep8-naming (N806/N801/N802, 10 hits)</name>
  <files>cement/, tests/, pyproject.toml (per-file-ignore for tests)</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — N" entry; § "Pitfall 6: Per-file-ignores explosion on N806"),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — N row)
  </read_first>
  <action>
Triage 10 N-family violations:
  - Most are N806 (non-lowercase variable in function, ~6 hits) — per
    RESEARCH.md these cluster in `tests/` and are intentional test
    fixture casing (e.g., `App = TestApp` aliases).
  - Remainder split among N801 (class name), N802 (function name) — fix
    case-by-case.

For tests/ N806: add a per-file-ignore per RESEARCH.md Pitfall 6
pattern. Edit `pyproject.toml` `[tool.ruff.lint]` block — append a new
sub-section AFTER the existing `unfixable = []` line:

    [tool.ruff.lint.per-file-ignores]
    "tests/**/*.py" = ["N806"]  # test-fixture variable casing
                                # is intentional. Justified 2026-04-29.

For cement/ N806/N801/N802 sites:
  - N806 in production code: rename the variable to lowercase. Mechanical.
  - N801 (CamelCase class): rename ONLY if the class is internal/private
    (leading underscore or not in `__all__`). If public (could be
    subclassed by downstream extensions), do NOT rename; per-call `#
    noqa: N801` with rationale.
  - N802 (snake_case function): rename ONLY if private. Public function
    names are ABI per the no-breakage rule on 3.0.x — `# noqa: N802`
    with one-line rationale.

After fixes:
  - `pdm run ruff check cement/ tests/ --select N` -> 0 violations.
  - `make test` -> 0.
  - `git diff cement/` shows no public-API signature changes (verify
    with grep for the renamed identifiers in cement-using example apps
    if any exist in this repo's docs).

COMMIT:
  Subject (≤ 78): `fix(lint): resolve N806/N801/N802 naming`
  Body (≤ 78/line, document tests/ per-file-ignore decision):

      Resolves pep8-naming (N family) fallout. Test-fixture variable
      casing is intentional per the convention already in use; absorbed
      via per-file-ignore on tests/**/*.py for N806. Production
      cement/ sites renamed where private; public-API sites preserved
      with per-call # noqa rationale (D-13 + 3.0.x no-breakage).

      Per D-04 one-commit-per-family.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select N 2>&1 |
  grep -qE 'All checks passed|^$' &&
grep -qE 'tests/\*\*.*N806' pyproject.toml &&
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve N8' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero N-family violations on `--select N`. tests/ N806 absorbed via
per-file-ignore with audit comment. Public-API surface unchanged. `make
test` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 7: fix(lint) — PT family pytest-style (PT013/PT012/etc, 27 hits)</name>
  <files>tests/</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — PT" entry — notes existing convention is `from pytest import raises`),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — PT row),
    tests/core/test_exc.py (the cited convention example)
  </read_first>
  <action>
Resolve 27 PT-family violations. Most are PT013
pytest-incorrect-import — RESEARCH.md notes the existing cement
convention is `from pytest import raises` (cited at
`tests/core/test_exc.py`). The 27 hits are tests that diverged from
this convention — bringing them in line is mechanical and matches the
existing codebase pattern, NOT a modernization (D-13 compliant).

For each PT13 hit:
  - Replace `import pytest` + `pytest.raises(...)` with
    `from pytest import raises` + `raises(...)`.
  - Replace `pytest.fixture` references with `from pytest import fixture` etc.
  - Preserve any other identifier that the test legitimately uses
    via `pytest.X` (PT013 is about import shape, not usage).

For non-PT013 PT codes (PT012 multiple-statement-with, PT004
fixture-naming, etc.), fix per ruff's recommendation in the error
message. None should require behavioral changes.

After fixes:
  - `pdm run ruff check cement/ tests/ --select PT` -> 0 violations.
  - `make test` -> 0 (test logic unchanged; only import shape changed).

COMMIT:
  Subject (≤ 78): `fix(lint): resolve PT013/PT012 pytest-style`
  Body (≤ 78/line):

      Resolves flake8-pytest-style (PT family) fallout. PT013
      converts `import pytest; pytest.raises(...)` to
      `from pytest import raises; raises(...)` per the existing
      convention in tests/core/test_exc.py. Pure import-shape change;
      no test logic affected.

      Per D-04 one-commit-per-family.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select PT 2>&1 |
  grep -qE 'All checks passed|^$' &&
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve PT' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero PT-family violations on `--select PT`. `make test` exits 0
(coverage held — pure import-style changes). Commit conforms.
  </done>
</task>

<task type="auto">
  <name>Task 8: fix(lint) — T20 flake8-print (T201, 8 hits)</name>
  <files>cement/, tests/</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — T20" entry — likely all in CLI templates or scripts),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — T20 row)
  </read_first>
  <action>
Audit each of the 8 T201 print-statement hits:
  - If the print is intentional CLI output (`cement/cli/`, output
    handlers, templates): add `# noqa: T201 - intentional CLI output`
    inline comment with one-line rationale.
  - If the print is leftover debug code: REMOVE it.
  - If the print should have been a `LOG.debug(...)`: replace it (ONLY
    if the call site is unambiguously framework-internal debug; do
    NOT change any user-facing output).

The `cement/ext/` extensions exclude is not in scope for ruff
(`cement/cli/templates/` and `cement/cli/contrib/` are excluded but
`cement/cli/main.py` is included). Most T201 hits will surface in CLI
output paths — add `# noqa` with rationale.

After fixes:
  - `pdm run ruff check cement/ tests/ --select T20` -> 0 violations.
  - `make test` -> 0.
  - Manually verify CLI smoke test (`pdm run cement --help`) still
    produces sensible output if any cement/cli/main.py print was
    touched.

COMMIT:
  Subject (≤ 78): `fix(lint): resolve T201 print-statements`
  Body (≤ 78/line):

      Resolves flake8-print (T20 family) fallout. CLI-output prints
      annotated with # noqa: T201 + intent rationale (D-13 — strict-
      minimum, prints are intentional). Any debug-leftover prints
      removed.

      Per D-04 one-commit-per-family.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select T20 2>&1 |
  grep -qE 'All checks passed|^$' &&
make test &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve T201' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero T20-family violations on `--select T20`. `make test` exits 0.
CLI output behavior unchanged. Commit conforms.
  </done>
</task>

<task type="auto">
  <name>Task 9: fix(lint) — YTT203 sys-version-info (1 hit, the stale Py3.4 shim)</name>
  <files>tests/ext/test_ext_argparse.py</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-04, D-13, D-14 — note: this is NOT a __future__ import; this IS a 3.9-shim equivalent that PYVER-03 wants caught),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Ruff Family Selection — YTT" entry; § "Runtime State Inventory" — NOT 3.9 traces row for tests/ext/test_ext_argparse.py:13 explicitly says fix this in YTT fallout, NOT Plan 01),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Source-Code Lint/Type Fallout" — YTT203 row),
    tests/ext/test_ext_argparse.py
  </read_first>
  <action>
Fix the single YTT203 violation at `tests/ext/test_ext_argparse.py:13`.
RESEARCH.md states the shim checks `sys.version_info[0] >= 3 and
sys.version_info[1] >= 4` (Python 3.4+) — universally true in our
py310 floor. Per RESEARCH.md the fix is to set
`ARGPARSE_SUPPORTS_DEFAULTS = True` unconditionally.

Read `tests/ext/test_ext_argparse.py` first to confirm the exact
shape (RESEARCH.md is from 2026-04-29 — minor drift possible but the
sys.version_info check is line ~13).

The likely current shape (verify):

    if (sys.version_info[0] >= 3 and sys.version_info[1] >= 4):
        ARGPARSE_SUPPORTS_DEFAULTS = True
    else:
        ARGPARSE_SUPPORTS_DEFAULTS = False

Replace with:

    ARGPARSE_SUPPORTS_DEFAULTS = True  # Python 3.4+ universal in
                                       # the cement 3.10 floor.

If `import sys` was used solely for this shim, also remove the
import (ruff's --select F401 would flag a leftover unused-sys import,
but F is already enabled — verify `pdm run ruff check
tests/ext/test_ext_argparse.py` exits clean post-fix).

After fix:
  - `pdm run ruff check cement/ tests/ --select YTT` -> 0 violations.
  - `pdm run ruff check cement/ tests/` -> 0 violations of ANY family.
    (This is the final ruff fix in Plan 02 — full clean is the
    target.)
  - `make comply-ruff` -> 0.
  - `make test` -> 0 (the test file's behavior is unchanged; the
    constant resolves to True either way on py310+).

COMMIT:
  Subject (≤ 78): `fix(lint): resolve YTT203 sys-version-info`
  Body (≤ 78/line):

      Removes stale sys.version_info[0] >= 3 and sys.version_info[1]
      >= 4 shim at tests/ext/test_ext_argparse.py:13. Python 3.4+ is
      universal under the new 3.10 floor; ARGPARSE_SUPPORTS_DEFAULTS
      set to True unconditionally. Closes the last ruff fallout from
      the 0.15 bump.

      Per D-04 one-commit-per-family. Phase 1 ruff segment is now
      fully clean; Plan 03 begins the mypy bump.
  </action>
  <verify>
    <automated>
pdm run ruff check cement/ tests/ --select YTT 2>&1 |
  grep -qE 'All checks passed|^$' &&
make comply-ruff &&
make test &&
! grep -E 'sys\.version_info\[0\] >= 3 and sys\.version_info\[1\] >= 4' \
  tests/ext/test_ext_argparse.py &&
grep -q 'ARGPARSE_SUPPORTS_DEFAULTS = True' \
  tests/ext/test_ext_argparse.py &&
git log -1 --pretty=%s | grep -E '^fix\(lint\): resolve YTT203' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
Zero YTT-family violations on `--select YTT`. The full `make
comply-ruff` exits 0 — Plan 02 ruff segment is GREEN. `make test`
exits 0. tests/ext/test_ext_argparse.py no longer contains the
stale py3.4 shim. Plan 03 (mypy) is unblocked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 02 is config + lint-fix only. No runtime code paths added; existing code modified mechanically per ruff's recommendations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Lint rule surface drifting via `pdm update` | mitigate | The codification (D-08 hybrid: extend-select + audited ignore + ~= pin + preview=false) IS the security control — new rule activations now require deliberate edits to pyproject.toml rather than silent absorption. |
| T-01-04 | Tampering | Supply chain on ruff package | mitigate | `~=0.15.12` compatible-release pin allows patch updates within 0.15.x but excludes 0.16+. Combined with `pdm.lock` (Phase 2 owns), exact resolution is captured. |
| T-01-05 | Repudiation | Per-rule-family fix commits | accept | D-04 one-commit-per-family makes bisect possible — every rule-fix is independently revertable, and every fix is annotated with the rule code in the commit subject. This is the audit trail. |

No new attack surface introduced — all changes are config or mechanical
lint cleanup (renames, # noqa annotations, import re-ordering).
</threat_model>

<verification>
1. **Per-task verify** is encoded in each `<verify>` block above. Run
   after every commit.
2. **Plan 02 boundary check** (after Task 9):
   - `make comply-ruff` exits 0
   - `make test` exits 0
   - `git log --oneline | head -10` shows Tasks 1-9 in order with the
     expected Conventional Commit subjects
3. **D-15 coupling verify:**
   - `git log -p pyproject.toml` shows the ruff pin update LANDS in
     the same commit as the preview flip and extend-select expansion
     (subject `chore: bump ruff to 0.15`).
4. **Drift detection verify (D-08):**
   - `pdm run ruff config | grep -E "^lint\."` shows preview = false,
     extend-select = the 11-family list, ignore = with at least the
     C901 entry (and possibly A001/A002).
5. **Public API preservation verify (D-13 + 3.0.x no-breakage):**
   - `git diff main..HEAD --stat cement/` should show file-level
     edits but no removals/additions of public symbols. A targeted
     check: `git diff main..HEAD cement/ | grep -E '^[-+]\s*(class
     |def )' | grep -v '^[-+]\s*(class _|def _)'` — non-private
     public class/def signatures are EXPECTED to be unchanged. Any
     diff here flags a potential public-API break — investigate.
</verification>

<success_criteria>
- ruff `~=0.15.12` pinned in pyproject.toml `[dependency-groups] dev` (TOOL-01)
- `[tool.ruff.lint] preview = false` (D-09, TOOL-04)
- `[tool.ruff.lint] extend-select` enumerates all 11 families per D-06 (TOOL-04)
- AUDIT POINT comment present in `[tool.ruff.lint]` (D-08, TOOL-04)
- `make comply-ruff` exits 0 across `cement/` and `tests/` (TOOL-01, ROADMAP success criterion #1)
- 9 atomic commits land in this plan, in the order: `chore: bump ruff` -> 8 `fix(lint):` family fixes
- Each commit subject ≤ 78 chars; each body line ≤ 78 chars (CLAUDE.md)
- D-15 coupling held: ruff pin lands with the bump commit
- D-13 strict-minimum held: no `Optional[X]` -> `X | None`, no
  `Dict` -> `dict`, no `from __future__ import annotations` removal,
  no f-string conversion, no pathlib migration
- `make test` exits 0 (coverage held; non-functional changes only) (TOOL-01)
- No public API signature changes (3.0.x no-breakage)
</success_criteria>

<output>
After completion, create
`.planning/phases/01-tooling-baseline-python-matrix/01-02-SUMMARY.md`
documenting:
- Total ruff violations resolved (broken down by family)
- Resolution path chosen for A-family (per-call noqa vs broad-ignore)
- Resolution path chosen for N806 (per-file-ignore on tests/)
- C901 absorbed-in-ignore decision (with Phase 3 cross-reference)
- Final state of `[tool.ruff.lint] ignore` list
- Confirmation that `make comply-ruff` and `make test` are GREEN
- Note: mypy still on `>=1.9.0` floor — Plan 03 begins the mypy bump
</output>
