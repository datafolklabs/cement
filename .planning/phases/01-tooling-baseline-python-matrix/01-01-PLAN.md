---
phase: 01-tooling-baseline-python-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/build_and_test.yml
  - Makefile
  - docker-compose.yml
  - docker/Dockerfile.dev-py39
  - cement/cli/templates/generate/todo-tutorial/Dockerfile
  - cement/cli/templates/generate/project/Dockerfile
  - README.md
  - .travis.yml
autonomous: true
requirements:
  - PYVER-01
  - PYVER-02
  - PYVER-03
must_haves:
  truths:
    - "pyproject.toml declares requires-python >= 3.10 and target-version py310"
    - "mypy python_version is 3.10 in pyproject.toml"
    - "GitHub Actions test-all matrix lists 3.10..3.14 + pypy3.10 (no 3.9)"
    - "Makefile dev target no longer references cement-py39"
    - "docker-compose.yml has no cement-py39 service block"
    - "docker/Dockerfile.dev-py39 no longer exists in repo"
    - "Both generate-template Dockerfiles use FROM python:3.10-alpine"
    - "README.md has no cement-py39 / Python 3.9 references"
    - ".travis.yml is removed (or both 3.8 and 3.9 entries are dropped)"
    - "make comply exits 0 on the new floor (Step 1 keeps green; tool versions unchanged)"
    - "make test exits 0 on the new floor"
  artifacts:
    - path: "pyproject.toml"
      provides: "Python version floor + tool target versions"
      contains: "requires-python = \">=3.10\""
    - path: ".github/workflows/build_and_test.yml"
      provides: "CI matrix definition without 3.9"
      contains: "[\"3.10\", \"3.11\", \"3.12\", \"3.13\", \"3.14\", \"pypy3.10\"]"
    - path: "cement/cli/templates/generate/project/Dockerfile"
      provides: "Generated-project Dockerfile on Python 3.10"
      contains: "FROM python:3.10-alpine"
    - path: "cement/cli/templates/generate/todo-tutorial/Dockerfile"
      provides: "Tutorial template Dockerfile on Python 3.10"
      contains: "FROM python:3.10-alpine"
  key_links:
    - from: "pyproject.toml requires-python"
      to: "GitHub Actions matrix python-version"
      via: "atomic D-05 commit"
      pattern: "both updated in same commit"
    - from: "Makefile dev target"
      to: "docker-compose.yml services"
      via: "cement-py39 removed in lockstep with Dockerfile.dev-py39 deletion"
      pattern: "no orphan service references after delete"
---

<objective>
Step 1 of Phase 1 (D-01): drop Python 3.9 from EVERY surface in a single
atomic commit per D-05. This is mechanical — version-string substitutions
and one file deletion. NO tool version bumps (those are Plans 02-04). NO
modernization (D-13). After this commit, `make comply && make test` MUST
still exit 0 on the existing tool versions (ruff 0.3.2 floor, mypy 1.9.0
floor, pytest 4.3.1 floor — current `>=` floors hold).

Purpose: Unblock the rest of the phase. Tool bumps in Plans 02-04 will
target py310 (the new floor). PYVER-01/02/03 close on this commit.

Output: One Conventional Commit covering all 9 file changes, subject:
`chore: drop python 3.9 from supported matrix`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md
@.planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md
@.planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md
@.planning/phases/01-tooling-baseline-python-matrix/01-VALIDATION.md
@CLAUDE.md
@pyproject.toml
@.github/workflows/build_and_test.yml
@Makefile
@docker-compose.yml

<interfaces>
<!-- Exact target text. Executor performs in-place edits — no codebase
     exploration required. RESEARCH.md § "Runtime State Inventory" and
     PATTERNS.md § "Pattern Assignments" are the verified inventory. -->

pyproject.toml line 19 — current vs target:
  current: requires-python = ">=3.9"
  target:  requires-python = ">=3.10"

pyproject.toml line 72 — current vs target:
  current: target-version = "py39"
  target:  target-version = "py310"

pyproject.toml line 110 — current vs target:
  current: python_version = "3.9"
  target:  python_version = "3.10"

.github/workflows/build_and_test.yml line 68 — current vs target:
  current: python-version: ["3.9", "3.10", "3.11", "3.12", "3.13", "3.14", "pypy3.10"]
  target:  python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "pypy3.10"]

Makefile line 16 — DELETE this line entirely:
  docker compose exec cement-py39 pdm install

docker-compose.yml lines 56-61 — DELETE this whole block:
  cement-py39:
    <<: *DEFAULTS
    image: "cement:dev-py39"
    build:
      context: .
      dockerfile: docker/Dockerfile.dev-py39

docker/Dockerfile.dev-py39 — DELETE entire file.

cement/cli/templates/generate/todo-tutorial/Dockerfile line 1:
  current: FROM python:3.9-alpine
  target:  FROM python:3.10-alpine

cement/cli/templates/generate/project/Dockerfile line 1:
  current: FROM python:3.9-alpine
  target:  FROM python:3.10-alpine

README.md edits (lines 45, 136, 142, 152, 154):
  line 45 current:   - Tested on Python 3.9+
  line 45 target:    - Tested on Python 3.10+
  line 136 current:  example mentions `cement-py39`
  line 136 target:   replace cement-py39 -> cement-py310 (keep parity with surviving services)
  line 142 current:  cement_cement-py39_1    /bin/bash                        Up
  line 142 target:   cement_cement-py310_1   /bin/bash                        Up
  line 152 current:  $ docker-compose exec cement-py39 /bin/bash
  line 152 target:   $ docker-compose exec cement-py310 /bin/bash
  line 154 current:  |> cement-py39 <| src #
  line 154 target:   |> cement-py310 <| src #

.travis.yml — DELETE entire file (RESEARCH.md Pitfall 7 + Open Question 2
recommend deletion: Travis is not active CI; GitHub Actions is. Deletion
satisfies D-05 "ALL 3.9 traces simultaneously" cleanly).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic Python 3.9 drop across all traces (single commit per D-05)</name>
  <files>
    pyproject.toml,
    .github/workflows/build_and_test.yml,
    Makefile,
    docker-compose.yml,
    docker/Dockerfile.dev-py39,
    cement/cli/templates/generate/todo-tutorial/Dockerfile,
    cement/cli/templates/generate/project/Dockerfile,
    README.md,
    .travis.yml
  </files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (locked decisions, especially D-05 atomic commit requirement, D-13 strict-minimum, D-14 keep __future__ imports),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Runtime State Inventory" — exhaustive 3.9-trace inventory with file:line + current vs target),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Pattern Assignments" — exact current vs target text for each file),
    pyproject.toml,
    .github/workflows/build_and_test.yml,
    Makefile,
    docker-compose.yml,
    cement/cli/templates/generate/todo-tutorial/Dockerfile,
    cement/cli/templates/generate/project/Dockerfile,
    README.md,
    CLAUDE.md (§ "Commit Conventions" — Conventional Commits, 78-char wrap, use make commit)
  </read_first>
  <action>
Perform ALL of the following edits in a single working tree, then create
ONE atomic commit (D-05). Do NOT split into multiple commits — D-05 is
explicit: "ALL 3.9 traces simultaneously". Do NOT touch any other 3.9
trace flagged in RESEARCH.md § "NOT 3.9 traces" (e.g., `cement/utils/fs.py`
`from __future__ import annotations` stays per D-14;
`tests/ext/test_ext_argparse.py:13` is YTT203 fallout deferred to Plan 02).

EDITS (use Edit tool for in-place line edits, Bash for file deletions):

1. `pyproject.toml`:
   - Line 19: `requires-python = ">=3.9"` -> `requires-python = ">=3.10"`
   - Line 72: `target-version = "py39"` -> `target-version = "py310"`
   - Line 110: `python_version = "3.9"` -> `python_version = "3.10"`
   - Do NOT touch `[tool.ruff.lint]` `preview`, `extend-select`, or
     `[dependency-groups] dev` pins — those are Plan 02/03/04 territory.

2. `.github/workflows/build_and_test.yml`:
   - Line 68: drop `"3.9"` from the matrix list. New value:
     `python-version: ["3.10", "3.11", "3.12", "3.13", "3.14", "pypy3.10"]`

3. `Makefile`:
   - DELETE line 16 entirely: `docker compose exec cement-py39 pdm install`
   - The remaining `cement-py310`, `-py311`, `-py312`, `-py313` lines stay.

4. `docker-compose.yml`:
   - DELETE the entire `cement-py39:` service block (lines 56-61, six
     lines including the service name line). The `cement-py310:` block
     becomes the new lowest-Python service.

5. `docker/Dockerfile.dev-py39`:
   - DELETE the entire file: `git rm docker/Dockerfile.dev-py39`

6. `cement/cli/templates/generate/todo-tutorial/Dockerfile`:
   - Line 1: `FROM python:3.9-alpine` -> `FROM python:3.10-alpine`
   - Treat this and `project/Dockerfile` as a lockstep pair — both MUST
     change in this commit (per RESEARCH.md PATTERNS § "lockstep pair").

7. `cement/cli/templates/generate/project/Dockerfile`:
   - Line 1: `FROM python:3.9-alpine` -> `FROM python:3.10-alpine`

8. `README.md`:
   - Line 45: `- Tested on Python 3.9+` -> `- Tested on Python 3.10+`
   - Line 136: replace `cement-py39` with `cement-py310` in the
     parenthetical example.
   - Lines 142, 152, 154: replace each `cement-py39` reference with
     `cement-py310` (keep example shape; this teaches the
     docker-compose-exec pattern, the version is incidental).

9. `.travis.yml`:
   - DELETE entire file: `git rm .travis.yml` (per RESEARCH.md Pitfall 7
     and Open Question 2 — Travis is not active CI; GitHub Actions is
     the live surface; deletion is the cleanest move and satisfies D-05
     "ALL 3.9 traces simultaneously"). Do NOT keep .travis.yml in any
     edited form.

VERIFICATION before commit:
  - Run `make comply` — MUST exit 0 (current tool floors, no fallout
    expected because tool versions are unchanged in this plan).
  - Run `make test` — MUST exit 0 (Phase 1 Step 1 is mechanical, no
    runtime behavior changes).
  - Run the PYVER-03 audit grep:
      grep -rn "3\.9\|sys\.version_info.*9\|py39\|python:3\.9" \
          cement/ tests/ pyproject.toml .github/ Makefile \
          docker-compose.yml docker/ \
          | grep -v 'from __future__ import annotations' \
          | grep -v 'cement/utils/version.py.*sys\.version_info\[0:3\]'
    Expected output: EMPTY. (`from __future__ import annotations` and
    `cement/utils/version.py` runtime version reporting are NOT 3.9
    traces per D-14 and RESEARCH.md NOT-3.9-traces inventory.)

COMMIT (use `make commit` per CLAUDE.md, OR git commit with HEREDOC):
  Subject: `chore: drop python 3.9 from supported matrix`
  Body (each line wrapped at 78 chars):

      Drops Python 3.9 from every project surface in lockstep per
      Phase 1 D-05 (atomic commit covers ALL 3.9 traces).

      Touched:
        - pyproject.toml: requires-python, ruff target-version,
          mypy python_version  ->  3.10
        - .github/workflows/build_and_test.yml: drop "3.9" from
          test-all matrix
        - Makefile: drop cement-py39 from dev target
        - docker-compose.yml: drop cement-py39 service block
        - docker/Dockerfile.dev-py39: deleted
        - cement/cli/templates/generate/{todo-tutorial,project}/
          Dockerfile: FROM python:3.9-alpine -> 3.10-alpine
        - README.md: prose + cement-py39 examples -> py310
        - .travis.yml: deleted (GitHub Actions is the active CI;
          Travis was dead infrastructure)

      Closes PYVER-01, PYVER-02, PYVER-03.

      Tool version bumps and the resulting lint/type fallout follow
      in subsequent commits (Plans 02-04).
  </action>
  <verify>
    <automated>
make comply && make test &&
! grep -rn '3\.9\|py39\|python:3\.9' \
  pyproject.toml .github/ Makefile docker-compose.yml docker/ \
  cement/cli/templates/generate/ README.md 2>/dev/null &&
test ! -e docker/Dockerfile.dev-py39 &&
test ! -e .travis.yml &&
grep -q 'requires-python = ">=3.10"' pyproject.toml &&
grep -q 'target-version = "py310"' pyproject.toml &&
grep -q 'python_version = "3.10"' pyproject.toml &&
grep -qF '"3.10", "3.11", "3.12", "3.13", "3.14", "pypy3.10"' \
  .github/workflows/build_and_test.yml &&
grep -q 'FROM python:3.10-alpine' \
  cement/cli/templates/generate/project/Dockerfile &&
grep -q 'FROM python:3.10-alpine' \
  cement/cli/templates/generate/todo-tutorial/Dockerfile &&
! grep -q 'cement-py39' Makefile docker-compose.yml &&
! grep -q 'Tested on Python 3.9' README.md &&
git log -1 --pretty=%s | grep -E '^chore: drop python 3\.9'
    </automated>
  </verify>
  <done>
Single atomic commit lands per D-05. `make comply` and `make test` both
exit 0 on existing tool floors. PYVER-03 grep audit returns empty.
Commit subject is exactly `chore: drop python 3.9 from supported matrix`,
body wrapped at 78 chars per CLAUDE.md. Plans 02-04 can now build on the
py310 floor.

  - pyproject.toml `requires-python = ">=3.10"`
  - pyproject.toml `target-version = "py310"`
  - pyproject.toml `python_version = "3.10"`
  - build_and_test.yml matrix has no `"3.9"`
  - Makefile dev target has no `cement-py39`
  - docker-compose.yml has no `cement-py39:` block
  - docker/Dockerfile.dev-py39 removed
  - both generate-template Dockerfiles `FROM python:3.10-alpine`
  - README.md has no Python 3.9 references
  - .travis.yml removed
  - `git log -1` subject matches `chore: drop python 3.9 from supported matrix`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 1 Step 1 is config/metadata only. No runtime code paths added or modified. No new external surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure / EoP | Python 3.9 stdlib (EOL Oct 2025) | mitigate | This plan removes 3.9 from supported matrix per PYVER-01. Future cement releases will not run on 3.9, eliminating exposure to unpatched stdlib CVEs in 3.9. |
| T-01-02 | Tampering | Generate-template Dockerfiles consumed by downstream `cement generate` users | accept | Bump from `python:3.9-alpine` to `python:3.10-alpine` is standard hygiene. No new attack surface; both base images are official Python images on alpine (same supply chain). User-facing change flagged for Phase 6 DOCS-03 changelog. |

No new attack surface introduced — config/metadata-only changes. RESEARCH.md
§ "Security Domain" assesses Phase 1 against ASVS V1-V14: only V14
(Configuration) applies, and the audit-point hybrid pattern (Plans 02-03)
is the relevant control, not Plan 01.
</threat_model>

<verification>
1. **Phase Step 1 boundary check:** `make comply && make test` exit 0 on
   the existing tool floors. (Tool versions unchanged in this plan; only
   3.9 traces drop.)
2. **PYVER-03 audit grep** (RESEARCH.md § "Code Examples — Verification
   grep for PYVER-03") returns empty after filtering the explicitly-
   deferred `from __future__ import annotations` and `version.py`
   `sys.version_info[0:3]` runtime reporter.
3. **Atomic-commit shape:** `git log -1 --pretty=%s` exactly equals
   `chore: drop python 3.9 from supported matrix`. Body lines all ≤ 78
   chars (verifiable with `git log -1 --format=%b | awk 'length>78'` —
   should print nothing).
4. **CI matrix smoke** (post-merge): `gh pr checks <PR>` shows the
   `test-all` matrix running 3.10/3.11/3.12/3.13/3.14/pypy3.10 (no 3.9
   row).
</verification>

<success_criteria>
- pyproject.toml `requires-python = ">=3.10"` (PYVER-02)
- pyproject.toml `target-version = "py310"` (D-05)
- pyproject.toml `python_version = "3.10"` (D-05)
- `.github/workflows/build_and_test.yml` matrix omits `"3.9"` (PYVER-01)
- `Makefile` `dev` target omits `cement-py39` line (PYVER-01)
- `docker-compose.yml` `cement-py39` service block removed (PYVER-01)
- `docker/Dockerfile.dev-py39` deleted (PYVER-01)
- Both generate-template Dockerfiles `FROM python:3.10-alpine` (PYVER-02)
- README.md has no Python 3.9 references (PYVER-02)
- `.travis.yml` deleted (D-05 atomic commit completeness)
- `make comply && make test` exit 0
- PYVER-03 audit grep returns empty
- One Conventional Commit, subject ≤ 78 chars, body ≤ 78 chars per line
</success_criteria>

<output>
After completion, create `.planning/phases/01-tooling-baseline-python-matrix/01-01-SUMMARY.md`
documenting:
- Files touched in the atomic D-05 commit
- Confirmation that PYVER-01/02/03 closed
- The PYVER-03 grep audit result (empty)
- The current `make comply && make test` baseline result
- Note: tool versions UNCHANGED in this plan; Plan 02 begins the bump sequence
</output>
