---
phase: 01-tooling-baseline-python-matrix
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - TOOL-03
must_haves:
  truths:
    - "pytest is bumped to >=9.0.3 in [dependency-groups] dev"
    - "pytest-cov is bumped to >=7.1.0 in [dependency-groups] dev"
    - "coverage is bumped to >=7.13.5 in [dependency-groups] dev"
    - "All three pins use floor (>=) not compatible-release (~=) per D-12"
    - "make test exits 0 with pytest 9.x / pytest-cov 7.x / coverage 7.13.x active"
    - "No deprecation warnings emitted by pytest, pytest-cov, or coverage themselves during make test (TOOL-03 / ROADMAP success criterion #3)"
    - "Coverage gate held — 100% coverage on cement/ preserved post-bump (Pitfall 3 verification)"
    - "make comply still exits 0 (no fallout from the test-tool bump)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Bumped pytest+pytest-cov+coverage floors per D-12 hybrid pin pattern"
      contains: "pytest>=9.0.3"
  key_links:
    - from: "pyproject.toml [dependency-groups] dev"
      to: "make test"
      via: "pdm install resolves new floors; pytest 9.x picks up natively"
      pattern: "floor pin admits stable patch updates without forcing minor-bump audits (D-12 — pytest breaks rarely)"
    - from: "pytest 9.0 dropping Python 3.9"
      to: "Phase 1 PYVER-01 (Plan 01)"
      via: "natural alignment — pytest 9.0 itself dropped 3.9; the bump confirms Plan 01's floor"
      pattern: "tool floor and Python floor are co-validated by pytest 9.0"
---

<objective>
Step 2 final segment of Phase 1 (D-03 strict serial sequence: ruff
done, mypy done, pytest+pytest-cov+coverage now). Bump pytest from
`>=4.3.1` to `>=9.0.3`, pytest-cov from `>=2.6.1` to `>=7.1.0`, and
coverage from `>=4.5.3` to `>=7.13.5`. All three are FLOOR pins (`>=`)
per D-12 hybrid — these tools break rarely so floor is the right
posture; only ruff and mypy use compatible-release (`~=`).

Per RESEARCH.md (verified this session 2026-04-29):
- pytest 9.0.3 surfaces no project-internal deprecation noise on
  cement's test suite. The 1674 warnings observed are cement's OWN
  intentional `DeprecationWarning`s already asserted by
  `tests/core/test_deprecations.py` — not pytest's deprecations.
  TOOL-03 success criterion is "no deprecation warnings emitted BY
  pytest/pytest-cov/coverage themselves" (ROADMAP § success criterion
  #3) — those are zero, verified.
- pytest-cov 7.0 dropped subprocess auto-measurement. cement's
  `cement/utils/shell.py` exec_cmd functions run subprocesses but the
  TEST PROCESS is what's measured (not forked children) — Pitfall 3
  flags this as no-op risk. The verification is that 100% coverage
  HOLDS post-bump on `cement/utils/shell.py` and `cement/cli/` (any
  silent drop indicates the assumption was wrong).
- pytest 9.0 itself drops Python 3.9 — this naturally aligns with
  PYVER-01 (Plan 01).

Per D-15: pin bumps land in a single commit (per the per-tool D-15
clause — but here all three are floor-only and tightly coupled,
landing as one `chore: bump pytest+pytest-cov+coverage` commit per
PATTERNS.md § "D-15 coupling — version pin lands with version bump").

Output: 1 atomic commit:
  1. `chore: bump pytest+pytest-cov+coverage`

This is the FINAL commit of Phase 1. The phase exit criterion is the
full `make test` green on this commit, and CI green across the
3.10/3.11/3.12/3.13/3.14/pypy3.10 matrix on the PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md
@.planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md
@.planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md
@.planning/phases/01-tooling-baseline-python-matrix/01-VALIDATION.md
@.planning/phases/01-tooling-baseline-python-matrix/01-03-SUMMARY.md
@CLAUDE.md
@pyproject.toml

<interfaces>
<!-- Target [dependency-groups] dev block AFTER Plan 04 (final shape).
     Plan 02 set ruff~=0.15.12; Plan 03 set mypy~=1.20.2; Plan 04 sets
     the three test-tool floors. -->

[dependency-groups]
dev = [
    "pytest>=9.0.3",                # was >=4.3.1  (D-12: floor)
    "pytest-cov>=7.1.0",            # was >=2.6.1  (D-12: floor)
    "coverage>=7.13.5",             # was >=4.5.3  (D-12: floor)
    "mypy~=1.20.2",                 # set in Plan 03; unchanged
    "ruff~=0.15.12",                # set in Plan 02; unchanged
    "mock>=5.1.0",                  # unchanged (D-12: floor)
    "pypng>=0.20220715.0",          # unchanged (D-12: floor)
    "requests>=2.31.0",             # unchanged (D-12: floor)
    "commitizen>=4.10.1",           # unchanged (D-12: floor)
]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: chore — bump pytest + pytest-cov + coverage to 9.0.3 / 7.1.0 / 7.13.5 (D-15 coupling)</name>
  <files>pyproject.toml</files>
  <read_first>
    .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-03, D-12, D-15),
    .planning/phases/01-tooling-baseline-python-matrix/01-RESEARCH.md (§ "Standard Stack" — pytest 9.0.3, pytest-cov 7.1.0, coverage 7.13.5 verified; § "Pitfall 2: pytest 9.0 PytestRemovedIn9Warning"; § "Pitfall 3: pytest-cov 7.0 dropped subprocess measurement"),
    .planning/phases/01-tooling-baseline-python-matrix/01-PATTERNS.md (§ "Pattern: D-15 coupling"),
    pyproject.toml,
    CLAUDE.md (Conventional Commits, 78-char wrap)
  </read_first>
  <action>
Edit `pyproject.toml` `[dependency-groups] dev` block. Three line
changes, ALL in this single commit:

  - From: `"pytest>=4.3.1",`        To: `"pytest>=9.0.3",`
  - From: `"pytest-cov>=2.6.1",`    To: `"pytest-cov>=7.1.0",`
  - From: `"coverage>=4.5.3",`      To: `"coverage>=7.13.5",`

Do NOT touch:
  - `mypy~=1.20.2` (set in Plan 03)
  - `ruff~=0.15.12` (set in Plan 02)
  - `mock>=5.1.0`, `pypng>=0.20220715.0`, `requests>=2.31.0`,
    `commitizen>=4.10.1` (per D-12, floor pins on stable tools, not
    in TOOL-* scope this phase)

Do NOT touch `[tool.pytest.ini_options]` — RESEARCH.md confirms no
config changes needed for pytest 9.0 in cement's test surface.

PRE-BUMP VERIFICATION (Pitfall 2 mitigation):
Before pushing the change, verify on CURRENT (pre-bump) pytest 8.x:

    pdm run pytest tests/core 2>&1 | grep -i 'PytestRemovedIn'

Expected: zero hits (confirmed in RESEARCH.md). If hits appear,
those are pre-existing warnings that pytest 9.0 will turn into errors
— resolve them BEFORE bumping (they would block the bump). RESEARCH.md
verified this clean state on 2026-04-29; if it has drifted since,
treat the resolution as additional in-scope fallout under TOOL-03 (do
NOT defer to a future phase — D-13 strict-minimum says fix only what
the bumped tools fail on, but pytest 9.0 turning warnings into errors
IS what the bump fails on).

Then run `pdm install` to refresh the local environment to the new
floors. Confirm:

  - `pdm run pytest --version` shows 9.0.x
  - `pdm run python -c "import pytest_cov; print(pytest_cov.__version__)"`
    shows 7.x
  - `pdm run python -c "import coverage; print(coverage.__version__)"`
    shows 7.13.x

POST-BUMP VERIFICATION (TOOL-03 success criterion + Pitfalls 2 and 3):

  1. Full test suite: `make test` MUST exit 0.

  2. Coverage gate: post-bump coverage on `cement/` MUST equal pre-bump
     coverage on `cement/` (Pitfall 3 — pytest-cov 7.0 dropped
     subprocess auto-measurement; if cement was secretly relying on
     this, coverage will drop). To validate:

        # Capture post-bump coverage report
        pdm run pytest --cov=cement tests --cov-report=term \
            2>&1 | tail -20

     Compare against the pre-bump baseline (capture before this commit
     by checking out HEAD~1 once, running, restoring). If
     `cement/utils/shell.py` or `cement/cli/main.py` shows a coverage
     drop, Pitfall 3 hit — apply the fix per RESEARCH.md
     `[run] patch = subprocess` opt-in via a `.coveragerc` (or equivalent
     `[tool.coverage.run]` block in pyproject.toml). If no drop, ship
     as-is.

  3. Pytest-internal deprecation noise: TOOL-03 / ROADMAP success
     criterion #3 says "make test runs without deprecation warnings
     emitted by pytest, pytest-cov, or coverage themselves." Verify:

        pdm run pytest tests/core \
            -W "error::PytestRemovedIn10Warning" \
            -W "error::PytestDeprecationWarning"

     MUST exit 0. If pytest emits its own deprecation warnings (NOT
     cement's intentional `DeprecationWarning`s), those are TOOL-03
     fallout — fix them in this same commit (or a `fix(test):` follow-
     up if the surface is non-trivial; per D-04 the family-split
     discipline applies but for the test-runner segment it's a single
     fix(test): commit if needed). RESEARCH.md verified zero such
     warnings on 2026-04-29; treat any new ones as drift to fix.

  4. ruff + mypy still green: `make comply` MUST exit 0 (the test-tool
     bump should not affect lint or types, but verify).

  5. `pdm.lock` policy: per RESEARCH.md Pitfall 4, leave the lockfile
     for Phase 2 (DEPS-01) to regenerate. If `pdm install` modifies
     `pdm.lock`, run `git checkout pdm.lock` to revert it before the
     commit. The Phase 1 PR carries pyproject.toml changes only;
     pdm.lock regen is Phase 2's job.

COMMIT (single commit per the test-tool D-15 coupling):
  Subject (≤ 78 chars): `chore: bump pytest+pytest-cov+coverage`
  Body (each line ≤ 78 chars):

      Bumps pytest 4.3.1 -> 9.0.3, pytest-cov 2.6.1 -> 7.1.0,
      coverage 4.5.3 -> 7.13.5. All three use floor (>=) pins per
      D-12 hybrid — these tools break rarely. pytest 9.0 itself
      drops Python 3.9, naturally aligning with Plan 01's PYVER-01.

      Verified per RESEARCH.md Pitfalls 2 and 3:
        - No pre-existing PytestRemovedIn warnings (zero turned into
          errors by pytest 9.0).
        - pytest-cov 7.0 subprocess-measurement removal is no-op for
          cement; 100% coverage gate holds post-bump.

      No deprecation warnings emitted by pytest/pytest-cov/coverage
      themselves on the cement test suite (TOOL-03 / ROADMAP success
      criterion #3).

      Closes TOOL-03. Phase 1 Step 2 complete; this is the final
      Phase 1 commit before PR open.
  </action>
  <verify>
    <automated>
grep -q 'pytest>=9.0.3' pyproject.toml &&
grep -q 'pytest-cov>=7.1.0' pyproject.toml &&
grep -q 'coverage>=7.13.5' pyproject.toml &&
pdm run pytest --version | grep -E '^pytest 9\.0\.' &&
pdm run python -c 'import pytest_cov; assert pytest_cov.__version__.startswith("7.")' &&
pdm run python -c 'import coverage; assert coverage.__version__.startswith("7.13")' &&
make comply &&
make test &&
pdm run pytest tests/core \
  -W error::PytestRemovedIn10Warning \
  -W error::PytestDeprecationWarning >/dev/null 2>&1 &&
git log -1 --pretty=%s | grep -E '^chore: bump pytest\+pytest-cov\+coverage' &&
git log -1 --format=%b | awk 'length>78 {exit 1}'
    </automated>
  </verify>
  <done>
pytest 9.0.x, pytest-cov 7.1.x, coverage 7.13.x active. `make comply
&& make test` exit 0. No pytest/pytest-cov/coverage internal
deprecation warnings emitted (verified via `-W
error::PytestRemovedIn10Warning -W error::PytestDeprecationWarning`).
100% coverage gate holds. Commit subject and body conform to
Conventional Commits + 78-char wrap. **Phase 1 implementation
complete** — open PR after this commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Plan 04 is dev-dep version-pin updates. No runtime code path or production config changed. Test-tool versions only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Coverage gate silently dropping due to pytest-cov 7.0 subprocess change | mitigate | Per RESEARCH.md Pitfall 3: explicit baseline-vs-post-bump coverage comparison in this task's verification. If cement/utils/shell.py or cement/cli/main.py shows coverage drop, apply `[run] patch = subprocess` opt-in. Verified no-op risk in research session. |
| T-01-09 | Tampering | pytest 9.0 turning previously-warning code into hard errors | mitigate | Pre-bump grep for `PytestRemovedIn*` warnings; resolve any hits BEFORE bumping. Verified zero hits in research session. |
| T-01-10 | Tampering | Supply chain on test-tool packages | accept | Floor pins (`>=`) on three rarely-breaking tools matches D-12 hybrid posture. `pdm.lock` (Phase 2 owns) captures exact resolution. The phase boundary is intentionally permissive on patch updates for these tools — drift risk is on ruff/mypy, not pytest-family. |

No new attack surface introduced — pyproject.toml dev-deps only.
</threat_model>

<verification>
1. **Per-task verify** is encoded in the `<verify>` block above.
2. **Plan 04 boundary check (= Phase 1 phase exit):**
   - `make comply` exits 0
   - `make test` exits 0 with NO pytest/pytest-cov/coverage internal
     deprecation warnings (TOOL-03 / ROADMAP success criterion #3)
   - 100% coverage on `cement/` held vs pre-Plan-04 baseline (Pitfall
     3 — explicit verification step)
   - `git log -p pyproject.toml` shows pytest, pytest-cov, coverage
     all bumped in the same commit (`chore: bump
     pytest+pytest-cov+coverage`)
3. **Phase 1 cumulative verify** (after Plan 04 Task 1):
   - PYVER-01: `! grep -q '"3\.9"' .github/workflows/build_and_test.yml`
   - PYVER-02: `grep -q 'requires-python = ">=3.10"' pyproject.toml`
   - PYVER-03: full grep audit per RESEARCH.md `§ "Code Examples —
     Verification grep for PYVER-03"` returns empty
   - TOOL-01: `make comply-ruff` exits 0 with ruff 0.15.x
   - TOOL-02: `make comply-mypy` exits 0 with mypy 1.20.x
   - TOOL-03: `make test` exits 0 with pytest 9.x / pytest-cov 7.x /
     coverage 7.13.x AND no pytest-internal deprecation warnings
   - TOOL-04: pyproject.toml `[tool.ruff.lint]` and `[tool.mypy]` both
     show AUDIT POINT comments and explicitly enumerated rule sets;
     no implicit defaults
4. **PR gate:** open PR; CI matrix (3.10, 3.11, 3.12, 3.13, 3.14,
   pypy3.10) MUST be GREEN end-to-end across all rows. Per
   RESEARCH.md Open Question 1: cross-version fallout is empirically
   verified by CI; if pypy3.10 (or any matrix row) fails, that's
   in-scope follow-up fallout under TOOL-* and gets fixed before the
   PR merges (per CONTEXT.md `<deferred>` "Pypy3.10 in CI matrix —
   planner verifies it stays green").
</verification>

<success_criteria>
- pytest `>=9.0.3` pinned in pyproject.toml `[dependency-groups] dev` (TOOL-03)
- pytest-cov `>=7.1.0` pinned (TOOL-03)
- coverage `>=7.13.5` pinned (TOOL-03)
- All three use floor (`>=`), not compatible-release, per D-12
- `make test` exits 0 with the new versions active
- No deprecation warnings emitted BY pytest/pytest-cov/coverage themselves
  during `make test` (TOOL-03 / ROADMAP success criterion #3)
- 100% coverage held on `cement/` (Pitfall 3 verified — coverage gate
  did not silently drop)
- `make comply` still exits 0 (no fallout from test-tool bump)
- Single Conventional Commit, subject ≤ 78 chars, body ≤ 78 chars/line
- D-15 per-tool coupling held (pin and bump in one commit)
- D-13 strict-minimum held (no test-config or test-code modernization)
- **Phase 1 cumulative success criteria #1-#5 (ROADMAP) all hold**
</success_criteria>

<output>
After completion, create
`.planning/phases/01-tooling-baseline-python-matrix/01-04-SUMMARY.md`
documenting:
- pytest, pytest-cov, coverage versions active (must be 9.0.x, 7.1.x,
  7.13.x respectively)
- Pre-bump vs post-bump coverage report comparison (Pitfall 3 evidence
  — coverage held)
- Confirmation of zero PytestRemovedIn / PytestDeprecation warnings
- Final pyproject.toml `[dependency-groups] dev` block snapshot
- Phase 1 close-out: all 5 ROADMAP success criteria checked
- Note: `pdm.lock` was deliberately NOT regenerated; Phase 2 DEPS-01
  owns that
- Hand-off: open PR; CI matrix runs across 3.10/3.11/3.12/3.13/3.14
  + pypy3.10 (Open Question 1 — pypy verified GREEN by CI, OR
  follow-up commit lands here if pypy fails)
</output>
