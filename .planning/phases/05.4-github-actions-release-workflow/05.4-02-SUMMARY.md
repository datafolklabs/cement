---
phase: 05.4-github-actions-release-workflow
plan: 02
subsystem: infra
tags: [github-actions, ci, reusable-workflow, workflow_call, release-gates, windows, macos]

# Dependency graph
requires:
  - phase: 05.4-01
    provides: scripts/cli-smoke-native.py (native cross-OS CLI smoke driver)
provides:
  - .github/workflows/gates.yml — reusable (on: workflow_call) single source of truth for all release gates
  - Six gate jobs (comply, test, test-all, cli-smoke-test, test-core-windows, cli-smoke-native)
  - New Windows core-test and macOS/Windows native-smoke gates that run on every PR (D-14)
  - build_and_test.yml refactored to a thin PR-triggered caller of gates.yml
affects: [release.yml, 05.4-03, 05.4-04, phase-06-release-cut]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Reusable workflow via on: workflow_call with a full-os-matrix boolean input"
    - "Serial fail-fast needs chain preserved; new independent OS jobs run in parallel"
    - "Windows/macOS jobs invoke pdm/python directly, never GNU Make (Pitfall 5)"
    - "Every uses: exact-tag pinned; reuse in-repo pins verbatim (CI-05)"

key-files:
  created:
    - .github/workflows/gates.yml
  modified:
    - .github/workflows/build_and_test.yml

key-decisions:
  - "gates.yml holds ONLY gate jobs — no publish/OIDC surface (T-05.4-04 mitigation); publish stays in release.yml"
  - "full-os-matrix defaults true so PR CI (D-14) and release both exercise Windows/macOS gates"
  - "test-core-windows runs core tests only (no docker services on Windows runners)"
  - "cli-smoke-native matrix: macos-latest + windows-latest across Python 3.10 and 3.14"
  - "PR caller sets full-os-matrix: true and omits secrets: inherit (gates need no secrets, T-05.4-04/06)"

patterns-established:
  - "Extract-and-parameterize: existing serial gate chain copied verbatim into workflow_call, not rewritten (D-13)"
  - "New OS gate jobs gated on if: ${{ inputs.full-os-matrix }} so a caller can opt out"

requirements-completed: [CI-05]

coverage:
  - id: D1
    description: "gates.yml is a valid reusable workflow (on: workflow_call, input full-os-matrix) exposing all six gate jobs with exact-pinned actions and no publish surface"
    requirement: "CI-05"
    verification:
      - kind: automated
        ref: "python -c yaml.safe_load(gates.yml): workflow_call present, six jobs present"
        status: pass
      - kind: automated
        ref: "grep no-floating-refs (@v[0-9]) == 0; grep gh-action-pypi-publish == 0; grep make in new jobs == 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "build_and_test.yml is a thin PR-triggered caller: single gates job uses ./.github/workflows/gates.yml with full-os-matrix: true, no inline gate bodies"
    requirement: "CI-05"
    verification:
      - kind: automated
        ref: "python yaml assert jobs.gates.uses == './.github/workflows/gates.yml'; grep -c runs-on == 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "Windows/macOS gate jobs actually run green on GitHub-hosted runners (deep validation)"
    verification: []
    human_judgment: true
    rationale: "Runner-side green requires pushing the branch and opening a PR so GitHub Actions executes the Windows/macOS jobs; cannot be proven locally. Sampled per VALIDATION.md after the plan wave."

# Metrics
duration: 2min
completed: 2026-07-12
status: complete
---

# Phase 05.4 Plan 02: Reusable Gates Workflow Summary

**Reusable `gates.yml` (`on: workflow_call`) that both PR CI and the future release workflow call as one source of truth for release gates (D-13), adding new Windows core-test and macOS/Windows native-smoke gates that run on every PR (D-14); build_and_test.yml collapsed to a thin caller.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-07-12T17:11:46Z
- **Completed:** 2026-07-12T17:12:52Z
- **Tasks:** 2
- **Files modified:** 2 (1 created, 1 refactored)

## Accomplishments
- Created `.github/workflows/gates.yml` as a reusable `workflow_call` workflow with the single boolean input `full-os-matrix` (default true).
- Extracted the four existing gate jobs (`comply`, `test`, `test-all`, `cli-smoke-test`) verbatim, preserving the serial `needs:` fail-fast chain (Phase 02 D-16).
- Added two NEW cross-OS gates that run in parallel with the matrix: `test-core-windows` (windows-latest, `pdm run pytest --cov=cement.core tests/core`) and `cli-smoke-native` (macos-latest + windows-latest × Python 3.10/3.14, `python scripts/cli-smoke-native.py`) — both gated on `if: ${{ inputs.full-os-matrix }}`, both invoking pdm/python directly (never `make`, Pitfall 5).
- Refactored `build_and_test.yml` into a thin PR caller: a single `gates` job that `uses: ./.github/workflows/gates.yml` with `full-os-matrix: true` — no duplicated gate logic, no top-level env.
- Kept gates.yml free of any publish/OIDC surface (T-05.4-04 mitigation) and every `uses:` exact-tag pinned (CI-05).

## Task Commits

Each task was committed atomically:

1. **Task 1: Create .github/workflows/gates.yml (reusable gate workflow)** - `dc36440d` (ci)
2. **Task 2: Refactor build_and_test.yml to a thin gates caller** - `2242977b` (ci)

## Files Created/Modified
- `.github/workflows/gates.yml` (created) - Reusable release-gate workflow; six jobs; `on: workflow_call` with `full-os-matrix` input.
- `.github/workflows/build_and_test.yml` (refactored) - Thin `on: [pull_request]` caller of gates.yml; 89 lines removed, 6 added.

## Decisions Made
- gates.yml contains ONLY gate jobs; the publish/OIDC job stays in release.yml (later wave) — avoids the reusable-workflow supply-chain elevation risk (T-05.4-04).
- PR caller omits `secrets: inherit` because none of the gates require secrets (T-05.4-04/06).
- `test-core-windows` runs core tests only — Windows runners have no Redis/Memcached/SMTP docker services, so the full `make test` surface is Linux-docker-only by design.
- `cli-smoke-native` uses `pdm build` + `python scripts/cli-smoke-native.py` (the lighter CLI+generate+install-generated tier), not the heavy docker/make depth.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None. Both YAML files parse; all Task 1 and Task 2 acceptance greps returned the expected counts (no floating refs, no `make` in new jobs, no publish surface, no inline `runs-on:` in the caller).

## D-13 Invariant Note
The plan's D-13 structural test (`grep -c 'uses: ./.github/workflows/gates.yml' .github/workflows/*.yml` == 2) is INTENTIONALLY 1 at this wave — this plan supplies the PR caller half; the release.yml caller lands in a later wave (05.4-03/04). This is expected per the plan's own acceptance note, not a deviation.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- gates.yml is ready to be called by release.yml (later wave) via the same `uses: ./.github/workflows/gates.yml` path with `full-os-matrix: true`.
- Deep runner-side validation (Windows/macOS jobs green) is deferred to PR CI when the branch is pushed — sampled per VALIDATION.md "after every plan wave".

## Self-Check: PASSED

- FOUND: `.github/workflows/gates.yml`
- FOUND: `.github/workflows/build_and_test.yml`
- FOUND: `.planning/phases/05.4-github-actions-release-workflow/05.4-02-SUMMARY.md`
- FOUND commit: `dc36440d` (Task 1)
- FOUND commit: `2242977b` (Task 2)

---
*Phase: 05.4-github-actions-release-workflow*
*Completed: 2026-07-12*
