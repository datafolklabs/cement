---
phase: 05.4-github-actions-release-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/testpypi-smoke.py
  - scripts/cli-smoke-native.py
  - scripts/bump_dev_version.py
autonomous: true
requirements: [REL-02, REL-05]
must_haves:
  truths:
    - "`python scripts/testpypi-smoke.py 3.0.16` installs cement from TestPyPI, imports it, runs a minimal App round-trip, and asserts get_version() == the arg"
    - "`python scripts/cli-smoke-native.py` runs on macOS/Windows without Docker and asserts a platform-agnostic version banner (Linux/Darwin/Windows)"
    - "`python scripts/bump_dev_version.py 3.0.17` rewrites the backend VERSION tuple to (3, 0, 17, 'final', 0) and prepends a fresh DEVELOPMENT changelog section"
    - "All three scripts pass `pdm run ruff check scripts/` and `pdm run mypy scripts/*.py`"
  artifacts:
    - scripts/testpypi-smoke.py
    - scripts/cli-smoke-native.py
    - scripts/bump_dev_version.py
  key_links:
    - "cli-smoke-native.py accepts Linux/Darwin/Windows banners (NOT the hardcoded 'Platform Linux' from cli-smoke-test.sh) so it runs on all 3 OS runners"
    - "bump_dev_version.py targets cement/core/backend.py VERSION (the source of truth read by get_version), preserving the trailing '# pragma: nocover' comment"
    - "testpypi-smoke.py installs PLAIN cement==X.Y.Z (zero-dep core resolves from TestPyPI alone; no [cli] extra)"
---

<objective>
Create the three cross-platform dev-tooling Python helper scripts the release
workflow depends on: the TestPyPI install/round-trip smoke (D-07/REL-02), the
native macOS/Windows CLI smoke (D-14), and the post-release dev-version bump
transform (D-12/REL-05).

Purpose: The workflows in later waves reference these scripts by path; they must
exist and be lint/type-clean first. Isolating them here keeps the pure,
locally-testable logic separate from YAML composition.
Output: scripts/testpypi-smoke.py, scripts/cli-smoke-native.py,
scripts/bump_dev_version.py — all ruff/mypy clean, outside the coverage surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.4-github-actions-release-workflow/05.4-CONTEXT.md
@.planning/phases/05.4-github-actions-release-workflow/05.4-RESEARCH.md
@.planning/phases/05.4-github-actions-release-workflow/05.4-PATTERNS.md
</context>

## Artifacts this phase produces

This plan CREATES the following new symbols (exclude from drift verification):
- `scripts/testpypi-smoke.py` — CLI: `python scripts/testpypi-smoke.py <version>`
- `scripts/cli-smoke-native.py` — CLI: `python scripts/cli-smoke-native.py`
- `scripts/bump_dev_version.py` — CLI: `python scripts/bump_dev_version.py <next-version>`

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/testpypi-smoke.py (TestPyPI install/import/round-trip)</name>
  <files>scripts/testpypi-smoke.py</files>
  <read_first>
    - scripts/audit-public-api.py (dev-script skeleton: shebang, module docstring with a phase contract-anchor block, documented exit codes, `from __future__ import annotations` script-internal line, pathlib usage)
    - scripts/devtools.py (canonical App-subclass usage: `class ...App(App): class Meta: ...`, `with App() as app: app.run()`)
    - cement/utils/version.py (get_version signature/return)
    - .planning/phases/05.4-github-actions-release-workflow/05.4-RESEARCH.md ("Code Examples" → "TestPyPI install-smoke round-trip"; "Common Pitfalls" → Pitfall 2)
  </read_first>
  <action>
    Create scripts/testpypi-smoke.py, an executable dev script that takes one
    argv positional (the version string, e.g. "3.0.16") and: (1) runs
    `pip install -i https://test.pypi.org/simple/ cement==<version>` via
    subprocess with check=True — install PLAIN cement (core is zero-dependency
    so it resolves from TestPyPI alone; do NOT install the [cli] extra, whose
    jinja2/pyyaml deps live only on real PyPI — Pitfall 2); (2) import App from
    cement and run a minimal round-trip with a subclass whose Meta sets
    label="smoke" and exit_on_close=False, using `with MyApp() as app: app.run()`;
    (3) assert cement.utils.version.get_version() equals the argv version and
    print a success line naming the version and running Python.
    Follow the audit-public-api.py script conventions: `#!/usr/bin/env python3`
    shebang, a module docstring containing a "Phase 05.4" contract-anchor block
    (cite D-07/REL-02) and documented exit codes (0 = smoke passed, non-zero =
    install/import/run failure), `from __future__ import annotations` as the
    first import (script-internal, does not affect cement/), pathlib where paths
    are handled, and full type annotations so mypy strict passes. This file lives
    under scripts/ and is NOT listed in pyproject.toml [tool.pdm.build] includes,
    so it ships nowhere and is outside the coverage-measured surface.
  </action>
  <verify>
    <automated>pdm run ruff check scripts/testpypi-smoke.py && pdm run mypy scripts/testpypi-smoke.py && python -c "import ast; ast.parse(open('scripts/testpypi-smoke.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check scripts/testpypi-smoke.py` exits 0
    - `pdm run mypy scripts/testpypi-smoke.py` exits 0
    - File begins with `#!/usr/bin/env python3` and the module docstring contains the tokens `D-07` and `REL-02`
    - The script installs from the TestPyPI simple index: `grep -c 'test.pypi.org' scripts/testpypi-smoke.py` >= 1
    - The pip install target is plain `cement==` with no cli extra: `grep -c 'cement\[cli\]' scripts/testpypi-smoke.py` == 0
    - Source contains `exit_on_close = False` and an assertion comparing `get_version()` to the argv version
  </acceptance_criteria>
  <done>The TestPyPI smoke script exists, is ruff+mypy clean, installs plain cement from TestPyPI, runs an App round-trip, and asserts the version.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/cli-smoke-native.py (cross-platform CLI smoke)</name>
  <files>scripts/cli-smoke-native.py</files>
  <read_first>
    - scripts/cli-smoke-test.sh (the assertion logic to port: `cement --version`/`--help`/`-h`, `cement generate -h`, `cement generate project -D`, install-generated, `myapp --version` — lines 29-59; and the LINE the native version must fix: line 34's Linux-only platform-banner grep)
    - scripts/audit-public-api.py (Python dev-script skeleton)
    - cement/utils/version.py (get_version_banner: emits `Platform {platform.platform()}` — lines 76-85)
    - .planning/phases/05.4-github-actions-release-workflow/05.4-RESEARCH.md ("Common Pitfalls" → Pitfall 3; "Assumptions Log" A6)
    - .planning/phases/05.4-github-actions-release-workflow/05.4-PATTERNS.md ("cli-smoke-native.py" section + "Cross-platform banner assertion")
  </read_first>
  <action>
    Create scripts/cli-smoke-native.py, an executable cross-platform smoke that
    drives the cement CLI on the runner's OWN Python (no Docker) via
    subprocess.run(..., check=True, capture_output=True, text=True). Port these
    assertions from cli-smoke-test.sh, re-expressed as string membership checks:
    `cement --version` output contains "Cement Framework 3.0." and "Python 3."
    and a platform banner that matches ANY of Linux/Darwin/Windows — do NOT
    re-use the shell script's Linux-only banner assertion (this OS-agnostic
    banner check is the load-bearing Pitfall-3 fix so the script passes on macOS
    and Windows); `cement --help` and `cement -h` contain "Cement Framework
    Developer Tools" and "generate"; `cement generate -h` contains project,
    todo-tutorial, plugin, script, extension; `cement generate project -D
    <tmpdir>/myapp` then `pip install .` in that dir then `myapp --version`
    contains "Cement Framework 3.0." and the same OS-agnostic banner. TIER
    EXPLICITLY (Pitfall 3 / A6): this native smoke is the LIGHTER
    CLI+generate+install-generated check; do NOT port the heavy generated-project
    install + comply + test depth (cli-smoke-test.sh lines 62-86) — that stays
    Linux-docker-only because those build/test targets and backing services are
    unavailable on Windows. Use tempfile/pathlib for the temp project dir. Same
    script conventions as Task 1: shebang, docstring with a Phase 05.4
    contract-anchor block citing D-14, documented exit codes, `from __future__
    import annotations` first, full type annotations, ruff+mypy clean. This
    script shells out to pdm/cement/pip only — never a GNU Make target (Pitfall 5).
  </action>
  <verify>
    <automated>pdm run ruff check scripts/cli-smoke-native.py && pdm run mypy scripts/cli-smoke-native.py && python -c "import ast; ast.parse(open('scripts/cli-smoke-native.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check scripts/cli-smoke-native.py` exits 0 and `pdm run mypy scripts/cli-smoke-native.py` exits 0
    - The banner assertion accepts all three platforms: `grep -Ec 'Darwin|Windows' scripts/cli-smoke-native.py` >= 1 AND the script does NOT hard-require Linux (`grep -c 'Platform Linux' scripts/cli-smoke-native.py` == 0)
    - Script contains no `make ` invocation: `grep -c 'make ' scripts/cli-smoke-native.py` == 0
    - Module docstring contains the token `D-14`
    - Asserts presence of `cement generate` subcommands project/plugin/script/extension
  </acceptance_criteria>
  <done>The native smoke script exists, is ruff+mypy clean, drives the CLI without Docker/make, and accepts Linux/Darwin/Windows banners.</done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/bump_dev_version.py (VERSION tuple + changelog bump)</name>
  <files>scripts/bump_dev_version.py</files>
  <read_first>
    - cement/core/backend.py (the VERSION tuple source of truth: `VERSION = (3, 0, 15, 'final', 0)  # pragma: nocover  # version constant`)
    - CHANGELOG.md (top-of-file section format: `# ChangeLog` H1, then `## <ver> - DEVELOPMENT (will be released as stable/<ver>)`)
    - scripts/audit-public-api.py (dev-script skeleton, pathlib, exit codes)
    - .planning/phases/05.4-github-actions-release-workflow/05.4-PATTERNS.md ("bump_dev_version.py" section)
    - .planning/phases/05.4-github-actions-release-workflow/05.4-RESEARCH.md ("Code Examples" → "Dev-bump PR to 3.0.17"; note VERSION lives in backend.py, not version.py)
  </read_first>
  <action>
    Create scripts/bump_dev_version.py, an executable file-transform that takes
    one argv positional (the next dev version, e.g. "3.0.17") and: (1) rewrites
    cement/core/backend.py's VERSION tuple to (major, minor, patch, 'final', 0)
    parsed from the argv version, using a regex that replaces ONLY the numeric
    tuple members and PRESERVES the exact trailing `# pragma: nocover  # version
    constant` comment verbatim (do not disturb the coverage pragma); (2) prepends
    a fresh `## <next-version> - DEVELOPMENT` section to CHANGELOG.md directly
    under the `# ChangeLog` H1, seeding the standard buckets per CLAUDE.md
    changelog policy (Bugs / Features / Refactoring / Misc / Deprecations). Do
    NOT edit pyproject.toml version — [tool.pdm.version] source="call" reads
    backend.py via get_version, so version flows automatically. Keep the changelog
    anchor regex (`^## +<ver>`) consistent with the D-02 preflight's inverse
    check. Same script conventions as Task 1: shebang, docstring with a Phase
    05.4 contract-anchor block citing D-12/REL-05, documented exit codes,
    `from __future__ import annotations` first, full type annotations, pathlib,
    ruff+mypy clean. Guard: exit non-zero with a clear message if backend.py or
    CHANGELOG.md is not shaped as expected (fail loud rather than silently
    produce a malformed bump).
  </action>
  <verify>
    <automated>pdm run ruff check scripts/bump_dev_version.py && pdm run mypy scripts/bump_dev_version.py && python -c "import ast; ast.parse(open('scripts/bump_dev_version.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check scripts/bump_dev_version.py` exits 0 and `pdm run mypy scripts/bump_dev_version.py` exits 0
    - Dry manual check: on a scratch copy, `python scripts/bump_dev_version.py 3.0.17` produces `VERSION = (3, 0, 17, 'final', 0)  # pragma: nocover  # version constant` in backend.py (trailing pragma comment intact) and a new `## 3.0.17 - DEVELOPMENT` line at the top of CHANGELOG.md — then revert the scratch changes
    - Module docstring contains the tokens `D-12` and `REL-05`
    - Script does NOT modify pyproject.toml: `grep -c 'pyproject' scripts/bump_dev_version.py` may appear only in comments, never as a write target
  </acceptance_criteria>
  <done>The dev-bump script exists, is ruff+mypy clean, rewrites the backend VERSION tuple preserving its pragma, and opens a fresh DEVELOPMENT changelog section without touching pyproject.toml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestPyPI index → runner | testpypi-smoke installs a package from an external index into the runner Python |
| local files → release version-of-record | bump_dev_version rewrites the VERSION tuple that becomes the published version |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05.4-01 | Tampering | testpypi-smoke.py index resolution | medium | mitigate | Install PLAIN `cement==X.Y.Z` from TestPyPI only (zero-dep core); do NOT pull the [cli] extra whose deps would force a real-PyPI extra-index and widen the dependency-confusion surface (Pitfall 2) |
| T-05.4-02 | Tampering | bump_dev_version.py file rewrite | medium | mitigate | Regex replaces only the numeric tuple members; preserves the `# pragma: nocover` comment; fail-loud guard if backend.py/CHANGELOG.md are not shaped as expected |
| T-05.4-03 | Information Disclosure | cli-smoke-native.py subprocess output | low | accept | Script captures CLI stdout only; no secrets are read or echoed |
| T-05.4-SC | Tampering | dev scripts (no package installs at author time) | low | accept | This plan installs no npm/pip/cargo packages into the product; scripts are pure stdlib + cement. RESEARCH Package Legitimacy Audit found no [ASSUMED]/[SUS]/[SLOP] items |
</threat_model>

<verification>
- `pdm run ruff check scripts/` exits 0 (all three new scripts lint-clean)
- `pdm run mypy scripts/*.py` exits 0
- Each script parses as valid Python (`python -c "import ast; ast.parse(...)"`)
- No script invokes `make` (Pitfall 5) and no script hard-requires "Platform Linux"
</verification>

<success_criteria>
Three dev-tooling scripts exist under scripts/, are ruff+mypy clean, and encode
the TestPyPI round-trip (REL-02), cross-platform CLI smoke (D-14), and dev-bump
transform (REL-05) that later waves wire into the workflows.
</success_criteria>

<output>
Create `.planning/phases/05.4-github-actions-release-workflow/05.4-01-SUMMARY.md` when done
</output>
