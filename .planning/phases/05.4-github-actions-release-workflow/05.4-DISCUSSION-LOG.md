# Phase 05.4: GitHub Actions Release Workflow - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-12
**Phase:** 05.4-github-actions-release-workflow
**Areas discussed:** Trigger & approval flow, TestPyPI rehearsal path, Naming & version conventions, Gate architecture & OS coverage

---

## Pre-discussion: Matched Todos

| Option | Description | Selected |
|--------|-------------|----------|
| Keep both out | Note as reviewed-but-deferred; workflow's emitted checklist covers GitBook | ✓ |
| Fold: GitBook optional-features doc | #778 GitBook docs, blocked on 3.0.16 | |
| Fold: GitBook todo-tutorial update | pdm-backend walkthrough update | |

**User's choice:** Keep both out — recorded under Reviewed Todos in CONTEXT.md `<deferred>`.

---

## Trigger & approval flow

| Option | Description | Selected |
|--------|-------------|----------|
| Tag push is canonical | Push tag 3.0.16 → gates → approval → publish; tag is source of truth | ✓ |
| Dispatch creates the tag | Version input → gates → approval → workflow tags + publishes | |
| Dispatch-only, no tag trigger | Releases always manually dispatched | |

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub Environment reviewers | Publish jobs target a 'release' environment with required reviewer; OIDC scoping attaches there | ✓ |
| Two separate workflows | Gates workflow + manually dispatched publish workflow | |
| No pause — tag IS the approval | Straight to publish once tag pushed | |

| Option | Description | Selected |
|--------|-------------|----------|
| One approval unlocks everything | Single pause after gates + TestPyPI; approval releases PyPI + all downstream | ✓ |
| Two stops: PyPI, then distribution | Second approval gates Docker/branch/Release | |
| Approval only on PyPI, rest pre-approval | Downstream runs in parallel with approval wait | |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes: version + changelog check | Pre-flight verifies tag == version and finalized CHANGELOG section | ✓ |
| Version check only | Tag == version, changelog unenforced | |
| No pre-flight | Gates alone decide | |

**User's choice:** All four recommended options.
**Notes:** Phase 6 criterion "no manual intervention beyond the user-approved publish step" anchored the approval design.

---

## TestPyPI rehearsal path

| Option | Description | Selected |
|--------|-------------|----------|
| Inline stage in every run | gates → build → TestPyPI → smoke → approval → PyPI | ✓ |
| Rehearsal-only, tag run skips it | TestPyPI only in manual rehearsal dispatch | |

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, dispatch = dry-run | Branch-runnable; ends after TestPyPI smoke; never publishes | ✓ |
| Dispatch mirrors tag run exactly | Dispatch can reach PyPI behind approval | |
| No dispatch at all | Tag push only | |

| Option | Description | Selected |
|--------|-------------|----------|
| All 5 CPythons, full round-trip | Install from TestPyPI + import + App().run() on 3.10–3.14 | ✓ |
| One Python, full round-trip | Newest Python spot-check | |
| Install + import only | No App().run() script | |

| Option | Description | Selected |
|--------|-------------|----------|
| skip-existing, smoke still runs | Idempotent re-runs; smoke tests existing artifact | ✓ |
| Hard fail on collision | Must bump dev version to rehearse again | |
| Unique dev-suffix per run | X.Y.Z.devN per rehearsal | |

**User's choice:** All four recommended options.

---

## Naming & version conventions

| Option | Description | Selected |
|--------|-------------|----------|
| Unprefixed 3.0.16 | Matches all existing cement tags | ✓ |
| v3.0.16 going forward | Ecosystem-default v-prefix, breaks repo history | |

| Option | Description | Selected |
|--------|-------------|----------|
| stable/3.0.x, fast-forward | Matches repo reality (stable/X.Y.x convention) | ✓ |
| stable/3.0 new convention | Literal issue wording; two conventions forever | |

| Option | Description | Selected |
|--------|-------------|----------|
| 3 / 3.0 / 3.0.16 + latest | Version tags + floating :latest, multi-arch buildx | ✓ |
| Version tags only, no latest | :latest stops moving | |

| Option | Description | Selected |
|--------|-------------|----------|
| Workflow opens a bump PR | Auto-PR to main: VERSION → 3.0.17 + new DEVELOPMENT changelog section | ✓ |
| Emit it on the post-release checklist | Manual step | |
| Workflow pushes bump directly to main | Bypasses PR review | |

**User's choice:** All four recommended options.

---

## Gate architecture & OS coverage

| Option | Description | Selected |
|--------|-------------|----------|
| Reusable workflow, both call it | Gates refactored to workflow_call; PR CI + release share one definition | ✓ |
| Self-contained release.yml | Duplicated gate jobs, drift risk | |
| You decide | Defer to research/planning | |

| Option | Description | Selected |
|--------|-------------|----------|
| PR CI too | Windows core tests + macOS/Windows smoke run on every PR | ✓ |
| Release-only | New OS jobs debut at release time | |
| Release + weekly cron on main | Scheduled rot-prevention middle ground | |

| Option | Description | Selected |
|--------|-------------|----------|
| Checklist-only | Mailing list + Slack stay human; no new secrets | ✓ |
| Automate Slack, checklist mailing list | Slack webhook on publish | |
| Automate both | Slack + SMTP send from workflow | |

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-created GitHub issue | 'X.Y.Z post-release checklist' issue with checkboxes, assigned to maintainer | ✓ |
| Job summary only | Vanishes once run page closed | |
| Appended to the GitHub Release body | Internal chores in public notes | |

**User's choice:** All four recommended options.

---

## Claude's Discretion

- Job graph/parallelization inside release.yml; action selection + exact version pinning (CI-05)
- RTD docs-trigger mechanics (.readthedocs.yaml version activation)
- Native-OS adaptation of scripts/cli-smoke-test.sh for macOS/Windows runners
- Concurrency guards, GITHUB_TOKEN permission scoping, Release-body extraction from CHANGELOG.md
- Whether pypy stays in the shared release-gate matrix (default: yes, as in PR CI)

## Deferred Ideas

- Automating Slack / mailing-list notifications — revisit after a release or two
- GitBook docs todos (2026-05-09 optional-features, 2026-07-11 todo-tutorial) — post-release human checklist items, not phase scope
