---
phase: 05.4-github-actions-release-workflow
verified: 2026-07-12T23:30:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 2
overrides:
  - must_have: "cement CLI smoke tests on Linux, macOS, and Windows (release gate success criterion 1, part of the Windows/macOS OS-matrix requirement)"
    reason: "test-core-windows and cli-smoke-native jobs were authored in gates.yml, verified GREEN on all 4 legs (macOS/Windows x Python 3.10/3.14) on a debut CI run, then intentionally commented out (not deleted) because tests/core hard-codes ~6 POSIX-path assertions that Windows normalizes differently. The repo has never had Windows/macOS CI; maintainer explicitly deferred to backlog item 999.2 (ROADMAP.md) rather than block the first automated-release iteration on a brand-new OS matrix. Re-enable is a one-line uncomment once 999.2 lands."
    accepted_by: "derks (maintainer, phase-context authorization for this verification run)"
    accepted_at: "2026-07-12"
  - must_have: "PR CI runs the NEW Windows core-test and macOS/Windows native-smoke jobs on every pull request (D-14) — 05.4-02-PLAN.md must_haves"
    reason: "Same deviation as above — the jobs are commented out in gates.yml (both the PR caller and release caller share this file), so neither PR CI nor release CI currently exercises them. Structurally ready (the `full-os-matrix` input plumbing exists in both callers); functionally deferred to backlog 999.2."
    accepted_by: "derks (maintainer, phase-context authorization for this verification run)"
    accepted_at: "2026-07-12"
re_verification: null
gaps: []
deferred:
  - truth: "Windows core-test and macOS/Windows native CLI smoke gates run on every PR and release (D-14, full success criterion 1)"
    addressed_in: "Phase 999.2 (backlog)"
    evidence: "ROADMAP.md Phase 999.2 'Cross-OS CI gates — Windows/macOS test portability (BACKLOG)': 'Re-enable the two cross-OS gate jobs shipped (commented out) in gates.yml during Phase 05.4 — test-core-windows and cli-smoke-native — and make tests/core pass on Windows... Requirements: extends D-14 (deferred from Phase 05.4)'"
  - truth: "stable/3.0.x branch-sync succeeds on the first real release"
    addressed_in: "Phase 6 (3.0.16 Release Cut)"
    evidence: "05.4-PROVISIONING.md section 6b: stable/3.0.x is verified NOT an ancestor of main today (3 obsolete RTD-era commits); a one-time '-s ours' ancestry merge is documented as REQUIRED before the first real tag push. This is pre-tag provisioning work explicitly scoped to before Phase 6's release cut, not a Phase 05.4 deliverable."
human_verification: []
---

# Phase 05.4: GitHub Actions Release Workflow Verification Report

**Phase Goal:** Replace the manual release checklist (issue #791) with a GitHub Actions–driven release workflow so that cutting a release becomes a tag push (or workflow_dispatch). The workflow automates: release gates (test matrix, coverage, compliance, Windows core tests, CLI smoke tests), PyPI distribution with TestPyPI validation path and OIDC (no long-lived credentials), Docker Hub multi-arch images (X, X.Y, X.Y.Z tags), stable/X.Y branch sync, docs builds (RTD), GitHub Release publication, and a short emitted post-release checklist. Pre-requisite for Phase 6 which RUNS the workflow.

**Verified:** 2026-07-12
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Tag push / workflow_dispatch runs all release gates (test matrix, coverage, compliance, core tests) and hard-stops on failure | ✓ VERIFIED (with accepted override on the Windows/macOS smoke legs) | `release.yml` `gates` job `needs: preflight` calls `./.github/workflows/gates.yml` with `full-os-matrix: true`; `gates.yml` runs `comply`→`test`→`test-all` (7-leg Python matrix incl. both PyPy legs)→`cli-smoke-test` as a serial `needs:` fail-fast chain. Live dry-run [run 29212487225](https://github.com/datafolklabs/cement/actions/runs/29212487225) shows all these jobs green. The Windows-core-test / macOS-Windows-native-smoke jobs were authored, verified green on all 4 legs, then commented out and deferred to backlog 999.2 (see override above) — this is an approved scope reduction, not a failure. |
| 2 | Gated run builds sdist+wheel, publishes to PyPI with TestPyPI validation, no long-lived PyPI secrets (OIDC) | ✓ VERIFIED | `build` job (`pdm build` + `upload-artifact@v7.0.1`) is isolated from all publish credentials. `testpypi-publish` (`environment: testpypi`, `id-token: write`, `gh-action-pypi-publish@v1.14.0` w/ `repository-url: test.pypi.org`, `skip-existing: true`) and `publish-pypi` (`environment: release`, `id-token: write`, real PyPI, no `repository-url`, no `skip-existing` — duplicate fails loud) both use OIDC exclusively; no `PYPI_TOKEN`/`TWINE_PASSWORD`-style secret anywhere in the file (`grep` confirms). Dry-run 29212487225 shows a real OIDC TestPyPI publish + 5-Python (`testpypi-smoke` matrix 3.10–3.14) install/import/round-trip/version-assert, all green. |
| 3 | Docker Hub images build linux/amd64 + linux/arm64 and push with X/X.Y/X.Y.Z tags (`datafolklabs/cement`) | ✓ VERIFIED (structural — execution deferred to Phase 6) | `docker` job (`needs: publish-pypi`): `setup-qemu-action` + `setup-buildx-action` + `docker/build-push-action@v7.3.0` with `platforms: linux/amd64,linux/arm64`, `push: true`, and 4 literal tags `datafolklabs/cement:{${{ github.ref_name }}, 3.0, 3, latest}`. Can only execute on a real tag push (Phase 6); per phase context this is verified structurally, not behaviorally, at this phase. |
| 4 | stable/X.Y branch auto fast-forwarded; docs builds (RTD) triggered for the release version | ✓ VERIFIED (structural — execution deferred to Phase 6) | `branch-sync` job (`contents: write`) does a plain (non-force) `git push origin "${{ github.sha }}:refs/heads/stable/3.0.x"` — fails by design if non-fast-forward. `tag-sync` job (added post-plan, documented in 05.4-05-SUMMARY.md as a gap-fix) force-repoints the moving tags `3`/`3.0`, and RTD's GitHub integration rebuilds `/en/3.0/` off the `3.0` tag push per `05.4-PROVISIONING.md` §5. `05.4-PROVISIONING.md` §6b documents a REQUIRED one-time ancestry reconcile (`stable/3.0.x` is currently NOT an ancestor of `main` — confirmed live via `git merge-base --is-ancestor`) before the first real tag; correctly scoped as pre-Phase-6 provisioning work, not a Phase 05.4 code gap. |
| 5 | GitHub Release published with changelog body; remaining human steps reduced to a short emitted post-release checklist | ✓ VERIFIED (structural — execution deferred to Phase 6) | `gh-release` job awk-extracts the `## <tag>` CHANGELOG section into `RELEASE_BODY.md` and runs `gh release create "$VER" --notes-file RELEASE_BODY.md` (built-in `gh`, zero third-party). `post-release-checklist` job emits a `gh issue create` with a 5-item checkbox list (GitBook changelog, GitBook stable docs, mailing list, Slack, merge dev-bump PR) assigned to `derks`, echoed to `$GITHUB_STEP_SUMMARY`. `dev-bump` job runs `scripts/bump_dev_version.py` (computed next version, not hardcoded) and opens a PR via `create-pull-request@v8.1.1`. |

**Score:** 5/5 truths verified (0 present-but-behavior-unverified; 2 accepted overrides folded into truth #1/#2's supporting must-haves — see frontmatter)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/testpypi-smoke.py` | TestPyPI install/import/round-trip smoke, D-07/REL-02 | ✓ VERIFIED | Exists, ruff+mypy clean (confirmed via direct re-run), installs plain `cement==<ver>` from `test.pypi.org/simple`, imports `App`, asserts `get_version() == argv[1]`, retries on transient index-propagation failures. |
| `scripts/cli-smoke-native.py` | Cross-platform CLI smoke, D-14 | ✓ VERIFIED | Exists, ruff+mypy clean, accepts Linux/Darwin/Windows/macOS banners (no hardcoded "Platform Linux"), drives `cement --version/--help/-h/generate -h/generate project/pip install/myapp --version` via subprocess only — zero `make` invocations. |
| `scripts/bump_dev_version.py` | VERSION tuple + changelog bump, D-12/REL-05 | ✓ VERIFIED | Exists, ruff+mypy clean, regex targets only the numeric VERSION tuple members in `cement/core/backend.py`, preserves the `# pragma: nocover` comment, prepends a `## <ver> - DEVELOPMENT` CHANGELOG section, fails loud (exit 3) on unexpected file shape, never touches `pyproject.toml`. |
| `.github/workflows/gates.yml` | Reusable `workflow_call` gate suite, D-13/CI-05 | ✓ VERIFIED (with documented deviation) | Parses; `on: workflow_call` with `full-os-matrix` boolean input; four active jobs (`comply`, `test`, `test-all`, `cli-smoke-test`) preserve the original serial fail-fast chain; two new jobs (`test-core-windows`, `cli-smoke-native`) are fully authored but commented out — see override. No publish/OIDC surface (`gh-action-pypi-publish` count == 0). All `uses:` exact-tag pinned. |
| `.github/workflows/build_and_test.yml` | Thin PR-triggered gates caller, CI-05 | ✓ VERIFIED | Parses; `on: [pull_request]`; single `gates` job `uses: ./.github/workflows/gates.yml` with `full-os-matrix: true`; zero inline job bodies (`runs-on:` count == 0). |
| `.github/workflows/release.yml` | Full tag-push / dry-run release pipeline | ✓ VERIFIED | Parses; 13 jobs (`preflight, gates, build, testpypi-publish, testpypi-smoke, dry-run-summary, publish-pypi, docker, branch-sync, tag-sync, gh-release, dev-bump, post-release-checklist`); strict tag trigger `['[0-9]+.[0-9]+.[0-9]+']` + `workflow_dispatch`; top-level `permissions: contents: read`; `concurrency: {group: release-pipeline, cancel-in-progress: false}`; exactly one `environment: release` declaration (on `publish-pypi`); exactly 2 jobs with `id-token: write` (`testpypi-publish`, `publish-pypi`); zero floating `uses:` refs across all 3 workflow files. |
| `.planning/phases/05.4-github-actions-release-workflow/05.4-PROVISIONING.md` | External-setup runbook, CI-04 | ✓ VERIFIED | Exists; documents all 6 external dependencies (release env, testpypi env, PyPI/TestPyPI trusted publishers, Docker Hub secrets, RTD automation, stable/3.0.x fast-forward) with exact match values, a pre-first-tag checklist, and a "Known accepted tradeoffs" section. |
| `.planning/phases/05.4-github-actions-release-workflow/05.4-SECURITY.md` | Threat register, 19 threats | ✓ VERIFIED | `threats_open: 0`, `status: verified`; 19/19 threats dispositioned (mitigate/accept) with concrete in-repo evidence citations; 2 accepted risks logged. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `build_and_test.yml` | `gates.yml` | `uses: ./.github/workflows/gates.yml` | ✓ WIRED | PR caller, `full-os-matrix: true`. |
| `release.yml` (`gates` job) | `gates.yml` | `uses: ./.github/workflows/gates.yml` | ✓ WIRED | Release caller, `needs: preflight`, `full-os-matrix: true`. D-13 invariant (`grep -c` == 2 across `.github/workflows/*.yml`) confirmed. |
| `gates.yml` `cli-smoke-native` job (commented) | `scripts/cli-smoke-native.py` | `python scripts/cli-smoke-native.py` | ⚠️ ORPHANED (accepted) | Job body correctly references the script but the job itself is commented out — same override as truth #1. |
| `release.yml` `testpypi-smoke` job | `scripts/testpypi-smoke.py` | `python scripts/testpypi-smoke.py "$VERSION"` matrix 3.10–3.14 | ✓ WIRED | Confirmed executing and passing on all 5 legs in the live dry-run. |
| `release.yml` `dev-bump` job | `scripts/bump_dev_version.py` | `python scripts/bump_dev_version.py "$NEXT"` | ✓ WIRED | `$NEXT` computed from `preflight.outputs.version` (released patch + 1), not hardcoded — repeatable across future releases. |
| `release.yml` `publish-pypi` | `release` environment | `environment: release` | ✓ WIRED | Sole job carrying this declaration; sole approval stop; sole other `id-token: write` job is `testpypi-publish` (env `testpypi`, no approval). |
| `release.yml` fan-out jobs (`docker`,`branch-sync`,`tag-sync`,`gh-release`,`dev-bump`,`post-release-checklist`) | `publish-pypi` | `needs: publish-pypi` (dev-bump/checklist also need `preflight`) | ✓ WIRED | All 6 fan-out jobs gated behind the single approval; confirmed via YAML parse. |

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Live workflow_dispatch dry-run of `release.yml` | `gh run view 29212487225 -R datafolklabs/cement` | `conclusion: success`, event `workflow_dispatch`, all 10 gate/build/testpypi jobs `success`, all 7 publish-side jobs `skipped` | ✓ PASS |
| Three referenced PRs (#792, #793, #794) merged | `gh pr view {792,793,794} -R datafolklabs/cement` | All `state: MERGED` | ✓ PASS |
| All 14 referenced commit hashes exist in repo history | `git cat-file -e <hash>` | All 14 found | ✓ PASS |
| Three dev scripts pass ruff + mypy | `pdm run ruff check` / `pdm run mypy` on all 3 scripts | "All checks passed!" / "Success: no issues found in 3 source files" | ✓ PASS |
| No floating `uses:` action refs across all 3 workflow files | `grep -E 'uses:.*@' ... \| grep -vE '@v[0-9]'` | empty | ✓ PASS |
| Exactly 1 `environment: release` declaration | `grep -c` | 1 | ✓ PASS |
| Exactly 2 real `id-token: write` permission lines (vs. comments) | `grep -n` inspection | 2 (lines 123, 197); other 3 matches are comments | ✓ PASS |
| CHANGELOG.md updated for phase 05.4 work | `grep -n -i "release.yml\|gates.yml\|dev-bump"` CHANGELOG.md | 3 Misc entries present, including the 999.2 deferral note | ✓ PASS |
| `stable/3.0.x` ancestry (pre-tag prerequisite) | `git merge-base --is-ancestor origin/stable/3.0.x main` | NOT an ancestor (as PROVISIONING.md §6b documents) | ℹ️ INFO — correctly flagged as required pre-Phase-6 work, not a Phase 05.4 gap |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| REL-02 | 01, 03 | Pre-release smoke-test on TestPyPI (install+import+round-trip on each supported Python) | ✓ SATISFIED (machinery); Pending in REQUIREMENTS.md by design | `testpypi-smoke.py` + `testpypi-smoke` matrix job (3.10–3.14), proven green in dry-run 29212487225. REQUIREMENTS.md correctly keeps this Pending until Phase 6 runs the workflow against a real tag (per phase instructions, not flagged as a gap). |
| REL-05 | 01, 04 | Post-release version bumped to next dev cycle | ✓ SATISFIED (machinery) | `bump_dev_version.py` + `dev-bump` job (computed next version, PR via create-pull-request). |
| CI-05 | 02 | Action versions pinned to current, no floating refs | ✓ SATISFIED | 0 floating refs across gates.yml/build_and_test.yml/release.yml. |
| CI-04 | 03, 05 | Release workflow validated end-to-end against TestPyPI before the cut | ✓ SATISFIED (machinery) | Live green dry-run 29212487225 through testpypi-smoke; publish-side correctly skipped. |
| REL-03 | 04 | Git tag + GitHub release notes with changelog | ✓ SATISFIED (machinery) | `gh-release` job structurally complete; execution deferred to Phase 6 real tag push. |
| REL-04 | 04 | PyPI publish; installs cleanly 3.10–3.14 | ✓ SATISFIED (machinery) | `publish-pypi` OIDC job structurally complete + proven pattern via the identical TestPyPI OIDC path in the dry-run. |
| DOCS-03 | 03, 04 | Changelog updated for the release, all user-visible changes | ✓ SATISFIED (machinery + this phase's own CHANGELOG entries) | Preflight's finalized-changelog guard + `gh-release`'s awk extraction; CHANGELOG.md itself already carries phase 05.4's own Misc entries. |

No orphaned requirements: all 7 requirement IDs declared across the 5 plans (CI-04, CI-05, REL-02, REL-03, REL-04, REL-05, DOCS-03) exactly match the phase's declared requirement set. Per phase instructions, their "Pending" status and "Phase 6" phase-mapping in REQUIREMENTS.md's traceability table is intentional and NOT treated as a gap (the requirement IDs flip to Complete only when Phase 6 runs the workflow against a real release). One traceability-table note: CI-05 is mapped to "Phase 2" in REQUIREMENTS.md's coverage table (a pre-existing mapping predating the 05.4 insertion) even though this phase's plan 02 also declares and substantively satisfies CI-05 (pinning the new gates.yml/release.yml actions). This is an informational note, not a gap — the requirement's original phase mapping in the table was never updated to reflect the 05.4 insertion, but the underlying pin requirement is verifiably met in both phases' artifacts.

### Anti-Patterns Found

None. Scanned all workflow files and all three new scripts for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented" — zero hits (two incidental matches on the literal string `todo-tutorial`, a real Cement CLI subcommand name, not a debt marker).

### Human Verification Required

None. All must-haves resolved to VERIFIED or an explicitly accepted override; no items require human judgment beyond what the phase context already confirms was maintainer-approved (the Windows/macOS gate deferral and the pre-Phase-6 provisioning checklist).

### Gaps Summary

No gaps. Two deviations from the original plan text were found and both are legitimate, pre-approved, and traceable:

1. **Windows/macOS OS-matrix gates commented out.** `gates.yml`'s `test-core-windows` and `cli-smoke-native` jobs are fully authored, were verified green on all 4 legs on the debut CI run, and are now commented out (not deleted) pending `tests/core` POSIX-path portability fixes. This is tracked as ROADMAP.md backlog Phase 999.2 with an explicit "extends D-14 (deferred from Phase 05.4)" requirement note, and is called out in the phase 05.4 CHANGELOG.md Misc entry. Treated as an accepted override per phase-context instruction, not a gap.
2. **stable/3.0.x branch divergence.** `05.4-PROVISIONING.md` §6b documents (and this verification independently confirmed via `git merge-base --is-ancestor`) that `stable/3.0.x` is not currently an ancestor of `main`, so `branch-sync` will fail until a one-time `-s ours` ancestry-reconcile merge lands. This is explicitly scoped as pre-first-tag provisioning work (Phase 6 prerequisite), documented in the Pre-first-tag checklist, not a Phase 05.4 code deliverable.

Both are recorded in the `deferred` section of this report's frontmatter rather than as gaps, since both are explicitly addressed by named later-phase work (999.2 and Phase 6 respectively) with matching roadmap/runbook evidence.

---

_Verified: 2026-07-12_
_Verifier: Claude (gsd-verifier)_
