---
phase: 05.4-github-actions-release-workflow
plan: 05
subsystem: release-automation
tags: [provisioning, testpypi, oidc, dry-run, ci-04]
requires:
  - "release.yml complete pipeline (05.4-03 + 05.4-04)"
  - "gates.yml reusable gate suite (05.4-02)"
  - "scripts/testpypi-smoke.py (05.4-01)"
provides:
  - "05.4-PROVISIONING.md external-setup runbook (amended through review)"
  - "TestPyPI trusted publisher + testpypi environment provisioned (maintainer)"
  - "GREEN workflow_dispatch dry-run — CI-04 acceptance gate satisfied"
affects:
  - ".planning/phases/05.4-github-actions-release-workflow/05.4-PROVISIONING.md"
tech-stack:
  patterns:
    - "provider-UI state captured as an exact-match-values runbook, confirmed via checkpoint"
    - "workflow_dispatch dry-run as the pre-merge-of-trust validation vehicle"
---

# 05.4-05 Summary — Provisioning + dry-run validation

## Outcome

**All 3 tasks complete. CI-04 acceptance gate GREEN:**
[run 29212487225](https://github.com/datafolklabs/cement/actions/runs/29212487225)
— preflight → 10 gate jobs (incl. genuine 7-leg Python matrix with both PyPy
legs) → isolated build → OIDC TestPyPI publish → 5-Python install smoke →
dry-run-summary, with all 7 publish-side jobs SKIPPED (event-gated, D-06).

## Task record

**Task 1 — 05.4-PROVISIONING.md runbook** (committed pre-checkpoint, then
substantially amended during the checkpoint session):
- Docker Hub token model corrected — PATs cannot be repo-scoped; documented
  OAT (Team/Business), bot-account PAT, and maintainer-PAT paths (verified
  against Docker docs 2026-07).
- RTD section rewritten for the real **major.minor moving-tag model**: RTD
  builds `/en/3.0/` from git tag `3.0`; the release workflow gained a
  `tag-sync` job re-pointing moving tags `3`/`3.0` (added to plan 05.4-04's
  shipped release.yml as a gap fix).
- `stable/3.0.x` section made explicit with live-verified repo state: branch
  has diverged from main (3 obsolete RTD-era commits) — one-time `-s ours`
  ancestry merge documented as REQUIRED before the first real release.
- Post-review additions: "Allow GitHub Actions to create and approve pull
  requests" repo setting (dev-bump prerequisite), `testpypi` environment
  deployment-branch restriction, TestPyPI stale-upload caveat.

**Task 2 — provisioning confirmed by maintainer:** TestPyPI trusted publisher
(repo `datafolklabs/cement`, workflow `release.yml`, env `testpypi`) and the
`testpypi` GitHub environment are DONE. All other items (release environment
reviewers, PyPI trusted publisher, Docker Hub token, RTD integration/tag
rules, stable/3.0.x ancestry merge) are DEFERRED to the pre-first-tag
checklist in the runbook — none are needed by the dry-run.

**Task 3 — workflow_dispatch dry-run:** required merging the workflow to the
default branch first (GitHub only registers dispatchable workflows from the
default branch — structural constraint discovered at first dispatch attempt).
Landed via PR #792 (rebase-merged after a 3-review cycle: agent self-review +
CodeRabbit + verification pass; all findings dispositioned on the PR).
Three dry-run attempts:
1. Run 29210275128 — FAILED in preflight: bare `pdm install` builds pylibmc
   without libmemcached headers. Fixed in PR #793 (preflight → zero-dep
   `pip install .`; dev-bump → no install, pure-stdlib script).
2. Run 29210928341 — first successful **OIDC TestPyPI publish** (fresh
   wheel+sdist with attestations); FAILED in testpypi-smoke: the smoke App
   inherited the wrapper script's argv and argparse rejected the version
   argument. Fixed in PR #794 (`Meta.argv = []`), verified end-to-end locally
   against the real TestPyPI artifact.
3. Run 29212487225 — **GREEN end-to-end.**

## Deviations

- **Task 3 executed by the orchestrator session, not a fresh executor** — the
  checkpoint/fix/merge loop (3 PRs, provider-side coordination) needed the
  accumulated session context.
- **Branch strategy pivot (user-approved):** after PR #792 merged, close-out
  artifacts land directly on `main` as the archive commit instead of
  round-tripping through the phase branch.
- **Requirement flips reverted** (review finding): CI-04/CI-05/DOCS-03/
  REL-02..05 stay Pending until Phase 6 runs the real release.

## Verification

- Dry-run run URL green through dry-run-summary: 29212487225 ✓
- Publish side skipped on dispatch (D-06 event gate held) ✓
- TestPyPI artifact independently smoke-tested locally (install + import +
  round-trip + version assert on the published bytes) ✓
- PROVISIONING.md contains all 6 service sections + pre-first-tag checklist ✓

## Self-Check: PASSED
