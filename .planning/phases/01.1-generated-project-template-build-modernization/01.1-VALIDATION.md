---
phase: 01.1
slug: generated-project-template-build-modernization
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-30
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

This phase adds a single static template file
(`cement/cli/templates/generate/project/pyproject.toml`). It does not add
runtime code, does not modify importable cement modules, and has no unit
test surface. Acceptance is end-to-end via the existing CLI smoke test.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + Docker (cli-smoke-test.sh) |
| **Config file** | `scripts/cli-smoke-test.sh`, wired via `Makefile:34-35` |
| **Quick run command** | `PYTHON_VERSIONS=3.10 make cli-smoke-test` (single-version sanity) |
| **Full suite command** | `make cli-smoke-test` (full 3.10–3.14 matrix) |
| **Estimated runtime** | ~30s single-version, ~2.5min full matrix |

The Python unit-test suite (`make test`) is **not** an acceptance gate
for this phase — no Python source under `cement/` is modified. The smoke
test is the only acceptance signal that the change actually works.

---

## Sampling Rate

- **After every task commit:** Run `PYTHON_VERSIONS=3.10 make cli-smoke-test`
  (single-version smoke, fastest signal that the template renders + installs)
- **After every plan wave:** Run `make cli-smoke-test` (full matrix)
- **Before `/gsd-verify-work`:** Full matrix must be green
- **Max feedback latency:** ~30 seconds (single-version smoke)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | (none — inserted phase, no REQ-IDs) | none | N/A — adds build-system declaration only, no new attack surface | smoke | `PYTHON_VERSIONS=3.10 make cli-smoke-test` | ✅ | ⬜ pending |
| 01.1-01-final | 01 | 1 | (none) | none | N/A | smoke (full matrix) | `make cli-smoke-test` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements:

- `scripts/cli-smoke-test.sh` — already present and functional
- `Makefile:34-35` — `make cli-smoke-test` target already wired
- `make test` (Python unit tests) — not exercised this phase; out of scope

No Wave 0 setup needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Generated `pyproject.toml` content matches D-03 verbatim | CONTEXT D-03 | Template variable rendering depends on cement version at generation time — verify the `{{ cement.version }}` placeholder rendered correctly by inspecting output | After smoke test runs, inspect `myapp/pyproject.toml` inside the test container: `docker exec cement-cli-smoke-test cat /tmp/.../myapp/pyproject.toml` and confirm `cement>=3.0.X` line is present and version-correct |

All other behaviors are covered by the smoke test exit code.

---

## Validation Sign-Off

- [x] All tasks have automated verify (smoke test)
- [x] Sampling continuity: only 1 main task, smoke test covers it directly
- [x] Wave 0 covers all needs (existing infrastructure)
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending — set to "approved YYYY-MM-DD" upon successful execution.
