---
phase: 05.2-ext-argparse-command-self-meta-accessor-670
verified: 2026-06-25T00:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
human_verification: []
---

# Phase 05.2: ext.argparse command self-meta accessor (#670) Verification Report

**Phase Goal:** Add a single additive, read-only accessor on ArgparseController
(self._command_meta) that returns the currently-dispatched command's CommandMeta
(label, parser_options['help'], etc.), so exposed @ex/@expose commands can read
their own decorator meta without the brittle dance -- resolving #670. Purely
additive: the func() dispatch signature is unchanged.

**Verified:** 2026-06-25
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | An exposed @ex/@expose command can read its own CommandMeta from inside its body via self._command_meta (D-01, D-02) | VERIFIED | `Base.command_meta_cmd` body reads `self._command_meta` and returns `meta.label : meta.parser_options['help']`; `test_command_meta_dispatched` asserts the round-trip |
| 2 | self._command_meta.parser_options['help'] returns the @ex(help=...) string for the running command (D-04, #670 ergonomic call site) | VERIFIED | `test_command_meta_dispatched` asserts `res == "command-meta-cmd : help for command_meta_cmd"` where the command was decorated `@expose(help='help for command_meta_cmd')` |
| 3 | self._command_meta returns None when no command is dispatched (no __dispatch__ on pargs) and never raises (D-03) | VERIFIED | `Base._default` reads `self._command_meta` on the no-sub-command path; `test_command_meta_none_outside_dispatch` asserts `res == "Inside Base.default : meta > None"`. WR-01 fix adds `test_command_meta_non_owning_controller_returns_none` covering the non-owning-controller case |
| 4 | The func() dispatch signature is unchanged -- every existing def cmd(self) command still receives zero extra positional args (additive-only, 3.0.x BC) | VERIFIED | `grep -n "return func()"` finds exactly one call at L895 with no extra args; the property is a side-channel read not touching `_dispatch`'s call to `func()` |
| 5 | make comply (ruff + mypy) is clean and full coverage on the new lines is 100% | VERIFIED | `make comply` exits 0 (ruff: "All checks passed!", mypy: "Success: no issues found in 51 source files"); pytest --cov=cement.ext.ext_argparse reports 318/318 stmts, 0 miss, 100.00% |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---------|----------|--------|---------|
| `cement/ext/ext_argparse.py` | Read-only `_command_meta` property on `ArgparseController` returning `CommandMeta | None` | VERIFIED | Property at L352-393; uses `@property` decorator; return annotation `"CommandMeta | None"`; implements WR-01-hardened guard via `getattr(member, '__cement_meta__', None)` |
| `tests/ext/test_ext_argparse.py` | Coverage for dispatched-command, help-read, and None branches of `_command_meta` | VERIFIED | 10 occurrences of `_command_meta`; three test functions: `test_command_meta_dispatched`, `test_command_meta_none_outside_dispatch`, `test_command_meta_non_owning_controller_returns_none`; 375 passed, 100% coverage |
| `CHANGELOG.md` | [ext.argparse] Features entry referencing #670 in the active 3.0.15 section | VERIFIED | Entry at L222-229 in the `Features:` bucket of `## 3.0.15 - DEVELOPMENT`; all lines <= 78 chars; sub-bullet `[Issue #670](https://github.com/datafolklabs/cement/issues/670)` at L229 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `ArgparseController._command_meta` | `self.app.pargs.__dispatch__` | `hasattr` guard + `.split('.')[1]` | VERIFIED | L382: `if not hasattr(self.app.pargs, '__dispatch__'): return None`; L384: `func_name = self.app.pargs.__dispatch__.split('.')[1]` |
| `ArgparseController._command_meta` | `getattr(self.__class__, func_name).__cement_meta__` | class-level CommandMeta lookup (WR-01-hardened form) | VERIFIED | L391: `member = getattr(self.__class__, func_name, None)`; L392: `meta: CommandMeta | None = getattr(member, '__cement_meta__', None)` -- WR-01 fix replaces the original `hasattr` + `__cement_meta__` direct access with double-`getattr` that collapses both branches and removes the `pragma: nocover` line |

### Data-Flow Trace (Level 4)

Not applicable. The accessor reads already-parsed runtime state (`pargs.__dispatch__`)
and returns an existing in-memory class-level object (`CommandMeta`). No data source
produces empty or hardcoded values -- the test drives a real dispatch path to verify
real data flows through.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---------|---------|--------|--------|
| Dispatched command reads own meta | `pdm run pytest tests/ext/test_ext_argparse.py::test_command_meta_dispatched` | assert `res == "command-meta-cmd : help for command_meta_cmd"` passes | PASS |
| None path on no-dispatch | `pdm run pytest tests/ext/test_ext_argparse.py::test_command_meta_none_outside_dispatch` | assert `res == "Inside Base.default : meta > None"` passes | PASS |
| Non-owning controller returns None (WR-01) | `pdm run pytest tests/ext/test_ext_argparse.py::test_command_meta_non_owning_controller_returns_none` | assert `foreign._command_meta is None` passes | PASS |
| Full argparse suite at 100% coverage | `pdm run pytest --cov=cement.ext.ext_argparse tests/ext/test_ext_argparse.py` | 375 passed, 318/318 stmts, 0 miss, 100.00% | PASS |
| make comply | `make comply` | ruff clean + mypy clean on 51 files | PASS |

### Probe Execution

No probes declared or applicable for this phase.

### Requirements Coverage

Phase 05.2 uses phase-local decision IDs (D-01..D-07 in 05.2-CONTEXT.md). No
entries exist in REQUIREMENTS.md for this phase -- confirmed by inspection.
REQUIREMENTS.md traceability table does not include any 05.2 row; the table ends
at Phase 05.1 (GEN-01..GEN-06). This is expected and correct.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|---------|
| D-01 | Read-only property named `_command_meta` on ArgparseController | SATISFIED | Property exists at `cement/ext/ext_argparse.py:352` |
| D-02 | Returns CommandMeta for the running command derived from `pargs.__dispatch__` | SATISFIED | Property derives `func_name` from `__dispatch__` and resolves `CommandMeta` via class-level lookup |
| D-03 | Returns None (never raises) when no `__dispatch__` on pargs | SATISFIED | `hasattr` guard at L382; WR-01 guard at L391-392 further hardens the never-raises contract |
| D-04 | Returns raw CommandMeta as-is, no wrapper or convenience shortcuts | SATISFIED | Property returns `meta` directly; caller accesses `.parser_options['help']` etc. |
| D-05 | Exposes only the currently-running command's meta | SATISFIED | Derives from `pargs.__dispatch__` (the active dispatch token) only |
| D-06 | func() dispatch signature unchanged, no signature injection | SATISFIED | `return func()` at L895 unchanged; no new args |
| D-07 | Cement 4 deferral comment near accessor and at `_dispatch` | SATISFIED | `# FIXME: Cement 4 may introduce opt-in func-signature injection` at L376 (property) and L866 (`_dispatch` block) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `cement/ext/ext_argparse.py` | 376 | `# FIXME:` without issue/PR reference | WARNING | D-07 deferral comment mandated by plan; "Cement 4" names the deferred release and CONTEXT.md documents the intent. Pre-existing unreferenced FIXMEs exist in `foundation.py`, `ext_plugin.py`, `ext_daemon.py`, `ext_logging.py` -- project-wide pattern predates this phase. Advisory only. |
| `cement/ext/ext_argparse.py` | 866 | `# FIXME:` without issue/PR reference | WARNING | Same D-07 deferral comment at `_dispatch` block. Same rationale. Advisory only. |

**Debt-marker gate determination:** The two `FIXME` markers were explicitly
mandated by the plan (Task 1 action: "using the file's plain-comment / `# FIXME:`
convention") as D-07 deferral notes. The deferred section of CONTEXT.md documents
the "Cement 4 / signature-injection" future path in detail. The project already
carries unreferenced FIXMEs in five other modules (foundation.py, ext_plugin.py,
ext_daemon.py, ext_logging.py, ext_generate.py) as a pre-existing convention.
These are advisory WARNINGs, not BLOCKERs: the intent is fully auditable via
CONTEXT.md + CHANGELOG #670 entry + the comments themselves naming the Cement 4
release. If a formal GitHub issue is desired as the tracking anchor, one could
be opened for the Cement 4 signature-injection feature.

### Human Verification Required

None. All verification items are programmatically confirmable and have been
confirmed.

### Gaps Summary

No gaps. All five must-haves are VERIFIED. All three artifact checks pass at
all four levels (exists, substantive, wired, data flowing). Both key links
are wired. All seven phase decisions are honored. Quality gates (`make comply`,
100% coverage) are confirmed live against the disk state, not from SUMMARY.md
claims. The WR-01 post-review hardening (commit `add383a4`) is confirmed present
in the source and covered by `test_command_meta_non_owning_controller_returns_none`.

---

_Verified: 2026-06-25_
_Verifier: Claude (gsd-verifier)_
