---
phase: 03-internal-refactor-coverage-hardening
plan: 07
type: execute
wave: 7
depends_on: [06]
files_modified:
  - cement/cli/main.py
  - cement/core/arg.py
  - cement/core/backend.py
  - cement/core/cache.py
  - cement/core/config.py
  - cement/core/controller.py
  - cement/core/extension.py
  - cement/core/foundation.py
  - cement/core/handler.py
  - cement/core/hook.py
  - cement/core/interface.py
  - cement/core/log.py
  - cement/core/mail.py
  - cement/core/output.py
  - cement/core/plugin.py
  - cement/core/template.py
  - cement/ext/ext_alarm.py
  - cement/ext/ext_argparse.py
  - cement/ext/ext_colorlog.py
  - cement/ext/ext_configparser.py
  - cement/ext/ext_daemon.py
  - cement/ext/ext_dummy.py
  - cement/ext/ext_generate.py
  - cement/ext/ext_jinja2.py
  - cement/ext/ext_json.py
  - cement/ext/ext_logging.py
  - cement/ext/ext_memcached.py
  - cement/ext/ext_mustache.py
  - cement/ext/ext_plugin.py
  - cement/ext/ext_print.py
  - cement/ext/ext_redis.py
  - cement/ext/ext_scrub.py
  - cement/ext/ext_smtp.py
  - cement/ext/ext_tabulate.py
  - cement/ext/ext_watchdog.py
  - cement/ext/ext_yaml.py
  - cement/utils/fs.py
  - cement/utils/shell.py
  - cement/utils/version.py
autonomous: true
requirements: [COV-03, COV-01]

must_haves:
  truths:
    - "Every pragma:nocover and pragma:no cover site in cement/ carries one of the 8 D-15 locked-vocabulary category labels appended after the existing pragma comment"
    - "D-24 conjunct #7 GREEN: locked-vocabulary grep returns empty"
    - "Live count is 141 sites across 39 files per RESEARCH.md verified 2026-05-03 (NOT 123 from CONTEXT.md)"
    - "Per-file commits per RESEARCH.md Open Question 2: 39 files times atomic-per-file matches Phase 1 D-04 family-split shape"
    - "100% coverage gate stays green (D-24 #1 — pragma comments do not change coverage measurement; they just append text)"
    - "make audit-public-api exits 0 — comment-only changes do not affect AST signature surface (D-24 #4)"
    - "If executor encounters a pragma site that does not fit the 8-category vocabulary, the fix is per D-16: a SEPARATE atomic CONTEXT amendment commit BEFORE adding a new label, NOT a free-form invention"
  artifacts:
    - path: "cement/"
      provides: "Every pragma site labeled with locked-vocabulary category from D-15"
  key_links:
    - from: "D-15 locked vocabulary (8 categories)"
      to: "Every pragma site in cement/"
      via: "Inline category comment appended after the pragma"
      pattern: "pragma:[[:space:]]*no[[:space:]]*cover"
    - from: "D-17 verification grep"
      to: "D-24 conjunct #7 acceptance"
      via: "empty result from grep"
      pattern: "(empty)"
---

<objective>
Audit every pragma:nocover and pragma:no cover site in cement/ (live
count: 141 sites across 39 files per RESEARCH.md verified 2026-05-03 —
CONTEXT.md said 123, RESEARCH.md A4 corrected to 141) and append a
locked-vocabulary category label per D-15.

The 8 locked categories (D-15):
1. abstract method — interface pass lines
2. TYPE_CHECKING import — deferred imports under if TYPE_CHECKING
3. platform-specific — env / OS / Windows fallbacks
4. untestable: dynamic import — runtime __import__ / importlib.import_module
5. untestable: subprocess — shell / git subprocess calls
6. untestable: signal handler — signal-handler internals
7. defensive: unreachable — else branches coverage.py cannot prove unreachable
8. version constant — module-level version tuples

Per RESEARCH.md Open Question 2 recommendation: per-file commits
(approx 30+ files times atomic-per-file matches Phase 1 D-04 family-
split shape; bisect anchor per file). Audit lands AFTER refactor
commits per D-18 — Wave 6 pathlib migration may have added or removed
pragma sites as a side effect, so this wave runs against the FINAL
shape.

Per D-16, if a site does not fit any of the 8 categories, the fix is to
add a category to D-15 in a SEPARATE atomic docs(03) commit FIRST, then
label the site. NEVER invent a free-form label.

Output: per-file atomic commits across 39 files, grouped into 4 task
batches (core, ext first half, ext second half, cli + utils).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-06-SUMMARY.md
@CLAUDE.md

<interfaces>
D-15 locked vocabulary (8 categories — verbatim from CONTEXT.md and RESEARCH.md):

1. abstract method — interface pass lines (cache.py, arg.py, log.py, plugin.py, config.py, output.py, controller.py, mail.py, template.py, handler.py — all interfaces)
2. TYPE_CHECKING import — deferred imports under if TYPE_CHECKING (every ext_*.py + several core/*.py)
3. platform-specific — env / OS / Windows fallbacks (foundation.py:58 SIGNALS, fs.py:245 HOME_DIR, ext_logging.py:23-36 NullHandler)
4. untestable: dynamic import — runtime __import__ / importlib.import_module (ext_plugin.py, ext_generate.py)
5. untestable: subprocess — shell / git subprocess calls (utils/version.py:97-106)
6. untestable: signal handler — signal-handler internals (foundation.py:123-125 frame walk)
7. defensive: unreachable — else branches coverage.py cannot prove unreachable (largest bucket)
8. version constant — module-level version tuples (backend.py:3)

D-17 verification grep (verbatim — locked-vocabulary regex; uses [[:space:]] per Pitfall 6 / Open Question 3 for cross-platform safety):

    grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ \
      | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)'

Must return EMPTY.

Existing pragma site shape variants (RESEARCH.md):
- "# pragma: nocover" (no space, ~110 sites)
- "# pragma: no cover" (with space, ~31 sites)

The locked-vocabulary regex pragma:[[:space:]]*no[[:space:]]*cover matches both. DO NOT canonicalize spelling — would create huge unrelated churn.

Append shape (per Code Examples Example 1 RESEARCH.md lines 590-677):
- BEFORE: pass    # pragma: nocover
- AFTER:  pass    # pragma: nocover  # abstract method

Two spaces before the appended # category matches existing project sibling-comment shape from cement/cli/main.py:53 + ext_logging.py:35 (PATTERNS.md lines 459-471).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight pragma site enumeration and per-file batching</name>
  <files>(read-only / capture only — no commit)</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-15 vocabulary; D-16 expansion rule; D-17 verification grep)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pitfall 4 — 141 not 123; Pragma site inventory lines 760-783; Code Examples Example 1 lines 590-677 — shape examples for all 8 categories; Open Question 2 — per-file vs single-commit recommendation; Pitfall 6 / Open Question 3 — [[:space:]] vs \s portability)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 451-531 — sibling-comment shape; locked vocabulary table)
  </read_first>
  <action>
    Step 1: Capture the live pragma site count and per-file breakdown:

        grep -rn 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ > /tmp/pragma-sites.txt
        wc -l /tmp/pragma-sites.txt

    Step 2: Group by file:

        grep -rn 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | awk -F: '{print $1}' | sort -u > /tmp/pragma-files.txt
        wc -l /tmp/pragma-files.txt

    Record both counts. RESEARCH.md says 141 sites across approx 30+ files; Wave 6 pathlib migration may have shifted some (added or removed). The truth is whatever grep returns now.

    Step 3: Verify the count is in the expected range. If it is drastically different from 141 (e.g., less than 100 or more than 200), investigate before proceeding — Wave 6 may have unintentionally affected pragma sites.

    Step 4: Pre-classify per file by skimming each pragma site context. Group files into 4 batches for sequential per-file commits in Tasks 2-5:
    - Batch A (Task 2): cement/core/*.py (approx 16 files: arg.py, backend.py, cache.py, config.py, controller.py, extension.py, foundation.py, handler.py, hook.py, interface.py, log.py, mail.py, output.py, plugin.py, template.py — list whichever files actually have pragma sites per /tmp/pragma-files.txt)
    - Batch B (Task 3): cement/ext/*.py first half (approx 10 files alphabetically: ext_alarm, ext_argparse, ext_colorlog, ext_configparser, ext_daemon, ext_dummy, ext_generate, ext_jinja2, ext_json, ext_logging)
    - Batch C (Task 4): cement/ext/*.py second half (approx 10 files: ext_memcached, ext_mustache, ext_plugin, ext_print, ext_redis, ext_scrub, ext_smtp, ext_tabulate, ext_watchdog, ext_yaml)
    - Batch D (Task 5): cement/cli/*.py + cement/utils/*.py (approx 4 files: cli/main.py, utils/fs.py, utils/shell.py, utils/version.py — whichever have pragma sites)

    Step 5: Verify the existing 100% coverage gate is still green (this task makes no changes):

        pdm run pytest --cov=cement -x tests
        make audit-public-api

    Both MUST exit 0.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ > /tmp/pragma-sites.txt && wc -l /tmp/pragma-sites.txt && grep -rn 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | awk -F: '{print $1}' | sort -u > /tmp/pragma-files.txt && wc -l /tmp/pragma-files.txt && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/pragma-sites.txt exists with the live pragma site count recorded
    - /tmp/pragma-files.txt exists with the per-file list
    - Live count is in the range 100-200 (catches Wave 6 unintended drift)
    - 4-batch grouping documented in the task summary for Tasks 2-5 to consume
    - pdm run pytest --cov=cement -x tests exits 0 with coverage 100%
    - make audit-public-api exits 0
    - No git commit (this is a pre-flight finding only)
  </acceptance_criteria>
  <done>Pragma site count + per-file batching captured; baseline gates green; Tasks 2-5 have the data they need.</done>
</task>

<task type="auto">
  <name>Task 2: Pragma audit Batch A — cement/core/*.py per-file commits</name>
  <files>cement/core/arg.py, cement/core/backend.py, cement/core/cache.py, cement/core/config.py, cement/core/controller.py, cement/core/extension.py, cement/core/foundation.py, cement/core/handler.py, cement/core/hook.py, cement/core/interface.py, cement/core/log.py, cement/core/mail.py, cement/core/output.py, cement/core/plugin.py, cement/core/template.py, CHANGELOG.md</files>
  <read_first>
    - /tmp/pragma-sites.txt + /tmp/pragma-files.txt (Task 1 output — exact site list)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-15 vocabulary; D-16 expansion rule; D-18 per-file commit allowance)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Code Examples Example 1 lines 590-677 — sample BEFORE/AFTER for each of the 8 categories)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 451-531 — sibling-comment shape from cli/main.py:53; locked vocabulary table; dual-spelling note)
    - cement/core/cache.py (representative interface file with abstract method pragmas — read to see the canonical pattern)
    - cement/core/foundation.py (representative file with multiple categories: TYPE_CHECKING imports, platform-specific SIGNALS, signal handler pragmas, defensive unreachable)
    - cement/core/backend.py (the version constant pragma site)
  </read_first>
  <action>
    For each cement/core/*.py file in /tmp/pragma-files.txt that has pragma sites, do ONE atomic per-file commit:

    Step 1: Read the file. For each pragma:nocover or pragma:no cover line in that file, classify the site by inspecting the surrounding code:
    - Is it a "pass" inside an interface method body? -> "abstract method"
    - Is it inside an "if TYPE_CHECKING:" block? -> "TYPE_CHECKING import"
    - Is it inside a platform check (e.g., "if platform.system() == 'Windows'", "if 'HOME' in os.environ")? -> "platform-specific"
    - Is it on a __import__ or importlib.import_module line? -> "untestable: dynamic import"
    - Is it on a subprocess.* line or shell-out call? -> "untestable: subprocess"
    - Is it inside a signal handler or frame-walking code? -> "untestable: signal handler"
    - Is it on an else: branch that coverage.py cannot prove unreachable? -> "defensive: unreachable"
    - Is it on a module-level VERSION = (...) tuple? -> "version constant"

    Step 2: Append the category label after the existing pragma comment, separated by two spaces:
    - BEFORE: pass    # pragma: nocover
    - AFTER:  pass    # pragma: nocover  # abstract method

    Preserve the existing pragma spelling (do not canonicalize "nocover" vs "no cover" — Pitfall 4 / RESEARCH.md State of the Art note).

    Step 3: If a site does not fit ANY of the 8 categories, STOP and per D-16 do this in a SEPARATE prior commit:
    1. Add a new category line to D-15 in CONTEXT.md
    2. Update the D-17 grep regex in CONTEXT.md and 03-VALIDATION.md
    3. Commit with subject "docs(03): expand pragma vocabulary"
    4. THEN apply the new label to the site in the per-file commit

    Step 4: After editing each file, run gates BEFORE committing:
    - pdm run pytest --cov=cement -x tests (must exit 0; coverage 100%)
    - make audit-public-api (must exit 0)

    Step 5: Commit per file. Subject format:

        refactor(test): audit pragma:nocover sites in core/<file>

    Body:

        Append D-15 locked-vocabulary category labels to <N> pragma
        sites in cement/core/<file>:
        - <category-1>: <count> sites
        - <category-2>: <count> sites
        - ...

        D-22 step 13 / D-18 per-file commit allowance.

    Stage exactly: cement/core/<file>, CHANGELOG.md (only after the LAST file in the batch — append a single Batch A summary entry to CHANGELOG.md to avoid 16 separate CHANGELOG entries; record the batch summary in the LAST commit's body).

    Step 6: After all Batch A files are committed, append CHANGELOG.md [dev] entry under Refactoring bucket:

        * [dev] Audit pragma:nocover sites in cement/core/ with D-15 locked-vocabulary category labels (per-file commits; Phase 03 D-15..D-18).

    Step 7: Verify the locked-vocabulary regex shows progress (will not be empty until Tasks 3-5 also land):

        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/core/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l

    Should now return 0 for cement/core/ specifically (cement/ext/, cement/cli/, cement/utils/ still pending).
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/core/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: cement/core/ pragma audit complete") && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - Every pragma site in cement/core/*.py carries one of the 8 locked-vocabulary category labels
    - Locked-vocabulary grep on cement/core/ specifically returns empty
    - pdm run pytest --cov=cement -x tests exits 0 with coverage 100%
    - make audit-public-api exits 0
    - One commit per cement/core/ file with pragma sites (per-file commits per D-18)
    - All commit subjects use the format "refactor(test): audit pragma:nocover sites in core/<file>"
    - CHANGELOG.md has one new [dev] entry summarizing Batch A
    - No commits invent free-form category labels (D-16 enforced)
  </acceptance_criteria>
  <done>Batch A complete; cement/core/ pragma audit passes the locked-vocabulary regex; gates green; per-file atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 3: Pragma audit Batch B — cement/ext/ first half per-file commits</name>
  <files>cement/ext/ext_alarm.py, cement/ext/ext_argparse.py, cement/ext/ext_colorlog.py, cement/ext/ext_configparser.py, cement/ext/ext_daemon.py, cement/ext/ext_dummy.py, cement/ext/ext_generate.py, cement/ext/ext_jinja2.py, cement/ext/ext_json.py, cement/ext/ext_logging.py, CHANGELOG.md</files>
  <read_first>
    - /tmp/pragma-sites.txt + /tmp/pragma-files.txt
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-15..D-18)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pragma site inventory — ext/* expected categories: TYPE_CHECKING import + platform-specific + dynamic import + defensive unreachable)
    - cement/ext/ext_logging.py (representative — has the most-decorated existing pragma site at line 35; mix of platform-specific NullHandler fallback + abstract pattern)
    - cement/ext/ext_plugin.py (representative — dynamic import pragmas at lines 150-153)
  </read_first>
  <action>
    Same pattern as Task 2, applied to each cement/ext/ file in Batch B (alphabetical first half: ext_alarm through ext_logging).

    Per file, per pragma site, classify against the 8-category vocabulary and append the label. Most ext/* sites are expected to be:
    - TYPE_CHECKING import (every ext_*.py imports App for type hints)
    - platform-specific (NullHandler fallbacks in ext_logging)
    - untestable: dynamic import (ext_plugin, ext_generate)
    - defensive: unreachable (various fallback else branches)

    Per-file atomic commits with subject format:

        refactor(test): audit pragma:nocover sites in ext/<file>

    Run gates after each file, before committing.

    After all Batch B files are committed, append CHANGELOG.md [dev] entry:

        * [dev] Audit pragma:nocover sites in cement/ext/ first half with D-15 locked vocabulary (Batch B).

    Verify Batch B subset of the locked-vocabulary regex is now clean:

        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ext/ext_alarm.py cement/ext/ext_argparse.py cement/ext/ext_colorlog.py cement/ext/ext_configparser.py cement/ext/ext_daemon.py cement/ext/ext_dummy.py cement/ext/ext_generate.py cement/ext/ext_jinja2.py cement/ext/ext_json.py cement/ext/ext_logging.py | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l

    Should return 0.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ext/ext_alarm.py cement/ext/ext_argparse.py cement/ext/ext_colorlog.py cement/ext/ext_configparser.py cement/ext/ext_daemon.py cement/ext/ext_dummy.py cement/ext/ext_generate.py cement/ext/ext_jinja2.py cement/ext/ext_json.py cement/ext/ext_logging.py 2>/dev/null | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: Batch B ext/ first half clean") && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - Every pragma site in Batch B files carries a locked-vocabulary category label
    - Locked-vocabulary grep on Batch B files returns empty
    - pdm run pytest --cov=cement -x tests exits 0 with coverage 100%
    - make audit-public-api exits 0
    - One commit per file with pragma sites in Batch B
    - CHANGELOG.md has one new [dev] entry summarizing Batch B
  </acceptance_criteria>
  <done>Batch B complete; ext/ first half pragma audit clean; gates green; per-file atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 4: Pragma audit Batch C — cement/ext/ second half per-file commits</name>
  <files>cement/ext/ext_memcached.py, cement/ext/ext_mustache.py, cement/ext/ext_plugin.py, cement/ext/ext_print.py, cement/ext/ext_redis.py, cement/ext/ext_scrub.py, cement/ext/ext_smtp.py, cement/ext/ext_tabulate.py, cement/ext/ext_watchdog.py, cement/ext/ext_yaml.py, CHANGELOG.md</files>
  <read_first>
    - /tmp/pragma-sites.txt + /tmp/pragma-files.txt
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-15..D-18)
    - cement/ext/ext_smtp.py (representative — defensive unreachable fallbacks at lines 189, 334, 345)
    - cement/ext/ext_watchdog.py (representative — single-tuple defensive at line 166; defensive elses at 48, 171, 173)
  </read_first>
  <action>
    Same pattern as Tasks 2 and 3, applied to each cement/ext/ file in Batch C (alphabetical second half: ext_memcached through ext_yaml).

    Per-file atomic commits with subject format:

        refactor(test): audit pragma:nocover sites in ext/<file>

    After all Batch C files committed, append CHANGELOG.md [dev] entry:

        * [dev] Audit pragma:nocover sites in cement/ext/ second half with D-15 locked vocabulary (Batch C).

    Verify Batch C subset clean:

        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ext/ext_memcached.py cement/ext/ext_mustache.py cement/ext/ext_plugin.py cement/ext/ext_print.py cement/ext/ext_redis.py cement/ext/ext_scrub.py cement/ext/ext_smtp.py cement/ext/ext_tabulate.py cement/ext/ext_watchdog.py cement/ext/ext_yaml.py | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l

    Should return 0.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ext/ext_memcached.py cement/ext/ext_mustache.py cement/ext/ext_plugin.py cement/ext/ext_print.py cement/ext/ext_redis.py cement/ext/ext_scrub.py cement/ext/ext_smtp.py cement/ext/ext_tabulate.py cement/ext/ext_watchdog.py cement/ext/ext_yaml.py 2>/dev/null | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: Batch C ext/ second half clean") && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - Every pragma site in Batch C files carries a locked-vocabulary category label
    - Locked-vocabulary grep on Batch C files returns empty
    - pdm run pytest --cov=cement -x tests exits 0 with coverage 100%
    - make audit-public-api exits 0
    - One commit per file with pragma sites in Batch C
    - CHANGELOG.md has one new [dev] entry summarizing Batch C
  </acceptance_criteria>
  <done>Batch C complete; ext/ second half pragma audit clean; gates green; per-file atomic commits landed.</done>
</task>

<task type="auto">
  <name>Task 5: Pragma audit Batch D — cement/cli/ + cement/utils/ per-file commits + GLOBAL D-17 verification</name>
  <files>cement/cli/main.py, cement/utils/fs.py, cement/utils/shell.py, cement/utils/version.py, CHANGELOG.md</files>
  <read_first>
    - /tmp/pragma-sites.txt + /tmp/pragma-files.txt
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-15..D-18; D-24 conjunct #7)
    - cement/cli/main.py:53 (representative — has existing sibling-comment shape "# pragma: nocover  # noqa: T201"; the new D-15 label appends after)
    - cement/utils/fs.py:237 (defensive unreachable backup else branch — already migrated to pathlib in Wave 6)
    - cement/utils/fs.py:245 (platform-specific HOME_DIR fallback)
    - cement/utils/version.py:97-106 (untestable: subprocess git log calls)
  </read_first>
  <action>
    Same pattern as Tasks 2-4, applied to each cement/cli/ and cement/utils/ file in Batch D.

    Per-file atomic commits with subject formats:

        refactor(test): audit pragma:nocover sites in cli/main.py
        refactor(test): audit pragma:nocover sites in utils/fs.py
        refactor(test): audit pragma:nocover sites in utils/shell.py
        refactor(test): audit pragma:nocover sites in utils/version.py

    Special note for cement/cli/main.py:53: existing line is "print(...)  # pragma: nocover  # noqa: T201". The D-15 label appends as a third sibling comment:

    BEFORE: print(f'AssertionError ...')  # pragma: nocover  # noqa: T201
    AFTER:  print(f'AssertionError ...')  # pragma: nocover  # noqa: T201  # defensive: unreachable

    Special note for cement/utils/fs.py:245: this site IS the canonical platform-specific example from Code Examples (RESEARCH.md). After append:
    AFTER: HOME_DIR = abspath('~')  # pragma: nocover  # platform-specific

    Special note for cement/utils/version.py:97: this site IS the canonical untestable: subprocess example. After append:
    AFTER: result = subprocess.check_output(...)  # pragma: nocover  # untestable: subprocess

    After all Batch D files committed, append CHANGELOG.md [dev] entry under Refactoring bucket:

        * [dev] Audit pragma:nocover sites in cement/cli/ + cement/utils/ with D-15 locked vocabulary (Batch D). Closes COV-03 / D-24 conjunct #7 across all of cement/.

    GLOBAL D-17 VERIFICATION (the entire phase's COV-03 acceptance):

        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l

    MUST return 0. This is D-24 conjunct #7. If non-zero, identify the unlabeled sites and either label them (if they fit a category) or trigger D-16 vocabulary expansion.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: D-24 conjunct #7 GREEN — all 141 pragma sites labeled with locked vocabulary") && pdm run pytest --cov=cement -x tests && make audit-public-api && make comply-ruff && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - Every pragma site across ALL of cement/ carries a locked-vocabulary category label
    - GLOBAL D-17 grep returns empty (D-24 conjunct #7 GREEN — COV-03 acceptance)
    - pdm run pytest --cov=cement -x tests exits 0 with coverage 100% (D-24 #1)
    - make audit-public-api exits 0 (D-24 #4)
    - make comply-ruff exits 0 (D-24 #2)
    - pdm run mypy cement exits 0 (D-24 #3)
    - One commit per Batch D file
    - CHANGELOG.md has one new [dev] entry summarizing Batch D and the closure of COV-03
  </acceptance_criteria>
  <done>Batch D complete; GLOBAL D-17 grep empty; D-24 conjunct #7 GREEN; COV-03 acceptance closed; gates all green; per-file atomic commits landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pragma comment append | Comment-only changes; no semantic effect. coverage.py honors both "nocover" and "no cover" spellings; the appended category label is purely informational. |
| Locked vocabulary enforcement | The D-17 grep is the gate. Any deviation (free-form label per D-16 violation) shows up immediately as a non-empty grep result. |

## STRIDE Threat Register

This is comment-only annotation work with NO new attack surface:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Inadvertent code change while editing pragma comment line | mitigate | Per-file diff inspection before commit. pdm run pytest --cov=cement -x tests after each file catches any inadvertent semantic change. |
| T-03-07-02 | DoS | Free-form label slips in (D-16 violation) | mitigate | Final D-17 grep (Task 5) is the gate. Any non-locked label shows as a non-empty grep result, blocking commit. |

**No new attack surface introduced.** Comment-only annotation work.
</threat_model>

<verification>
After this plan completes:
1. GLOBAL D-17 grep returns empty (D-24 conjunct #7 GREEN — COV-03 acceptance closed)
2. pdm run pytest --cov=cement -x tests exits 0 with coverage 100% (D-24 #1)
3. make audit-public-api exits 0 (D-24 #4)
4. make comply-ruff exits 0 (D-24 #2)
5. pdm run mypy cement exits 0 (D-24 #3)
6. **COV-02 wave check (per checker fix):** `make test` at wave end generates `coverage-report/index.html` — verify `test -f coverage-report/index.html && test -d coverage-report/` exits 0 (D-24 conjunct #5)
7. Approximately 39 per-file commits visible (one per file with pragma sites in Tasks 2-5)
8. 4 CHANGELOG entries (Batch A, B, C, D summaries)

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-07-01 | 7 | COV-03 (pre-flight) | #1, #4 | grep + pytest | grep pragma sites and per-file batching |
| 03-07-02 | 7 | COV-03 | #1, #4, #7 (partial) | grep + pytest | locked-vocab grep on cement/core/ returns empty |
| 03-07-03 | 7 | COV-03 | #1, #4, #7 (partial) | grep + pytest | locked-vocab grep on Batch B files returns empty |
| 03-07-04 | 7 | COV-03 | #1, #4, #7 (partial) | grep + pytest | locked-vocab grep on Batch C files returns empty |
| 03-07-05 | 7 | COV-03 | #1, #2, #3, #4, #7 | global grep + all gates | GLOBAL D-17 grep returns empty + all 4 D-24 gates green |
</verification>

<success_criteria>
- D-24 conjunct #7 GREEN: GLOBAL locked-vocabulary grep returns empty across all of cement/
- COV-03 acceptance closed
- D-24 conjuncts #1, #2, #3, #4 stay green
- Approximately 39 atomic per-file commits landed (one per file with pragma sites)
- 4 CHANGELOG entries summarizing the per-batch sweep
- No free-form category labels invented (D-16 enforced by the D-17 grep)
</success_criteria>

<output>
After completion, create .planning/phases/03-internal-refactor-coverage-hardening/03-07-SUMMARY.md with:
- Live pragma count from /tmp/pragma-sites.txt (record actual; expected approx 141 per RESEARCH.md but Wave 6 may have shifted it)
- Per-batch breakdown (Batch A core, B ext first half, C ext second half, D cli + utils): file count, site count, commit count
- Per-category breakdown across all of cement/ (e.g., abstract method: N sites, TYPE_CHECKING import: N sites, etc.)
- Total commit count (approx 39 per-file commits)
- D-17 GLOBAL grep result: must be EMPTY (paste the actual command output as evidence)
- Confirmation D-24 conjunct #7 GREEN
- Note any D-16 vocabulary expansion that was triggered (should be zero per RESEARCH.md classification, but record any new categories added)
</output>
