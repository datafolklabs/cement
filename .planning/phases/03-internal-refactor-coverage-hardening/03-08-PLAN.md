---
phase: 03-internal-refactor-coverage-hardening
plan: 08
type: execute
wave: 8
depends_on: [07]
files_modified:
  - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
autonomous: true
requirements: [REFACTOR-01, REFACTOR-02, REFACTOR-03, REFACTOR-04, COV-01, COV-02, COV-03]

must_haves:
  truths:
    - "03-VERIFICATION.md is finalized with the full D-24 9-conjunct verification (Wave 5 added pre-baselines; this wave records post-counts, deltas, and gate exit statuses)"
    - "Pre/post Any count + delta is recorded (REFACTOR-02 acceptance / D-24 #6)"
    - "Baseline-vs-post audit-public-api diff is recorded as empty (D-04 / D-24 #4)"
    - "make audit-public-api exit status recorded as 0 (D-24 #4)"
    - "REFACTOR-01 acceptance-via-coverage rationale recorded (D-20 / D-24 #1)"
    - "Locked-vocabulary grep result recorded as empty (D-17 / D-24 #7)"
    - "os.path grep result in scoped files recorded as empty OR only # boundary:-tagged sites (D-14 / D-24 #8)"
    - "from __future__ import annotations grep across cement/ recorded as empty (D-08 / D-24 #9)"
    - "make superclean && make init && make test && make comply final reset+verification recorded green (defense-in-depth per RESEARCH.md Runtime State Inventory)"
    - "All 9 D-24 conjuncts GREEN; phase verification status flips to passed; ROADMAP Phase 3 row marked complete"
  artifacts:
    - path: ".planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md"
      provides: "Finalized phase verification artifact with all 9 D-24 conjuncts as evidence"
      min_lines: 80
  key_links:
    - from: "Wave 5 pre-baselines in 03-VERIFICATION.md"
      to: "Wave 8 post-counts + deltas"
      via: "appended sections under same headings"
      pattern: "Post-tightening count|Post-audit count|Post-migration"
    - from: "ROADMAP.md Phase 3 row"
      to: "Phase 3 status complete"
      via: "checkbox + completion date"
      pattern: "[x] \\*\\*Phase 3"
---

<objective>
Finalize Phase 03 by capturing the full D-24 9-conjunct verification
in 03-VERIFICATION.md and updating ROADMAP.md to mark Phase 3 complete.

Wave 5 created 03-VERIFICATION.md with pre-baselines (Any count,
pragma count, pathlib pre-migration count). This wave appends post-
counts, deltas, and the full evidence for every D-24 conjunct.

D-22 step 14 — `docs(03): record Any-baseline + REFACTOR-01
verification` was the title given in CONTEXT for this finalization
commit. The Wave 5 plan used the same subject for its in-progress
baseline; to avoid duplicate subjects, this wave's commit uses:

    docs(03): finalize phase verification + D-24 9-conjunct evidence

The ROADMAP update is a separate small commit:

    docs(roadmap): mark Phase 3 complete

Output: 2 commits (finalize 03-VERIFICATION.md + mark Phase 3 complete
in ROADMAP).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-01-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-02-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-03-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-04-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-05-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-06-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-07-SUMMARY.md
@.planning/phases/01-tooling-baseline-python-matrix/01-VERIFICATION.md
@.planning/phases/02-dependencies-ci-pipeline/02-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final superclean+init reset and capture all D-24 conjunct evidence</name>
  <files>(read-only / capture only — no commit)</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-24 — full 9-conjunct acceptance)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md (current Wave 5 baseline state — needs post-counts appended)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Runtime State Inventory — recommends make superclean+init for cache reset before final verification)
    - All Wave 1-7 SUMMARY files (use to cross-check the actual final commit count, finding counts, etc.)
  </read_first>
  <action>
    Step 1: Run the final reset to clear any stale .mypy_cache / .ruff_cache / .pytest_cache from intermediate waves:

        make superclean && make init

    Step 2: Run all 9 D-24 conjunct verifications in sequence and capture each result:

    Conjunct #1 (REFACTOR-01 / COV-01 — make test passes at 100% coverage):

        make test 2>&1 | tee /tmp/d24-01.txt
        echo "Exit: $?"

    Conjunct #2 (TOOL-04 — comply-ruff with UP family enabled):

        make comply-ruff 2>&1 | tee /tmp/d24-02.txt
        echo "Exit: $?"

    Conjunct #3 (REFACTOR-02 — comply-mypy):

        make comply-mypy 2>&1 | tee /tmp/d24-03.txt
        echo "Exit: $?"

    Conjunct #4 (D-04 / SC-3 — audit-public-api):

        make audit-public-api 2>&1 | tee /tmp/d24-04.txt
        echo "Exit: $?"

    Conjunct #5 (COV-02 — coverage HTML clean):

        ls -la coverage-report/index.html 2>&1 | tee /tmp/d24-05.txt
        echo "Exit: $?"

    Conjunct #6 (REFACTOR-02 / SC-4 — Any count strictly lower):

        POST_COUNT=$(grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l)
        echo "Post-tightening Any count: $POST_COUNT" | tee /tmp/d24-06.txt
        # Compare with Wave 5 pre-count from 03-VERIFICATION.md

    Conjunct #7 (COV-03 / SC-2 — locked vocabulary grep empty):

        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | tee /tmp/d24-07.txt
        wc -l /tmp/d24-07.txt
        # MUST be 0 lines

    Conjunct #8 (REFACTOR-03 / SC-5 — os.path grep in scoped files empty or # boundary: only):

        grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | tee /tmp/d24-08.txt
        # MUST be empty OR all hits tagged # boundary: on same/adjacent line
        grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l
        # MUST be 0

    Conjunct #9 (D-08 — from __future__ import annotations grep empty):

        grep -rn 'from __future__ import annotations' cement/ | tee /tmp/d24-09.txt
        wc -l /tmp/d24-09.txt
        # MUST be 0 lines

    Step 3: Aggregate all 9 results. Determine pass/fail for each. ALL 9 MUST PASS.

    Step 4: Capture the actual commit count for the phase summary:

        git log --oneline main..HEAD | wc -l
        git log --oneline main..HEAD | tee /tmp/phase-03-commits.txt
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && make test && make comply-ruff && make comply-mypy && make audit-public-api && test -f coverage-report/index.html && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #7 PASS") && grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #8 PASS") && grep -rn 'from __future__ import annotations' cement/ | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #9 PASS")</automated>
  </verify>
  <acceptance_criteria>
    - make superclean && make init completed cleanly (cache fully reset)
    - All 9 D-24 conjuncts captured to /tmp/d24-XX.txt files with their exit statuses recorded
    - Conjunct #1: make test exits 0, coverage 100%
    - Conjunct #2: make comply-ruff exits 0
    - Conjunct #3: make comply-mypy exits 0
    - Conjunct #4: make audit-public-api exits 0
    - Conjunct #5: coverage-report/index.html exists, no warnings
    - Conjunct #6: Any post-count strictly lower than Wave 5 pre-count
    - Conjunct #7: locked-vocabulary grep returns 0 lines
    - Conjunct #8: os.path grep in scoped files returns 0 untagged lines
    - Conjunct #9: from __future__ grep returns 0 lines
    - Phase commit count captured (expected approx 19-25: Wave 1 (1) + Wave 2 (1) + Wave 3 (5) + Wave 4 (1) + Wave 5 (2) + Wave 6 (4) + Wave 7 (~39 per-file) + Wave 8 (2) — actual will vary based on Wave 7 file count)
  </acceptance_criteria>
  <done>All 9 D-24 conjuncts verified GREEN; superclean+init reset confirmed; evidence files captured for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 03-VERIFICATION.md with full D-24 evidence</name>
  <files>.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md (current Wave 5 in-progress state)
    - /tmp/d24-01.txt through /tmp/d24-09.txt (Task 1 captured evidence)
    - /tmp/phase-03-commits.txt (commit count + log)
    - .planning/phases/01-tooling-baseline-python-matrix/01-VERIFICATION.md (canonical structure to mirror — Goal Achievement, Required Artifacts, Behavioral Spot-Checks, Requirements Coverage, Gaps Summary)
    - .planning/phases/02-dependencies-ci-pipeline/02-VERIFICATION.md (newer pattern with PENDING-evidence shapes)
  </read_first>
  <action>
    Update .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md to its final form. Replace the placeholder "TBD" sections with the captured Task 1 evidence. Keep the existing pre-baseline content; APPEND post-counts/deltas/D-24 evidence.

    Update the frontmatter:

        ---
        phase: 03-internal-refactor-coverage-hardening
        verified: <YYYY-MM-DDTHH:MM:SSZ>
        status: passed
        score: 9/9 D-24 conjuncts GREEN
        overrides_applied: 0
        ---

    Add or finalize these sections (mirror Phase 1/2 structure):

    1. # Phase 03 Verification (existing intro; update status from in-progress to passed)

    2. ## D-24 9-Conjunct Acceptance (NEW table — primary evidence)

       Use a table with columns: # | Gate | Command | Expected | Result | Status. Populate every row from /tmp/d24-XX.txt. Use the exact 9 rows from 03-VALIDATION.md "D-24 Acceptance Conjunction" table.

    3. ## Any-in-core baseline (UPDATE existing section)

       - Pre-tightening count: <N> (recorded in Wave 5)
       - Post-tightening count: <M> (from /tmp/d24-06.txt)
       - Delta: -<N - M> sites (must be strictly positive)
       - Per-site classification: link to Wave 5 commit body for details

    4. ## Pragma audit baseline (UPDATE existing section)

       - Pre-audit count: <X> (recorded in Wave 5)
       - Post-audit count: <Y> (current grep)
       - All sites carry locked-vocabulary category labels per D-15
       - D-17 verification grep result: EMPTY (paste actual command output)

    5. ## Pathlib boundary baseline (UPDATE existing section)

       - Pre-migration os.path callsites in scoped files: <P> (recorded in Wave 5)
       - Post-migration: <Q> (current grep — must be 0 untagged)
       - All 4 named files migrated: cement/utils/fs.py, cement/core/foundation.py, cement/core/template.py, cement/core/config.py
       - Surviving os.path callsites carry # boundary: comment per D-14
       - D-19 protected .format(**template_dict) callsites: unchanged in foundation.py + template.py

    6. ## Public API baseline (UPDATE existing section)

       - Baseline captured Wave 1: 03-PUBLIC-API-BASELINE.txt (<L> lines)
       - Post-refactor diff: empty (make audit-public-api exit 0 across all 8 waves)
       - Permanent dev affordance per D-05: scripts/audit-public-api.py + Makefile target retained

    7. ## REFACTOR-01 acceptance-via-coverage rationale (NEW section per D-20)

       Per D-20, REFACTOR-01 (dead code removed without affecting public API or test coverage) is satisfied via the existing 100% coverage gate (`fail_under = 100` + `--cov-fail-under=100`). The post-refactor make test passes at 100% coverage, which by definition implies zero unreachable code at the statement level.

       Risk acknowledgement (D-21): two classes of dead-but-covered code remain undetected:
       (a) covered-but-functionally-dead code (executes in tests without meaningful asserts)
       (b) unused private helpers reachable via import but not called by any production caller

       Future milestones (3.2.0 cleanup, dedicated audit milestone) may re-open with vulture if these gaps prove to bite. Recorded here so the deferral is intentional, not oversight.

    8. ## Behavioral Spot-Checks (NEW table per Phase 1/2 pattern)

       Sample 3-5 representative behavior tests that exercise refactored code (e.g., fs.abspath returns str, foundation App._find_config_files works with Path internals, template render still works with .format(**template_dict) protection).

    9. ## Requirements Coverage (NEW table per Phase 1/2 pattern)

       Map each REQ-ID to SATISFIED status:
       - REFACTOR-01: SATISFIED (D-20 acceptance via 100% coverage gate)
       - REFACTOR-02: SATISFIED (Any post-count <M> < pre-count <N>; D-24 #6 GREEN)
       - REFACTOR-03: SATISFIED (D-24 #8 GREEN; 4 scoped files migrated; boundary preserved)
       - REFACTOR-04: SATISFIED (UP032 sweep + opportunistic stdlib idioms via Wave 3-4)
       - COV-01: SATISFIED (D-24 #1 GREEN; 100% coverage held across every commit)
       - COV-02: SATISFIED (D-24 #5 GREEN; HTML report clean)
       - COV-03: SATISFIED (D-24 #7 GREEN; locked-vocabulary grep empty)

    10. ## ROADMAP Success Criteria Mapping (NEW table — explicit map to ROADMAP Phase 3 SC #1-5)

        - SC #1: 100% coverage report with zero drift -> SATISFIED via D-24 #1
        - SC #2: coverage-report/ HTML generates without warnings + every pragma:no cover line has inline justification -> SATISFIED via D-24 #5 + #7
        - SC #3: diff of public symbols between main and refactor branch is empty -> SATISFIED via D-24 #4 (audit-public-api exit 0 byte-for-byte)
        - SC #4: mypy strict mode reports fewer Any occurrences in cement/core/ than pre-refactor baseline; concrete number captured in phase summary -> SATISFIED via D-24 #6 (delta <N> -> <M>, recorded above)
        - SC #5: os.path usage in cement/utils/fs.py and core internals migrated to pathlib where it does not change a public signature; remaining os.path callsites explicitly documented as boundary-preserving -> SATISFIED via D-24 #8 (zero untagged os.path in scoped files)

    11. ## Gaps Summary (NEW section per Phase 1/2 pattern)

        - None. All 9 D-24 conjuncts GREEN. All 7 phase requirements (REFACTOR-01..04, COV-01..03) SATISFIED.
        - Deferred items (NOT gaps): vulture-based dead-code audit (D-21 acknowledged), High-priority untestable pragma blocks (CONCERNS.md), pathlib migration in cement/cli/ + cement/ext/, FIXME-comment cleanup, mypy strictness knob tightening.

    Append CHANGELOG.md [dev] entry under Misc bucket:

        * [dev] Phase 03 verification finalized: all 9 D-24 conjuncts GREEN; REFACTOR-01..04 + COV-01..03 satisfied; record in 03-VERIFICATION.md.

    Verify gates one final time (Wave 8 must not break the gates it certifies):

        make test
        make comply-ruff
        make comply-mypy
        make audit-public-api

    All 4 must exit 0.

    Commit:

        docs(03): finalize phase verification + D-24 9-conjunct evidence

        Update 03-VERIFICATION.md from in-progress (Wave 5 baseline) to
        passed status. Record full D-24 9-conjunct acceptance evidence:

        1. make test passes at 100% coverage (D-24 #1)
        2. make comply-ruff passes with UP+FA family (D-24 #2)
        3. make comply-mypy passes (D-24 #3)
        4. make audit-public-api exits 0 (D-24 #4)
        5. coverage-report/ HTML clean (D-24 #5)
        6. Any in cement/core/ count: <N> -> <M> (delta -<N - M>; D-24 #6)
        7. Locked-vocabulary pragma grep: empty (D-24 #7)
        8. os.path in scoped files: empty or # boundary: only (D-24 #8)
        9. from __future__ import annotations: empty (D-24 #9)

        REFACTOR-01..04 + COV-01..03 SATISFIED. ROADMAP Phase 3 success
        criteria #1-5 mapped GREEN. No gaps. Deferred items recorded
        as intentional (D-21 vulture; signal-handler refactor;
        Cement 4 territory).

        D-22 step 14 (renamed from CONTEXT-suggested title to avoid
        duplication with Wave 5 baseline commit subject).

    Stage exactly: .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md, CHANGELOG.md.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c 'D-24 9-Conjunct' /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -ge 1 && echo "PASS: D-24 section present") && grep -c 'status: passed' /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -ge 1 && echo "PASS: status=passed") && make test && make comply-ruff && make comply-mypy && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - 03-VERIFICATION.md frontmatter status is "passed" with score "9/9 D-24 conjuncts GREEN"
    - File contains the D-24 9-Conjunct Acceptance section with all 9 rows populated
    - Pre/post Any count and delta recorded (must be strictly positive in reduction direction)
    - Pre/post pragma count recorded
    - Pre/post pathlib os.path callsite count recorded
    - REFACTOR-01 acceptance-via-coverage rationale section present (D-20 / D-21)
    - Requirements Coverage table maps all 7 REQ-IDs to SATISFIED
    - ROADMAP Success Criteria mapping table maps SC #1-5 to SATISFIED
    - Gaps Summary section confirms zero gaps with deferred items recorded
    - All 4 gates (test, comply-ruff, comply-mypy, audit-public-api) exit 0 post-commit
    - Single commit `docs(03): finalize phase verification + D-24 9-conjunct evidence` exists
    - CHANGELOG.md has new [dev] entry
  </acceptance_criteria>
  <done>03-VERIFICATION.md finalized with full evidence; all 9 conjuncts GREEN; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md to mark Phase 3 complete</name>
  <files>.planning/ROADMAP.md, CHANGELOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 3 row at line 19, Phases table at line 144-152, Phase 3 detail at lines 92-102)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md (final state from Task 2 — confirms passed)
    - .planning/phases/02-dependencies-ci-pipeline/ (the previous phase's completion shape — `(completed 2026-05-02; ...)` style)
  </read_first>
  <action>
    Update .planning/ROADMAP.md in three places:

    Step 1: Line 19 — change Phase 3 row from unchecked to checked with completion date:

    BEFORE:
        - [ ] **Phase 3: Internal Refactor & Coverage Hardening** - Cleanup-only refactor under the 100% coverage gate

    AFTER:
        - [x] **Phase 3: Internal Refactor & Coverage Hardening** - Cleanup-only refactor under the 100% coverage gate (completed <YYYY-MM-DD>; all 9 D-24 conjuncts GREEN — see 03-VERIFICATION.md)

    Step 2: Lines 92-102 (Phase 3 detail block) — update **Plans**: line. Change from "TBD" to the actual plan count and add per-wave checkbox list mirroring Phase 2's pattern:

        **Plans**: 8 plans across 8 waves (sequential due to file-ownership invariants)

          **Wave 1**
          - [x] 03-01-PLAN.md — Capture public API baseline (D-04; gates all subsequent waves)

          **Wave 2** (blocked on Wave 1)
          - [x] 03-02-PLAN.md — Re-enable ruff UP+FA family with AUDIT POINT comment (D-06)

          **Wave 3** (blocked on Wave 2)
          - [x] 03-03-PLAN.md — UP family auto-fixes (UP006/UP007/UP045/UP032) + CONVENTIONS.md PEP 604/585 refresh

          **Wave 4** (blocked on Wave 3 — UP must precede FA per D-08)
          - [x] 03-04-PLAN.md — FA100 future-annotations strip across cement/

          **Wave 5** (blocked on Wave 4)
          - [x] 03-05-PLAN.md — Any tightening in cement/core/ + 03-VERIFICATION.md baselines

          **Wave 6** (blocked on Wave 5; A7 symlink pre-flight first)
          - [x] 03-06-PLAN.md — pathlib migration: utils/fs.py, core/config.py, core/foundation.py, core/template.py (atomic per file per D-13)

          **Wave 7** (blocked on Wave 6)
          - [x] 03-07-PLAN.md — pragma:nocover audit with D-15 locked vocabulary (per-file commits across approx 39 files)

          **Wave 8** (blocked on Wave 7 — final acceptance gate)
          - [x] 03-08-PLAN.md — Finalize 03-VERIFICATION.md + mark Phase 3 complete (D-22 step 14)

          **Cross-cutting constraints** (carried through every plan)
          - 100% coverage gate must remain green after each commit (Phase 2 D-10/D-11/D-12)
          - All commits follow Conventional Commits + 78-char wrap
          - CHANGELOG.md updated phase-by-phase per CLAUDE.md
          - No public-API breakage on the 3.0.x track (D-04 audit gate enforces byte-for-byte)
          - audit-public-api exit 0 on every commit

    Step 3: Lines 144-152 (Phases progress table) — update Phase 3 row:

    BEFORE:
        | 3. Internal Refactor & Coverage Hardening | 0/TBD | Not started | - |

    AFTER:
        | 3. Internal Refactor & Coverage Hardening | 8/8 | Complete   | <YYYY-MM-DD> |

    Append CHANGELOG.md [dev] entry under Misc bucket:

        * [dev] Phase 03 (Internal Refactor & Coverage Hardening) complete: REFACTOR-01..04 + COV-01..03 satisfied; ROADMAP updated.

    Verify ROADMAP is internally consistent:
    - `grep -c '\[x\] \*\*Phase 3' ROADMAP.md` returns 1 (Phase 3 marked complete)
    - All 8 sub-plan checkboxes are `[x]`
    - Plans count "8/8 Complete" matches the 8 PLAN files in the phase directory

    Commit:

        docs(roadmap): mark Phase 3 complete

        Update ROADMAP.md to mark Phase 3 (Internal Refactor & Coverage
        Hardening) complete:
        - Phase 3 row checked with completion date
        - Plans block expanded to per-wave checkbox list (8 plans across
          8 waves; sequential due to file-ownership invariants)
        - Progress table row updated to "8/8 Complete"

        All 9 D-24 conjuncts GREEN per 03-VERIFICATION.md.
        REFACTOR-01..04 + COV-01..03 SATISFIED.

    Stage exactly: .planning/ROADMAP.md, CHANGELOG.md.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c '\[x\] \*\*Phase 3' /Users/derks/Development/DFL/cement/.planning/ROADMAP.md | (read n; test "$n" -eq 1 && echo "PASS: Phase 3 marked complete") && grep -c '8/8 ' /Users/derks/Development/DFL/cement/.planning/ROADMAP.md | (read n; test "$n" -ge 1 && echo "PASS: 8/8 progress recorded")</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 3 row line is checked `[x]` with completion date
    - ROADMAP.md Phase 3 detail block lists 8 sub-plans all checked `[x]`
    - ROADMAP.md progress table row for Phase 3 shows "8/8" and "Complete"
    - All 8 sub-plans correctly named (03-01-PLAN.md through 03-08-PLAN.md)
    - Single commit `docs(roadmap): mark Phase 3 complete` exists
    - CHANGELOG.md has new [dev] entry
  </acceptance_criteria>
  <done>ROADMAP.md updated to reflect Phase 3 complete; one atomic commit landed; phase fully closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification artifact -> downstream evidence | 03-VERIFICATION.md is the canonical evidence for Phase 03 acceptance. Future phases (Phase 5 deprecations, Phase 6 release cut) trust it. |
| ROADMAP completion marker -> milestone tracking | Phase 3 row flip is the signal Phase 4/5 can begin. |

## STRIDE Threat Register

This is documentation-only finalization with NO new attack surface:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | Verification artifact records false-positive (claims pass when gates failed) | mitigate | Wave 8 Task 1 captures actual command outputs to /tmp/d24-XX.txt files; Task 2 transcribes from those files. Re-running gates post-commit (Task 2 final verify) confirms artifact accuracy. |

**No new attack surface introduced.** Documentation finalization only.
</threat_model>

<verification>
After this plan completes:
1. 03-VERIFICATION.md status: passed (9/9 D-24 conjuncts GREEN)
2. ROADMAP.md Phase 3 row marked [x] complete
3. Phase 3 fully closed; Phase 4 and Phase 5 are unblocked
4. Two Wave 8 commits visible:
   - docs(03): finalize phase verification + D-24 9-conjunct evidence
   - docs(roadmap): mark Phase 3 complete
5. Total Phase 3 commit count approx 19-25 across 8 waves

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-08-01 | 8 | All 7 REQ + D-24 | All 9 | shell + grep + ls | superclean+init + all 9 D-24 commands |
| 03-08-02 | 8 | Documentation | #1, #2, #3, #4 | grep + gates | grep verification artifact + re-run gates |
| 03-08-03 | 8 | Roadmap update | none (doc-only) | grep | ROADMAP consistency check |
</verification>

<success_criteria>
- All 9 D-24 conjuncts GREEN (verified post-superclean+init reset)
- 03-VERIFICATION.md finalized with status=passed and 9/9 score
- ROADMAP.md updated: Phase 3 row checked, 8/8 plans complete, progress table updated
- Two atomic commits landed (verification + roadmap)
- 2 new CHANGELOG entries
- Phase 3 fully closed; ready for /gsd-verify-work final pass and Phase 4 start
</success_criteria>

<output>
After completion, create .planning/phases/03-internal-refactor-coverage-hardening/03-08-SUMMARY.md with:
- All 9 D-24 conjunct results (paste each gate's exit status + key output)
- Final Any count delta (e.g., 40 -> 32, -8)
- Final pragma count (should equal pre-count; only labels were appended)
- Final os.path in scoped files count (must be 0 untagged)
- Final from __future__ count (must be 0)
- Total Phase 3 commit count
- 2 commit SHAs + subjects
- 03-VERIFICATION.md final status
- ROADMAP.md update summary
- Confirmation Phase 3 closed and Phases 4/5 unblocked
</output>
