---
phase: 03-internal-refactor-coverage-hardening
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - cement/core/cache.py
  - cement/core/config.py
  - cement/core/foundation.py
  - cement/core/handler.py
  - cement/core/mail.py
  - cement/core/template.py
  - cement/core/output.py
  - cement/core/exc.py
  - cement/core/interface.py
  - cement/core/extension.py
  - cement/core/hook.py
  - cement/core/meta.py
  - cement/core/arg.py
  - cement/core/log.py
  - cement/core/controller.py
  - cement/core/plugin.py
  - cement/core/backend.py
  - cement/core/deprecations.py
  - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
autonomous: true
requirements: [REFACTOR-02, COV-01]

must_haves:
  truths:
    - "03-VERIFICATION.md exists with the recorded pre-tightening Any count in cement/core/ (D-09; expected 40 sites per RESEARCH.md verified 2026-05-03, NOT the ~25 estimate from CONTEXT.md)"
    - "Post-tightening Any count in cement/core/ is STRICTLY LOWER than the pre-count (D-09 / D-24 conjunct #6 acceptance)"
    - "Every surviving Any in cement/core/ carries an inline comment justifying it (D-09 — judgment per RESEARCH.md must-stay classification)"
    - "100% coverage gate stays green (D-24 conjunct #1)"
    - "make audit-public-api exits 0 — public type signatures with Any in their declarations remain SYNTACTICALLY identical (Dict[str, Any] in a public signature is part of the public API per D-12 / D-01; tightening only happens on internals or where the narrower type is provably correct)"
    - "make comply-mypy exits 0 (D-24 conjunct #3)"
  artifacts:
    - path: ".planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md"
      provides: "Pre/post Any count, delta, and per-site survival justification (D-09 verification)"
      min_lines: 30
    - path: "cement/core/"
      provides: "Tighter type hints; surviving Any with inline justification"
  key_links:
    - from: "Pre-count grep (Task 1)"
      to: "03-VERIFICATION.md baseline section"
      via: "recorded number"
      pattern: "Any count.*40"
    - from: "Post-tightening grep (Task 2 verification)"
      to: "03-VERIFICATION.md delta section"
      via: "recorded number"
      pattern: "Post.*<.*Pre"
---

<objective>
Tighten `Any` type hints in `cement/core/` per D-09. The realistic
delta per RESEARCH.md per-site analysis is 5-10 sites (NOT the "25 → 14"
CONTEXT.md estimate suggests — RESEARCH.md A3 corrected this to MEDIUM
confidence on a smaller delta).

The pre-count is recorded as the FIRST commit of this wave in
03-VERIFICATION.md (D-09 first-commit-captures-baseline). The
hand-tightening pass lands as a SECOND commit (RESEARCH.md A3
recommendation: single commit, since 5-10 sites is bisect-friendly as
one unit).

D-09 exemptions stay as `Any`:
- Every `**kw: Any` / `**kwargs: Any` (handler contract — pluggable kwargs by design)
- `_parsed_args: Any` (argparse Namespace opacity)
- `frame: Any` in CaughtSignal (signal frame is Python-runtime opaque)
- `Dict[str, Any]` (now `dict[str, Any]` post-Wave 3) returns/get/set in
  cache.py and config.py — config values are user-arbitrary; tightening
  to `dict[str, str]` would BREAK apps storing dicts/lists
- `config_defaults: dict[str, Any]` — defaults ARE arbitrary by contract
- `template.py` template-data Dict — user-arbitrary
- `output.py` render-data Dict — user-arbitrary

Each surviving `Any` carries an inline comment naming the actual reason
(handler contract, argparse opacity, user-arbitrary config, signal
frame opacity, etc.) per D-09 — mirrors COV-03 pragma policy.

Output: 2 commits — `docs(03): record Any-baseline + REFACTOR-01
verification` (creates 03-VERIFICATION.md with pre-count) and
`refactor(core): tighten Any types in cement/core/` (the actual
tightening pass).

Note on REFACTOR-01: D-22 step 14 finalization (which adds the post-
count + verifies REFACTOR-01 acceptance-via-coverage) is in Wave 8.
This plan only adds the pre-count and does the tightening; the FINAL
verification artifact lands in Wave 8 after pragma audit completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-04-SUMMARY.md
@.planning/phases/01-tooling-baseline-python-matrix/01-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- 03-VERIFICATION.md frontmatter pattern (verbatim from PATTERNS.md analog 01-VERIFICATION.md): -->
```markdown
---
phase: 03-internal-refactor-coverage-hardening
verified: 2026-05-XX (in-progress)
status: in-progress
score: TBD
overrides_applied: 0
---
```

<!-- D-09 baseline grep (verbatim from CONTEXT.md): -->
```bash
grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l
```

<!-- Per RESEARCH.md verified 2026-05-03 this returns 40 (NOT ~25). -->

<!-- D-09 must-stay sites (verbatim from RESEARCH.md "Any-in-core inventory"): -->
<!-- - Every **kw: Any / **kwargs: Any (handler contract): cache.py:30,52, arg.py:33, interface.py:35,66, mail.py:35, hook.py:136, extension.py:82, meta.py:14,17,30, handler.py:65,124,332, foundation.py:772,808,875,1077,1081,1165, output.py:30, template.py:113 -->
<!-- - _parsed_args: Any = None (foundation.py:808) -->
<!-- - frame: Any in CaughtSignal.__init__ (exc.py:43) -->

<!-- Tightenable candidates (RESEARCH.md classification — judgment call per site): -->
<!-- - foundation.py:961 def run(self) -> Union[None, Any] → after UP007 should be `None | Any` — could tighten to just `Any` -->
<!-- - Other sites: 5-10 realistic delta. Hand-classify each. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-tightening Any baseline in 03-VERIFICATION.md</name>
  <files>.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-09 — first-commit captures count; D-22 step 8)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md ("Any-in-core inventory" lines 786-813 — full per-site classification: 12 must-stay, 12-15 tightenable candidates, 5-10 realistic delta)
    - .planning/phases/01-tooling-baseline-python-matrix/01-VERIFICATION.md (analog frontmatter and section structure)
    - .planning/phases/02-dependencies-ci-pipeline/02-VERIFICATION.md (analog with PENDING-evidence pattern — Phase 03 uses similar in-progress shape since FINAL verification is Wave 8)
  </read_first>
  <action>
    Step 1: Capture the live pre-count:

        grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | tee /tmp/any-pre.txt
        wc -l /tmp/any-pre.txt

    Record the exact count. RESEARCH.md verified 40 on 2026-05-03; the truth is whatever grep returns at execution time.

    Step 2: Create `.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md` with this STARTING content (the artifact will get filled in by Wave 8 — this commit just establishes the baseline section):

        ---
        phase: 03-internal-refactor-coverage-hardening
        verified: 2026-05-XX (in-progress; finalized in Wave 8)
        status: in-progress
        score: TBD (final score in Wave 8)
        overrides_applied: 0
        ---

        # Phase 03 Verification

        > **Status:** in-progress. Pre-tightening baselines captured here.
        > Final D-24 9-conjunct verification, post-counts, deltas, and
        > REFACTOR-01 acceptance-via-coverage rationale land in Wave 8
        > (D-22 step 14).

        ## Any-in-core baseline (D-09)

        **Pre-tightening count:** <N> sites (per `grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l` on <DATE>)

        Replace `<N>` with the actual count from `wc -l /tmp/any-pre.txt` and `<DATE>` with today's date (YYYY-MM-DD).

        **Per-site classification** (per RESEARCH.md "Any-in-core inventory"):

        - **Must-stay** (handler contract / argparse opacity / signal frame opacity / user-arbitrary config — see RESEARCH.md classification):
          - All `**kw: Any` / `**kwargs: Any` sites
          - `_parsed_args: Any` (foundation.py)
          - `frame: Any` in CaughtSignal (exc.py)
          - `Dict[str, Any]` / `dict[str, Any]` returns/get/set in cache.py, config.py, mail.py, handler.py, output.py, template.py — config + render data is user-arbitrary
        - **Tightenable candidates** (5-10 realistic per RESEARCH.md A3 — hand-classified during Task 2):
          - To be enumerated in the Task 2 commit body and recorded in this file's "Post-tightening" section after the tightening commit lands

        **Post-tightening count:** TBD (recorded in Wave 8 after Task 2 lands)
        **Delta:** TBD (D-24 conjunct #6 — must be strictly positive in the reduction direction)

        ## Pragma audit baseline (COV-03)

        **Pre-audit count:** <M> sites (per `grep -rn 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | wc -l` on <DATE>)

        RESEARCH.md verified **141 sites on 2026-05-03**, NOT the 123 from CONTEXT.md. Replace `<M>` with the actual current count from a fresh grep.

        Wave 7 will apply the locked-vocabulary D-15 labels. Wave 8 records the post-audit grep result (must be empty per D-17 / D-24 conjunct #7).

        ## Pathlib boundary baseline (REFACTOR-03)

        **Pre-migration os.path callsites in scoped files:**

            grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | wc -l

        Record the exact count. Wave 6 migrates these 4 files; survivors carry `# boundary: str` per D-14.

        ## Public API baseline (D-04)

        Captured in Wave 1 as `.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt` (frozen snapshot). `make audit-public-api` enforces byte-for-byte match across all subsequent Phase 03 commits. D-24 conjunct #4 GREEN.

    Step 3: Append CHANGELOG.md `[dev]` entry under Misc bucket:

        * [dev] Capture Phase 03 Any-in-core baseline (D-09) + pragma + pathlib pre-counts in 03-VERIFICATION.md (in-progress; finalized in Wave 8).

    Step 4: Verify gates (this commit touches no source code):
    1. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100%
    2. `make audit-public-api` MUST exit 0

    Step 5: Commit:

        docs(03): record Any-baseline + REFACTOR-01 verification

        Capture pre-tightening Any-in-core count (<N> sites per D-09),
        pragma site count (<M> sites — RESEARCH.md verified 141, not the
        CONTEXT.md 123), and pathlib pre-migration os.path callsite count
        in 03-VERIFICATION.md.

        Status: in-progress. Wave 8 (D-22 step 14) finalizes the
        artifact with post-counts, deltas, and the full D-24 9-conjunct
        verification.

        D-22 step 8 prefix (the matching tightening commit follows in
        the next task; 14-step finalization is Wave 8).

    Stage exactly: `.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && test -f /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md && grep -c 'Any-in-core baseline' /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md && grep -E ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md` exists
    - File has YAML frontmatter with `phase: 03-internal-refactor-coverage-hardening` and `status: in-progress`
    - File contains `Any-in-core baseline` section heading
    - File contains the actual pre-count number captured from `grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l`
    - File contains pragma pre-count and pathlib pre-count baselines
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - Single commit `docs(03): record Any-baseline + REFACTOR-01 verification` exists
    - Commit touches exactly 2 files: `.planning/.../03-VERIFICATION.md`, `CHANGELOG.md`
  </acceptance_criteria>
  <done>03-VERIFICATION.md created with all three pre-baselines (Any, pragma, pathlib) recorded; gates green; one commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: Hand-tighten Any in cement/core/ (D-09 single-commit pass)</name>
  <files>cement/core/foundation.py, cement/core/handler.py, cement/core/mail.py, (other cement/core/*.py per per-site judgment), CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-09 — exemptions: **kw: Any, _parsed_args: Any may stay; surviving Any carries inline justification; D-22 step 8)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md ("Any-in-core inventory" full classification at lines 786-813; A3 — single-commit recommendation; Code Examples Example 2 lines 681-705 — compliant Any-reduction shapes)
    - /tmp/any-pre.txt (Task 1 baseline — list of all 40 sites by file:line)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md (the pre-count to compare against)
    - cement/core/foundation.py (read sites at lines listed in /tmp/any-pre.txt — line numbers may have drifted post Waves 1-4; re-grep if needed)
    - cement/core/handler.py, cement/core/mail.py, cement/core/cache.py, cement/core/config.py, cement/core/template.py, cement/core/output.py, cement/core/interface.py (other Any-bearing files)
  </read_first>
  <action>
    **Per-site approach:** For each Any site in `/tmp/any-pre.txt`, classify and act:

    1. **Must-stay** (per D-09 / RESEARCH.md classification):
       - `**kw: Any` / `**kwargs: Any` — handler contract, MUST stay
       - `_parsed_args: Any` (foundation.py) — argparse Namespace opacity, MUST stay
       - `frame: Any` (exc.py CaughtSignal) — signal frame opacity, MUST stay
       - `Dict[str, Any]` / `dict[str, Any]` in cache get/set, config get/set, mail config_defaults, handler config_defaults, foundation render-data, output render-data, template template-data — user-arbitrary, MUST stay (tightening would break downstream apps)

       For each must-stay site, ADD an inline comment naming the reason (D-09 — mirror COV-03 pragma policy). Examples (per Code Examples Example 2):
       - `**kw: Any` → `**kw: Any  # handler contract: pluggable kwargs by design`
       - `_parsed_args: Any = None` → `_parsed_args: Any = None  # argparse Namespace is opaque per attr access`
       - `frame: Any` → `frame: Any  # signal frame is Python-runtime opaque`
       - `dict[str, Any]` (config get) → `dict[str, Any]  # user-arbitrary config values`
       - `dict[str, Any]` (template data) → `dict[str, Any]  # user-arbitrary template data`

    2. **Tightenable** (judgment per site — RESEARCH.md A3 says 5-10 realistic):
       - `def run(self) -> Union[None, Any]` (foundation.py:961, post-UP007 → `None | Any`) — likely just `Any` is enough; tighten if a narrower type is provably correct
       - Any `: Any` parameter where the actual passed value is a known specific type from the call sites — tighten to that type
       - Any `-> Any` return where the function only ever returns one type — tighten to that type
       - Any `Any]` collection where the collection only ever holds one type — tighten

       For each tightening, run `pdm run pytest --cov=cement -x tests` AFTER the change to confirm no test breaks AND coverage stays 100%.

    **Critical:** The audit-public-api gate (D-04 / D-24 #4) treats public type signatures as part of the public API. Tightening a public function's `Any` parameter or return type is a PUBLIC API CHANGE and would fail the audit gate. So tightening is restricted to:
    - INTERNAL helpers (underscore-prefixed)
    - Local variables / locals' type annotations
    - Class attributes that are NOT in the AST audit's class-member output (verify by checking 03-PUBLIC-API-BASELINE.txt — if the class.attr appears there, the type is public)

    **Pre-flight:** Before changing any public signature, check 03-PUBLIC-API-BASELINE.txt:

        grep "<module>:<Class>.<attr>" .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt

    If the symbol appears, the parameter/return TYPE is part of the public API per D-12 / D-01 — leave the Any with an inline justification comment (must-stay).

    **Single-commit per RESEARCH.md A3 recommendation:** Land all tightening + all justification comments in ONE commit. If blast radius exceeds expectations (>15 sites changed), split into two commits — `refactor(core): annotate must-stay Any sites with justification` and `refactor(core): tighten tightenable Any sites in cement/core/`.

    **Verification BEFORE commit:**
    1. `grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l` MUST be STRICTLY LOWER than the Task 1 pre-count (D-09 acceptance / D-24 conjunct #6)
    2. Every line that still matches `: Any\b|-> Any\b|Any\]` MUST have an inline `#` comment on the same line OR within the prior 1-2 lines (manual visual inspection — RESEARCH.md flagged this as a manual verification per 03-VALIDATION.md "Manual-Only Verifications")
    3. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100% (Pitfall 3)
    4. `make audit-public-api` MUST exit 0 (D-24 #4 — no public-signature change)
    5. `pdm run mypy cement` MUST exit 0 (D-24 #3)

    Append CHANGELOG.md `[core]` entry under Refactoring bucket (record actual delta):

        * [core] Tighten `Any` types in cement/core/ where narrower types are provably correct; surviving Any carries inline justification per D-09 (delta: <pre> → <post>, <pre - post> sites tightened).

    Commit:

        refactor(core): tighten Any types in cement/core/

        Hand-tighten <N> Any sites in cement/core/ where the actual
        narrower type is known. Surviving Any sites carry inline
        justification per D-09:

        - **kw: Any → **kw: Any  # handler contract: pluggable kwargs
        - _parsed_args: Any → _parsed_args: Any  # argparse opacity
        - frame: Any → frame: Any  # signal frame opacity
        - dict[str, Any] (config) → ... # user-arbitrary config values
        - dict[str, Any] (template) → ... # user-arbitrary template data

        Tightened sites (record exact list):
        - cement/core/foundation.py:<line> <Any → narrower>
        - cement/core/<file>:<line> <Any → narrower>
        - ...

        Delta: <pre> → <post> (-<pre - post>). D-09 acceptance: post
        STRICTLY LOWER than pre. D-24 conjunct #6 GREEN.

        Public type signatures unchanged (audit-public-api exit 0):
        tightening restricted to internals (underscore-prefixed
        helpers, local variables, non-public class attributes per
        03-PUBLIC-API-BASELINE.txt cross-check).

        Verified: 100% coverage, audit-public-api exit 0, mypy clean.

        D-22 step 8 of the suggested 14-step sequence.

    Stage modified `cement/core/*.py` files + `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && PRE=$(cat /tmp/any-pre.txt | wc -l) && POST=$(grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l) && test "$POST" -lt "$PRE" && echo "PASS: Any count $PRE -> $POST (strictly lower)" && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l` returns a value STRICTLY LOWER than the Task 1 pre-count (D-09 acceptance / D-24 conjunct #6)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 #1)
    - `make audit-public-api` exits 0 (D-24 #4 — public surface unchanged)
    - `pdm run mypy cement` exits 0 (D-24 #3)
    - Every surviving `Any` site in cement/core/*.py has an inline comment within 1-2 lines justifying it (manual review per RESEARCH.md / 03-VALIDATION.md "Manual-Only Verifications" — sample-check by grepping a few sites)
    - Single commit `refactor(core): tighten Any types in cement/core/` exists with subject ≤ 78 chars
    - Commit body lists every site that was tightened (with file:line and from→to)
    - Commit body records the actual delta (e.g., `40 → 32, -8 sites`)
    - CHANGELOG.md has new `[core]` entry recording the actual delta
  </acceptance_criteria>
  <done>Any tightening pass complete; D-24 conjunct #6 GREEN; surviving Any justified; gates all green; one commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type-hint change → runtime behavior | mypy enforces type correctness statically; runtime is unaffected by hint changes (Python is dynamically typed). |
| Tightened type → public API | D-12 / D-01 contract: public type signatures are part of the public API. The audit-public-api gate (D-04) catches any public-surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Tightening a public function's Any silently changes the public API surface | mitigate | Pre-flight check against 03-PUBLIC-API-BASELINE.txt (Task 2 action). Audit-public-api gate (D-04 / D-24 #4) verifies no public-symbol change. mypy strict mode catches accidental wider returns. |
| T-03-05-02 | Information Disclosure | Inline justification comment leaks sensitive context | accept | Comments are public source code; no secrets. Justifications are framework-design rationale (handler contract, argparse opacity, etc.). |

**No new attack surface introduced.** Defense-in-depth via audit-public-api + mypy + pre-baseline cross-check.
</threat_model>

<verification>
After this plan completes:
1. `grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l` strictly lower than pre-count (D-24 #6)
2. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 #1)
3. `make audit-public-api` exits 0 (D-24 #4)
4. `pdm run mypy cement` exits 0 (D-24 #3)
5. `make comply-ruff` still exits 0 (Wave 4 baseline preserved)
6. **COV-02 wave check (per checker fix):** `make test` at wave end generates `coverage-report/index.html` — verify `test -f coverage-report/index.html && test -d coverage-report/` exits 0 (D-24 conjunct #5)
7. Two Wave 5 commits visible: `docs(03): record Any-baseline + REFACTOR-01 verification` and `refactor(core): tighten Any types in cement/core/`
8. `03-VERIFICATION.md` exists with pre-counts captured

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-05-01 | 5 | REFACTOR-02 (baseline) | #1, #4 | shell + grep | `grep -nE ': Any\b\|-> Any\b\|Any\]' cement/core/*.py \| wc -l` (record) + `pdm run pytest --cov=cement -x tests && make audit-public-api` |
| 03-05-02 | 5 | REFACTOR-02, COV-01 | #1, #3, #4, #6 | grep + pytest + audit | `(POST < PRE) && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement` |
</verification>

<success_criteria>
- 03-VERIFICATION.md exists with all three pre-baselines (Any, pragma, pathlib)
- Any count in cement/core/ strictly lower than pre-count (D-24 conjunct #6 GREEN)
- Every surviving Any has inline justification comment
- D-24 conjuncts #1, #3, #4 stay green
- Two atomic commits landed (verification baseline + tightening pass)
- CHANGELOG.md has 2 new entries (1 [dev], 1 [core])
- Wave 8 will finalize 03-VERIFICATION.md with post-counts and the full D-24 conjunction
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-05-SUMMARY.md` with:
- Files modified count + breakdown
- 2 commit SHAs + subjects
- Pre-count vs post-count for Any in cement/core/ (e.g., "40 → 32, delta -8")
- Per-site list of tightened sites (file:line, from-type → to-type)
- Per-site list of must-stay Any sites with justification text (sample of 3-5 representative)
- Confirmation gates green at each step
- Note any unexpected behavior (e.g., a tightening attempt that broke a test — record what type would have been required)
</output>
