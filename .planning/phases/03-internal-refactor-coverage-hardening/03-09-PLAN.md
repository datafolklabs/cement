---
phase: 03-internal-refactor-coverage-hardening
plan: 09
type: execute
wave: 9
depends_on: [08]
files_modified:
  - cement/utils/fs.py
  - tests/utils/test_fs.py
  - scripts/audit-public-api.py
  - CHANGELOG.md
  - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
autonomous: true
requirements: [REFACTOR-03]

must_haves:
  truths:
    - "cement.utils.fs:abspath() preserves symlinks (CR-01 BC contract restored — Path.resolve no longer follows links on the public surface)"
    - "cement.utils.fs:abspath('~nosuchuser/foo') returns the input unchanged without raising RuntimeError (CR-02 BC contract restored — silent fallthrough on unknown ~user)"
    - "scripts/audit-public-api.py reads source files with explicit encoding='utf-8' (WR-02 latent locale fragility removed)"
    - "All 9 D-24 conjuncts re-verified GREEN against the fix (make test, make comply-ruff, make comply-mypy, make audit-public-api, coverage HTML present, Any count 40, locked-vocab grep empty, os.path scoped grep 0 untagged, future-annotations grep empty)"
    - "Public API baseline (03-PUBLIC-API-BASELINE.txt, 934 lines) remains byte-for-byte identical post-fix"
    - "Test count is 318/318 passing at 100% coverage (was 316; +2 new regression tests in tests/utils/test_fs.py)"
    - "03-VERIFICATION.md Verifier Audit section records W-01 (CR-01+CR-02) and W-02 (WR-02) as RESOLVED with Plan 09 / Wave 9 sub-blocks containing fix commit SHAs and re-run empirical reproductions"
    - "CHANGELOG.md active 3.0.15 DEVELOPMENT section gains two Bugs entries documenting the fs.abspath BC restoration and the audit-script encoding fix"
  artifacts:
    - path: "cement/utils/fs.py"
      provides: "abspath() body restored to os.path.abspath(os.path.expanduser(path)) — preserves symlinks and silent ~user fallthrough"
      contains: "return os.path.abspath(os.path.expanduser(path))"
    - path: "tests/utils/test_fs.py"
      provides: "Two new regression tests pinning the BC contract — test_abspath_preserves_symlinks + test_abspath_unknown_user_does_not_raise"
      contains: "def test_abspath_preserves_symlinks"
    - path: "scripts/audit-public-api.py"
      provides: "ast.parse called with explicit UTF-8 encoding for portability across non-UTF-8 locales"
      contains: "py.read_text(encoding='utf-8')"
    - path: "CHANGELOG.md"
      provides: "Two new [utils.fs] / [dev] Bugs entries in the active 3.0.15 DEVELOPMENT section"
      contains: "preserve symlinks"
    - path: ".planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md"
      provides: "Verifier Audit W-01 + W-02 marked RESOLVED with Plan 09 / Wave 9 resolution sub-blocks"
      contains: "Resolution (Plan 09 / Wave 9)"
  key_links:
    - from: "cement/utils/fs.py:abspath()"
      to: "os.path.abspath(os.path.expanduser(path))"
      via: "single-line body restoration (revert pathlib semantics on this leaf utility only — REFACTOR-03 boundary preserved elsewhere)"
      pattern: "return os.path.abspath\\(os.path.expanduser\\(path\\)\\)"
    - from: "tests/utils/test_fs.py:test_abspath_preserves_symlinks"
      to: "fs.abspath(symlink_path) returns symlink path string (not resolved target)"
      via: "tmpdir + os.symlink + assert path equality (skip on Windows)"
      pattern: "def test_abspath_preserves_symlinks"
    - from: "tests/utils/test_fs.py:test_abspath_unknown_user_does_not_raise"
      to: "fs.abspath('~nosuchuser_xyz_phase03_gap/foo') == os.path.abspath(...) without raising"
      via: "direct call + assert no RuntimeError"
      pattern: "def test_abspath_unknown_user_does_not_raise"
    - from: "scripts/audit-public-api.py:181"
      to: "py.read_text(encoding='utf-8')"
      via: "explicit kwarg per PEP 3120"
      pattern: "py.read_text\\(encoding='utf-8'\\)"
---

<objective>
Close the three findings surfaced in 03-REVIEW.md and acknowledged in
03-VERIFICATION.md "Verifier Audit" (W-01, W-02):

  1. **CR-01** — `cement.utils.fs:abspath()` now silently follows
     symlinks (BC-break on the 3.0.x public surface).
  2. **CR-02** — `cement.utils.fs:abspath()` now raises `RuntimeError`
     on unknown `~user` paths (BC-break on the 3.0.x public surface).
  3. **WR-02** — `scripts/audit-public-api.py:181` calls
     `Path.read_text()` without explicit `encoding='utf-8'` (latent
     locale fragility on Windows / non-UTF-8 CI containers).

CR-01 + CR-02 share a single-site fix in `cement/utils/fs.py:102`:
revert the Wave 6 pathlib body to the pre-Wave-6 stdlib semantics
`os.path.abspath(os.path.expanduser(path))`. This is a
REFACTOR-03 follow-up — the leaf utility's pathlib migration provided
no readability win and a real BC cost. The boundary discipline for
`cement/core/{config,foundation,template}.py` is preserved unchanged;
only `abspath()` reverts.

WR-02 is a one-line kwarg addition in
`scripts/audit-public-api.py`. PEP 3120 guarantees Python source files
are UTF-8 by default — the explicit kwarg is correct and safe.

Two regression tests pin the restored BC contract so it cannot
silently re-break in a future pathlib pass.

Per CLAUDE.md "Zero public-API breakage on the 3.0.x track —
including subclass-exposed internals." The signature-only
`make audit-public-api` gate cannot detect behavioral regressions;
these regression tests + the source revert do.

Output: 5 atomic commits (per CLAUDE.md Conventional Commits +
78-char wrap):

  - `fix(utils.fs): preserve symlinks and silent ~user fallthrough in abspath()`
  - `test(utils.fs): cover symlink preservation and unknown-~user fallthrough`
  - `fix(dev.audit-script): explicit UTF-8 encoding in audit-public-api.py`
  - `docs(changelog): record fs.abspath BC fix + audit script encoding`
  - `docs(03): record Wave 9 gap closure (CR-01, CR-02, WR-02)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-REVIEW.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt
@.planning/phases/03-internal-refactor-coverage-hardening/03-06-SUMMARY.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-08-SUMMARY.md
@CLAUDE.md
@CHANGELOG.md
@cement/utils/fs.py
@tests/utils/test_fs.py
@scripts/audit-public-api.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from the live source. -->
<!-- The executor should rely on these directly — no exploratory reads needed. -->

From cement/utils/fs.py (current Wave 6 state — the regression site):
```python
import os
import shutil
import tempfile
from datetime import datetime
from pathlib import Path as _Path
from types import TracebackType
from typing import Any

# ... Tmp class ...

def abspath(path: str, strip_trailing_slash: bool = True) -> str:
    """
    Return an absolute path, while also expanding the ``~`` user directory
    shortcut.

    Args:
        path (str): The original path to expand.

    Returns:
        str: The fully expanded, absolute path to the given ``path``

    Example:

        .. code-block:: python

            from cement.utils import fs

            fs.abspath('~/some/path')
            fs.abspath('./some.file')

    """

    return str(_Path(path).expanduser().resolve(strict=False))   # ← REGRESSION SITE (line 102)
```

NOTE on the `_Path` import: 8 other callsites in fs.py use `_Path`
(lines 65, 67, 133, 153, 171, 190, 229, 236 per `grep -n '_Path'`).
DO NOT remove the `from pathlib import Path as _Path` import; only
the `abspath()` body changes. `os` is already imported (line 3).

From scripts/audit-public-api.py (line 181 — the regression site):
```python
mod_name = module_name_for(py, cement_root)
try:
    tree = ast.parse(py.read_text())   # ← REGRESSION SITE (line 181)
except SyntaxError as e:
    print(f"SYNTAX ERROR in {py}: {e}", file=sys.stderr)
    return 2
```

From tests/utils/test_fs.py (current shape — the existing single
abspath test that the new regression tests will live alongside):
```python
import os
import re

from pytest import raises

from cement.utils import fs


def test_abspath(tmp):
    path = fs.abspath('.')
    assert path.startswith('/')
```

Public API baseline (03-PUBLIC-API-BASELINE.txt:839):
```
cement.utils.fs:abspath
```
Signature is unchanged by this fix; the audit-public-api gate
will continue to pass with empty diff.

CHANGELOG.md active section (line 3):
```
## 3.0.15 - DEVELOPMENT (will be released as stable/3.0.16)

Bugs:

- `[ext.smtp]` Fix `timeout` passed as `local_hostname` ...
- ... (existing entries)
```
The two new entries append at the END of the existing Bugs list.

03-VERIFICATION.md Verifier Audit (lines 730-946) records W-01 and
W-02 with empirical reproductions, severity classification, and
three recommended dispositions. The user picked disposition #1
(fix now); this plan implements that disposition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore os.path semantics in cement.utils.fs:abspath() (CR-01 + CR-02)</name>
  <files>cement/utils/fs.py</files>
  <read_first>
    - cement/utils/fs.py (full file — confirm `_Path` is used at lines 65, 67, 133, 153, 171, 190, 229, 236; `os` is imported at line 3; abspath body is at line 102)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-REVIEW.md lines 49-133 (CR-01 + CR-02 sections — full empirical reproductions and proposed fix verbatim)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md lines 783-880 (W-01 in the Verifier Audit — independent reproduction confirming the BC-break)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (search for "A7 symlink pre-flight" and the abspath/expanduser pathlib equivalence note — context that Wave 6 used `Path(p).expanduser().resolve(strict=False)` based on a non-symlink-only equivalence assumption that breaks when symlinks ARE present, and on a silent-fallthrough assumption that breaks for unknown ~user)
  </read_first>
  <action>
    Single-line body change in `cement/utils/fs.py:102`. Use the Edit
    tool to replace the line:

    BEFORE (current Wave 6 body — the regression site):

        return str(_Path(path).expanduser().resolve(strict=False))

    AFTER (restored pre-Wave-6 stdlib semantics with mandatory
    `# boundary: D-14` inline tag — `os.path` retention on a public-
    surface scoped file requires the tag per D-12/D-14 to keep
    D-24 conjunct #8 GREEN):

        return os.path.abspath(os.path.expanduser(path))  # boundary: D-14 (CR-01/CR-02)

    The inline `# boundary: D-14 (CR-01/CR-02)` comment is mandatory.
    Without it, the conjunct #8 grep
    (`grep 'os\.path' ... | grep -v '# boundary:'`) would return 2
    untagged matches (`os.path.abspath` and `os.path.expanduser` on
    the same line) and break the D-24 acceptance gate. The tag style
    follows the precedent in `cement/core/foundation.py:53`
    (`join = os.path.join  # boundary: ...`). Verify the line is
    < 100 chars (it is — 84 chars including the 4-space leading
    indent inside the function body) so ruff E501 stays clean.

    Constraints:

    - DO NOT touch the docstring, parameter signature, return type
      annotation, or the `strip_trailing_slash` parameter (IN-01 is
      explicitly out of scope for this plan; pre-existing baseline).
    - DO NOT remove the `from pathlib import Path as _Path` import
      at line 7 — it is used by 8 other callsites in fs.py (Tmp.remove,
      join, join_exists, ensure_dir_exists, ensure_parent_dir_exists,
      backup) and removing it would break unrelated functions.
    - DO NOT touch `cement/core/{config,foundation,template}.py` —
      the boundary discipline for those files (D-12 / D-14 boundary
      tags, D-19 protected `.format(**template_dict)` callsites) is
      preserved unchanged. This fix is localized to fs.py:102 only.
    - The `os` module is already imported (line 3); no new import is
      needed.
    - The boundary tag MUST be inline on the `return` line (not on a
      preceding comment line) because the conjunct #8 grep is line-
      based; a preceding-line comment would not exclude the
      `return` line from the filtered grep.

    After the edit, run the gates locally to confirm nothing else
    breaks (Task 2 will add the new regression tests; the existing
    test_abspath single-line `path.startswith('/')` assertion still
    passes since `os.path.abspath('.')` still starts with `/` on
    Linux/macOS):

        make test
        make comply-ruff
        make comply-mypy
        make audit-public-api

    All 4 must exit 0. If audit-public-api shows ANY diff, abort —
    something else changed unexpectedly.

    Commit (Conventional Commits, 78-char subject + body wrap, per
    CLAUDE.md):

        fix(utils.fs): preserve symlinks and silent ~user fallthrough
        in abspath()

        Phase 03 Wave 6 migrated cement/utils/fs.py:abspath() from
        os.path.abspath(os.path.expanduser(path)) to
        str(Path(path).expanduser().resolve(strict=False)) on the
        assumption these were equivalent. They are not:

        - Path.resolve() FOLLOWS symlinks; os.path.abspath does NOT.
          Downstream apps with symlinked dotfiles (~/.config -> repo)
          had their paths silently rewritten through the symlink
          target. (CR-01)
        - Path.expanduser() raises RuntimeError on unknown ~user;
          os.path.expanduser silently returns the input unchanged.
          Stale ~deleteduser/path entries in user config files
          crashed App.setup() with an unhandled exception. (CR-02)

        Both regressions are on cement.utils.fs:abspath which is in
        03-PUBLIC-API-BASELINE.txt and feeds every config-dir,
        plugin-dir, template-dir, plugin-file, config-file, and
        HOME_DIR resolution in the framework. They violate the
        project-level "Zero public-API breakage on the 3.0.x track"
        rule (CLAUDE.md > Constraints).

        Restore pre-Wave-6 stdlib semantics with an inline
        `# boundary: D-14 (CR-01/CR-02)` tag on the return line so
        D-24 conjunct #8 (the `os.path`-in-scoped-files filtered
        grep) stays GREEN. The pathlib boundary discipline for
        cement/core/{config,foundation,template}.py is preserved
        unchanged; only this leaf utility reverts to stdlib because
        it is on the public surface.

        Closes 03-REVIEW.md CR-01 + CR-02 (Plan 09 / Wave 9 gap
        closure).

    Stage exactly: cement/utils/fs.py
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -F "os.path.abspath(os.path.expanduser(path))  # boundary: D-14" cement/utils/fs.py | wc -l | (read n; test "$n" -eq 1 && echo "PASS: stdlib body + boundary tag present") && grep -F "_Path(path).expanduser().resolve" cement/utils/fs.py | wc -l | (read n; test "$n" -eq 0 && echo "PASS: pathlib body removed") && grep -c "from pathlib import Path as _Path" cement/utils/fs.py | (read n; test "$n" -eq 1 && echo "PASS: _Path import retained") && grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: D-24 conjunct #8 GREEN (no untagged os.path in scoped files)") && make test && make comply-ruff && make comply-mypy && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - cement/utils/fs.py contains exactly one occurrence of `os.path.abspath(os.path.expanduser(path))  # boundary: D-14` (body restored AND boundary-tagged inline on the same line)
    - cement/utils/fs.py does NOT contain `_Path(path).expanduser().resolve` (regression body fully removed)
    - cement/utils/fs.py still contains `from pathlib import Path as _Path` (other callsites unchanged)
    - cement/utils/fs.py docstring and signature for abspath are byte-for-byte unchanged (verify visual diff against pre-edit state)
    - The boundary-tagged line is < 100 chars (ruff E501 clean — no `# noqa` needed)
    - `grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l` returns 0 (D-24 conjunct #8 GREEN — boundary tag effective)
    - `make test` exits 0 with 100% coverage; the existing test_abspath still passes
    - `make comply-ruff` exits 0
    - `make comply-mypy` exits 0
    - `make audit-public-api` exits 0 with empty diff against 03-PUBLIC-API-BASELINE.txt (934 lines)
    - Single commit `fix(utils.fs): preserve symlinks and silent ~user fallthrough in abspath()` exists; only cement/utils/fs.py staged
  </acceptance_criteria>
  <done>cement/utils/fs.py:abspath() body restored to stdlib semantics; all gates green; one atomic commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests pinning the abspath BC contract</name>
  <files>tests/utils/test_fs.py</files>
  <behavior>
    - Test 1 (`test_abspath_preserves_symlinks`): Build a tmpdir with a real directory `target/` and a symlink `link -> target`. Assert `fs.abspath(link_path)` returns the symlink path string verbatim (NOT the resolved target path). Skip on Windows (`sys.platform == 'win32'`) since symlink creation requires admin privileges there.
    - Test 2 (`test_abspath_unknown_user_does_not_raise`): Call `fs.abspath('~nosuchuser_xyz_phase03_gap/foo')` and assert (a) it does NOT raise, and (b) it returns `os.path.abspath('~nosuchuser_xyz_phase03_gap/foo')` verbatim. Pick a username unlikely to exist on any developer machine or CI runner.
    - Both tests run under the existing `tmp` pytest fixture (defined elsewhere in the test suite — see `test_abspath(tmp)` for precedent).
    - Test count goes from 316 → 318 passing; coverage stays at 100%.
  </behavior>
  <read_first>
    - tests/utils/test_fs.py (full file — confirm pytest fixture conventions, import style, the existing test_abspath shape; the new tests live alongside it)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-REVIEW.md lines 86-92 + lines 130-133 (proposed regression tests verbatim)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md lines 798-822 (empirical reproductions of CR-01 + CR-02 — the test cases must reproduce these exact scenarios and assert the FIXED behavior)
    - cement/utils/fs.py (post-Task-1 state — confirm body is now `return os.path.abspath(os.path.expanduser(path))`; both tests must pass against this body and FAIL against the old `_Path(path).expanduser().resolve(strict=False)` body)
  </read_first>
  <action>
    Append two new test functions at the END of
    `tests/utils/test_fs.py` (after `test_backup_timestamp`, after
    line 83). Use the Edit tool with the existing final line as the
    insertion anchor.

    Required new imports at the top of the file (add after the
    existing imports if not already present):

        import sys

    The new test bodies, verbatim:

    ```python


    def test_abspath_preserves_symlinks(tmp):
        # Phase 03 Wave 9 (CR-01 regression test): fs.abspath() MUST
        # NOT follow symlinks. Pre-Wave-6 used os.path.abspath which
        # preserves symlink paths; Wave 6 briefly used Path.resolve
        # which followed symlinks; Wave 9 restored os.path semantics.
        # Skip on Windows where symlink creation needs admin.
        if sys.platform == 'win32':
            return  # pragma: nocover  # platform-specific

        target_dir = os.path.join(tmp.dir, 'target')
        link_path = os.path.join(tmp.dir, 'link')
        os.makedirs(target_dir)
        os.symlink(target_dir, link_path)

        result = fs.abspath(link_path)

        # The result MUST be the symlink path itself, not the
        # resolved target. This is the BC contract the 3.0.x track
        # requires (CLAUDE.md > Constraints: zero public-API
        # breakage).
        assert result == link_path
        assert 'link' in result
        assert 'target' not in os.path.basename(result)


    def test_abspath_unknown_user_does_not_raise(tmp):
        # Phase 03 Wave 9 (CR-02 regression test): fs.abspath() with
        # an unknown ~user prefix MUST silently fall through and
        # return the input as-is (os.path.expanduser semantics),
        # NOT raise RuntimeError (Path.expanduser semantics).
        # Stale ~deleteduser/path entries in user config files
        # otherwise crash App.setup() mid-flight.
        bogus = '~nosuchuser_xyz_phase03_gap/foo'

        # Must not raise
        result = fs.abspath(bogus)

        # Must match os.path semantics verbatim (silent fallthrough
        # then absolute-path normalization)
        assert result == os.path.abspath(bogus)
    ```

    Run the new tests in isolation to confirm they pass:

        pdm run pytest tests/utils/test_fs.py::test_abspath_preserves_symlinks tests/utils/test_fs.py::test_abspath_unknown_user_does_not_raise -v

    Both should report PASSED. Then run the full gate to confirm
    nothing else broke and coverage stayed at 100%:

        make test

    Should report 318 passed (was 316; +2 new tests) at 100.00%
    coverage (3241/3241 stmts).

    Quick TDD-style sanity check (optional but strongly recommended):
    BEFORE staging, temporarily revert cement/utils/fs.py:102 back
    to `return str(_Path(path).expanduser().resolve(strict=False))`
    and re-run the two new tests in isolation. They should both
    FAIL — test_abspath_preserves_symlinks should fail because
    Path.resolve follows the symlink, and
    test_abspath_unknown_user_does_not_raise should fail with
    RuntimeError. Then revert the fs.py revert (restore Task 1's
    fix) and confirm both tests pass again. This proves the tests
    pin the BC contract correctly. Do NOT commit the temporary
    revert — only commit the test additions.

    Run the rest of the gates:

        make comply-ruff
        make comply-mypy
        make audit-public-api

    All must exit 0.

    Commit (Conventional Commits, 78-char subject + body wrap):

        test(utils.fs): cover symlink preservation and unknown-~user
        fallthrough

        Two regression tests pinning the BC contract on
        cement.utils.fs:abspath() restored in the prior commit:

        - test_abspath_preserves_symlinks: builds tmpdir with a real
          directory + symlink, asserts fs.abspath(link) returns the
          symlink path verbatim, NOT the resolved target. Skipped on
          Windows where symlink creation needs admin privileges.
        - test_abspath_unknown_user_does_not_raise: calls
          fs.abspath('~nosuchuser_xyz_phase03_gap/foo'), asserts no
          RuntimeError and the result matches os.path.abspath of
          the same input verbatim.

        Test count: 316 -> 318 passing at 100% coverage. Both tests
        fail against the old Wave 6 pathlib body (verified locally
        by temporary revert), so they will catch any future
        regression in this code path.

        Closes 03-REVIEW.md CR-01 + CR-02 regression-test gap.

    Stage exactly: tests/utils/test_fs.py
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c "def test_abspath_preserves_symlinks" tests/utils/test_fs.py | (read n; test "$n" -eq 1 && echo "PASS: symlink test present") && grep -c "def test_abspath_unknown_user_does_not_raise" tests/utils/test_fs.py | (read n; test "$n" -eq 1 && echo "PASS: unknown-user test present") && grep -c "import sys" tests/utils/test_fs.py | (read n; test "$n" -ge 1 && echo "PASS: sys import present") && pdm run pytest tests/utils/test_fs.py::test_abspath_preserves_symlinks tests/utils/test_fs.py::test_abspath_unknown_user_does_not_raise -v && make test && make comply-ruff && make comply-mypy && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - tests/utils/test_fs.py contains `def test_abspath_preserves_symlinks` (exactly once)
    - tests/utils/test_fs.py contains `def test_abspath_unknown_user_does_not_raise` (exactly once)
    - tests/utils/test_fs.py contains `import sys` somewhere in the imports
    - `pdm run pytest tests/utils/test_fs.py::test_abspath_preserves_symlinks tests/utils/test_fs.py::test_abspath_unknown_user_does_not_raise -v` reports both PASSED
    - `make test` reports 318 passed (was 316) at 100.00% coverage 3241/3241 stmts
    - `make comply-ruff` exits 0
    - `make comply-mypy` exits 0
    - `make audit-public-api` exits 0 with empty diff
    - Single commit `test(utils.fs): cover symlink preservation and unknown-~user fallthrough` exists; only tests/utils/test_fs.py staged
  </acceptance_criteria>
  <done>Two regression tests added; both pass against the Task-1 fix; FAIL against the pre-Task-1 regression body (verified by temporary revert sanity check); 318/318 passing at 100% coverage; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Add explicit UTF-8 encoding to scripts/audit-public-api.py (WR-02)</name>
  <files>scripts/audit-public-api.py</files>
  <read_first>
    - scripts/audit-public-api.py line 181 (the regression site — `tree = ast.parse(py.read_text())`)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-REVIEW.md lines 172-198 (WR-02 section verbatim — explains the locale fragility, identifies em-dashes in cement/core/foundation.py audit-point comments as the actual decode hazard, and proposes the one-line fix)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md lines 882-894 (W-02 confirmation — "Verified unfixed on live HEAD")
  </read_first>
  <action>
    Single-line change in `scripts/audit-public-api.py:181`. Use the
    Edit tool to replace the line:

    BEFORE:

            tree = ast.parse(py.read_text())

    AFTER:

            tree = ast.parse(py.read_text(encoding='utf-8'))

    Per PEP 3120, Python source files are guaranteed UTF-8 by
    default — the explicit kwarg is correct and safe. This makes
    `make audit-public-api` portable across non-UTF-8 locales
    (Windows cp1252, locale-stripped Docker containers, hardened
    CI), which matters because the audit script is the permanent
    regression check for the 3.0.x → 3.2.x line (Phase 03 D-05).

    No new test is needed — the fix is mechanical and the
    regression behavior (does it crash on em-dashes in a non-UTF-8
    locale?) is environment-dependent and out of test-suite scope.
    The `make audit-public-api` gate continues to verify the audit
    script runs end-to-end on the current CI/dev environment.

    Run the gates:

        make audit-public-api
        make test
        make comply-ruff

    All must exit 0. (Mypy does not type-check scripts/ by default
    per pyproject.toml [tool.mypy] — `make comply-mypy` is unaffected
    by this change but is included in the verify block as defense-
    in-depth.)

    Commit (Conventional Commits, 78-char subject + body wrap):

        fix(dev.audit-script): explicit UTF-8 encoding in
        audit-public-api.py

        scripts/audit-public-api.py:181 called Path.read_text() with
        no encoding argument — defaults to
        locale.getpreferredencoding(False) which is platform-dependent.
        On non-UTF-8 locales (Windows cp1252, locale-stripped Docker,
        some macOS/CI containers), source files containing non-ASCII
        characters fail to decode and the script crashes mid-walk.
        Em-dashes already exist in cement/core/foundation.py
        audit-point comments, so this is a real (not theoretical)
        latent crash.

        Per PEP 3120 Python source files are guaranteed UTF-8 by
        default; the explicit kwarg is correct and safe. The audit
        script is the permanent regression check for the 3.0.x ->
        3.2.x line (D-05) so portability across the supported
        Python 3.10-3.14 matrix and non-UTF-8 CI runners matters.

        Closes 03-REVIEW.md WR-02 (Plan 09 / Wave 9 gap closure).

    Stage exactly: scripts/audit-public-api.py
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -F "py.read_text(encoding='utf-8')" scripts/audit-public-api.py | wc -l | (read n; test "$n" -eq 1 && echo "PASS: utf-8 encoding kwarg added") && grep -F "ast.parse(py.read_text())" scripts/audit-public-api.py | wc -l | (read n; test "$n" -eq 0 && echo "PASS: bare read_text removed") && make audit-public-api && make test && make comply-ruff && make comply-mypy</automated>
  </verify>
  <acceptance_criteria>
    - scripts/audit-public-api.py contains exactly one occurrence of `py.read_text(encoding='utf-8')`
    - scripts/audit-public-api.py does NOT contain bare `ast.parse(py.read_text())` (without encoding kwarg)
    - `make audit-public-api` exits 0 with empty diff against the 934-line baseline
    - `make test` exits 0 at 100% coverage with 318 passing
    - `make comply-ruff` exits 0
    - `make comply-mypy` exits 0
    - Single commit `fix(dev.audit-script): explicit UTF-8 encoding in audit-public-api.py` exists; only scripts/audit-public-api.py staged
  </acceptance_criteria>
  <done>Audit script reads source files with explicit UTF-8; all gates green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Append Bugs entries to CHANGELOG.md active 3.0.15 DEVELOPMENT section</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md lines 1-30 (active 3.0.15 DEVELOPMENT section + existing Bugs bucket — confirm the wrap style, hyphen vs asterisk bullet style, [area] prefix convention)
    - CLAUDE.md "Changelog Maintenance" section (rules for entry format, bucket choice, and 78-char wrap)
    - cement/utils/fs.py post-Task-1 (confirm the fix is in place — the changelog entry's "regression introduced in Phase 03 Wave 6" framing must be accurate)
  </read_first>
  <action>
    Append two new entries to the EXISTING `Bugs:` bucket in the
    `## 3.0.15 - DEVELOPMENT` section of CHANGELOG.md. The Bugs
    bucket starts at line 5 (per the live file). Use the Edit tool
    to add the two lines after the LAST existing Bugs bullet (the
    `[ext.watchdog]` line at line 18) and BEFORE the blank line that
    separates Bugs from Features.

    Match the existing bullet style: hyphen + space + backtick-wrapped
    `[area]` prefix + plain prose, wrapped at 78 chars on
    continuation lines. The two new entries (verbatim, including
    indentation for continuation lines):

    ```
    - `[utils.fs]` Restore `os.path` semantics in `abspath()` —
      preserves symlink paths and silently falls through on unknown
      `~user` prefixes (regression introduced by the Phase 03 Wave 6
      pathlib migration; restores 3.0.x BC contract on the public
      `cement.utils.fs:abspath` surface)
    - `[dev]` Use explicit `encoding='utf-8'` in
      `scripts/audit-public-api.py` so the
      `make audit-public-api` regression gate is portable across
      non-UTF-8 locales (Windows cp1252, locale-stripped Docker, etc.)
    ```

    DO NOT touch any other section of the file. DO NOT touch
    Refactoring, Misc, or any prior released version section. The
    insertion is purely additive at the end of the active Bugs
    bucket.

    Verify the changelog still parses cleanly (no broken markdown):

        head -40 CHANGELOG.md

    Should show the new entries appended at the end of the Bugs
    bucket, with proper bullet alignment and the blank line + `Features:`
    header preserved unchanged after them.

    Run the gates one final time as defense-in-depth (CHANGELOG is
    docs-only but we re-verify nothing else regressed):

        make test
        make comply-ruff
        make comply-mypy
        make audit-public-api

    All must exit 0.

    Commit (Conventional Commits, 78-char subject + body wrap):

        docs(changelog): record fs.abspath BC fix + audit script
        encoding

        Append two entries to the active 3.0.15 DEVELOPMENT Bugs
        bucket:

        - [utils.fs] os.path semantics restored in abspath() (Phase
          03 Wave 6 regression; symlink preservation + unknown-~user
          silent fallthrough)
        - [dev] Explicit UTF-8 encoding in audit-public-api.py for
          locale portability

        Per CLAUDE.md > Changelog Maintenance: phase-by-phase update;
        active DEVELOPMENT section; bucket by Conventional Commit
        type (fix: -> Bugs).

    Stage exactly: CHANGELOG.md
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c "Restore .os.path. semantics in .abspath" CHANGELOG.md | (read n; test "$n" -ge 1 && echo "PASS: fs.abspath entry present") && grep -c "explicit .encoding='utf-8'." CHANGELOG.md | (read n; test "$n" -ge 1 && echo "PASS: audit-script entry present") && grep -c "## 3.0.15 - DEVELOPMENT" CHANGELOG.md | (read n; test "$n" -eq 1 && echo "PASS: active DEVELOPMENT section intact") && make test && make comply-ruff && make comply-mypy && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md `## 3.0.15 - DEVELOPMENT` Bugs bucket contains a `[utils.fs]` line mentioning "Restore" and "abspath" and "symlink" or "regression"
    - CHANGELOG.md `## 3.0.15 - DEVELOPMENT` Bugs bucket contains a `[dev]` line mentioning "explicit" and "encoding='utf-8'" and "audit-public-api"
    - The two new entries are inside the `## 3.0.15 - DEVELOPMENT` section (NOT in any prior released section)
    - The `Features:` header still appears intact after the Bugs bucket
    - All four gates (`make test`, `make comply-ruff`, `make comply-mypy`, `make audit-public-api`) exit 0
    - Single commit `docs(changelog): record fs.abspath BC fix + audit script encoding` exists; only CHANGELOG.md staged
  </acceptance_criteria>
  <done>Two CHANGELOG entries appended to active 3.0.15 Bugs bucket; markdown still valid; all gates green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: Re-run all 9 D-24 conjuncts and update 03-VERIFICATION.md Verifier Audit (W-01, W-02 RESOLVED)</name>
  <files>.planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md (entire file — note the existing structure: frontmatter, top-of-file passed banner, all 9 conjunct sections, Verifier Audit section starting at line 732 with W-01 (lines 783-880) and W-02 (lines 882-894), Final verifier assessment at line 911)
    - cement/utils/fs.py (post-Task-1 state — must contain `return os.path.abspath(os.path.expanduser(path))`)
    - tests/utils/test_fs.py (post-Task-2 state — must contain both new test functions)
    - scripts/audit-public-api.py (post-Task-3 state — must contain `py.read_text(encoding='utf-8')`)
    - CHANGELOG.md (post-Task-4 state — must contain the two new Bugs entries)
  </read_first>
  <action>
    This task does THREE things atomically: (a) re-run all 9 D-24
    conjuncts and capture evidence, (b) re-run the empirical
    reproductions for CR-01 + CR-02 to demonstrate the BC behavior
    is restored, and (c) update 03-VERIFICATION.md to record the
    resolutions.

    Step 1: Capture the four fix commit SHAs from prior tasks:

        FS_FIX_SHA=$(git log --oneline -10 | grep 'fix(utils.fs): preserve symlinks' | awk '{print $1}')
        TEST_FIX_SHA=$(git log --oneline -10 | grep 'test(utils.fs): cover symlink' | awk '{print $1}')
        AUDIT_FIX_SHA=$(git log --oneline -10 | grep 'fix(dev.audit-script): explicit UTF-8' | awk '{print $1}')
        CHANGELOG_SHA=$(git log --oneline -10 | grep 'docs(changelog): record fs.abspath' | awk '{print $1}')
        echo "fs:        $FS_FIX_SHA"
        echo "test:      $TEST_FIX_SHA"
        echo "audit:     $AUDIT_FIX_SHA"
        echo "changelog: $CHANGELOG_SHA"

    All 4 must be non-empty 7-char SHAs.

    Step 2: Re-run all 9 D-24 conjuncts. The fix is a 1-line
    semantic revert + boundary tag + 2 new tests + 1 kwarg + docs;
    no annotation-syntax changes that would invalidate caches, so
    `make superclean && make init` defense-in-depth is NOT required:

        make test 2>&1 | tee /tmp/d24-09-01.txt
        make comply-ruff 2>&1 | tee /tmp/d24-09-02.txt
        make comply-mypy 2>&1 | tee /tmp/d24-09-03.txt
        make audit-public-api 2>&1 | tee /tmp/d24-09-04.txt
        ls -la coverage-report/index.html | tee /tmp/d24-09-05.txt
        grep -nE ': Any\b|-> Any\b|Any\]' cement/core/*.py | wc -l | tee /tmp/d24-09-06.txt
        grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ \
          | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' \
          | wc -l | tee /tmp/d24-09-07.txt
        grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py \
          | grep -v '# boundary:' | wc -l | tee /tmp/d24-09-08.txt
        grep -rn 'from __future__ import annotations' cement/ | wc -l | tee /tmp/d24-09-09.txt

    Expected results (every one must match exactly):

    - #1: exit 0; **318 passed** (was 316; +2 new tests); 100.00% coverage 3241/3241 stmts
    - #2: exit 0; All checks passed!
    - #3: exit 0; Success: no issues found in 51 source files
    - #4: exit 0; silent (empty diff against the 934-line baseline)
    - #5: file exists with non-zero size (mtime updated by Step 2 #1)
    - #6: 40 (unchanged from Wave 8 — abspath signature did not change)
    - #7: 0 (unchanged from Wave 8 — no pragma changes)
    - #8: 0 (Task 1 added `# boundary: D-14 (CR-01/CR-02)` inline on the
      restored `return` line; conjunct #8 grep filters it out)
    - #9: 0 (no `from __future__ import annotations` reintroduced)

    Note on conjunct #8: Task 1 already added the `# boundary: D-14`
    inline tag on the restored `return` line. If conjunct #8 here
    returns ANY untagged sites, audit immediately — Task 1 may not
    have applied the tag, in which case land a small follow-up
    commit `fix(utils.fs): tag pathlib-revert with boundary comment`
    BEFORE Task 5's docs commit (do NOT amend Task 1's commit per
    CLAUDE.md "Always create NEW commits rather than amending"). If
    you have to land that follow-up, mention it as a 6th commit in
    the Resolution sub-block in Step 4 below.

    Step 3: Re-run the empirical reproductions for CR-01 + CR-02
    to demonstrate the BC behavior is restored. Use a small Python
    one-liner per check:

        # CR-01 reproduction (post-fix — should now PRESERVE symlink)
        python3 -c "
        import os, tempfile
        from cement.utils import fs
        with tempfile.TemporaryDirectory() as d:
            target = os.path.join(d, 'target')
            link = os.path.join(d, 'link')
            os.makedirs(target)
            os.symlink(target, link)
            result = fs.abspath(link)
            print('input:  ', link)
            print('result: ', result)
            print('PASS' if result == link else 'FAIL')
        " | tee /tmp/d24-09-cr01-fixed.txt

        # CR-02 reproduction (post-fix — should now NOT raise)
        python3 -c "
        import os
        from cement.utils import fs
        bogus = '~nosuchuser_xyz_phase03_gap/foo'
        try:
            result = fs.abspath(bogus)
            expected = os.path.abspath(bogus)
            print('input:   ', bogus)
            print('result:  ', result)
            print('expected:', expected)
            print('PASS' if result == expected else 'FAIL')
        except RuntimeError as e:
            print('FAIL: still raises RuntimeError:', e)
        " | tee /tmp/d24-09-cr02-fixed.txt

    Both reproductions must print `PASS` on the last line.

    Step 4: Update 03-VERIFICATION.md. The Verifier Audit section
    (starting at line 732) currently records W-01 and W-02 as open
    findings with three recommended human dispositions. The user
    chose disposition #1 (fix now). Append a Resolution sub-block
    UNDER each of W-01 and W-02 (do NOT delete the original W-01
    and W-02 blocks — append after them).

    Use the Edit tool with anchor strings that uniquely identify
    each insertion point.

    For W-01: the original block ends with the line "If the user
    chooses **disposition 1** (fix now), CR-01/CR-02 should be
    addressed via a small follow-up plan (`/gsd-plan-phase --gaps`)
    before merge." (around line 940). Insert the following AFTER
    that line and BEFORE the W-02 header (`#### W-02`):

    ```markdown

    **Resolution (Plan 09 / Wave 9):**

    User chose disposition #1 (fix now). The Wave 9 gap-closure
    plan landed 5 atomic commits:

    | Commit | Subject |
    |--------|---------|
    | `<FS_FIX_SHA>` | `fix(utils.fs): preserve symlinks and silent ~user fallthrough in abspath()` |
    | `<TEST_FIX_SHA>` | `test(utils.fs): cover symlink preservation and unknown-~user fallthrough` |
    | `<AUDIT_FIX_SHA>` | `fix(dev.audit-script): explicit UTF-8 encoding in audit-public-api.py` |
    | `<CHANGELOG_SHA>` | `docs(changelog): record fs.abspath BC fix + audit script encoding` |
    | (this commit) | `docs(03): record Wave 9 gap closure (CR-01, CR-02, WR-02)` |

    `cement/utils/fs.py:abspath()` body restored to
    `os.path.abspath(os.path.expanduser(path))` (with `# boundary: D-14`
    inline tag per D-12 / D-14 — `os.path` retention on public
    surface is deliberate; pathlib semantics broke BC).

    **Empirical re-reproduction (post-fix):**

    CR-01 — symlink preservation:

    ```
    input:   /tmp/.../link  (link -> target)
    result:  /tmp/.../link  (preserved — NOT resolved)
    PASS
    ```

    CR-02 — unknown ~user silent fallthrough:

    ```
    input:    ~nosuchuser_xyz_phase03_gap/foo
    result:   /Users/.../~nosuchuser_xyz_phase03_gap/foo  (no RuntimeError)
    expected: /Users/.../~nosuchuser_xyz_phase03_gap/foo
    PASS
    ```

    **Regression tests pinning the BC contract:**

    - `tests/utils/test_fs.py::test_abspath_preserves_symlinks`
    - `tests/utils/test_fs.py::test_abspath_unknown_user_does_not_raise`

    Both pass against the restored body and FAIL against the Wave 6
    `_Path(path).expanduser().resolve(strict=False)` body (verified
    locally during Task 2 with a temporary revert sanity check).

    **Test count:** 316 → 318 passing at 100% coverage (3241/3241
    stmts); coverage gate held.

    **W-01 STATUS: RESOLVED.** CR-01 + CR-02 closed; 3.0.x BC
    contract on `cement.utils.fs:abspath` restored.
    ```

    For W-02: the original block ends with the line
    "**Recommended:** trivial fix `py.read_text(encoding='utf-8')` —
    queue for the next ad-hoc patch." (around line 894). Insert the
    following AFTER that line and BEFORE the next `### Items confirmed
    correctly deferred` header:

    ```markdown

    **Resolution (Plan 09 / Wave 9):**

    Fixed in commit `<AUDIT_FIX_SHA>`
    (`fix(dev.audit-script): explicit UTF-8 encoding in audit-public-api.py`).
    `scripts/audit-public-api.py:181` now reads source files with
    `py.read_text(encoding='utf-8')` per PEP 3120. The
    `make audit-public-api` gate continues to exit 0 with empty
    diff against the 934-line baseline.

    **W-02 STATUS: RESOLVED.**
    ```

    Step 5: Bump the frontmatter `verified:` timestamp and update
    the top-of-file status banner. The frontmatter (lines 1-7)
    currently shows:

        ---
        phase: 03-internal-refactor-coverage-hardening
        verified: 2026-05-04T04:26:40Z
        status: passed
        score: 9/9 D-24 conjuncts GREEN
        overrides_applied: 0
        ---

    Update `verified` to the current UTC timestamp (use `date -u +%Y-%m-%dT%H:%M:%SZ`).
    Keep `status: passed` (the human disposition was "fix now"
    rather than "defer" or "override", so the original `passed`
    verdict is preserved). Keep `score: 9/9 D-24 conjuncts GREEN`
    (re-verified post-fix).

    Step 6: Add a Wave 9 row to the "Phase 3 Commit Audit" table
    (currently at lines 581-603 in 03-VERIFICATION.md). The current
    table has rows for Waves 1-8. Add a Wave 9 row at the bottom:

        | 9 | 03-09 | 4 | 1 | gap closure: CR-01 + CR-02 (utils.fs:abspath BC restoration) + WR-02 (audit-script encoding); regression tests; CHANGELOG; verification update |

    Step 7: Update the **Total commits** line above the table
    (currently "**Total commits:** **82**"). Wave 9 adds 5 commits
    (4 source/test/changelog + 1 docs) so update to **87**.

    Step 8: Verify the markdown is still well-formed:

        # Sanity-check the file structure
        head -20 .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
        grep -c "Resolution (Plan 09 / Wave 9)" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
        # Must equal 2 (one for W-01, one for W-02)

        grep -c "W-01 STATUS: RESOLVED" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
        # Must equal 1

        grep -c "W-02 STATUS: RESOLVED" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
        # Must equal 1

    Step 9: Final defense-in-depth gate run:

        make test
        make comply-ruff
        make comply-mypy
        make audit-public-api

    All 4 must exit 0. The 9-conjunct re-run from Step 2 should also
    show all 9 GREEN.

    Commit (Conventional Commits, 78-char subject + body wrap):

        docs(03): record Wave 9 gap closure (CR-01, CR-02, WR-02)

        Update 03-VERIFICATION.md to record the Wave 9 gap closure:

        - W-01 (CR-01 + CR-02 — fs.abspath symlink preservation +
          silent ~user fallthrough) marked RESOLVED. Resolution
          sub-block records the 4 fix commit SHAs and re-runs the
          empirical CR-01 + CR-02 reproductions; both now show
          PASS (BC behavior restored).
        - W-02 (audit-script encoding) marked RESOLVED. Resolution
          sub-block records the audit-script commit SHA.
        - Frontmatter `verified:` timestamp bumped; status remains
          `passed` (user disposition was fix-now, not defer/override).
        - Phase 3 Commit Audit table gains a Wave 9 row (5 commits:
          4 source/test/changelog + 1 docs); total commit count
          82 -> 87.

        All 9 D-24 conjuncts re-verified GREEN against the live
        post-fix tree. Public API baseline (934 lines) byte-for-byte
        identical. Test count 316 -> 318 at 100% coverage.

    Stage exactly: .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c "Resolution (Plan 09 / Wave 9)" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -eq 2 && echo "PASS: both Resolution sub-blocks present") && grep -c "W-01 STATUS: RESOLVED" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -eq 1 && echo "PASS: W-01 RESOLVED") && grep -c "W-02 STATUS: RESOLVED" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -eq 1 && echo "PASS: W-02 RESOLVED") && grep -c "status: passed" .planning/phases/03-internal-refactor-coverage-hardening/03-VERIFICATION.md | (read n; test "$n" -ge 1 && echo "PASS: status passed retained") && make test && make comply-ruff && make comply-mypy && make audit-public-api && grep -nE 'pragma:[[:space:]]*no[[:space:]]*cover' cement/ | grep -vE '# (abstract method|TYPE_CHECKING import|platform-specific|untestable: dynamic import|untestable: subprocess|untestable: signal handler|defensive: unreachable|version constant)' | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #7 GREEN") && grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #8 GREEN") && grep -rn 'from __future__ import annotations' cement/ | wc -l | (read n; test "$n" -eq 0 && echo "D-24 #9 GREEN")</automated>
  </verify>
  <acceptance_criteria>
    - All 9 D-24 conjuncts re-verified GREEN against the post-fix tree:
      - #1: `make test` exits 0; **318 passed** (was 316; +2 regression tests); 100.00% coverage 3241/3241 stmts
      - #2: `make comply-ruff` exits 0
      - #3: `make comply-mypy` exits 0
      - #4: `make audit-public-api` exits 0; empty diff against 934-line baseline
      - #5: coverage-report/index.html exists with non-zero size
      - #6: `Any` count in cement/core/*.py == 40 (unchanged from Wave 8)
      - #7: locked-vocabulary pragma grep returns 0 lines
      - #8: `os.path` in scoped files filtered by `# boundary:` returns 0 lines (the new fs.py:abspath callsite carries `# boundary: D-14` inline tag added in Task 1)
      - #9: `from __future__ import annotations` grep returns 0 lines
    - 03-VERIFICATION.md frontmatter `verified:` timestamp updated to current UTC; `status: passed` retained; `score: 9/9 D-24 conjuncts GREEN` retained
    - 03-VERIFICATION.md contains exactly 2 occurrences of "Resolution (Plan 09 / Wave 9)" (one under W-01, one under W-02)
    - 03-VERIFICATION.md contains exactly 1 occurrence of "W-01 STATUS: RESOLVED"
    - 03-VERIFICATION.md contains exactly 1 occurrence of "W-02 STATUS: RESOLVED"
    - 03-VERIFICATION.md original W-01 and W-02 blocks are NOT deleted (the Resolution sub-blocks are appended underneath)
    - 03-VERIFICATION.md Phase 3 Commit Audit table contains a Wave 9 row
    - Empirical CR-01 + CR-02 post-fix reproductions both print PASS
    - Single commit `docs(03): record Wave 9 gap closure (CR-01, CR-02, WR-02)` exists; only 03-VERIFICATION.md staged
  </acceptance_criteria>
  <done>All 9 D-24 conjuncts re-verified GREEN; W-01 + W-02 marked RESOLVED in 03-VERIFICATION.md with full evidence; one atomic commit landed; phase verification artifact finalized post-Wave-9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cement.utils.fs:abspath()` -> downstream user paths | Public surface that processes user-supplied path strings (config files, env vars, CLI args). The fix restores stdlib semantics that downstream apps depend on. |
| `scripts/audit-public-api.py` -> source file decoding | Internal dev gate that reads cement/*.py source files. Encoding fragility is a portability concern, not a security boundary. |
| Verification artifact -> downstream phase trust | 03-VERIFICATION.md is the canonical evidence for Phase 03 acceptance; Phase 5 (deprecations) and Phase 6 (release cut) trust its claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Tampering | abspath() body silently re-regresses to pathlib semantics in a future refactor | mitigate | Two regression tests added in Task 2 (test_abspath_preserves_symlinks + test_abspath_unknown_user_does_not_raise) pin the BC contract; both fail against the Wave 6 pathlib body. Inline `# boundary: D-14` tag on the `os.path` callsite documents the deliberate retention. |
| T-03-09-02 | Information Disclosure | UTF-8 encoding kwarg masks a real decode error in malformed source files | accept | The kwarg only changes which encoding is assumed; decode errors on non-UTF-8 source files would still raise UnicodeDecodeError. PEP 3120 guarantees Python source files are UTF-8 so a decode error on a real cement/*.py file would itself be a signal of corruption — not a regression to hide. |
| T-03-09-03 | Tampering | Verification artifact records false-positive resolution (claims fix landed when it didn't) | mitigate | Task 5 captures the four fix commit SHAs from `git log`, re-runs the empirical CR-01 + CR-02 reproductions against the live post-fix tree, and re-runs all 9 D-24 conjuncts. The Resolution sub-blocks reference real commit SHAs that downstream readers can `git show` to verify. |
| T-03-09-04 | Repudiation | CHANGELOG entries omit user-visible BC restoration | mitigate | Task 4 appends two `[utils.fs]` and `[dev]` entries to the active 3.0.15 DEVELOPMENT Bugs bucket per CLAUDE.md "Changelog Maintenance" — phase-by-phase update; surfaces the BC restoration to downstream users reading the changelog before the 3.0.16 cut. |

**No new attack surface introduced.** All changes are restoring or
narrowing existing behavior; the regression tests and CHANGELOG
entries strengthen the audit trail.
</threat_model>

<verification>
After this plan completes:

1. `cement/utils/fs.py:abspath()` body is `return os.path.abspath(os.path.expanduser(path))` with inline `# boundary: D-14` tag.
2. `tests/utils/test_fs.py` contains `test_abspath_preserves_symlinks` and `test_abspath_unknown_user_does_not_raise`; both pass.
3. `scripts/audit-public-api.py:181` calls `ast.parse(py.read_text(encoding='utf-8'))`.
4. `CHANGELOG.md` active 3.0.15 DEVELOPMENT section has two new Bugs entries documenting the fixes.
5. `03-VERIFICATION.md` Verifier Audit section has W-01 and W-02 marked RESOLVED with Plan 09 / Wave 9 sub-blocks.
6. All 9 D-24 conjuncts re-verified GREEN against the post-fix tree.
7. 5 atomic commits visible in git log:
   - `fix(utils.fs): preserve symlinks and silent ~user fallthrough in abspath()`
   - `test(utils.fs): cover symlink preservation and unknown-~user fallthrough`
   - `fix(dev.audit-script): explicit UTF-8 encoding in audit-public-api.py`
   - `docs(changelog): record fs.abspath BC fix + audit script encoding`
   - `docs(03): record Wave 9 gap closure (CR-01, CR-02, WR-02)`
8. Total Phase 3 commit count: 82 → 87.
9. Test count: 316 → 318 at 100% coverage 3241/3241 stmts.
10. Public API baseline (934 lines) remains byte-for-byte identical.

Per-task verification map:

| Task ID | Wave | Issue Addressed | Test Type | Automated Command |
|---------|------|-----------------|-----------|-------------------|
| 03-09-01 | 9 | CR-01 + CR-02 | grep + gates | grep `os.path.abspath(os.path.expanduser` + 4 gates |
| 03-09-02 | 9 | CR-01 + CR-02 regression coverage | grep + pytest | grep test fns + pytest invocation + full make test |
| 03-09-03 | 9 | WR-02 | grep + gates | grep `read_text(encoding='utf-8')` + 4 gates |
| 03-09-04 | 9 | Documentation (CLAUDE.md changelog policy) | grep | grep both new entries in active DEVELOPMENT section |
| 03-09-05 | 9 | Verification artifact update + 9-conjunct re-verify | grep + 9 conjunct commands | Resolution sub-blocks present + all 9 D-24 GREEN |
</verification>

<success_criteria>
- All 3 REVIEW findings closed: CR-01 (symlink preservation), CR-02 (silent ~user fallthrough), WR-02 (audit-script encoding)
- All 9 D-24 conjuncts re-verified GREEN against the post-fix tree (especially #1 with 318 passing tests, #4 audit-public-api empty diff, #8 os.path scoped grep with new boundary tag)
- All 7 phase requirements (REFACTOR-01..04, COV-01..03) remain SATISFIED — no regression
- Public API baseline (03-PUBLIC-API-BASELINE.txt, 934 lines) remains byte-for-byte identical
- Test count: 316 → 318 passing at 100% coverage; coverage gate held
- 03-VERIFICATION.md frontmatter still `status: passed`; W-01 + W-02 marked RESOLVED with full Resolution sub-blocks
- 5 atomic commits landed (per CLAUDE.md Conventional Commits + 78-char wrap)
- 2 new CHANGELOG.md entries in the active 3.0.15 DEVELOPMENT Bugs bucket
- Phase 03 ready to merge to main with the BC contract on cement.utils.fs:abspath fully restored
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-09-SUMMARY.md` with:

- All 9 D-24 conjunct results captured post-fix (paste each gate's exit status + key output, especially the 318-pass count and any/pragma/os.path/future grep results)
- The 5 commit SHAs + subjects (in order)
- Pre-fix vs post-fix empirical CR-01 + CR-02 reproductions side by side, demonstrating the BC behavior is restored
- Confirmation that the public API baseline diff remains empty
- Confirmation that test count went 316 → 318 and coverage stayed at 100%
- 03-VERIFICATION.md final state: frontmatter `verified:` timestamp, status retained as `passed`, W-01 + W-02 sub-blocks present
- CHANGELOG.md entries (paste verbatim)
- Confirmation Phase 03 BC contract on `cement.utils.fs:abspath` is restored and Phase 03 is ready to merge to main
</output>
