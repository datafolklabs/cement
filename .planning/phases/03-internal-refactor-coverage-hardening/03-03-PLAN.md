---
phase: 03-internal-refactor-coverage-hardening
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - cement/**/*.py
  - tests/**/*.py
  - .planning/codebase/CONVENTIONS.md
autonomous: true
requirements: [REFACTOR-04, COV-01]

must_haves:
  truths:
    - "ruff `make comply-ruff` exits 0 against the UP rule family across cement/ + tests/ (D-24 conjunct #2 restored)"
    - "Each UP rule fix lands as one atomic commit per rule code per Phase 1 D-04 family-split discipline (D-22 steps 3-6)"
    - "UP032 sweep DOES NOT touch the protected `.format(**template_dict)` callsites in cement/core/foundation.py lines 1359, 1364, 1372, 1377, 1385, 1390, 1478, 1483, 1488, 1492, 1553, 1558, 1563, 1567 OR cement/core/template.py:359+ (D-19 protected list)"
    - "100% coverage gate stays green after every commit (Phase 2 D-10/D-11/D-12)"
    - "make audit-public-api exits 0 after every commit (D-04 / D-24 conjunct #4) — public API surface unchanged"
    - "CONVENTIONS.md type-annotation guidance refreshed from full-form to PEP 604/585 syntax (recommended landing per RESEARCH.md A5)"
  artifacts:
    - path: "cement/"
      provides: "PEP 585 / PEP 604 modern annotation syntax across all .py files"
      contains: "list["
    - path: "tests/"
      provides: "Same modernization applied to tests/"
      contains: "list["
    - path: ".planning/codebase/CONVENTIONS.md"
      provides: "Refreshed type-annotation convention documenting PEP 604/585 as project standard"
      contains: "PEP 604"
  key_links:
    - from: "ruff UP family enabled in pyproject.toml (Wave 2)"
      to: "Auto-fix surface across cement/ + tests/"
      via: "ruff check --select UPxxx --fix"
      pattern: "ruff check --select UP"
    - from: "pre-commit verification"
      to: "Protected .format(**template_dict) callsites in foundation.py + template.py"
      via: "git diff grep for `.format(`"
      pattern: "\\.format\\("
---

<objective>
Apply ruff `UP` family auto-fixes per-rule across `cement/` + `tests/`.
Each rule code lands as its own atomic commit per Phase 1 D-04 family-
split discipline (D-22 steps 3-6, plus the recommended CONVENTIONS.md
refresh from RESEARCH.md A5):

- UP006 — `List` → `list`, `Dict` → `dict`, `Tuple` → `tuple`, `Type` → `type` (PEP 585)
- UP007 — `Union[X, Y]` → `X | Y` (PEP 604)
- UP045 — `Optional[X]` → `X | None` (PEP 604)
- CONVENTIONS.md refresh — replace full-form guidance with PEP 604/585
- UP032 — printf-style → f-string (REFACTOR-04 closeout)

UP family rules are SIBLINGS — no semantic dependency between them.
Order: UP006 → UP007 → UP045 → CONVENTIONS.md refresh → UP032. After
this plan, `make comply-ruff` is GREEN on UP family. Wave 4 then
handles FA100 future-annotations strip.

Purpose: Mechanical modernization of all type annotations (cement/ +
tests/) to PEP 604 / PEP 585 syntax. UP032 closes REFACTOR-04 by
converting LOG.debug %-format sites to f-strings — but MUST preserve
the protected `.format(**template_dict)` template-substitution sites
in foundation.py + template.py (D-19 absolute prohibition).

Output: 5 commits in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-02-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UP006 sweep — List/Dict/Tuple/Type to builtin generics (PEP 585)</name>
  <files>cement/**/*.py, tests/**/*.py, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-07 — UP scope = cement/ + tests/; D-22 step 3 — atomic per rule code)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 2 lines 367-401 — per-rule selective fix; Pitfall 3 — coverage drop signal)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 211-269 — fs.py PEP 604 anchor showing target shape)
    - cement/utils/fs.py (current state — already-modern Tmp.__exit__ at lines 75-79 uses `type[BaseException] | None`; the target shape for the rest)
    - cement/__init__.py (verify __all__ list still 14 entries post-UP006)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-02-SUMMARY.md (per-rule UP finding count to verify expected delta)
  </read_first>
  <action>
    Apply ruff UP006 auto-fix:

        pdm run ruff check --select UP006 --fix cement/ tests/

    UP006 rewrites:
    - `from typing import List` → may be removed if unused; `List[X]` → `list[X]`
    - `from typing import Dict` → `Dict[K, V]` → `dict[K, V]`
    - `from typing import Tuple` → `Tuple[X, Y]` → `tuple[X, Y]`
    - `from typing import Type` → `Type[X]` → `type[X]`

    Verification BEFORE commit:
    1. `pdm run ruff check --select UP006 cement/ tests/` MUST exit 0 (no remaining UP006 findings)
    2. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100% (Pitfall 3 — refactor must not drop branch coverage)
    3. `make audit-public-api` MUST exit 0 (public type signatures change SYNTAX but not the AST-walker output)
    4. `make comply-mypy` MUST exit 0 (PEP 585 builtin generics are mypy-recognized in Python 3.10+)
    5. `git diff cement/__init__.py` should NOT show changes to `__all__` — only to imports if any typing imports were removed

    Append CHANGELOG.md `[core]` entry under `## 3.0.16 - DEVELOPMENT` Refactoring bucket:

        * [core] Modernize type annotations to PEP 585 builtin generics (UP006: `List` → `list`, `Dict` → `dict`, `Tuple` → `tuple`, `Type` → `type`).

    Commit with conventional message (subject ≤ 78 chars, body wrapped at 78 chars):

        fix(lint): resolve UP006 List → list, Dict → dict, Tuple → tuple

        Apply ruff UP006 auto-fix across cement/ + tests/ per Phase 03 D-07.
        PEP 585 builtin generics replace `typing.List`, `typing.Dict`,
        `typing.Tuple`, `typing.Type` everywhere. Unused typing imports
        auto-removed by ruff in the same pass.

        Verified: 100% coverage gate green, audit-public-api exit 0
        (public type signatures change syntax but not AST surface), mypy
        clean, comply-ruff clean on UP006.

        D-22 step 3 of the suggested 14-step sequence.

    Stage exactly the modified `.py` files plus `CHANGELOG.md`. Do NOT use `git add -A`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run ruff check --select UP006 cement/ tests/ && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check --select UP006 cement/ tests/` exits 0 (no UP006 findings remaining)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0 (D-24 conjunct #4)
    - `pdm run mypy cement` exits 0 (D-24 conjunct #3)
    - `grep -rE 'from typing import.*\b(List|Dict|Tuple|Type)\b' cement/ tests/` returns empty (or only TYPE_CHECKING-deferred imports)
    - Single commit `fix(lint): resolve UP006 List → list, Dict → dict, Tuple → tuple` exists with subject ≤ 78 chars
    - CHANGELOG.md has new `[core]` entry under Refactoring bucket
  </acceptance_criteria>
  <done>UP006 fixed mechanically; all four gates green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: UP007 sweep — Union[X, Y] to X | Y (PEP 604)</name>
  <files>cement/**/*.py, tests/**/*.py, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-07; D-22 step 4)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 2 — per-rule selective fix)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (PEP 604 anchor in fs.py:75-79)
  </read_first>
  <action>
    Apply ruff UP007 auto-fix:

        pdm run ruff check --select UP007 --fix cement/ tests/

    UP007 rewrites: `Union[X, Y]` → `X | Y`. May also remove `from typing import Union` if unused.

    Verification BEFORE commit (same gates as Task 1):
    1. `pdm run ruff check --select UP007 cement/ tests/` MUST exit 0
    2. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100%
    3. `make audit-public-api` MUST exit 0
    4. `pdm run mypy cement` MUST exit 0

    Append CHANGELOG.md `[core]` entry under Refactoring bucket:

        * [core] Modernize union types to PEP 604 syntax (UP007: `Union[X, Y]` → `X | Y`).

    Commit:

        fix(lint): resolve UP007 Union[X, Y] → X | Y

        Apply ruff UP007 auto-fix across cement/ + tests/. PEP 604 union
        syntax replaces `typing.Union`. Unused typing.Union imports
        auto-removed.

        Verified: 100% coverage, audit-public-api exit 0, mypy clean,
        comply-ruff clean on UP007.

        D-22 step 4 of the suggested 14-step sequence.

    Stage modified `.py` files + `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run ruff check --select UP007 cement/ tests/ && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check --select UP007 cement/ tests/` exits 0
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - `grep -rE 'from typing import.*\bUnion\b' cement/ tests/` returns empty (or only TYPE_CHECKING)
    - Single commit `fix(lint): resolve UP007 Union[X, Y] → X | Y` exists
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>UP007 fixed; gates green; one commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: UP045 sweep — Optional[X] to X | None (PEP 604)</name>
  <files>cement/**/*.py, tests/**/*.py, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-07; D-22 step 5)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 2 — per-rule selective fix)
  </read_first>
  <action>
    Apply ruff UP045 auto-fix:

        pdm run ruff check --select UP045 --fix cement/ tests/

    UP045 rewrites: `Optional[X]` → `X | None`. May remove `from typing import Optional` if unused.

    Verification BEFORE commit:
    1. `pdm run ruff check --select UP045 cement/ tests/` MUST exit 0
    2. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100%
    3. `make audit-public-api` MUST exit 0
    4. `pdm run mypy cement` MUST exit 0

    Append CHANGELOG.md `[core]` entry under Refactoring bucket:

        * [core] Modernize Optional types to PEP 604 syntax (UP045: `Optional[X]` → `X | None`).

    Commit:

        fix(lint): resolve UP045 Optional[X] → X | None

        Apply ruff UP045 auto-fix across cement/ + tests/. PEP 604 syntax
        replaces `typing.Optional`. Unused typing.Optional imports
        auto-removed.

        Verified: 100% coverage, audit-public-api exit 0, mypy clean,
        comply-ruff clean on UP045.

        D-22 step 5 of the suggested 14-step sequence.

    Stage modified `.py` files + `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run ruff check --select UP045 cement/ tests/ && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check --select UP045 cement/ tests/` exits 0
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - `grep -rE 'from typing import.*\bOptional\b' cement/ tests/` returns empty (or only TYPE_CHECKING)
    - Single commit `fix(lint): resolve UP045 Optional[X] → X | None` exists
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>UP045 fixed; gates green; one commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Refresh CONVENTIONS.md type-annotation guidance (PEP 604/585)</name>
  <files>.planning/codebase/CONVENTIONS.md, CHANGELOG.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (current full-form guidance: "Type annotations use full form: `Dict[str, Any]`, `Optional[str]`, `List[str]`, `Tuple[int, str]`")
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (canonical_refs note: "CONVENTIONS.md SHOULD be updated as part of Phase 5 docs work (DOCS-04) or via a small docs: commit here if convenient")
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Open Question 1 — recommendation to land CONVENTIONS.md refresh HERE between UP045 and UP032; Assumption A5 — recommended commit shape)
  </read_first>
  <action>
    Refresh `.planning/codebase/CONVENTIONS.md` per RESEARCH.md A5 recommendation. The source-of-truth doc must not lie during the interval between Phase 03 and Phase 5 DOCS-04.

    Locate the "Type Annotations" section (search for `Type Annotations` header and the line `Type annotations use full form: `Dict[str, Any]`, `Optional[str]`, `List[str]`, `Tuple[int, str]``). Replace the existing bullets in that section with this PEP 604/585-aware version. Use this EXACT block (preserve the section header and surrounding sections):

        ## Type Annotations
        - mypy config in `pyproject.toml` enforces strict mode
        - Always annotate function parameters and return types: `def send(self, msg: str, **kw: Any) -> dict[str, Any]:`
        - Use `TYPE_CHECKING` block for deferred imports to prevent circular dependencies
        - Private Meta attributes require `# type: ignore` due to metaclass pattern (framework constraint)
        - Use **PEP 585 builtin generics** (Python 3.10+): `dict[str, Any]`, `list[str]`, `tuple[int, str]`, `type[Handler]` — NOT `Dict[str, Any]`, `List[str]`, `Tuple[int, str]`, `Type[Handler]`
        - Use **PEP 604 union syntax** (Python 3.10+): `str | None`, `int | str`, `dict[str, Any] | None` — NOT `Optional[str]` or `Union[int, str]`
        - Phase 03 D-06 enabled ruff `UP` family enforcement; UP006/UP007/UP045 mechanically rewrite legacy syntax to the modern forms on every `make comply-ruff-fix`
        - The `from __future__ import annotations` import is NOT used (Phase 03 D-08 / FA100 removed it from all 28 files); Python 3.10+ runtime supports the modern syntax natively

    Append CHANGELOG.md `[dev]` entry under Misc bucket:

        * [dev] Refresh CONVENTIONS.md type-annotation guidance to PEP 604/585 syntax (RESEARCH.md A5; lands alongside UP045 sweep so source-of-truth doc stays accurate moment-to-moment).

    Verify gates (this commit touches no source code):
    1. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100%
    2. `make audit-public-api` MUST exit 0

    Commit:

        docs(codebase): refresh CONVENTIONS to PEP 604/585 syntax

        Update .planning/codebase/CONVENTIONS.md type-annotation guidance
        to document PEP 585 builtin generics (`list[X]`, `dict[K, V]`,
        `tuple[X, Y]`, `type[H]`) and PEP 604 union syntax (`X | Y`,
        `X | None`) as the project convention.

        Lands here (between UP045 and UP032 commits) per RESEARCH.md A5
        recommendation so the source-of-truth doc never lies during the
        interval between Phase 03 and Phase 5 DOCS-04. Phase 5 still has
        plenty of docstring-review work for DOCS-04.

        No source files touched. 100% coverage and audit gates unchanged.

    Stage exactly: `.planning/codebase/CONVENTIONS.md`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -c 'PEP 604' /Users/derks/Development/DFL/cement/.planning/codebase/CONVENTIONS.md | (read n; test "$n" -ge 1 && echo "PASS: PEP 604 documented") && grep -c 'PEP 585' /Users/derks/Development/DFL/cement/.planning/codebase/CONVENTIONS.md | (read n; test "$n" -ge 1 && echo "PASS: PEP 585 documented") && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/codebase/CONVENTIONS.md` contains `PEP 604` (`grep -c 'PEP 604' .planning/codebase/CONVENTIONS.md` >= 1)
    - `.planning/codebase/CONVENTIONS.md` contains `PEP 585` (`grep -c 'PEP 585' .planning/codebase/CONVENTIONS.md` >= 1)
    - The deprecated full-form line "Type annotations use full form: `Dict[str, Any]`, `Optional[str]`, `List[str]`, `Tuple[int, str]`" is REMOVED (`grep -c 'Type annotations use full form' .planning/codebase/CONVENTIONS.md` == 0)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - Single commit `docs(codebase): refresh CONVENTIONS to PEP 604/585 syntax` exists
    - Commit touches exactly 2 files: `.planning/codebase/CONVENTIONS.md`, `CHANGELOG.md`
  </acceptance_criteria>
  <done>CONVENTIONS.md refreshed; gates still green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: UP032 sweep — printf-style to f-string (REFACTOR-04 closeout) WITH protected-site verification</name>
  <files>cement/**/*.py, tests/**/*.py, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-19 — protected line list: foundation.py lines 1359, 1364, 1372, 1377, 1385, 1390, 1478, 1483, 1488, 1492, 1553, 1558, 1563, 1567 + template.py:359+ MUST NOT be touched)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pitfall 1 — UP032 dict-spread limitation; Code Examples lines 692-705 — Any reduction; State of the Art table — UP032 exception for `.format(**dict)`)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 352-359 — PROTECTED foundation.py lines + verification grep; lines 405-407 — PROTECTED template.py:359+)
    - cement/core/foundation.py lines 1350-1570 (read the actual current state to verify line numbers haven't drifted post-Phase 1/2 lint fixes — line drift is a known phenomenon per STATE.md "Line drift acknowledged")
    - cement/core/template.py lines 350-400 (verify the `.format(**template_dict)` callsites at and after line 359)
  </read_first>
  <action>
    **CRITICAL — D-19 PROTECTED CALLSITES (MUST NOT BE REWRITTEN):**

    The following `.format(**template_dict)` callsites are cement's own template substitution path. UP032 SHOULD NOT touch them per ruff issue #9227 (verified HIGH confidence in RESEARCH.md A1), but per-file diff verification is mandatory belt-and-braces.

    PROTECTED (`cement/core/foundation.py`):
    - Line 1359
    - Line 1364
    - Line 1372
    - Line 1377
    - Line 1385
    - Line 1390
    - Line 1478
    - Line 1483
    - Line 1488
    - Line 1492
    - Line 1553
    - Line 1558
    - Line 1563
    - Line 1567

    PROTECTED (`cement/core/template.py`): every `.format(**template_dict)` callsite at line 359 and beyond.

    **Pre-flight (read both files first):** Use Grep to confirm the line numbers haven't drifted post Phase 1/2. If they HAVE drifted, the new line numbers are the protected ones — re-record them in the commit body.

    **Step 1: Capture pre-state of protected lines:**

        for ln in 1359 1364 1372 1377 1385 1390 1478 1483 1488 1492 1553 1558 1563 1567; do
            sed -n "${ln}p" cement/core/foundation.py
        done > /tmp/protected-foundation-pre.txt
        grep -nE '\.format\(\*\*' cement/core/template.py > /tmp/protected-template-pre.txt

    **Step 2: Apply UP032 auto-fix:**

        pdm run ruff check --select UP032 --fix cement/ tests/

    **Step 3: Verify protected lines are UNCHANGED (post-state matches pre-state):**

        for ln in 1359 1364 1372 1377 1385 1390 1478 1483 1488 1492 1553 1558 1563 1567; do
            sed -n "${ln}p" cement/core/foundation.py
        done > /tmp/protected-foundation-post.txt
        grep -nE '\.format\(\*\*' cement/core/template.py > /tmp/protected-template-post.txt

        diff -u /tmp/protected-foundation-pre.txt /tmp/protected-foundation-post.txt
        diff -u /tmp/protected-template-pre.txt /tmp/protected-template-post.txt

    Both diffs MUST be empty. If either shows changes:
    1. ABORT the commit
    2. Restore the affected lines via `git checkout -- cement/core/foundation.py` (or template.py)
    3. Add `# noqa: UP032` to the rewritten line(s) in a SEPARATE commit FIRST: `fix(lint): exempt template substitution from UP032`
    4. Re-run the auto-fix; protected lines now skipped due to noqa

    **Step 4: Run all gates BEFORE commit:**
    1. `pdm run ruff check --select UP032 cement/ tests/` MUST exit 0 (no remaining UP032 findings outside any noqa-protected sites)
    2. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100% (Pitfall 1 warning sign — failures in tests/core/test_foundation.py::test_config_* or tests/core/test_template.py::test_* signal a protected site was rewritten)
    3. `make audit-public-api` MUST exit 0
    4. `pdm run mypy cement` MUST exit 0

    **Step 5: Verify all UP family is now clean:**

        pdm run ruff check --select UP cement/ tests/

    MUST exit 0 — Wave 3 has fully resolved UP family. (FA family will be Wave 4.)

    Append CHANGELOG.md `[core]` entry under Refactoring bucket:

        * [core] Convert printf-style format strings to f-strings (UP032). Protected `.format(**template_dict)` template substitution callsites in foundation.py + template.py preserved per Phase 03 D-19.

    Commit:

        fix(lint): resolve UP032 printf → f-string

        Apply ruff UP032 auto-fix across cement/ + tests/. The ~14
        LOG.debug('%s' % ...) sites convert to f-strings mechanically.

        Protected per D-19 (verified unchanged via per-file diff against
        the pre-fix state):
        - cement/core/foundation.py lines 1359, 1364, 1372, 1377, 1385,
          1390, 1478, 1483, 1488, 1492, 1553, 1558, 1563, 1567
          (`.format(**template_dict)` template substitution path)
        - cement/core/template.py:359+ (same template-substitution
          pattern)

        Verified: 100% coverage, audit-public-api exit 0, mypy clean,
        UP family fully clean (`pdm run ruff check --select UP cement/
        tests/` exits 0).

        D-22 step 6 of the suggested 14-step sequence. Closes
        REFACTOR-04 (printf-style modernization).

    Stage modified `.py` files + `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && pdm run ruff check --select UP cement/ tests/ && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `pdm run ruff check --select UP032 cement/ tests/` exits 0 (no UP032 findings remaining outside noqa-protected sites)
    - `pdm run ruff check --select UP cement/ tests/` exits 0 (UP family fully resolved)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0 (D-24 conjunct #4)
    - `pdm run mypy cement` exits 0 (D-24 conjunct #3)
    - `make comply-ruff` exits 0 EXCEPT for FA family findings (Wave 4 fixes those). Verify with: `pdm run ruff check cement/ tests/ --select=ALL --ignore=FA 2>&1 | tail -1` → reports `All checks passed!` or similar
    - All 14 protected lines in cement/core/foundation.py are UNCHANGED from pre-fix state (the diff captured in Step 3 must be empty)
    - All `.format(**template_dict)` lines in cement/core/template.py are UNCHANGED from pre-fix state
    - Single commit `fix(lint): resolve UP032 printf → f-string` exists
    - CHANGELOG.md has new `[core]` entry mentioning D-19 protection
  </acceptance_criteria>
  <done>UP032 fixed mechanically; protected D-19 callsites verified unchanged via diff; UP family fully clean; REFACTOR-04 printf modernization closed; one atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff auto-fix → source files | Ruff rewrites cement/ + tests/ source. Verification gates (coverage + audit + mypy) catch any breakage. |
| UP032 → protected template-substitution | The most sensitive boundary in this plan. UP032's known limitation (per RESEARCH.md A1) plus per-file diff verification provides defense-in-depth. |

## STRIDE Threat Register

This phase is internal refactor with NO new attack surface — the changes are syntactic only:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | UP032 unexpectedly rewrites `.format(**template_dict)` | mitigate | Per-file diff verification BEFORE every UP032 commit (Step 3 of Task 5). If a protected line is changed, abort and add `# noqa: UP032` in a separate commit FIRST. RESEARCH.md Pitfall 1 documents the warning signs (test failures in test_foundation/test_template). |
| T-03-03-02 | Tampering | UP rules silently change public API surface | mitigate | The audit-public-api gate (D-04 / D-24 #4) runs after every commit; any signature change surfaces immediately. |

**No new attack surface introduced.** All changes are mechanical syntactic modernization with defense-in-depth verification.
</threat_model>

<verification>
After all 5 tasks complete:
1. `pdm run ruff check --select UP cement/ tests/` exits 0 (UP family fully clean)
2. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 #1)
3. `make audit-public-api` exits 0 (D-24 #4)
4. `pdm run mypy cement` exits 0 (D-24 #3)
5. `make comply-ruff` may still exit non-zero ONLY due to FA family findings — Wave 4 fixes
6. Five Wave 3 commits visible in `git log --oneline -10`:
   - `fix(lint): resolve UP006 List → list, Dict → dict, Tuple → tuple`
   - `fix(lint): resolve UP007 Union[X, Y] → X | Y`
   - `fix(lint): resolve UP045 Optional[X] → X | None`
   - `docs(codebase): refresh CONVENTIONS to PEP 604/585 syntax`
   - `fix(lint): resolve UP032 printf → f-string`
7. Protected D-19 lines in foundation.py + template.py UNCHANGED (verified via diff)

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-03-01 | 3 | REFACTOR-04, COV-01 | #1, #2, #3, #4 | ruff + pytest | `pdm run ruff check --select UP006 cement/ tests/ && pdm run pytest --cov=cement -x tests` |
| 03-03-02 | 3 | REFACTOR-04, COV-01 | #1, #2, #3, #4 | ruff + pytest | `pdm run ruff check --select UP007 cement/ tests/ && pdm run pytest --cov=cement -x tests` |
| 03-03-03 | 3 | REFACTOR-04, COV-01 | #1, #2, #3, #4 | ruff + pytest | `pdm run ruff check --select UP045 cement/ tests/ && pdm run pytest --cov=cement -x tests` |
| 03-03-04 | 3 | REFACTOR-04 (doc) | #1, #4 | grep | `grep -c 'PEP 604' .planning/codebase/CONVENTIONS.md` (>= 1) |
| 03-03-05 | 3 | REFACTOR-04 closeout, COV-01 | #1, #2, #3, #4 | ruff + pytest + diff | `pdm run ruff check --select UP cement/ tests/ && pdm run pytest --cov=cement -x tests` + protected-line diff |
</verification>

<success_criteria>
- UP family fully resolved across cement/ + tests/ (`pdm run ruff check --select UP cement/ tests/` exits 0)
- D-24 conjuncts #1 (coverage 100%), #3 (mypy), #4 (audit-public-api) green after every commit
- Protected D-19 callsites in foundation.py (14 lines) and template.py:359+ UNCHANGED post-UP032
- 5 atomic commits landed (UP006, UP007, UP045, CONVENTIONS refresh, UP032)
- CONVENTIONS.md no longer documents the deprecated full-form annotation style
- CHANGELOG.md has 4 new entries (3 [core], 1 [dev])
- REFACTOR-04 mechanically closed (UP032 printf → f-string sweep complete)
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-03-SUMMARY.md` with:
- Files modified count + breakdown by Wave 3 commit
- 5 commit SHAs + subjects
- Per-rule fix count (e.g., UP006: 47 sites, UP007: 23 sites, UP045: 8 sites, UP032: 14 sites)
- Confirmation that protected D-19 sites unchanged (paste the empty diff output as evidence)
- Coverage % after each commit (must be 100% throughout)
- Note any unexpected protected-line drift (line numbers may have shifted post Phase 1/2 — record actual line numbers if so)
- Any UP032 sites that required `# noqa: UP032` exemption (should be zero per RESEARCH.md A1, but record if any surfaced)
</output>
