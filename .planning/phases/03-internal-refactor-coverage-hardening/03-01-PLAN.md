---
phase: 03-internal-refactor-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit-public-api.py
  - Makefile
  - .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt
autonomous: true
requirements: [COV-01]  # Wave 1 = gate-installation only. REFACTOR-01..04 + COV-02/COV-03 are delivered by downstream plans (02-08); this plan touches no cement/ source.

must_haves:
  truths:
    - "scripts/audit-public-api.py exists and is executable; runs via stdlib ast only (no new dev-dep)"
    - "Running the script emits sorted module:Class.method (or module:NAME) lines, one per line, for every non-underscore public symbol under cement/"
    - "make audit-public-api is a STANDALONE Makefile target (not chained into make test, make comply, or any other target) that exits 0 when post-refactor surface matches baseline byte-for-byte after sort"
    - "03-PUBLIC-API-BASELINE.txt contains the captured snapshot AT THIS COMMIT — every later phase commit must not change it"
    - "100% coverage gate (Phase 2 D-10/D-11/D-12) still green: make test exits 0 with coverage 100% (this plan adds files only; touches no cement/ source)"
    - "Plan 01 is the gate-installation wave: it delivers COV-01 (the audit gate that all subsequent waves keep green); REFACTOR-01..04 + COV-02 + COV-03 are delivered by Plans 02-08 (per checker-flagged requirement-coverage fix)"
  artifacts:
    - path: "scripts/audit-public-api.py"
      provides: "AST-walking public-API surface enumerator for cement/ (D-02)"
      min_lines: 80
    - path: "Makefile"
      provides: "audit-public-api target (D-03 — STANDALONE, NOT chained)"
      contains: "audit-public-api:"
    - path: ".planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt"
      provides: "Frozen public API surface snapshot (D-04)"
      min_lines: 100
  key_links:
    - from: "Makefile (audit-public-api target)"
      to: "scripts/audit-public-api.py"
      via: "pdm run python scripts/audit-public-api.py"
      pattern: "pdm run python scripts/audit-public-api.py"
    - from: "Makefile (audit-public-api target)"
      to: ".planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt"
      via: "diff -u against captured baseline"
      pattern: "diff -u .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt"
    - from: "Makefile .PHONY"
      to: "audit-public-api target"
      via: "PHONY declaration"
      pattern: "audit-public-api"
---

<objective>
Capture the public API baseline BEFORE any refactor commit lands. This
plan creates the AST-walk audit script, the standalone Makefile target,
and the frozen baseline snapshot file. Once committed, every subsequent
plan in this phase MUST keep `make audit-public-api` at exit 0 — that's
the byte-for-byte enforcement of the no-public-API-breakage rule on the
3.0.x track (D-01 contract via D-04 audit gate).

Purpose: Lock down the no-breakage contract enforcement surface on the
first commit so the rest of the phase's work (UP family, FA strip,
pathlib migration, Any tightening, pragma audit) cannot silently change
the public API. Per D-05, this script + Makefile target + baseline file
are PERMANENT dev affordances — they live past Phase 3 for every 3.0.x
patch and Phase 5 deprecations to use as a regression check.

Output: One commit `docs(03): capture public API baseline` adding three
files (script + Makefile target + baseline). Zero source files in
`cement/` are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@CLAUDE.md
@scripts/cli-smoke-test.sh
@scripts/devtools.py
@Makefile

<interfaces>
<!-- Existing Makefile target shape (verbatim from Makefile:34-35): -->
```makefile
cli-smoke-test:
	bash scripts/cli-smoke-test.sh
```

<!-- Existing .PHONY line (Makefile:7) — must add `audit-public-api`: -->
```makefile
.PHONY: init dev up down test test-core cli-smoke-test comply-fix commit docs clean superclean dist dist-upload docker docker-push
```

<!-- Existing chained target shape (Makefile:28-32) — DO NOT mirror this: -->
```makefile
test: comply
	pdm run pytest --cov=cement tests
```

<!-- D-03 invariant: audit-public-api MUST NOT have a prerequisite list. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/audit-public-api.py AST-walk script</name>
  <files>scripts/audit-public-api.py</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-01 public surface definition; D-02 script requirements; D-04 baseline format; D-05 permanent dev affordance policy)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 1 — full canonical AST walker source code lines 239-345; Pitfall 5 — first-baseline reality-not-aspiration; Open Question 4 — __all__ sanity check; Assumption A6 — decorator stack handling)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (script header analog from scripts/devtools.py:1; dash-named filename rationale; note on `from __future__ import annotations` being permitted in scripts/ since FA100 only fires inside cement/)
    - scripts/devtools.py (in-tree Python script header analog — shebang + module imports shape)
    - scripts/cli-smoke-test.sh (permanent-dev-affordance precedent — D-05 policy anchor)
    - cement/__init__.py (verify __all__ contents — sanity check first baseline includes all 14 entries per Open Question 4)
    - pyproject.toml lines 90-100 (verify ruff include scope — confirm scripts/ is OUT of scope so the script's own `from __future__ import annotations` doesn't trigger FA100)
  </read_first>
  <action>
    Create `scripts/audit-public-api.py` (dash-named per D-02 / CONTEXT § Specifics — diverges from CONVENTIONS.md "Module files: lowercase with underscores" because this is a `scripts/` utility, not an importable cement module). The script MUST:

    1. Begin with `#!/usr/bin/env python3` shebang (matches scripts/devtools.py shape).
    2. Have a module docstring documenting purpose: that it walks the AST of every `.py` module under `cement/`, prints sorted `<dotted.module>:<NAME>` or `<dotted.module>:<ClassName>.<member>` lines, and is used to detect public-API drift via diff against `03-PUBLIC-API-BASELINE.txt`. Reference Phase 03 D-01..D-05 in the docstring.
    3. Use stdlib `ast` only — NO new dev dependency (D-02). Imports allowed: `ast`, `sys`, `pathlib.Path`, `from __future__ import annotations` (the `from __future__` is fine here per pyproject.toml ruff include scope — `scripts/` is OUT of scope).
    4. Implement `is_public(name)` returning `not name.startswith('_')`.
    5. Implement `collect_module_symbols(tree, mod_name)` that walks `tree.body` and emits `mod_name:NAME` for: `ast.Assign` targets that are public `ast.Name` nodes; `ast.AnnAssign` targets that are public `ast.Name` nodes; public `ast.FunctionDef` / `ast.AsyncFunctionDef` / `ast.ClassDef` nodes. For each public ClassDef, also call `collect_class_members`.
    6. Implement `collect_class_members(cls, mod_name)` that walks `cls.body` and emits `mod_name:cls.name.member` for: public `ast.FunctionDef` / `ast.AsyncFunctionDef` (methods including @property/@classmethod/@staticmethod); public `ast.Assign` and `ast.AnnAssign` (class attrs including dataclass fields); nested public `ast.ClassDef` (the canonical case is `Meta` inner class — recurse one level emitting `mod_name:OuterClass.Meta.<attr>`).
    7. Implement `module_name_for(path, root)` that returns the dotted module path (e.g., `cement.core.foundation`) for a `.py` file under `cement_root`.
    8. Implement `main()` that:
       - Resolves repo root from `Path(__file__).resolve().parent.parent`
       - Walks `cement_root.rglob('*.py')` sorted
       - SKIPS files whose relative path begins `cement/cli/templates/` or `cement/cli/contrib/` (matches pyproject.toml `[tool.coverage.run] omit` per Phase 2 D-12)
       - Calls `ast.parse(py.read_text())` (catch SyntaxError → print to stderr, return 2)
       - Collects symbols, prints `sorted(set(symbols))` one per line to stdout
       - Returns 0
    9. End with `if __name__ == '__main__': sys.exit(main())`.

    Use the EXACT canonical pattern from RESEARCH.md lines 239-345 — copy verbatim. After writing the file, `chmod +x scripts/audit-public-api.py`.

    **Sanity-check before next task:** Run the script and pipe to stdout. Output MUST contain at least 14 lines starting with `cement:` (matching `__all__` in cement/__init__.py — per Open Question 4 / Assumption A6). If fewer than 14, the AST walker is missing a case (likely re-export aliases like `Controller = ArgparseController`); fix before proceeding.
  </action>
  <verify>
    <automated>test -x /Users/derks/Development/DFL/cement/scripts/audit-public-api.py && python3 /Users/derks/Development/DFL/cement/scripts/audit-public-api.py | head -20 && python3 /Users/derks/Development/DFL/cement/scripts/audit-public-api.py | grep -c '^cement:' | (read n; test "$n" -ge 14 && echo "PASS: $n cement.__init__ symbols (>= 14 expected)" || (echo "FAIL: only $n cement.__init__ symbols"; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-public-api.py` exists and is executable (`test -x scripts/audit-public-api.py` exit 0)
    - File contains `#!/usr/bin/env python3` on line 1
    - File contains a module docstring referencing `Phase 03` and `03-PUBLIC-API-BASELINE.txt` and `D-01` (grep `grep -c 'D-01' scripts/audit-public-api.py` >= 1)
    - File imports only stdlib (`grep -E '^(import|from) ' scripts/audit-public-api.py | grep -vE '^(import (ast|sys)|from (__future__|pathlib)|from typing)'` returns empty — no third-party imports)
    - Running `python3 scripts/audit-public-api.py` exits 0 and prints sorted output to stdout
    - Output contains at least 14 lines beginning `cement:` (matches `__all__` entries in cement/__init__.py per Open Question 4)
    - Output is sorted (`python3 scripts/audit-public-api.py | sort -c` exits 0)
    - Output excludes any line containing `_` immediately after `:` or `.` (no private symbols leaked — `python3 scripts/audit-public-api.py | grep -E ':_|\\._' | wc -l` returns 0; underscore-suffix dunder names like `__init__` are also excluded since they start with `_`)
  </acceptance_criteria>
  <done>Script exists, executable, runs clean against current cement/ tree, emits expected sorted public-symbol list including all 14 cement/__init__.py __all__ entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add standalone make audit-public-api Makefile target</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (current state — lines 7 .PHONY, 28-32 test target, 34-35 cli-smoke-test target — these are the analogs)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-03 STANDALONE invariant — NOT chained into make test, make comply, or any other target)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 95-131 — Makefile target shape exact-analog from cli-smoke-test:34-35; D-03 invariant verification)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Makefile target source lines 358-364; Pitfall 6 — recommend `[[:space:]]` over `\s` for cross-platform safety; not directly applicable to this target but informs hygiene)
  </read_first>
  <action>
    Edit `Makefile`:

    1. Add `audit-public-api` to the `.PHONY` line (Makefile:7). Insert between `cli-smoke-test` and `comply-fix` so the order matches the alphabetical-ish existing flow. Final shape:
       ```
       .PHONY: init dev up down test test-core cli-smoke-test audit-public-api comply-fix commit docs clean superclean dist dist-upload docker docker-push
       ```

    2. Add the new target. Place it immediately after the `cli-smoke-test:` block (Makefile:34-35) for visual grouping with the other "permanent dev affordance" target (the cli-smoke-test target was the precedent per D-05). Use this EXACT shape:
       ```makefile
       audit-public-api:
       	@pdm run python scripts/audit-public-api.py > /tmp/cement-public-api.txt
       	@diff -u .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt /tmp/cement-public-api.txt
       ```
       (Note tabs not spaces — Makefile syntax. The `@` prefix suppresses command echo; matches the project's clean-output convention for diff-style targets.)

    **D-03 invariant — verify before commit:** The new target line MUST be exactly `audit-public-api:` with NO prerequisite list (no `audit-public-api: comply` or similar). Compare against the chained `test: comply` target (Makefile:28) — that's the SHAPE TO AVOID per D-03. Match the `cli-smoke-test:` shape (no prerequisites).

    **Cross-target verification:** After editing, grep `grep -E '^audit-public-api' Makefile` MUST show exactly one line: `audit-public-api:` (no `:` followed by other tokens). Also grep `grep -E '(test|comply|comply-ruff|comply-mypy|comply-fix):.*audit-public-api' Makefile` MUST return empty — no other target depends on audit-public-api.
  </action>
  <verify>
    <automated>grep -E '^audit-public-api:' /Users/derks/Development/DFL/cement/Makefile | wc -l | (read n; test "$n" -eq 1 && echo "PASS: 1 audit-public-api target") && grep -E '^audit-public-api:[[:space:]]+[a-zA-Z]' /Users/derks/Development/DFL/cement/Makefile | wc -l | (read n; test "$n" -eq 0 && echo "PASS: no prerequisites on audit-public-api (D-03)") && grep -c 'audit-public-api' /Users/derks/Development/DFL/cement/Makefile | (read n; test "$n" -ge 2 && echo "PASS: target appears in .PHONY and target body")</automated>
  </verify>
  <acceptance_criteria>
    - `audit-public-api` token appears in `.PHONY` line (`grep -E '^\.PHONY:.*audit-public-api' Makefile` exit 0)
    - `audit-public-api:` target exists at column 0 (`grep -E '^audit-public-api:' Makefile` exit 0)
    - Target has NO prerequisites — line matches `^audit-public-api:$` exactly (grep `grep -E '^audit-public-api:[[:space:]]*$' Makefile | wc -l` returns 1)
    - Target body contains `pdm run python scripts/audit-public-api.py`
    - Target body contains `diff -u .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt`
    - No other Makefile target lists `audit-public-api` as a prerequisite (`grep -E ':.*audit-public-api' Makefile | grep -v '^audit-public-api:' | grep -v '^\\.PHONY' | wc -l` returns 0)
  </acceptance_criteria>
  <done>Standalone audit-public-api target wired into Makefile with NO prerequisites and not chained into any other target. .PHONY updated. Both invariants grep-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Capture 03-PUBLIC-API-BASELINE.txt frozen snapshot and commit Wave 1</name>
  <files>.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-04 — exact format and capture-as-FIRST-commit requirement)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 535-565 — baseline file format example; production command)
    - CLAUDE.md (Conventional Commits + 78-char wrap; Changelog Maintenance — append [dev] entry to ## 3.0.16 - DEVELOPMENT Misc bucket per Phase 03 canonical_refs in CONTEXT)
    - CHANGELOG.md (locate `## 3.0.16 - DEVELOPMENT` header to append the [dev] entry)
  </read_first>
  <action>
    1. Generate the baseline file by running the script and redirecting stdout:
       ```bash
       pdm run python scripts/audit-public-api.py > .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt
       ```
       The output is already sorted by `main()`. File MUST be plain text, one symbol per line, sorted, no trailing blank lines.

    2. Verify the baseline is non-trivial: `wc -l .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt` MUST return >= 100 lines (cement has well over 100 public symbols across the full tree).

    3. Verify `make audit-public-api` exits 0 IMMEDIATELY (the freshly captured baseline is by definition equal to the current surface):
       ```bash
       make audit-public-api && echo "PASS: baseline matches current surface (exit 0)"
       ```

    4. Append a one-line entry to `CHANGELOG.md` under `## 3.0.16 - DEVELOPMENT` Misc bucket (per Phase 03 canonical_refs in CONTEXT.md). Use this exact line format (mirror existing `[dev]` entries):
       ```
       * [dev] Add `make audit-public-api` target + AST-walk public surface enumerator + baseline snapshot (Phase 03 D-02..D-05).
       ```

    5. Run the full sample to confirm the 100% coverage gate is still green (this plan touches no cement/ source, so coverage MUST be unchanged):
       ```bash
       pdm run pytest --cov=cement -x tests
       ```
       Exit code MUST be 0 with coverage 100%.

    6. Commit with `make commit` or direct git commit using the conventional message:
       ```
       docs(03): capture public API baseline

       Add scripts/audit-public-api.py (stdlib ast walker), make
       audit-public-api standalone Makefile target (NOT chained per D-03),
       and 03-PUBLIC-API-BASELINE.txt frozen snapshot. The audit gate
       enforces D-01 no-public-API-breakage on the 3.0.x track byte-for-
       byte across every subsequent Phase 03 commit.

       Per D-05 the script + target stay in-tree as permanent dev
       affordances post-Phase-03 (mirror scripts/cli-smoke-test.sh).

       Closes Wave 1 of Phase 03; gates every later wave (Wave 2 ruff
       config through Wave 8 verification artifact).
       ```
       Wrap body lines at 78 chars. Stage exactly: `scripts/audit-public-api.py`, `Makefile`, `.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt`, `CHANGELOG.md`. Do NOT use `git add -A`.
  </action>
  <verify>
    <automated>test -f /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt && wc -l /Users/derks/Development/DFL/cement/.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt | awk '{if ($1 >= 100) print "PASS: "$1" lines"; else {print "FAIL: only "$1" lines"; exit 1}}' && cd /Users/derks/Development/DFL/cement && make audit-public-api && cd /Users/derks/Development/DFL/cement && pdm run pytest --cov=cement -x tests</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt` exists with >= 100 lines
    - File is sorted (`sort -c .planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt` exit 0)
    - File contains `cement:App` (one of the 14 __all__ entries — sanity check)
    - File contains `cement.core.foundation:App` and `cement.core.foundation:App.Meta` (well-known public symbols)
    - `make audit-public-api` exits 0 (D-04 / D-24 conjunct #4 baseline)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (Phase 2 D-10/D-11 still green; D-24 conjunct #1)
    - `CHANGELOG.md` has a new `[dev]` entry under `## 3.0.16 - DEVELOPMENT` Misc bucket referencing `audit-public-api`
    - Single commit `docs(03): capture public API baseline` exists with subject ≤ 78 chars (`git log -1 --format=%s | awk '{print length}' | (read n; test $n -le 78)`)
    - Commit body lines wrap at ≤ 78 chars
    - Commit touches exactly 4 files: `scripts/audit-public-api.py`, `Makefile`, `.planning/phases/03-internal-refactor-coverage-hardening/03-PUBLIC-API-BASELINE.txt`, `CHANGELOG.md` (verify with `git show --stat HEAD`)
  </acceptance_criteria>
  <done>Baseline file committed; D-04 audit gate is now ENFORCING; CHANGELOG entry added; 100% coverage gate still green; Wave 1 closed and ready to unblock Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev → script stdout | The audit script reads cement/*.py via Path.read_text() and prints to stdout. No untrusted input crosses any boundary. |
| Repo → planning artifact | The baseline file is committed to the repo; future executors trust it as the canonical public surface snapshot. |

## STRIDE Threat Register

This phase is internal refactor with NO new attack surface:
- No new endpoints, no new auth paths, no new data flows
- No new dependencies (stdlib `ast` only per D-02)
- No new dynamic imports (the script uses `ast.parse` not `importlib`)
- No I/O beyond reading source files and printing to stdout
- No subprocess calls, no network calls

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | scripts/audit-public-api.py | accept | Script is committed to repo; tampering detection is via `git diff` like every other repo file. No runtime authentication needed. |
| T-03-01-02 | Information Disclosure | 03-PUBLIC-API-BASELINE.txt | accept | The public API surface is by definition public information — anyone can `pdm run python scripts/audit-public-api.py` against the open-source repo. No secret leakage possible. |
| T-03-01-03 | Denial of Service | AST parse errors | mitigate | Script catches `SyntaxError` per file and exits 2 with the file path on stderr (per RESEARCH.md Pattern 1 main() implementation). Future malformed files surface immediately, not silently. |

**No new attack surface introduced.** Public API audit is config-only (Makefile target + stdlib script + planning artifact). The D-04 audit gate adds DEFENSIVE depth — it makes future accidental public-API exposure impossible to merge silently.
</threat_model>

<verification>
After all 3 tasks complete:
1. `make audit-public-api` exits 0 (D-24 conjunct #4)
2. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 conjunct #1)
3. `make comply-ruff` exits 0 (D-24 conjunct #2 — this plan touches no cement/ source so should be unchanged from Phase 2 baseline)
4. `make comply-mypy` exits 0 (D-24 conjunct #3)
5. Single Wave 1 commit `docs(03): capture public API baseline` is visible in `git log --oneline -5`

Per-task verification map (anchors to D-24 conjunction):

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-01-01 | 1 | COV-01 | gates #4 | smoke | `python3 scripts/audit-public-api.py \| head -20 && python3 scripts/audit-public-api.py \| grep -c '^cement:'` (must be ≥14) |
| 03-01-02 | 1 | COV-01 | gates #4 | grep | `grep -E '^audit-public-api:[[:space:]]*$' Makefile \| wc -l` (must be 1) |
| 03-01-03 | 1 | COV-01 | #1, #4 | shell | `make audit-public-api && pdm run pytest --cov=cement -x tests` |
</verification>

<success_criteria>
- scripts/audit-public-api.py exists, executable, stdlib-only, emits sorted output (≥14 cement: entries)
- Makefile has STANDALONE `audit-public-api` target (NO prerequisites, NOT chained)
- 03-PUBLIC-API-BASELINE.txt exists with ≥100 lines, sorted
- `make audit-public-api` exits 0 (D-24 conjunct #4 GREEN — gates every subsequent commit)
- `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 conjunct #1 still green)
- One commit `docs(03): capture public API baseline` (Conventional Commits, 78-char wrap)
- CHANGELOG.md has [dev] entry under ## 3.0.16 - DEVELOPMENT Misc bucket
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-01-SUMMARY.md` with:
- Files created (3): scripts/audit-public-api.py, .planning/.../03-PUBLIC-API-BASELINE.txt, CHANGELOG.md updated
- Files modified (1): Makefile
- Single commit SHA + subject
- Baseline line count (`wc -l .planning/.../03-PUBLIC-API-BASELINE.txt`)
- `make audit-public-api` exit status (must be 0)
- `pdm run pytest --cov=cement -x tests` exit status + coverage % (must be 0 / 100%)
- Confirmation that D-04 audit gate is ENFORCING for all subsequent waves
- Note any unexpected findings (e.g., re-export aliases not captured by AST walker — would require script adjustment)
</output>
