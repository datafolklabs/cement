---
phase: 03-internal-refactor-coverage-hardening
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - pyproject.toml
autonomous: true
requirements: [REFACTOR-04, COV-01]

must_haves:
  truths:
    - "ruff `[tool.ruff.lint] extend-select` lists `UP` and `FA` family codes"
    - "An AUDIT POINT comment block sibling to extend-select names UP+FA as the audit point on the next ruff bump (Phase 1 D-08 hybrid pattern, lifted into Phase 03 D-06)"
    - "make comply-ruff MAY go red after this commit (UP + FA findings surface) — that is EXPECTED and the entire purpose of Wave 3. The 100% coverage gate (Phase 2 D-10/D-11/D-12) MUST stay green. The audit-public-api gate (D-04 / D-24 #4) MUST stay green (this plan touches no cement/ source)"
  artifacts:
    - path: "pyproject.toml"
      provides: "ruff config with UP+FA family added to extend-select; refreshed AUDIT POINT comment"
      contains: "\"UP\""
  key_links:
    - from: "pyproject.toml [tool.ruff.lint] extend-select"
      to: "ruff UP family rules (UP006/UP007/UP032/UP045)"
      via: "lint rule discovery"
      pattern: "\"UP\""
    - from: "pyproject.toml [tool.ruff.lint] extend-select"
      to: "ruff FA family rules (FA100/FA102)"
      via: "lint rule discovery"
      pattern: "\"FA\""
---

<objective>
Re-enable ruff `UP` (pyupgrade) family and add `FA` (flake8-future-
annotations) family to `[tool.ruff.lint] extend-select` per D-06.
Phase 1 D-07 explicitly banned `UP`/`SIM` — Phase 03 lifts the `UP` ban.
`SIM` stays out. The AUDIT POINT comment block is refreshed to call out
the new families per the Phase 1 D-08 hybrid pattern.

This plan opens the auto-fix surface for Wave 3. After it lands, `make
comply-ruff` will surface UP/FA findings; Wave 3 fixes them per-rule
per Phase 1 D-04 family-split discipline.

Purpose: The 14-step D-22 sequence (step 2) — config-only commit that
unblocks the modernization sweep. Pin and bump are inseparable per the
Phase 1 D-15 coupling pattern (config-knob equivalent here is the
`extend-select` change landing with the AUDIT POINT comment in the
same commit).

Output: One commit `chore(ruff): re-enable UP family with AUDIT POINT
comment` modifying only `pyproject.toml` (and CHANGELOG.md). Zero
source files in `cement/` are touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-01-SUMMARY.md
@.planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md
@CLAUDE.md
@pyproject.toml

<interfaces>
<!-- Existing extend-select block (verbatim from pyproject.toml:101-118 — line numbers may drift; the executor reads the file first to confirm): -->
```toml
[tool.ruff.lint]
# AUDIT POINT (D-08): re-review on every ruff bump. New rules added by
# ruff to a selected family fire as CI failures, forcing a deliberate
# add-with-comment to `ignore` or a fix. See Phase 1 RESEARCH.md.
preview = false
extend-select = [
    "E",   # pycodestyle errors
    "F",   # Pyflakes
    "W",   # pycodestyle warnings
    "B",   # flake8-bugbear (NEW, D-06)
    "I",   # isort (NEW, D-06)
    "A",   # flake8-builtins (NEW, D-06)
    "C90", # mccabe complexity (NEW, D-06)
    "N",   # pep8-naming (NEW, D-06)
    "PT",  # flake8-pytest-style (NEW, D-06)
    "T20", # flake8-print (NEW, D-06)
    "YTT", # flake8-2020 (NEW, D-06)
]
```
<!-- The "(NEW, D-06)" tags refer to PHASE 1 D-06; do not confuse with Phase 03 D-06. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UP+FA families to extend-select and refresh AUDIT POINT comment</name>
  <files>pyproject.toml, CHANGELOG.md</files>
  <read_first>
    - pyproject.toml (full current state — locate `[tool.ruff.lint]` block; line numbers from PATTERNS.md may have drifted post Phase 1/2; read the actual file to find the block)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-06 — UP+FA family addition with sibling AUDIT POINT comment; D-07 — UP scope = cement/ + tests/; D-08 — FA family mechanizes future-annotations removal)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 135-198 — exact ruff config block analog; existing AUDIT POINT comment pattern; refreshed AUDIT POINT comment recommendation)
    - .planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md (D-08 hybrid AUDIT POINT pattern — the precedent Phase 03 D-06 reuses)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-01-SUMMARY.md (confirm Wave 1 baseline is committed and audit gate enforcing)
    - CLAUDE.md (Conventional Commits, 78-char wrap; CHANGELOG `## 3.0.16 - DEVELOPMENT` Misc bucket)
    - CHANGELOG.md (locate active dev section to append the [dev] entry)
  </read_first>
  <action>
    Edit `pyproject.toml`:

    1. Locate the `[tool.ruff.lint]` block. The current `extend-select` list ends with `"YTT", # flake8-2020 (NEW, D-06)` (note: Phase 1 D-06, NOT to be confused with Phase 03 D-06).

    2. Append two NEW entries to the `extend-select` list (after `"YTT"`, before the closing `]`). Use these EXACT lines:
       ```toml
           "UP",  # pyupgrade (NEW, Phase 03 D-06; lifts Phase 1 D-07 UP ban)
           "FA",  # flake8-future-annotations (NEW, Phase 03 D-06; mechanizes
                  # `from __future__ import annotations` removal — D-08)
       ```

    3. REPLACE the existing AUDIT POINT comment block above the `preview = false` line with this refreshed version:
       ```toml
       # AUDIT POINT (D-08, Phase 03 D-06): re-review on every ruff bump. New
       # rules added by ruff to a selected family fire as CI failures. The
       # UP and FA families (Phase 03) are the highest-churn surfaces — a
       # UP rule graduating from preview lands as a deliberate config
       # decision, not silent CI red. See Phase 1 RESEARCH.md + Phase 03 D-06.
       ```

    4. Append a one-line entry to `CHANGELOG.md` under `## 3.0.16 - DEVELOPMENT` Misc bucket:
       ```
       * [dev] Enable ruff `UP` (pyupgrade) and `FA` (flake8-future-annotations) families in extend-select with refreshed AUDIT POINT comment (Phase 03 D-06).
       ```

    5. Verify the 100% coverage gate is still green AND the audit gate is still green (this plan touches no cement/ source):
       ```bash
       pdm run pytest --cov=cement -x tests   # must exit 0 with 100% coverage
       make audit-public-api                  # must exit 0
       ```

    6. **EXPECTED**: `make comply-ruff` will likely go RED at this point because UP/FA findings surface. That is the entire purpose of Wave 3. Do NOT fix any UP/FA findings in this commit — the per-rule fix commits are Wave 3's job. Document the finding count in the commit body for Wave 3 traceability:
       ```bash
       pdm run ruff check --select UP,FA cement/ tests/ 2>&1 | tail -5
       ```
       Capture the total finding count (last line typically reads `Found X errors.`); include in commit body.

    7. Commit with this conventional message:
       ```
       chore(ruff): re-enable UP family with AUDIT POINT comment

       Add `UP` (pyupgrade) and `FA` (flake8-future-annotations) family
       codes to [tool.ruff.lint] extend-select per Phase 03 D-06. Lifts
       the Phase 1 D-07 ban on UP — SIM stays out of scope.

       AUDIT POINT comment refreshed to call out UP+FA as the highest-
       churn audit surfaces on the next ruff bump (Phase 1 D-08 hybrid
       pattern continues to apply).

       This commit ALONE will surface UP+FA findings via `make comply-
       ruff` (count: <N> errors). The per-rule fix commits land in
       Wave 3 (UP006, UP007, UP045, UP032 in any order — they are
       family siblings) and Wave 4 (FA100 future-annotations strip,
       gated on UP006/UP007/UP045 per D-08).

       100% coverage gate stays green; audit-public-api stays green
       (this commit modifies pyproject.toml only).
       ```
       Wrap body lines at 78 chars. Replace `<N>` with the actual count from step 6. Stage exactly: `pyproject.toml`, `CHANGELOG.md`. Do NOT use `git add -A`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -E '^\s*"UP"' pyproject.toml | wc -l | (read n; test "$n" -eq 1 && echo "PASS: UP added") && grep -E '^\s*"FA"' pyproject.toml | wc -l | (read n; test "$n" -eq 1 && echo "PASS: FA added") && grep -c 'Phase 03 D-06' pyproject.toml | (read n; test "$n" -ge 1 && echo "PASS: refreshed AUDIT POINT comment present") && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[tool.ruff.lint] extend-select` list contains `"UP"` (`grep -E '^\\s*"UP"' pyproject.toml | wc -l` returns 1)
    - `pyproject.toml` `[tool.ruff.lint] extend-select` list contains `"FA"` (`grep -E '^\\s*"FA"' pyproject.toml | wc -l` returns 1)
    - Refreshed AUDIT POINT comment present (`grep -c 'Phase 03 D-06' pyproject.toml` >= 1)
    - `SIM` is NOT added (`grep -E '^\\s*"SIM"' pyproject.toml | wc -l` returns 0 — D-06 explicitly excludes SIM)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 conjunct #1 still green)
    - `make audit-public-api` exits 0 (D-24 conjunct #4 still green)
    - `make comply-ruff` may exit non-zero (EXPECTED — Wave 3 will fix; this gate's restoration is the next-wave's success criterion, not this plan's)
    - Single commit `chore(ruff): re-enable UP family with AUDIT POINT comment` exists with subject ≤ 78 chars
    - Commit body lines wrap at ≤ 78 chars
    - Commit touches exactly 2 files: `pyproject.toml`, `CHANGELOG.md`
    - Commit body includes the actual UP+FA finding count from `pdm run ruff check --select UP,FA cement/ tests/` (so Wave 3 has a sanity check)
    - CHANGELOG.md has new `[dev]` entry referencing `UP` and `FA`
  </acceptance_criteria>
  <done>UP+FA families enabled in ruff config; AUDIT POINT comment refreshed; coverage gate + audit gate still green; finding count captured in commit body for Wave 3 traceability; one atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo config → ruff lint pipeline | Config-only change; ruff reads pyproject.toml at lint time. No untrusted input crosses any boundary. |

## STRIDE Threat Register

This is a config-only change with NO new attack surface:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | pyproject.toml extend-select list | accept | Config tampering detection is via `git diff` like every other repo config. The AUDIT POINT comment + Phase 1 D-08 hybrid pattern is the long-term mitigation — any future ruff bump will surface drift. |

**No new attack surface introduced.** Phase 03 D-06 is purely config-as-code.
</threat_model>

<verification>
After this plan completes:
1. `make audit-public-api` exits 0 (D-24 conjunct #4)
2. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 conjunct #1)
3. `make comply-ruff` MAY exit non-zero — that is EXPECTED and is exactly what Wave 3 fixes
4. `make comply-mypy` exits 0 (D-24 conjunct #3 — config change does not affect mypy)
5. Single Wave 2 commit `chore(ruff): re-enable UP family with AUDIT POINT comment` is visible

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-02-01 | 2 | REFACTOR-04, COV-01 | #1, #4 (must hold); #2 (intentionally goes red, fixed by Wave 3) | grep + shell | `grep -cE '^\\s*"(UP\|FA)"' pyproject.toml` (must be 2) + `pdm run pytest --cov=cement -x tests` |
</verification>

<success_criteria>
- pyproject.toml extend-select contains `"UP"` and `"FA"` (each exactly once)
- AUDIT POINT comment refreshed with Phase 03 D-06 reference
- D-24 conjuncts #1 (coverage) and #4 (audit-public-api) remain green
- D-24 conjunct #2 (comply-ruff) intentionally goes red — Wave 3 restores it
- One atomic commit `chore(ruff): re-enable UP family with AUDIT POINT comment`
- CHANGELOG.md has new [dev] entry referencing UP and FA family additions
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-02-SUMMARY.md` with:
- Files modified (2): pyproject.toml, CHANGELOG.md
- Single commit SHA + subject
- UP+FA finding count from `pdm run ruff check --select UP,FA cement/ tests/` (this is the Wave 3 expected fix volume)
- `make audit-public-api` exit status (must be 0)
- `pdm run pytest --cov=cement -x tests` exit status + coverage % (must be 0 / 100%)
- `make comply-ruff` exit status (likely non-zero — this is OK per plan objective)
- Note any per-rule breakdown of UP findings (e.g., "UP006: 47, UP007: 23, UP032: 14, UP045: 8") — useful for Wave 3 plan tasks
</output>
