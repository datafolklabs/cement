---
phase: 03-internal-refactor-coverage-hardening
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - cement/utils/fs.py
  - cement/core/foundation.py
  - cement/core/template.py
  - cement/core/config.py
autonomous: true
requirements: [REFACTOR-03, COV-01]

must_haves:
  truths:
    - "A7 symlink pre-flight check (`find tests/ -type l`) is executed before the first migration commit; finding is recorded in the cement/utils/fs.py commit body so .resolve() vs .absolute() decision is auditable"
    - "cement/utils/fs.py migrates os.path internals to pathlib; public functions still return `str` via `str(p)` (D-12 boundary contract); `HOME_DIR` stays `str` constant; `Tmp.dir`/`Tmp.file` stay `str` instance attributes"
    - "cement/core/foundation.py migrates os.path to pathlib internals (5+ sites); public type signatures unchanged"
    - "cement/core/template.py migrates os.path to pathlib internals (~14+ sites — biggest single-file blast); .format(**template_dict) callsites at line 359+ untouched (D-19)"
    - "cement/core/config.py migrates the single os.path.exists callsite to pathlib"
    - "100% coverage gate stays green after EVERY migration commit (D-24 #1 — Pitfall 3 protection)"
    - "make audit-public-api exits 0 after EVERY migration commit (D-04 / D-24 #4) — public type signatures unchanged"
    - "Any surviving `os.path.*` callsite in scoped files carries inline `# boundary:` comment (D-14); D-24 conjunct #8 GREEN: `grep -rn 'os\\.path' cement/utils/fs.py cement/core/` returns empty OR only `# boundary:`-tagged sites"
  artifacts:
    - path: "cement/utils/fs.py"
      provides: "pathlib internals; str at every public boundary"
      contains: "from pathlib import Path"
    - path: "cement/core/foundation.py"
      provides: "pathlib internals for path-handling sections; public signatures unchanged"
      contains: "from pathlib import Path"
    - path: "cement/core/template.py"
      provides: "pathlib internals for template walk; protected .format(**template_dict) callsites preserved"
      contains: "from pathlib import Path"
    - path: "cement/core/config.py"
      provides: "pathlib internal for parse_file existence check"
      contains: "from pathlib import Path"
  key_links:
    - from: "cement/utils/fs.py public funcs (abspath, join, etc.)"
      to: "Internal Path locals + str() boundary"
      via: "str(p) at every return"
      pattern: "return str\\("
    - from: "cement/core/* internal os.path callers"
      to: "pathlib equivalents from RESEARCH.md table (lines 442-453)"
      via: "Path(p).<method>"
      pattern: "Path\\("
---

<objective>
Migrate `os.path` to `pathlib.Path` INTERNALS in 4 named files per
D-11. The boundary-preservation rule (D-12 / D-14) is absolute: locals
are `Path` for clarity; every public return goes back through `str(p)`.
`HOME_DIR` stays `str`; `Tmp.dir`/`Tmp.file` stay `str` instance
attributes; ZERO bytes change to public type signatures (D-12 contract;
D-04 audit gate verifies).

Order per D-13:
1. `cement/utils/fs.py` FIRST (foundational; smallest blast; defines
   `abspath` which the other 3 files depend on)
2. `cement/core/config.py` (1 callsite — natural warm-up after fs.py)
3. `cement/core/foundation.py` (5+ callsites)
4. `cement/core/template.py` (~14+ callsites — biggest single-file
   blast; protected `.format(**template_dict)` callsites at line 359+
   MUST NOT be touched per D-19)

Each file is its own atomic commit per D-13 (matches Phase 1 D-04
atomic-per-concern shape; bisect anchor per file).

Pre-flight (RESEARCH.md A7 — HIGHEST-RISK assumption): `find tests/
-type l` BEFORE the fs.py migration. `Path.resolve(strict=False)` and
`os.path.abspath` are NOT semantically equivalent for symlinks
(`resolve` follows; `abspath` does not). If symlinks exist in test
fixtures, use `Path(p).expanduser().absolute()` instead of
`.resolve(strict=False)` to preserve original semantics. Finding is
recorded in the fs.py migration commit body for auditability.

Output: 4 atomic commits + 1 pre-flight task (no commit; finding only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-05-SUMMARY.md
@cement/utils/fs.py
@cement/core/config.py
@CLAUDE.md

<interfaces>
<!-- os.path → pathlib equivalents (verbatim from RESEARCH.md lines 442-453): -->
| os.path call | pathlib equivalent | Notes |
|--------------|---------------------|-------|
| `os.path.exists(p)` | `Path(p).exists()` | Identical semantics |
| `os.path.isfile(p)` | `Path(p).is_file()` | Identical semantics |
| `os.path.isdir(p)` | `Path(p).is_dir()` | Identical semantics |
| `os.path.join(a, b, c)` | `Path(a) / b / c` | Returns Path; wrap in `str()` for boundary |
| `os.path.basename(p)` | `Path(p).name` | str-typed |
| `os.path.dirname(p)` | `str(Path(p).parent)` | parent is Path; convert |
| `os.path.splitext(p)[0]` / `[1]` | `Path(p).stem` / `Path(p).suffix` | suffix includes the dot |
| `os.path.abspath(p)` | `str(Path(p).resolve(strict=False))` OR `str(Path(p).absolute())` if symlink-preserving needed (per A7) |
| `os.path.expanduser(p)` | `Path(p).expanduser()` | Identical |
| `os.makedirs(p)` | `Path(p).mkdir(parents=True, exist_ok=True)` | (template.py uses this) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: A7 symlink pre-flight check (find tests/ -type l)</name>
  <files>(read-only / capture only — no commit)</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Assumption A7 — HIGHEST-RISK; lines 891-893; Caution on Path.resolve() at line 455)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-12 boundary-preservation; D-13 atomic per-file commits)
    - cement/utils/fs.py (read full file — small, ~250 lines; the pre-migration anchor)
  </read_first>
  <action>
    Step 1: Run the symlink pre-flight check across the entire repo:

        find . -type l 2>/dev/null | grep -v '\.venv' | grep -v 'node_modules' | grep -v '\.git' | tee /tmp/symlinks-found.txt

    Step 2: Specifically check tests/ fixtures:

        find tests/ -type l 2>/dev/null | tee /tmp/symlinks-tests.txt

    Step 3: Check the cement/ tree:

        find cement/ -type l 2>/dev/null | tee /tmp/symlinks-cement.txt

    Step 4: Decision tree (per RESEARCH.md A7):
    - **If `/tmp/symlinks-tests.txt` is EMPTY** AND `/tmp/symlinks-cement.txt` is EMPTY: use `Path(p).expanduser().resolve(strict=False)` per RESEARCH.md Code Examples Example 3 (matches `os.path.abspath(os.path.expanduser(p))` semantics for non-symlink paths).
    - **If symlinks exist in tests/ or cement/ fixtures**: use `Path(p).expanduser().absolute()` instead. `.absolute()` does NOT resolve symlinks; `.resolve()` DOES. The cement test suite may rely on un-resolved abspath behavior — using `.absolute()` preserves the original semantics.

    Step 5: Record the finding for inclusion in Task 2 (cement/utils/fs.py migration) commit body. Format:

        A7 symlink pre-flight (per RESEARCH.md):
        - tests/ symlinks: <count> found (<list>)
        - cement/ symlinks: <count> found (<list>)
        - Decision: use <Path(p).expanduser().resolve(strict=False) | Path(p).expanduser().absolute()>

    Step 6: Verify the existing 100% coverage gate is still green (this task makes no changes):

        pdm run pytest --cov=cement -x tests
        make audit-public-api

    Both MUST exit 0.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && find tests/ -type l 2>/dev/null > /tmp/symlinks-tests.txt && find cement/ -type l 2>/dev/null > /tmp/symlinks-cement.txt && echo "tests/ symlinks: $(wc -l < /tmp/symlinks-tests.txt)" && echo "cement/ symlinks: $(wc -l < /tmp/symlinks-cement.txt)" && pdm run pytest --cov=cement -x tests && make audit-public-api</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/symlinks-tests.txt` exists (may be empty — that is itself a finding)
    - `/tmp/symlinks-cement.txt` exists
    - Decision recorded for Task 2 to use in fs.py commit body
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - No git commit (this is a pre-flight finding only)
  </acceptance_criteria>
  <done>A7 symlink finding captured; Task 2 has the data needed to choose between `.resolve()` and `.absolute()`; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate cement/utils/fs.py to pathlib internals (foundational — D-13 ordering FIRST)</name>
  <files>cement/utils/fs.py, CHANGELOG.md</files>
  <read_first>
    - cement/utils/fs.py (FULL current file — read every line; the file is ~250 lines)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-11 scope; D-12 boundary; D-13 fs.py FIRST; D-14 # boundary: comment marker)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 3 lines 402-455 — boundary-preserving migration; Code Examples Example 3 lines 707-743; Anti-Patterns "DON'T return Path from any public function")
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 202-294 — fs.py migration target shapes; existing modern-syntax anchor; existing pragma site at line 245)
    - /tmp/symlinks-tests.txt + /tmp/symlinks-cement.txt (Task 1 output — informs .resolve() vs .absolute() decision)
  </read_first>
  <action>
    **Migration plan for cement/utils/fs.py (~12 os.path callsites per RESEARCH.md):**

    1. Add `from pathlib import Path` to imports (alongside existing `import os`, etc. — do NOT remove `import os` since some non-path os usage may remain, e.g., `os.environ` at line 242).

    2. Migrate each function:
       - `abspath(path: str, ...) -> str` (line ~104): replace `os.path.abspath(os.path.expanduser(path))` with the A7-decided form (either `str(Path(path).expanduser().resolve(strict=False))` OR `str(Path(path).expanduser().absolute())` per Task 1 finding). PUBLIC signature unchanged: still `def abspath(path: str, strip_trailing_slash: bool = True) -> str`.
       - `join(*args: str, **kwargs: Any) -> str` (line ~107-129): replace `os.path.join(...)` with iterative `Path` `/` operator wrapped in `str()`. CAUTION: original signature accepts `**kwargs` but `os.path.join` accepts only positional — verify call sites never pass non-positional kwargs. If kwargs are unused, document as ignored in the function body.
       - `backup(...)` (line ~210-240): migrate any `os.path.exists` / `os.path.isfile` / `os.path.dirname` calls per the equivalents table.
       - `Tmp.__init__` / `Tmp.remove` (lines ~50-71): tempfile-related; migrate `os.path` usage to `Path`. `Tmp.dir`/`Tmp.file` MUST stay `str` instance attributes (D-12 contract).
       - HOME_DIR block (lines 241-245): `if 'HOME' in os.environ: HOME_DIR = abspath(os.environ['HOME'])` — `os.environ` is fine to keep (it's not `os.path`). `HOME_DIR` MUST stay a `str` constant per D-12.

    3. **Boundary verification:** After migration, every public function in fs.py MUST return `str` (or `bool`). Grep for return-type annotations:

           grep -nE 'def [a-z][a-zA-Z_]*\(.*\) -> ' cement/utils/fs.py | grep -v '_'

       Every public function's return type MUST still be `str` or `bool`. NONE may be `Path`.

    4. **Surviving os.path callsites** (D-14): If any `os.path.*` call survives in fs.py (e.g., a `__fspath__`-equivalent edge), tag it with `# boundary:` comment on the same or adjacent line:

           # ... os.path.X(...)  # boundary: <reason>

    5. Verification BEFORE commit:
       - `grep -rn 'os\.path' cement/utils/fs.py | grep -v '# boundary:' | wc -l` MUST return 0 (D-14 / D-24 #8 partial)
       - `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100% (Pitfall 3)
       - `make audit-public-api` MUST exit 0 (D-12 / D-24 #4)
       - `pdm run mypy cement` MUST exit 0 (D-24 #3)
       - `make comply-ruff` MUST exit 0 (D-24 #2)

    Append CHANGELOG.md `[core]` entry under Refactoring bucket:

        * [core] Migrate `cement/utils/fs.py` os.path internals to pathlib; public functions still return `str` (D-12 boundary preservation). Closes self-flagged TODO loop and lays foundation for cement/core/* migrations.

    Commit:

        refactor(utils.fs): migrate os.path to pathlib internals

        Replace os.path callsites in cement/utils/fs.py with pathlib.Path
        equivalents per Phase 03 D-11. Boundary-preservation rule (D-12)
        held: locals are Path for clarity; every public return goes
        through str(p). HOME_DIR stays str constant; Tmp.dir/Tmp.file
        stay str instance attributes. ZERO bytes change to public type
        signatures.

        A7 symlink pre-flight (per RESEARCH.md):
        - tests/ symlinks: <N> found
        - cement/ symlinks: <N> found
        - Decision: <.resolve(strict=False) | .absolute()>

        Verified: 100% coverage gate green, audit-public-api exit 0
        (public signatures byte-identical), mypy clean, comply-ruff
        clean. No surviving os.path callsites in this file (or all
        survivors carry # boundary: comments).

        D-22 step 9 / D-13 atomic-per-file FIRST commit (foundational —
        cement/core/config.py, foundation.py, template.py follow in
        next tasks).

    Stage exactly: `cement/utils/fs.py`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn 'os\.path' cement/utils/fs.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: 0 untagged os.path in fs.py") && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement && make comply-ruff</automated>
  </verify>
  <acceptance_criteria>
    - `cement/utils/fs.py` contains `from pathlib import Path`
    - `grep -rn 'os\.path' cement/utils/fs.py | grep -v '# boundary:' | wc -l` returns 0 (no untagged os.path calls)
    - `grep -nE 'def [a-z][a-zA-Z_]*\(.*\) -> Path' cement/utils/fs.py | wc -l` returns 0 (no public function returns Path — D-12 boundary)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - `make comply-ruff` exits 0
    - Single commit `refactor(utils.fs): migrate os.path to pathlib internals` exists
    - Commit body includes A7 symlink pre-flight finding (counts + decision)
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>fs.py pathlib migration complete with boundary preservation; A7 finding documented; gates all green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate cement/core/config.py to pathlib internals (1 callsite — warm-up)</name>
  <files>cement/core/config.py, CHANGELOG.md</files>
  <read_first>
    - cement/core/config.py (FULL file — small)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 410-447 — config.py migration target shape; the single os.path.exists callsite)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-13 — config.py is natural warm-up after fs.py per ordering)
  </read_first>
  <action>
    Migration plan for cement/core/config.py (1 known callsite at ~line 226):

    1. Add `from pathlib import Path` to imports.

    2. Replace `if os.path.exists(file_path):` (line ~226) with `if Path(file_path).exists():`.

    3. Search for any other `os.path.*` calls — there should be none, but verify:

           grep -n 'os\.path' cement/core/config.py

       If others surface, migrate per the equivalents table.

    4. Decide whether to remove `import os` if it becomes unused after migration. Check with:

           grep -E 'os\.[a-z]' cement/core/config.py | grep -v 'os\.path'

       If empty, the `import os` can be removed. If it has `os.environ` etc., leave the import.

    5. Verification BEFORE commit (same gate set):
       - `grep -rn 'os\.path' cement/core/config.py | grep -v '# boundary:' | wc -l` returns 0
       - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
       - `make audit-public-api` exits 0
       - `pdm run mypy cement` exits 0

    Append CHANGELOG.md `[core]` entry:

        * [core] Migrate `cement/core/config.py` os.path.exists to pathlib (D-11 scope continuation).

    Commit:

        refactor(core.config): migrate os.path to pathlib internals

        Replace os.path.exists callsite in cement/core/config.py
        parse_file with Path(p).exists() per Phase 03 D-11. Boundary
        contract held: file_path stays str at the public boundary.

        Smallest of the four pathlib commits (1 callsite); natural
        warm-up after cement/utils/fs.py per D-13 ordering.

        Verified: 100% coverage, audit-public-api exit 0, mypy clean.

        D-22 step 12 / D-13 atomic-per-file commit.

    Stage exactly: `cement/core/config.py`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn 'os\.path' cement/core/config.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: 0 untagged os.path in config.py") && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `cement/core/config.py` contains `from pathlib import Path`
    - `grep -rn 'os\.path' cement/core/config.py | grep -v '# boundary:' | wc -l` returns 0
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - Single commit `refactor(core.config): migrate os.path to pathlib internals` exists
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>config.py pathlib migration complete; gates green; one commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Migrate cement/core/foundation.py to pathlib internals (~5 callsites)</name>
  <files>cement/core/foundation.py, CHANGELOG.md</files>
  <read_first>
    - cement/core/foundation.py (FULL file — large; use grep first to locate os.path callsites: `grep -nE 'os\.path' cement/core/foundation.py`)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 298-358 — foundation.py migration target shape; canonical os.path.isdir → Path.is_dir migration at line 1315-1317; PROTECTED .format(**template_dict) lines must NOT be touched)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-19 protected line list — foundation.py:1359..1567 .format() callsites; D-13 atomic commits; D-12 boundary preservation)
  </read_first>
  <action>
    Migration plan for cement/core/foundation.py (~5+ os.path callsites per RESEARCH.md):

    1. Pre-flight: locate all callsites:

           grep -nE 'os\.path' cement/core/foundation.py

       Record line numbers and the surrounding context for each. The known canonical callsite per PATTERNS.md is `_find_config_files` at lines 1315-1317.

    2. Add `from pathlib import Path` to imports if not already present.

    3. Migrate each callsite per the equivalents table:
       - `os.path.isdir(p)` → `Path(p).is_dir()`
       - `os.path.exists(p)` → `Path(p).exists()`
       - `os.path.join(a, b)` → `str(Path(a) / b)` (use `str()` if the result crosses a public boundary)
       - `os.path.abspath(p)` → use the project's own `abspath` from `cement/utils/fs.py` if already imported, OR `str(Path(p).expanduser().resolve(strict=False))` per Task 1 A7 decision

    4. **D-19 PROTECTION ABSOLUTE:** The `.format(**template_dict)` callsites at `cement/core/foundation.py` lines 1359, 1364, 1372, 1377, 1385, 1390, 1478, 1483, 1488, 1492, 1553, 1558, 1563, 1567 MUST NOT be touched (these are template substitution, not path handling — line numbers may have drifted post Waves 3-5; re-grep to confirm). They are NOT path-related; UP032 already left them alone in Wave 3. Just don't accidentally edit those line ranges.

    5. **Boundary-preservation:** Internal locals can be `Path`; any value that crosses a public boundary (return, public method parameter, instance attribute) goes back through `str()`. Verify no public type signatures change (audit gate enforces).

    6. Surviving `os.path.*` callsites (if any) get `# boundary:` comments per D-14.

    7. Verification BEFORE commit:
       - `grep -rn 'os\.path' cement/core/foundation.py | grep -v '# boundary:' | wc -l` returns 0
       - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (Pitfall 3 highest-risk wave)
       - `make audit-public-api` exits 0
       - `pdm run mypy cement` exits 0

    Append CHANGELOG.md `[core]` entry:

        * [core] Migrate `cement/core/foundation.py` os.path internals to pathlib (D-11 scope; D-12 boundary preservation; D-19 protected `.format(**template_dict)` callsites untouched).

    Commit:

        refactor(core.foundation): migrate os.path to pathlib internals

        Replace <N> os.path callsites in cement/core/foundation.py with
        pathlib.Path equivalents per Phase 03 D-11. Boundary-preservation
        rule (D-12) held: internal locals are Path; values crossing
        public boundaries go back through str(). No public type signature
        changes (audit-public-api exit 0).

        D-19 PROTECTED template-substitution callsites unchanged (lines
        1359, 1364, 1372, 1377, 1385, 1390, 1478, 1483, 1488, 1492,
        1553, 1558, 1563, 1567 — not path-related, not touched in this
        commit).

        Verified: 100% coverage, audit-public-api exit 0, mypy clean.

        D-22 step 10 / D-13 atomic-per-file commit.

    Stage exactly: `cement/core/foundation.py`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn 'os\.path' cement/core/foundation.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: 0 untagged os.path in foundation.py") && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `cement/core/foundation.py` contains `from pathlib import Path`
    - `grep -rn 'os\.path' cement/core/foundation.py | grep -v '# boundary:' | wc -l` returns 0
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - The 14 D-19 protected lines (or their drifted equivalents) are STILL `.format(**template_dict)` — verify with `grep -c '\.format(\*\*' cement/core/foundation.py` (should be ≥ 14)
    - Single commit `refactor(core.foundation): migrate os.path to pathlib internals` exists
    - Commit body lists migrated line numbers
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>foundation.py pathlib migration complete; D-19 protected callsites verified untouched; gates green; one atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 5: Migrate cement/core/template.py to pathlib internals (~14 callsites — biggest blast)</name>
  <files>cement/core/template.py, CHANGELOG.md</files>
  <read_first>
    - cement/core/template.py (FULL file — biggest single-file blast; use grep first: `grep -nE 'os\.path|os\.walk|os\.makedirs' cement/core/template.py`)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 363-407 — template.py migration target shape; canonical os.walk → pathlib pattern; PROTECTED .format(**template_dict) at line 359+)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pattern 3 + os.path equivalents table; Pitfall 1 — protected template substitution; State of the Art)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-13 atomic commit per file; D-19 PROTECTED template.py:359+)
  </read_first>
  <action>
    Migration plan for cement/core/template.py (~14+ os.path callsites — biggest single-file blast):

    1. Pre-flight: locate all path-related callsites:

           grep -nE 'os\.path|os\.walk|os\.makedirs' cement/core/template.py

       Record line numbers. Per PATTERNS.md the heavy section is the `os.walk` template-render loop at lines 186-260 (line numbers may have drifted post Waves 3-5 — re-grep).

    2. Add `from pathlib import Path` to imports.

    3. Migrate each callsite per the equivalents table:
       - `os.path.exists(p)` → `Path(p).exists()`
       - `os.path.join(a, b)` → `Path(a) / b` (returns Path; convert to str at boundary)
       - `os.makedirs(p)` → `Path(p).mkdir(parents=True, exist_ok=True)` (matches existing semantics if branched on `if not exists`)
       - `os.walk(src)` — pathlib has no direct equivalent; either:
         - Keep `os.walk(src)` and tag with `# boundary: stdlib walk preferred for triple-tuple yield` per D-14, OR
         - Replace with `Path(src).rglob('*')` and adapt the loop logic to walk Path objects

         Decision: keep `os.walk` and tag with `# boundary:` UNLESS converting to `rglob` is straightforward for the existing loop shape. Pragmatic choice — `os.walk` is well-tested in this template code and a wholesale loop refactor is higher risk than the boundary-tag accommodation. Document the decision in the commit body.

    4. **D-19 PROTECTION ABSOLUTE:** `.format(**template_dict)` callsites at `cement/core/template.py:359+` MUST NOT be touched (these are template substitution; line numbers may have drifted — re-grep `grep -nE '\.format\(\*\*' cement/core/template.py` to confirm current line numbers; verify the line count matches pre-migration).

    5. Surviving `os.path.*` or `os.walk` callsites get `# boundary:` per D-14.

    6. Verification BEFORE commit (HIGHEST-RISK wave per Pitfall 3):
       - `grep -rn 'os\.path' cement/core/template.py | grep -v '# boundary:' | wc -l` returns 0
       - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (run with `--cov-report=term-missing` to verify branch coverage on template.py)
       - `make audit-public-api` exits 0
       - `pdm run mypy cement` exits 0
       - `.format(**template_dict)` callsite count is unchanged from pre-migration: `grep -c '\.format(\*\*' cement/core/template.py` returns the same number as before the commit

    Append CHANGELOG.md `[core]` entry:

        * [core] Migrate `cement/core/template.py` os.path internals to pathlib (~14 callsites; biggest single-file blast). D-19 protected `.format(**template_dict)` callsites untouched. `os.walk` (if retained) carries `# boundary:` comment.

    Commit:

        refactor(core.template): migrate os.path to pathlib internals

        Replace <N> os.path callsites in cement/core/template.py with
        pathlib.Path equivalents per Phase 03 D-11. Largest single-file
        blast in the pathlib scope. Boundary-preservation rule (D-12)
        held; internal locals are Path; public-boundary values go
        through str().

        D-19 PROTECTED template-substitution `.format(**template_dict)`
        callsites at line 359+ unchanged.

        os.walk decision: <retained with # boundary: comment | replaced
        with Path.rglob>. Rationale: <why>.

        Verified: 100% coverage (per-file branch coverage clean via
        `--cov-report=term-missing`), audit-public-api exit 0, mypy
        clean, .format(**dict) callsite count unchanged.

        D-22 step 11 / D-13 atomic-per-file commit. Closes the pathlib
        migration scope (D-11 scope satisfied across all 4 named files).

    Stage exactly: `cement/core/template.py`, `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn 'os\.path' cement/core/template.py | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: 0 untagged os.path in template.py") && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement && grep -rn 'os\.path' cement/utils/fs.py cement/core/ | grep -v '# boundary:' | wc -l | (read n; test "$n" -eq 0 && echo "PASS: D-24 conjunct #8 GREEN — 0 untagged os.path in scoped files")</automated>
  </verify>
  <acceptance_criteria>
    - `cement/core/template.py` contains `from pathlib import Path`
    - `grep -rn 'os\.path' cement/core/template.py | grep -v '# boundary:' | wc -l` returns 0
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100%
    - `make audit-public-api` exits 0
    - `pdm run mypy cement` exits 0
    - `.format(**template_dict)` callsite count in template.py unchanged from pre-migration (D-19)
    - **D-24 conjunct #8 GREEN:** `grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l` returns 0 across ALL FOUR scoped files
    - Single commit `refactor(core.template): migrate os.path to pathlib internals` exists
    - Commit body documents os.walk decision (retained with boundary tag OR replaced with rglob)
    - CHANGELOG.md has new `[core]` entry
  </acceptance_criteria>
  <done>template.py pathlib migration complete; D-19 protected callsites verified untouched; D-24 conjunct #8 GREEN across all 4 scoped files; gates green; one atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pathlib internal → str public boundary | The most critical boundary in this plan. D-12 contract is enforced by the audit-public-api gate (D-04). Every public function in fs.py + scoped core/* files MUST still return `str` (or `bool`). |
| `Path.resolve()` symlink resolution vs `os.path.abspath` | Per A7, these have different semantics for symlinks. Pre-flight check (Task 1) determines which to use. |
| D-19 protected callsites | `.format(**template_dict)` template substitution in foundation.py + template.py. NOT path-related but in the same files; defense via per-file diff verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | Public function in fs.py inadvertently returns Path instead of str | mitigate | Audit-public-api gate (D-04 / D-24 #4) catches return-type changes. Boundary-verification grep (Task 2 step 3): `grep -nE 'def [a-z][a-zA-Z_]*\(.*\) -> ' cement/utils/fs.py | grep -v '_'` MUST show only `-> str` and `-> bool`. |
| T-03-06-02 | Tampering | Symlink resolution semantics changes silently | mitigate | A7 pre-flight (Task 1) chooses `.resolve(strict=False)` vs `.absolute()` based on actual symlink presence. Decision documented in fs.py commit body. |
| T-03-06-03 | DoS | Coverage drop in template.py refactor (Pitfall 3) | mitigate | Run `pdm run pytest --cov=cement --cov-report=term-missing tests` after template.py commit. `--cov-fail-under=100` (Phase 2 D-10) hard-fails the commit. |
| T-03-06-04 | Tampering | Accidentally edit D-19 protected `.format(**template_dict)` lines | mitigate | Per-task `grep -c '\.format(\*\*' cement/core/foundation.py cement/core/template.py` count comparison before vs after commit. Per PATTERNS.md "PROTECTED — DO NOT TOUCH" warnings. |

**No new attack surface introduced.** Migration is purely a semantic-preserving idiom modernization with multiple defense-in-depth gates.
</threat_model>

<verification>
After this plan completes:
1. `grep -rn 'os\.path' cement/utils/fs.py cement/core/foundation.py cement/core/template.py cement/core/config.py | grep -v '# boundary:' | wc -l` returns 0 (D-24 #8 GREEN)
2. All 4 scoped files contain `from pathlib import Path`
3. No public function in any of the 4 files returns `Path` (D-12 boundary contract)
4. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 #1)
5. `make audit-public-api` exits 0 (D-24 #4)
6. `pdm run mypy cement` exits 0 (D-24 #3)
7. `make comply-ruff` exits 0 (D-24 #2 still green from Wave 4)
8. D-19 `.format(**template_dict)` callsite counts in foundation.py + template.py unchanged from pre-migration
9. Four Wave 6 commits visible in `git log --oneline -10`:
   - `refactor(utils.fs): migrate os.path to pathlib internals`
   - `refactor(core.config): migrate os.path to pathlib internals`
   - `refactor(core.foundation): migrate os.path to pathlib internals`
   - `refactor(core.template): migrate os.path to pathlib internals`

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-06-01 | 6 | REFACTOR-03 (pre-flight) | #1, #4 | shell | `find tests/ -type l && find cement/ -type l && pdm run pytest --cov=cement -x tests && make audit-public-api` |
| 03-06-02 | 6 | REFACTOR-03, COV-01 | #1, #2, #3, #4, #8 | grep + pytest + audit | `grep -rn 'os\\.path' cement/utils/fs.py \| grep -v '# boundary:' \| wc -l` (must be 0) + gates |
| 03-06-03 | 6 | REFACTOR-03, COV-01 | #1, #3, #4, #8 | grep + pytest + audit | `grep -rn 'os\\.path' cement/core/config.py \| grep -v '# boundary:' \| wc -l` (must be 0) + gates |
| 03-06-04 | 6 | REFACTOR-03, COV-01 | #1, #3, #4, #8 | grep + pytest + audit | `grep -rn 'os\\.path' cement/core/foundation.py \| grep -v '# boundary:' \| wc -l` (must be 0) + gates + D-19 check |
| 03-06-05 | 6 | REFACTOR-03, COV-01 | #1, #3, #4, #8 | grep + pytest + audit | All 4 scoped files: `grep -rn 'os\\.path' cement/utils/fs.py cement/core/ \| grep -v '# boundary:' \| wc -l` (must be 0) + gates + D-19 check |
</verification>

<success_criteria>
- All 4 named files migrated to pathlib internals (D-11)
- Boundary preservation held: no public function returns Path; HOME_DIR, Tmp.dir, Tmp.file stay str (D-12)
- D-24 conjunct #8 GREEN: zero untagged os.path in scoped files
- D-19 protected callsites in foundation.py + template.py UNCHANGED
- D-24 conjuncts #1, #2, #3, #4 stay green
- 4 atomic commits landed (one per file, ordered fs.py → config.py → foundation.py → template.py)
- A7 symlink finding documented in fs.py commit body
- CHANGELOG.md has 4 new [core] entries
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-06-SUMMARY.md` with:
- A7 symlink pre-flight finding (counts + decision: .resolve vs .absolute)
- Files modified count + per-file os.path callsite delta (pre vs post — must be all-pre to 0-post excluding any # boundary: tagged survivors)
- 4 commit SHAs + subjects
- Confirmation D-19 protected callsite counts unchanged in foundation.py + template.py
- os.walk decision in template.py (retained or replaced)
- Coverage % at each commit (must be 100% throughout)
- D-24 conjunct #8 final grep result (must be empty across all 4 files)
- Note any unexpected branch-coverage drop and how it was resolved
</output>
