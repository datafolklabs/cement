---
phase: 03-internal-refactor-coverage-hardening
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - cement/**/*.py
autonomous: true
requirements: [REFACTOR-04, COV-01]

must_haves:
  truths:
    - "Zero `from __future__ import annotations` statements remain across cement/ (D-08 / FA100 / D-24 conjunct #9)"
    - "make comply-ruff exits 0 fully (UP family from Wave 3 + FA family from this plan resolved) — D-24 conjunct #2 fully restored"
    - "100% coverage gate stays green (Phase 2 D-10/D-11/D-12 / D-24 conjunct #1)"
    - "make audit-public-api exits 0 (D-04 / D-24 conjunct #4) — public surface unchanged"
    - "mypy stays clean — TYPE_CHECKING-wrapped circular imports verified safe per RESEARCH.md A2 (D-24 conjunct #3)"
    - "After this commit, `make superclean && make init` cache reset is run before Wave 5 to clear stale .mypy_cache from the FA strip"
  artifacts:
    - path: "cement/"
      provides: "All 28 (or 21 — count drift; RESEARCH.md says 28, live grep on 2026-05-03 shows 21) files free of `from __future__ import annotations`"
      contains: "(absence verified by grep)"
  key_links:
    - from: "ruff FA family enabled in pyproject.toml (Wave 2)"
      to: "Auto-strip surface across cement/"
      via: "ruff check --select FA100 --fix"
      pattern: "ruff check --select FA"
    - from: "TYPE_CHECKING blocks in cement/core/handler.py:20, interface.py:17, extension.py:18, mail.py:13, hook.py:13"
      to: "Circular import safety (RESEARCH.md A2 — verified 2026-05-03)"
      via: "if TYPE_CHECKING: from ..core.foundation import App"
      pattern: "if TYPE_CHECKING:"
---

<objective>
Drop `from __future__ import annotations` from every file in `cement/`
that currently has it (Phase 1 D-14 explicitly deferred this to Phase
03; D-08 closes the loop). Removal MUST happen AFTER Wave 3 (UP006 +
UP007 + UP045) per D-08 — so the modernization is a single coherent
migration: PEP 604/585 native syntax replaces the future-deferred
syntax.

Ruff FA100 (added to extend-select in Wave 2) mechanizes the detection
and strip via `ruff check --select FA100 --fix`. The 28-file count from
CONTEXT/RESEARCH may drift to ~21 by execution time (live grep on
2026-05-03 returns 21 files); the executor uses the live count.

Critical safety check (RESEARCH.md A2 / Pitfall 2): All cross-module
circular imports in cement/core/* are already TYPE_CHECKING-wrapped
(verified for handler.py:20, interface.py:17, extension.py:18,
mail.py:13, hook.py:13 on 2026-05-03 — HIGH confidence). Same-module
forward refs are caught by `make comply-mypy` if any exist.

Purpose: Single atomic commit closes D-08 / D-24 conjunct #9. After
this commit, `make comply-ruff` is FULLY green (UP from Wave 3 + FA
from this plan). Cache reset (`make superclean && make init`) lands
before Wave 5 to ensure stale .mypy_cache doesn't mask any forward-ref
issues.

Output: One commit `fix(lint): resolve FA100 future-annotations
imports`. Optionally a follow-up `chore(dev): superclean + init for
cache reset` commit if the executor decides to capture the cache reset
in git history (otherwise it's just a local action).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md
@.planning/phases/03-internal-refactor-coverage-hardening/03-03-SUMMARY.md
@.planning/phases/01-tooling-baseline-python-matrix/01-CONTEXT.md
@CLAUDE.md
@cement/utils/fs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight forward-ref grep + capture pre-strip baseline</name>
  <files>(read-only / capture only — no commit)</files>
  <read_first>
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pitfall 2 — full forward-ref analysis; Same-module forward refs warning; verified TYPE_CHECKING list at line 529-536)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-08 — strip 28 files; CONTEXT.md "code_context" section flags cement/utils/fs.py:3-4 self-flagged TODO)
    - cement/core/foundation.py (read full file or grep for TYPE_CHECKING; verify all forward-ref edges are wrapped)
    - cement/core/handler.py, cement/core/interface.py, cement/core/extension.py, cement/core/mail.py, cement/core/hook.py (the 5 RESEARCH.md A2 verified files)
    - cement/utils/fs.py (the self-flagged TODO file from CONTEXT.md "code_context" — `# derks@2024-06-22: remove after 3.9 is EOL?`)
  </read_first>
  <action>
    **No commit in this task — pre-flight verification only.**

    Step 1: Get the live count of `from __future__ import annotations` across cement/:

        grep -rn "from __future__ import annotations" cement/ | tee /tmp/future-annotations-pre.txt
        wc -l /tmp/future-annotations-pre.txt

    Record the count (RESEARCH.md says 28; live grep on 2026-05-03 returned 21 — the truth is whatever grep returns at execution time). This count is the expected number of files Task 2 will modify.

    Step 2: Pre-flight grep for same-module forward refs (per RESEARCH.md Pitfall 2 step 1). For each cement/core file with `from __future__`, look for type annotations that reference a class defined later in the SAME module:

        for f in $(grep -l "from __future__ import annotations" cement/core/*.py); do
            echo "=== $f ==="
            grep -nE ': (App|Handler|Interface|Extension|Controller|Plugin|Cache|Config|Log|Output|Mail|Template|Hook)\b' "$f" | head -10
        done

    For any hits, verify the referenced class is either (a) imported from another module (cross-module — TYPE_CHECKING wraps already verified safe), (b) defined ABOVE the annotation in the same file (forward ref unnecessary, safe), or (c) defined BELOW (CAUTION — strip may break it).

    Step 3: Verify TYPE_CHECKING-wrapped circular imports are intact (RESEARCH.md A2 5-file verified list):

        for f in cement/core/handler.py cement/core/interface.py cement/core/extension.py cement/core/mail.py cement/core/hook.py; do
            grep -A1 "if TYPE_CHECKING:" "$f" | head -5
            echo "---"
        done

    Step 4: Run `pdm run mypy cement` to confirm baseline is clean BEFORE the FA strip — any mypy error after the strip is attributable to the strip.

    Step 5: Note in the summary if any same-module forward ref was found that may need a string annotation `"ClassName"` post-strip.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn "from __future__ import annotations" cement/ | tee /tmp/future-annotations-pre.txt && wc -l /tmp/future-annotations-pre.txt && pdm run mypy cement</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/future-annotations-pre.txt` exists and has at least 1 line (the live count of files with `from __future__ import annotations` — record exact count for Task 2 cross-check)
    - `pdm run mypy cement` exits 0 (baseline clean BEFORE strip)
    - All 5 RESEARCH.md A2-verified files (handler.py, interface.py, extension.py, mail.py, hook.py) confirmed to have `if TYPE_CHECKING:` block guarding their `from ..core.foundation import App` (or equivalent)
    - Same-module forward-ref pre-flight grep recorded — any flagged sites noted for Task 2 attention
  </acceptance_criteria>
  <done>Pre-flight complete; live count captured; baseline mypy green; Task 2 has the cross-check data it needs.</done>
</task>

<task type="auto">
  <name>Task 2: Apply FA100 strip + full gate verification + commit</name>
  <files>cement/**/*.py, CHANGELOG.md</files>
  <read_first>
    - /tmp/future-annotations-pre.txt (Task 1 output — live count and file list)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-CONTEXT.md (D-08 — strip 28 files; D-22 step 7)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-RESEARCH.md (Pitfall 2 — full forward-ref handling; Pitfall 3 — coverage drop signal; Runtime State Inventory — `make superclean && make init` recommended after FA100 to clear .mypy_cache)
    - .planning/phases/03-internal-refactor-coverage-hardening/03-PATTERNS.md (lines 224-240 — fs.py self-flagged TODO comment that this commit closes)
  </read_first>
  <action>
    Step 1: Apply ruff FA100 auto-fix:

        pdm run ruff check --select FA100 --fix cement/ tests/

    FA100 strips `from __future__ import annotations` from every file where ruff determines the import is no longer needed (which is true post-Wave 3 since PEP 604/585 syntax is native in Python 3.10+, and the project's `requires-python = ">=3.10"` per pyproject.toml).

    Step 2: Verify the strip is complete:

        grep -rn "from __future__ import annotations" cement/

    MUST return EMPTY (D-24 conjunct #9; D-08 acceptance).

    Step 3: Handle the cement/utils/fs.py self-flagged TODO comment. The file has this preamble (verbatim from PATTERNS.md):

        """Common File System Utilities."""

        # derks@2024-06-22: remove after 3.9 is EOL?
        from __future__ import annotations

    After ruff strips the `from __future__` line, the comment `# derks@2024-06-22: remove after 3.9 is EOL?` is now ORPHANED — it refers to a line that no longer exists. Remove the orphaned comment as part of this commit (single sed/edit — the comment is on the line immediately above where the import was). The self-flagged loop closes here per CONTEXT canonical_refs and PATTERNS.md.

    Step 4: Run all four gates BEFORE commit (Pitfall 2 warning sign — `NameError: name 'X' is not defined` at module import time would surface in `make test` collection):

    1. `pdm run ruff check --select FA100 cement/ tests/` MUST exit 0 (no remaining FA100 findings)
    2. `pdm run pytest --cov=cement -x tests` MUST exit 0 with coverage 100% (catches forward-ref NameErrors at test collection)
    3. `make audit-public-api` MUST exit 0 (annotation strip changes runtime evaluation but not the AST signature surface)
    4. `pdm run mypy cement` MUST exit 0 (any masked forward-ref issue surfaces here)

    Step 5: Verify FULL ruff green:

        make comply-ruff

    MUST exit 0 fully — UP family (Wave 3) + FA family (this commit) are now both clean. D-24 conjunct #2 RESTORED.

    Step 6: Cache reset (per RESEARCH.md Runtime State Inventory):

        make superclean && make init

    Then re-run the gates as a defense-in-depth check against stale .mypy_cache:

        make test && make comply

    Both MUST exit 0. (No commit for the cache reset — it's a local action; the cache files are gitignored.)

    Step 7: Append CHANGELOG.md `[core]` entry under Refactoring bucket:

        * [core] Drop `from __future__ import annotations` from all cement/ files (FA100; Phase 1 D-14 deferral closed). PEP 604/585 syntax (Wave 3) is native in Python 3.10+; the future import is no longer needed. Closes the cement/utils/fs.py self-flagged TODO from 2024-06-22.

    Step 8: Commit:

        fix(lint): resolve FA100 future-annotations imports

        Apply ruff FA100 auto-fix across cement/ to drop `from __future__
        import annotations` from every file. PEP 604/585 syntax (Wave 3:
        UP006/UP007/UP045) is native in Python 3.10+; the future-import
        is no longer needed.

        Closes Phase 1 D-14 deferral (CONTEXT canonical_refs). Closes
        the cement/utils/fs.py self-flagged TODO `# derks@2024-06-22:
        remove after 3.9 is EOL?` — orphaned comment removed in same
        commit.

        Verified safe: all cross-module circular imports in cement/core/*
        are TYPE_CHECKING-wrapped (handler.py:20, interface.py:17,
        extension.py:18, mail.py:13, hook.py:13 — RESEARCH.md A2 HIGH
        confidence). 100% coverage, audit-public-api exit 0, mypy
        clean, comply-ruff fully green.

        D-22 step 7 of the suggested 14-step sequence. D-24 conjunct #9
        (no `from __future__ import annotations` in cement/) GREEN.

    Stage modified `.py` files + `CHANGELOG.md`.
  </action>
  <verify>
    <automated>cd /Users/derks/Development/DFL/cement && grep -rn "from __future__ import annotations" cement/ | wc -l | (read n; test "$n" -eq 0 && echo "PASS: 0 future-annotations imports remain in cement/") && pdm run pytest --cov=cement -x tests && make audit-public-api && pdm run mypy cement && make comply-ruff</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from __future__ import annotations" cement/` returns EMPTY (D-08 / D-24 conjunct #9 GREEN)
    - `pdm run ruff check --select FA100 cement/ tests/` exits 0
    - `make comply-ruff` exits 0 FULLY (UP family from Wave 3 + FA family from this plan, both clean — D-24 conjunct #2 RESTORED)
    - `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 conjunct #1)
    - `make audit-public-api` exits 0 (D-24 conjunct #4)
    - `pdm run mypy cement` exits 0 (D-24 conjunct #3)
    - cement/utils/fs.py NO LONGER contains the orphaned `# derks@2024-06-22: remove after 3.9 is EOL?` comment (`grep -c "derks@2024-06-22" cement/utils/fs.py` returns 0)
    - Single commit `fix(lint): resolve FA100 future-annotations imports` exists with subject ≤ 78 chars
    - CHANGELOG.md has new `[core]` entry referencing FA100 + Phase 1 D-14 closure
    - After `make superclean && make init`, the gates are STILL green (cache reset confirms no stale-cache masking)
  </acceptance_criteria>
  <done>FA100 cleanly stripped from all cement/ files; orphaned TODO comment removed; cache reset confirms green; D-24 conjuncts #2 (comply-ruff full) and #9 (no future-annotations) RESTORED; one atomic commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ruff FA100 → cement/ source | Strip changes annotation evaluation timing (deferred → immediate) but not runtime behavior unless a forward ref is masked. |
| TYPE_CHECKING-wrapped imports | Already-existing safety net for cross-module circular references; verified intact pre-strip (Task 1) and verified safe post-strip via mypy + pytest (Task 2). |

## STRIDE Threat Register

This is a syntactic-cleanup commit with NO new attack surface:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | FA100 strips a future-annotations line whose deferred-eval was masking a forward ref | mitigate | Pre-flight forward-ref grep (Task 1 step 2). Post-strip mypy + pytest collection (Task 2 step 4) catches any masked NameError. RESEARCH.md A2 verifies 5 critical cement/core files HIGH confidence safe. |
| T-03-04-02 | DoS | Stale .mypy_cache masks a real type error | mitigate | `make superclean && make init` cache reset before Wave 5 (Task 2 step 6) ensures gates run against fresh caches. |

**No new attack surface introduced.** Defense-in-depth via mypy + pytest + cache reset.
</threat_model>

<verification>
After this plan completes:
1. `grep -rn "from __future__ import annotations" cement/` returns EMPTY (D-24 #9)
2. `make comply-ruff` exits 0 FULLY (D-24 #2 fully restored — UP + FA both clean)
3. `pdm run pytest --cov=cement -x tests` exits 0 with coverage 100% (D-24 #1)
4. `make audit-public-api` exits 0 (D-24 #4)
5. `pdm run mypy cement` exits 0 (D-24 #3)
6. Single Wave 4 commit `fix(lint): resolve FA100 future-annotations imports` is visible
7. `make superclean && make init && make test && make comply` all green (cache-reset defense-in-depth)

Per-task verification map:

| Task ID | Wave | Requirement | Conjunct(s) | Test Type | Automated Command |
|---------|------|-------------|-------------|-----------|-------------------|
| 03-04-01 | 4 | REFACTOR-04 (pre-flight) | #3 (mypy baseline) | grep + mypy | `grep -rn "from __future__ import annotations" cement/ \| wc -l` (record count) + `pdm run mypy cement` |
| 03-04-02 | 4 | REFACTOR-04, COV-01 | #1, #2, #3, #4, #9 | grep + ruff + pytest + mypy | `grep -rn "from __future__ import annotations" cement/ \| wc -l` (must be 0) + `make comply-ruff && pdm run pytest --cov=cement -x tests && pdm run mypy cement && make audit-public-api` |
</verification>

<success_criteria>
- D-24 conjunct #9 GREEN: zero `from __future__ import annotations` in cement/
- D-24 conjunct #2 fully restored: `make comply-ruff` exits 0 (UP + FA both clean)
- D-24 conjuncts #1, #3, #4 stay green
- One atomic commit `fix(lint): resolve FA100 future-annotations imports`
- cement/utils/fs.py self-flagged TODO closed (orphaned comment removed)
- Cache reset (`make superclean && make init`) confirms gates still green post-strip
- CHANGELOG.md has new [core] entry
</success_criteria>

<output>
After completion, create `.planning/phases/03-internal-refactor-coverage-hardening/03-04-SUMMARY.md` with:
- Live pre-strip count of `from __future__ import annotations` files (RESEARCH says 28; 2026-05-03 grep said 21; record actual)
- Files modified count and list
- Single commit SHA + subject
- Confirmation that grep post-strip returns 0
- Cache reset (`make superclean && make init`) outcome — confirmation that gates still green
- Note any same-module forward-ref edge that required a string annotation (`"ClassName"`) post-strip (should be zero per RESEARCH.md A2 HIGH confidence; record if any surfaced)
- Confirmation that orphaned `# derks@2024-06-22:` comment in cement/utils/fs.py is gone
</output>
