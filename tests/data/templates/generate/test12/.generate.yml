---

variables:

-   name: app_name
    prompt: App Name
    default: myapp

-   name: feature1
    type: boolean
    default: true
    extend:
        -   when: true
            variables:
            exclude:
            ignore:
