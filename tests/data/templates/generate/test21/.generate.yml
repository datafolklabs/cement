---

variables:

-   name: app_name
    prompt: App Name
    default: myapp

-   name: feature_choice
    type: choice
    prompt: "Pick"
    default: "A"
    options:
        -   prompt: "A label with no value key"
