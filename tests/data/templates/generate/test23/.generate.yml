---

variables:
    -   name: app_name
        prompt: App Name
        default: myapp

    -   name: legacy_bool_feature
        type: boolean
        default: true
        extend:
            -   when: true
                variables: null
                exclude: null
                ignore: null
            -   when: false
                ignore:
                    - '.*nonexistent.*'
