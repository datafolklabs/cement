---

variables:
    -   name: app_name
        prompt: App Name
        default: myapp

features:
    -   name: legacy_bool_feature
        default: true
        enabled:
            variables: null
            exclude: null
            ignore: null
        disabled:
            ignore:
                - '.*nonexistent.*'
