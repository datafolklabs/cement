---

variables:
    -   name: app_name
        prompt: App Name
        default: myapp

    -   name: feature_choice
        type: choice
        prompt: "Pick"
        default: "A"
        options: ["A"]
        extend:
            -   when: "A"
                variables: null
                exclude: null
                ignore: null
