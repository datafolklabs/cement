---

variables:
    -   name: app_name
        prompt: App Name
        default: myapp

features:
    -   name: feature_choice
        prompt_mode: select
        prompt: "Pick"
        default: "A"
        options:
            -   value: "A"
                variables: null
                exclude: null
                ignore: null
